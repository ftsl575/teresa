---
phase: 04-cache-redirect
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spec_classifier/docs/dev/ONE_BUTTON_RUN.md
autonomous: true
requirements: [CACHE-04]
requirements_addressed: [CACHE-04]
must_haves:
  truths:
    - "`docs/dev/ONE_BUTTON_RUN.md` 'Workspace cleanup' section names `clean.ps1` as default-on with `-NoClean` opt-out (the strings `-NoClean` and `clean.ps1` co-occur in that section)."
    - "The 'What run.ps1 does' numbered list mentions the start-of-run cleanup step with the `-NoClean` opt-out."
    - "The 'Useful run.ps1 switches' code block contains a `-NoClean` entry."
  artifacts:
    - path: "spec_classifier/docs/dev/ONE_BUTTON_RUN.md"
      provides: "Updated quick-start doc reflecting Phase 4's clean-by-default + cache-redirect contract"
      contains:
        - "-NoClean"
        - "clean.ps1"
  key_links:
    - from: "ONE_BUTTON_RUN.md 'Workspace cleanup' section"
      to: "run.ps1 (Plan 04-01) `-NoClean` switch"
      via: "documented contract — operator reads doc, invokes run.ps1 -NoClean"
      pattern: "-NoClean"
---

<objective>
Reflect Phase 4's new clean-by-default + cache-redirect contract in `spec_classifier/docs/dev/ONE_BUTTON_RUN.md` via three coordinated edits per D-11. After this plan lands, the doc accurately describes (a) what `run.ps1` now does first (clean sweep, skippable with `-NoClean`), (b) the new `-NoClean` switch among the useful flags, and (c) the rewritten "Workspace cleanup" section reflecting `clean.ps1` as default-on rather than a manual sidebar.

Purpose: Closes CACHE-04. Phase 6's mechanical drift sweep depends on this section being accurate post-Phase-4 — the doc IS the source of operator truth. Without this edit, an operator reading `ONE_BUTTON_RUN.md` after Phase 4 would believe `clean.ps1` is still a manual command and would not learn about the `-NoClean` flag.

Output: A single edited `spec_classifier/docs/dev/ONE_BUTTON_RUN.md` with three coordinated text edits and no other doc changes (D-12 — no other docs are touched in Phase 4; cross-doc reconciliation is Phase 6's job).
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md

# Source file this plan owns
@spec_classifier/docs/dev/ONE_BUTTON_RUN.md

<interfaces>
<!-- Existing prose to be edited. Sections delimited by current line numbers. -->

From spec_classifier/docs/dev/ONE_BUTTON_RUN.md:10-19 (current "What run.ps1 does" numbered list):
```markdown
## What run.ps1 does

1. Finds the repo root
2. Runs the classification pipeline for each active vendor (dell, cisco, hpe, lenovo, huawei, xfusion)
3. Runs `batch_audit.py` (E-code audit; add `-NoAi` to skip the LLM step)
4. Runs `cluster_audit.py`
5. Saves logs to the OUTPUT directory
6. Runs pytest if not `-SkipTests`
7. Prints a summary
```

From spec_classifier/docs/dev/ONE_BUTTON_RUN.md:36-44 (current "Useful run.ps1 switches" code block):
```markdown
## Useful run.ps1 switches

```powershell
.\run.ps1 -NoAi                        # rule-based audit only (no OPENAI_API_KEY needed)
.\run.ps1 -Vendor dell                 # one vendor end-to-end
.\run.ps1 -TestsOnly                   # pytest only
.\run.ps1 -SkipTests                   # full run without pytest at the end
.\run.ps1 -Vendor huawei -NoAi -SkipTests  # smoke run
```
```

From spec_classifier/docs/dev/ONE_BUTTON_RUN.md:46-53 (current "Workspace cleanup" section):
```markdown
## Workspace cleanup

```powershell
.\spec_classifier\scripts\clean.ps1
```

Removes `__pycache__`, `.pytest_cache`, `.ruff_cache`, `.mypy_cache` from the working tree.
Does not touch: golden files, OUTPUT.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Three coordinated edits to ONE_BUTTON_RUN.md per D-11</name>
  <files>spec_classifier/docs/dev/ONE_BUTTON_RUN.md</files>
  <read_first>
    - spec_classifier/docs/dev/ONE_BUTTON_RUN.md (entire file — 53 lines)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § "Implementation Decisions" § "Area D" (D-11, D-12)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § "Specifics" (tone guidance: imperative, terse, code-fenced PowerShell; no "why" prose)
  </read_first>
  <action>
Make three coordinated edits to `spec_classifier/docs/dev/ONE_BUTTON_RUN.md`. Keep the existing tone: imperative, terse, code-fenced PowerShell. Do NOT add prose explaining *why* the cache is redirected — that belongs in `RUN_PATHS_AND_IO_LAYOUT.md` (out of scope for Phase 4 per D-12; Phase 6 owns cross-doc reconciliation).

**Edit 1 — Insert a new step at the top of the "What run.ps1 does" numbered list (currently lines 10-19) and renumber existing steps (per D-11.1):**

Replace the current numbered list (steps 1-7) with an 8-step list where the new step 1 covers cleanup. Result MUST read:

```markdown
## What run.ps1 does

1. Cleans prior `__pycache__` / `.pytest_cache` from `temp_root` and the working tree (skip with `-NoClean`)
2. Finds the repo root
3. Runs the classification pipeline for each active vendor (dell, cisco, hpe, lenovo, huawei, xfusion)
4. Runs `batch_audit.py` (E-code audit; add `-NoAi` to skip the LLM step)
5. Runs `cluster_audit.py`
6. Saves logs to the OUTPUT directory
7. Runs pytest if not `-SkipTests`
8. Prints a summary
```

The new step 1 wording mirrors `clean.ps1`'s actual sweep semantics (per `spec_classifier/scripts/clean.ps1` — sweeps both temp_root and the repo working tree). All subsequent steps shift by +1.

**Edit 2 — Add a `-NoClean` entry to the "Useful run.ps1 switches" code block (currently lines 36-44) per D-11.2:**

Insert one new line in the PowerShell code-fence, immediately AFTER the `-SkipTests` line and BEFORE the `-Vendor huawei -NoAi -SkipTests` smoke-run line. Result MUST read:

```markdown
## Useful run.ps1 switches

```powershell
.\run.ps1 -NoAi                        # rule-based audit only (no OPENAI_API_KEY needed)
.\run.ps1 -Vendor dell                 # one vendor end-to-end
.\run.ps1 -TestsOnly                   # pytest only
.\run.ps1 -SkipTests                   # full run without pytest at the end
.\run.ps1 -NoClean                     # skip the start-of-run clean.ps1 sweep
.\run.ps1 -Vendor huawei -NoAi -SkipTests  # smoke run
```
```

The column alignment of the inline `#` comments MUST match the existing block's whitespace style (the comment column begins at byte 40, after the longest flag combination `.\run.ps1 -Vendor huawei -NoAi -SkipTests` plus 2 spaces). For the new line specifically, `.\run.ps1 -NoClean` plus padding spaces is shorter — line up the `#` so it matches the existing column visually. Concretely: pad the new line so the `#` sits in the same column as the existing `# rule-based audit only` line (pad with spaces between `-NoClean` and `#`).

**Edit 3 — Rewrite the "Workspace cleanup" section (currently lines 46-53) per D-11.3:**

Replace the entire section (everything from the `## Workspace cleanup` heading through the end of the section, i.e., lines 46-53) with:

```markdown
## Workspace cleanup

`run.ps1` invokes `.\spec_classifier\scripts\clean.ps1` automatically at the start of every run. Pass `-NoClean` to opt out. To clean manually without running the pipeline:

```powershell
.\spec_classifier\scripts\clean.ps1
```
```

This rewrite satisfies the CACHE-04 acceptance gate: the strings `-NoClean` and `clean.ps1` co-occur in the "Workspace cleanup" section. Do NOT preserve the old "Removes `__pycache__`, `.pytest_cache`, ..." sentence — `clean.ps1`'s actual sweep is broader (also `.ruff_cache`, `.mypy_cache`, `diag/`, and the temp_root partner of each cache type), and inlining the list here invites drift the moment `clean.ps1` is updated. The doc tells operators what to invoke; `clean.ps1` itself documents what it sweeps via its top-of-file comment.

**Out-of-scope reaffirmation (do NOT do):**
- Do NOT edit any other doc in `spec_classifier/docs/` (D-12 — Phase 6 owns cross-doc reconciliation against the post-Phase-4-and-5 tree).
- Do NOT edit root `README.md`, root `CLAUDE.md`, root `CONTRIBUTING.md`, or `LAUNCHER_README.md`. Phase 6 sweeps those.
- Do NOT add prose explaining the `temp_root` mechanism — Phase 4's quick-start scope is operator commands, not architecture.
- Do NOT touch `spec_classifier/config.local.yaml.example:11`'s stale comment (Phase 5 / ORPH-02 owns that rewrite).
- Do NOT edit any file under `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}` (D-22 protected paths).
  </action>
  <verify>
    <automated>
# All grep checks run from repo root. ALL must exit 0.
grep -F '1. Cleans prior `__pycache__` / `.pytest_cache`' spec_classifier/docs/dev/ONE_BUTTON_RUN.md && \
grep -F '8. Prints a summary' spec_classifier/docs/dev/ONE_BUTTON_RUN.md && \
grep -F '.\run.ps1 -NoClean' spec_classifier/docs/dev/ONE_BUTTON_RUN.md && \
grep -F 'skip the start-of-run clean.ps1 sweep' spec_classifier/docs/dev/ONE_BUTTON_RUN.md && \
grep -F '`run.ps1` invokes `.\spec_classifier\scripts\clean.ps1` automatically' spec_classifier/docs/dev/ONE_BUTTON_RUN.md && \
grep -F 'Pass `-NoClean` to opt out.' spec_classifier/docs/dev/ONE_BUTTON_RUN.md
    </automated>
  </verify>
  <acceptance_criteria>
    - The "What run.ps1 does" numbered list contains exactly 8 steps, with step 1 referencing the cleanup. Verified: `grep -cE '^[0-9]+\. ' spec_classifier/docs/dev/ONE_BUTTON_RUN.md` returns ≥ `8` (only counts top-level numbered list lines; the doc has no other numbered lists per current shape).
    - `grep -c 'skip with `-NoClean`' spec_classifier/docs/dev/ONE_BUTTON_RUN.md` returns ≥ `1` (the new step 1 references the opt-out).
    - The "Useful run.ps1 switches" code block contains a `-NoClean` line: `grep -c '\.\\\\run\.ps1 -NoClean ' spec_classifier/docs/dev/ONE_BUTTON_RUN.md` returns ≥ `1`.
    - The "Workspace cleanup" section co-occurrence acceptance gate (CACHE-04 verbatim): both `-NoClean` and `clean.ps1` appear within the section. Verified by:
      `awk '/^## Workspace cleanup/{flag=1} /^## /&&!/^## Workspace cleanup/{flag=0} flag' spec_classifier/docs/dev/ONE_BUTTON_RUN.md | grep -c '\-NoClean'` ≥ `1` AND
      `awk '/^## Workspace cleanup/{flag=1} /^## /&&!/^## Workspace cleanup/{flag=0} flag' spec_classifier/docs/dev/ONE_BUTTON_RUN.md | grep -c 'clean\.ps1'` ≥ `1`.
    - `git diff --stat HEAD~ -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` shows ZERO bytes changed (D-22 guard).
    - `git diff --stat HEAD~ -- spec_classifier/golden/` is empty (goldens byte-equal gate).
    - The diff scope is strictly limited to `spec_classifier/docs/dev/ONE_BUTTON_RUN.md`: `git diff --stat HEAD~ -- spec_classifier/docs/` lists ONLY `ONE_BUTTON_RUN.md` (D-12 — no other doc edits in Phase 4).
    - The old "Removes `__pycache__`, `.pytest_cache`, `.ruff_cache`, `.mypy_cache` from the working tree." sentence is REMOVED (replaced by the rewritten section): `grep -c 'Removes `__pycache__`, `.pytest_cache`, `.ruff_cache`' spec_classifier/docs/dev/ONE_BUTTON_RUN.md` returns `0`.
  </acceptance_criteria>
  <done>
    - All 3 edits landed in `spec_classifier/docs/dev/ONE_BUTTON_RUN.md`.
    - The "Workspace cleanup" section co-occurrence gate (CACHE-04) is satisfied.
    - The diff is scoped to ONE_BUTTON_RUN.md only; no other doc touched.
    - D-22 guard satisfied: zero diff inside protected paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ONE_BUTTON_RUN.md | Doc is read-only at runtime; not parsed by any code path |

## STRIDE Threat Register

`security_enforcement` is not declared in init context for v1.1. This is a doc-only edit with no runtime surface. Threat register kept minimal (no entries) — markdown text changes do not introduce STRIDE-relevant attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Doc-only edit; no runtime parsing, no env-var flow, no subprocess invocation, no untrusted-input handling. |
</threat_model>

<verification>
**Plan 3 verification (run from repo root):**

1. **Static checks (automated):** All grep predicates in Task 3.1's `<verify><automated>` block exit 0.
2. **Section co-occurrence (CACHE-04 gate):** Both `-NoClean` and `clean.ps1` appear within the "Workspace cleanup" section (verified via `awk` section extraction in `<acceptance_criteria>`).
3. **Diff scope (D-12):** `git diff --stat HEAD~ -- spec_classifier/docs/` lists ONLY `ONE_BUTTON_RUN.md` — no other doc files in `spec_classifier/docs/` are modified.
4. **D-22 guard:** `git diff --stat HEAD~ -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` outputs no rows.
5. **Goldens byte-equal:** `git diff --stat -- spec_classifier/golden/` outputs no rows.
6. **Pytest skip-ratio gate (manual smoke after running `pytest -q` from `spec_classifier/`):** session does NOT trip `conftest.py::pytest_sessionfinish` — record `passed/skipped/failed/xfailed` counts in SUMMARY (doc edits should be no-op for test discovery).
</verification>

<success_criteria>
1. The "What run.ps1 does" list has 8 steps with the new step 1 referencing the cleanup with `-NoClean` opt-out.
2. The "Useful run.ps1 switches" code block contains a `-NoClean` entry with a one-line description.
3. The "Workspace cleanup" section is rewritten so that `-NoClean` and `clean.ps1` co-occur (CACHE-04 acceptance gate).
4. No other file under `spec_classifier/docs/` is modified (D-12).
5. D-22 guard: zero diff inside protected paths.
6. Goldens byte-equal gate: zero diff under `spec_classifier/golden/`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cache-redirect/04-03-SUMMARY.md` describing:
- The 3 edits made to `ONE_BUTTON_RUN.md` (with line-range deltas pre/post).
- Pytest counts (`passed/skipped/failed/xfailed`) from `pytest -q` in `spec_classifier/`.
- Confirmation of D-22 guard, goldens byte-equal, skip-ratio gates, and the diff-scope guard (only ONE_BUTTON_RUN.md changed under `spec_classifier/docs/`).
- Any deviations from CONTEXT.md decisions (expected: none).
</output>
