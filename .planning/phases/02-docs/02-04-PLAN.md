---
phase: 02-docs
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - spec_classifier/README.md
autonomous: true
requirements: [DOC-02]
tags: [docs, spec-classifier-readme, drift-fix]
must_haves:
  truths:
    - "spec_classifier/README.md CLI usage examples match main.py argparse spec"
    - "spec_classifier/README.md config-layering description matches main.py:_load_config"
    - "spec_classifier/README.md output-paths section matches src/diagnostics/run_manager.py"
    - "spec_classifier/README.md golden workflow matches Makefile + main.py --save-golden / --update-golden"
    - "spec_classifier/README.md no longer references CURRENT_STATE.md (archived in 02-02)"
    - "spec_classifier/README.md every cross-reference points at an existing file"
  artifacts:
    - path: "spec_classifier/README.md"
      provides: "Refreshed deep CLI/config reference; drift fixes only; no structural rewrite"
      min_lines: 250
  key_links:
    - from: "spec_classifier/README.md"
      to: "spec_classifier/main.py"
      via: "CLI flags table cross-checked"
      pattern: "--vendor|--input|--batch-dir|--save-golden|--update-golden"
    - from: "spec_classifier/README.md"
      to: "spec_classifier/docs/"
      via: "every doc cross-reference resolves to an existing file"
      pattern: "docs/(user|dev|product|rules|taxonomy|schemas)/[A-Z_]+\\.md"
---

<objective>
Refresh `spec_classifier/README.md` (currently 286 lines) for accuracy against post-Phase-1 reality. Drift fixes only — no structural rewrite per D-13. Audit CLI usage, config layering, output paths, and golden workflow against live code; remove `CURRENT_STATE.md` references (archived in Plan 02-02); fix any broken cross-references; preserve `C:\venv` literal per Phase 1 D-04.

Purpose: DOC-02 — eliminate doc drift in the deep CLI/config reference. Verification gate (02-06) re-runs the README's Quick Start verbatim.

Output:
- spec_classifier/README.md (drift-fixed in place; no structural overhaul)
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs/02-CONTEXT.md
@.planning/phases/01-hygiene/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@spec_classifier/README.md
@spec_classifier/main.py
@spec_classifier/Makefile
@spec_classifier/config.local.yaml.example

<interfaces>
<!-- Cross-reference checklist: every claim in the README must match the live code. -->

CLI flags to cross-verify against `spec_classifier/main.py:main()` argparse block (~line 244):
- `--vendor` choices: `dell`, `cisco`, `hpe`, `lenovo`, `huawei`, `xfusion` (verify dynamic from config.yaml `vendor_rules`)
- `--input PATH` (single-file mode)
- `--batch-dir PATH` (batch mode)
- `--config PATH` (default `config.yaml`)
- `--output-dir PATH` (override config `paths.output_root`)
- `--save-golden` (no confirmation)
- `--update-golden` (interactive y/N)

Config layering reference: `spec_classifier/main.py:_load_config` (lines 69–86) — deep merge of `config.yaml` ← `config.local.yaml`.

Output path pattern reference: `spec_classifier/src/diagnostics/run_manager.py` — `run-YYYY-MM-DD__HH-MM-SS-<stem>` per file; `run-…-TOTAL/` aggregation.

Per-run artifacts list (must match): `classification.jsonl`, `run_summary.json`, `cleaned_spec.xlsx`, `<stem>_annotated.xlsx`, `<stem>_branded.xlsx` (Dell only), `unknown_rows.csv`, `header_rows.csv`, `rows_raw.json`, `rows_normalized.json`, `run.log`. Audit-injected: `<stem>_annotated_audited.xlsx`.

Make targets (verify present): `make generate_golden_dell`, `make generate_golden_cisco`, `make generate_golden_hpe`, `make generate_golden`, `make test`, `make test-unit`. Lenovo / Huawei / xFusion targets do NOT exist (per .planning/codebase/TESTING.md).

Section list expected to remain (current header order, unchanged structurally):
- Title (`# Spec Classifier (...)`)
- Requirements
- Virtual Environment
- Quick Start
- One-button run (Windows)
- Output (Per-run artifacts table, TOTAL folder)
- CLI Reference
- Vendor Support
- Test Data
- Running Tests
- Updating Golden
- Troubleshooting
- Audit & Cluster Analysis (`batch_audit.py`, `cluster_audit.py`, Recommended Workflow)
- Documentation (links)

Drift items known up-front:
1. Line 39 (Quick Start lead paragraph) — Russian: "По умолчанию вход и выход — каталоги …" — translate or rewrite in English.
2. Line 61 — Russian "Где искать результат: …" — translate.
3. Line 71 — Russian "Запускает тесты + batch-прогон всех вендоров …" — translate or rewrite.
4. Line 76 — Russian "Только тесты / Чистка мусора / Настройка путей" — translate.
5. Line 82+ — confirm "default `output` relative to cwd" — true given `config.yaml`.
6. Line 113 — `--vendor` choices list `{dell,cisco,hpe,lenovo,xfusion,huawei}` — verify all 6 present and order matches `main.py` (don't worry about order; just presence).
7. Lines 158, 186, 188, 222, 226, 239, 242, 245, 248, 266, 269, 272, 277 — `<USERNAME>` placeholder usage; confirm Phase 1 left these intact.
8. Lines 176, 188 — Russian fragments inside test/golden examples — translate.
9. Line 204 — Russian: "Cisco файл содержит другой лист" — translate.
10. Line 205 — Russian: "Cisco branded spec не создаётся" — translate.
11. Lines 286-287 — currently links to `docs/DOCS_INDEX.md` and `CHANGELOG.md`; verify both still resolve.
12. Confirm no reference to `CURRENT_STATE.md` exists (Plan 02-02 archived it). If found, remove or replace with archive pointer.
13. The "scripts/run_full.ps1" / "scripts/run_tests.ps1" sections (lines 67-77) reference scripts that may no longer exist — Phase 1 D-09 confirmed `scripts/clean.ps1` lives but did not enumerate other scripts. Verify against `spec_classifier/scripts/` tree before keeping or rewriting these references.

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Cross-reference audit + drift inventory of spec_classifier/README.md</name>
  <read_first>
    - spec_classifier/README.md (full file — 286 lines)
    - spec_classifier/main.py (lines 1-100 for imports, lines 240-300 for argparse / `_load_config`)
    - spec_classifier/Makefile (verify generate_golden_* and test* targets)
    - spec_classifier/config.local.yaml.example (verify config-layering description matches)
    - spec_classifier/scripts/ (verify which scripts exist; the README references `scripts/run_full.ps1` and `scripts/run_tests.ps1` — confirm presence or absence)
  </read_first>
  <action>
    Step 1: Inventory drift. Run these checks in PowerShell to enumerate gaps:
    ```powershell
    # Russian residue
    Select-String -Path spec_classifier/README.md -Pattern '[А-Яа-я]' | Select-Object LineNumber, Line
    # CURRENT_STATE refs (must be 0 or all replaced with archive pointer)
    Select-String -Path spec_classifier/README.md -Pattern 'CURRENT_STATE' -SimpleMatch
    # Cross-reference targets
    Select-String -Path spec_classifier/README.md -Pattern 'docs/[a-z]+/[A-Z_]+\.md' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique
    # Scripts references
    Select-String -Path spec_classifier/README.md -Pattern 'scripts/[a-z_]+\.ps1' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique
    # CLI flag enumeration
    Select-String -Path spec_classifier/README.md -Pattern '--[a-z][a-z-]+' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique
    ```

    Step 2: Cross-check each finding:
    - For each `docs/...md` reference: confirm `Test-Path spec_classifier/<path>` returns True.
    - For each `scripts/*.ps1` reference: confirm `Test-Path spec_classifier/<path>` returns True. If `scripts/run_full.ps1` and/or `scripts/run_tests.ps1` do NOT exist, the README's "One-button run" section must be rewritten or removed (the launchers moved to repo-root `run.ps1` long ago — verify by running `Test-Path spec_classifier/scripts/run_full.ps1`).
    - For each CLI flag: cross-check against `spec_classifier/main.py` argparse block. If a flag in the README is not in argparse, fix the README. If a flag in argparse is missing from the README's CLI Reference table, leave the omission for now unless it's a critical user-facing one (only `--save-golden` / `--update-golden` / `--vendor` / `--input` / `--batch-dir` / `--config` / `--output-dir` are user-facing).

    Step 3: Record findings in a temporary working note (do NOT commit). Move on to Task 4.2 with the inventory in hand.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $russian=(Select-String -Path 'spec_classifier/README.md' -Pattern '[А-Яа-я]' -AllMatches); if($russian){ Write-Output \"audit-finding: russian-residue lines: $($russian.LineNumber -join ',')\" }; $cs=(Select-String -Path 'spec_classifier/README.md' -Pattern 'CURRENT_STATE\.md' -SimpleMatch); if($cs){ Write-Output \"audit-finding: CURRENT_STATE.md references at lines: $($cs.LineNumber -join ',')\" }; $sf=Test-Path 'spec_classifier/scripts/run_full.ps1'; $st=Test-Path 'spec_classifier/scripts/run_tests.ps1'; Write-Output \"scripts/run_full.ps1: $sf\"; Write-Output \"scripts/run_tests.ps1: $st\"; Write-Output 'audit-complete'; exit 0"</automated>
  </verify>
  <done>Drift inventory complete: list of Russian-residue lines, CURRENT_STATE references, broken cross-references, and missing/present `scripts/*.ps1` paths captured for use in Task 4.2.</done>
</task>

<task type="auto">
  <name>Task 4.2: Apply drift fixes to spec_classifier/README.md</name>
  <read_first>
    - spec_classifier/README.md (full file)
    - Inventory captured in Task 4.1
    - .planning/phases/01-hygiene/01-CONTEXT.md (D-04: C:\venv stays literal)
    - .planning/phases/02-docs/02-CONTEXT.md (D-12 specific drift items; D-13 no structural rewrite)
  </read_first>
  <action>
    Apply targeted edits to `spec_classifier/README.md`. Do NOT restructure the file — section ordering and headings stay as listed in `<interfaces>` above. Each edit below is independent; apply all that the Task 4.1 inventory flagged.

    1. **Translate Russian fragments.** For each line surfaced by Task 4.1's Russian-residue probe, translate to English. Concrete known cases:
       - Quick Start lead paragraph (line 39): "По умолчанию вход и выход — каталоги `input` и `output` относительно текущей директории (или из config). Репозиторий остаётся только с кодом." → "By default, INPUT and OUTPUT are the directories `input/` and `output/` relative to the current directory (or as set in config). The repository stays code-only."
       - Quick Start trailing comments (lines 45-58): translate inline:
         - `# Создать папки (один раз)` → `# Create folders (one-time)`
         - `# Положить .xlsx в input/, затем:` → `# Drop .xlsx files into input/, then:`
         - `# Одиночный Dell` → `# Single Dell file`
         - `# Одиночный Cisco CCW` → `# Single Cisco CCW file`
         - `# Batch: все .xlsx из input` → `# Batch: every .xlsx in input`
       - Line 61 ("Где искать результат: …"): translate to "Where to find results: `output/dell_run/run-YYYY-MM-DD__HH-MM-SS-<stem>/` (Dell), `output/cisco_run/run-.../` (Cisco), or `output/hpe_run/run-.../` (HPE). Details: [docs/user/RUN_PATHS_AND_IO_LAYOUT.md](docs/user/RUN_PATHS_AND_IO_LAYOUT.md), [docs/user/CLI_CONFIG_REFERENCE.md](docs/user/CLI_CONFIG_REFERENCE.md)."
       - Line 71 ("Запускает тесты + batch-прогон …"): translate to "Runs the test suite + a batch pipeline pass over every vendor. Logs land in `temp_root/diag/runs/<timestamp>/` (`temp_root` from `config.local.yaml`)."
       - Line 76 ("Только тесты / Чистка мусора / Настройка путей"): translate to:
         - `Only tests: \`.\scripts\run_tests.ps1\``
         - `Clean transient artifacts: \`.\scripts\clean.ps1\``
         - `Configure paths: copy \`config.local.yaml.example\` → \`config.local.yaml\` and set your paths. Details: [docs/dev/ONE_BUTTON_RUN.md](docs/dev/ONE_BUTTON_RUN.md).`
       - Line 176 / 188 ("Cisco тесты"): "Cisco-specific tests"
       - Line 204 ("Cisco файл содержит другой лист"): "Cisco file contains a different sheet"
       - Line 205 ("Cisco branded spec не создаётся"): "Cisco branded spec is not generated"

    2. **One-button run section drift fix.** If Task 4.1 confirmed `spec_classifier/scripts/run_full.ps1` and `scripts/run_tests.ps1` DO NOT exist (which is the post-launcher-unification reality per commit `0080f45 build(launcher): unify run scripts into run.ps1 + PyQt6 GUI`), rewrite the "One-button run (Windows)" section (lines ~65–77) to point at the repo-root `run.ps1` instead:

       ```markdown
       ## One-button run (Windows)

       From the repo root:

       ```powershell
       ..\run.ps1                              # full pipeline + audit + cluster + pytest
       ..\run.ps1 -TestsOnly                   # pytest only
       ..\run.ps1 -Vendor huawei -NoAi -SkipTests   # smoke (single vendor, no AI)
       ```

       The orchestrator lives at the repo root, not under `spec_classifier/scripts/`. See [`../LAUNCHER_README.md`](../LAUNCHER_README.md) for full launcher details.

       Configure paths: copy `config.local.yaml.example` → `config.local.yaml` and set `paths.input_root` / `paths.output_root`. Details: [docs/dev/ONE_BUTTON_RUN.md](docs/dev/ONE_BUTTON_RUN.md).
       ```

       If `scripts/clean.ps1` DOES still exist (Phase 1 D-09 confirmed `clean.ps1` lives), keep its mention.

       If, against expectation, `scripts/run_full.ps1` exists, leave the section structure alone but apply the translation fixes from step 1.

    3. **Remove CURRENT_STATE.md references.** For every line surfaced by the `CURRENT_STATE.md` probe in Task 4.1, replace with one of:
       - Delete the reference outright if the surrounding sentence is purely about historical state.
       - Replace with `the project's GSD planning state in [.planning/STATE.md](../.planning/STATE.md)` if the surrounding sentence is about "where to find current project status".
       - Replace with `the historical snapshot at [.planning/archive/CURRENT_STATE-2026-05-10.md](../.planning/archive/CURRENT_STATE-2026-05-10.md)` if the surrounding sentence is about "the post-audit state at a given date".

    4. **Fix any broken cross-references.** For every `docs/<area>/<FILE>.md` link Task 4.1 surfaced as missing on disk, either correct the path or remove the broken link. The full set of files in `spec_classifier/docs/` per `.planning/codebase/STRUCTURE.md` is:
       ```
       docs/DOCS_INDEX.md
       docs/dev/CONTRIBUTING.md
       docs/dev/NEW_VENDOR_GUIDE.md
       docs/dev/ONE_BUTTON_RUN.md
       docs/dev/OPERATIONAL_NOTES.md
       docs/dev/TESTING_GUIDE.md
       docs/product/TECHNICAL_OVERVIEW.md
       docs/rules/RULES_AUTHORING_GUIDE.md
       docs/schemas/DATA_CONTRACTS.md
       docs/taxonomy/cycle2_summary.md
       docs/taxonomy/hw_type_taxonomy.md
       docs/user/CLI_CONFIG_REFERENCE.md
       docs/user/RUN_PATHS_AND_IO_LAYOUT.md
       docs/user/USER_GUIDE.md
       ```
       Any reference outside this set is broken — either fix or drop.

    5. **CLI Reference table sanity check.** Ensure the table at lines ~111–121 lists `--vendor` (with all 6 choices), `--input`, `--batch-dir`, `--config`, `--output-dir`, `--save-golden`, `--update-golden`. If the `--vendor` choices listed are stale (e.g., missing `xfusion` or `huawei`), update to the current 6.

    6. **Preserve `C:\venv` literal** per Phase 1 D-04. The "Virtual Environment" section (lines ~17–35) MUST keep `C:\venv` as the documented suggestion. Verify the framing is "default suggestion / override via config.local.yaml" — if any phrasing implies it's a hard requirement, soften it.

    7. **Length self-check.** Final length should be 250–290 lines. The file may shrink slightly (translated text is often shorter than Russian); that's fine. If it expands beyond 295, drop any redundant `(audit_1G P0-N)` markers from the prose.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='spec_classifier/README.md'; $lines=(Get-Content $f).Count; $c=Get-Content $f -Raw; $err=@(); if($lines -lt 240 -or $lines -gt 310){ $err += \"line-count: $lines (target 250-290; tolerance 240-310)\" }; if(($c | Select-String -Pattern '[А-Яа-я]' -AllMatches).Matches.Count -gt 0){ $err += 'Russian residue remaining' }; if($c -match 'CURRENT_STATE\.md' -and $c -notmatch '\.planning/archive/CURRENT_STATE'){ $err += 'CURRENT_STATE reference without archive pointer' }; foreach($t in '--vendor','--save-golden','--update-golden','--batch-dir','C:\venv','config.local.yaml.example','docs/DOCS_INDEX.md'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; $brokenLinks=@(); foreach($m in [regex]::Matches($c,'docs/[a-z]+/[A-Za-z_]+\.md')){ $p=\"spec_classifier/$($m.Value)\"; if(-not (Test-Path $p)){ $brokenLinks += $m.Value } }; if($brokenLinks.Count -gt 0){ $err += \"broken docs/ links: $($brokenLinks -join ',')\" }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <done>spec_classifier/README.md drift-fixed in place: zero Russian residue, no CURRENT_STATE.md references (or all replaced with archive pointer), every docs/* link resolves to a file on disk, CLI Reference table lists all 6 vendor choices, C:\venv preserved as suggestion, file length 250–290 lines, no structural rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → Quick Start commands | Risk: revised Quick Start commands behave differently from original |
| Author → cross-reference targets | Risk: replaced link points at non-existent file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | One-button run section rewrite | mitigate | Rewrite uses `..\run.ps1` invocation that has been live since commit `0080f45`. Plan 02-06 verification gate executes `..\run.ps1 -Vendor huawei -NoAi -SkipTests` from the repo root verbatim and PASS-WITH-CAVEAT for missing INPUT. |
| T-02-04-02 | Tampering | docs/* link replacements | mitigate | Task 4.2 step 4 cross-checks every docs/ link against the filesystem; verify `<automated>` script greps for any link target that does not exist. |
| T-02-04-03 | Information Disclosure | Translated Russian fragments | accept | Translations are mechanical (CLI commands) or descriptive (where to find output); no secrets introduced. |
| T-02-04-04 | Tampering | CLI flag drift | mitigate | Task 4.2 step 5 cross-checks against `main.py` argparse block. If a flag has changed name or default since the README was written, the rewrite reflects current code. |
</threat_model>

<verification>
After both tasks complete:

1. `wc -l spec_classifier/README.md` — 250–290 lines.
2. `Select-String -Path spec_classifier/README.md -Pattern '[А-Яа-я]' -AllMatches` returns 0 matches.
3. `Select-String -Path spec_classifier/README.md -Pattern 'CURRENT_STATE\.md' -SimpleMatch` returns 0 matches OR every match is preceded by the archive path `.planning/archive/CURRENT_STATE-2026-05-10.md`.
4. Every `docs/<area>/<FILE>.md` link resolves on disk.
5. `git grep -c 'C:\\Users\\G\\' spec_classifier/README.md` returns 0.
6. Goldens unchanged: `git diff --stat HEAD -- spec_classifier/golden/` empty.
</verification>

<success_criteria>
- spec_classifier/README.md drift fixed; English-only; cross-references intact; CURRENT_STATE references purged or pointed at archive; C:\venv preserved as suggestion; goldens untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-docs/02-04-SUMMARY.md` summarizing:
- Lines before / after
- Per-edit row: line-number, before-snippet, after-snippet (terse — one row per drift fix applied)
- Cross-reference verification table (every docs/ link checked, status PASS/FAIL/REPLACED)
- Confirmation that `C:\venv` and `<USERNAME>` placeholder usage is intact
</output>
