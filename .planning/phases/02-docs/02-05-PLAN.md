---
phase: 02-docs
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - spec_classifier/docs/DOCS_INDEX.md
  - spec_classifier/docs/dev/CONTRIBUTING.md
  - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md
  - spec_classifier/docs/dev/ONE_BUTTON_RUN.md
  - spec_classifier/docs/dev/OPERATIONAL_NOTES.md
  - spec_classifier/docs/dev/TESTING_GUIDE.md
  - spec_classifier/docs/product/TECHNICAL_OVERVIEW.md
  - spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md
  - spec_classifier/docs/schemas/DATA_CONTRACTS.md
  - spec_classifier/docs/taxonomy/cycle2_summary.md
  - spec_classifier/docs/taxonomy/hw_type_taxonomy.md
  - spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md
  - spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md
  - spec_classifier/docs/user/USER_GUIDE.md
  - .planning/phases/02-docs/02-DOC-AUDIT.md
  - .planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md
autonomous: true
requirements: [DOC-03]
tags: [docs, audit, docs-index, cross-references]
must_haves:
  truths:
    - "DOCS_INDEX.md is in 1:1 correspondence with spec_classifier/docs/**/*.md"
    - "Every cross-reference inside spec_classifier/docs/ resolves to an existing file"
    - "Every doc's factual claims about commands/flags/file paths match live code"
    - "Stale content has been rewritten (purpose still relevant) or deleted (purpose obsolete) per D-15"
    - "spec_classifier/docs/dev/CONTRIBUTING.md has a one-line note pointing to Phase 3 WF-02 per D-20"
    - "Per-doc findings captured in 02-DOC-AUDIT.md; CONTRIBUTING.md legacy assessment captured in 02-CONTRIBUTING-AUDIT.md"
    - "References to power_cord/_E8/core-parser/batch_audit-Excel/HW_TYPE_VOCAB-dup/YAML-rule-order are STILL ACCURATE (verified, not fixed) per D-21/D-22"
  artifacts:
    - path: "spec_classifier/docs/DOCS_INDEX.md"
      provides: "1:1 index of all 13 docs files; every entry resolves to an existing file"
      min_lines: 25
    - path: ".planning/phases/02-docs/02-DOC-AUDIT.md"
      provides: "Per-doc audit findings table"
      min_lines: 50
    - path: ".planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md"
      provides: "Legacy CONTRIBUTING.md assessment + Phase 3 flag"
      min_lines: 20
    - path: "spec_classifier/docs/dev/CONTRIBUTING.md"
      provides: "One-line Phase 3 WF-02 note added per D-20"
      contains: "Phase 3"
  key_links:
    - from: "spec_classifier/docs/DOCS_INDEX.md"
      to: "every file under spec_classifier/docs/"
      via: "Key Documents table"
      pattern: "docs/[a-z]+/[A-Z_a-z]+\\.md|^[a-zA-Z_]+\\.md"
---

<objective>
Audit each of the 13 doc files in `spec_classifier/docs/` end-to-end. For each: verify cross-references and factual claims; rewrite or delete stale content per the D-15 stale-content rule (rewrite if purpose still relevant; delete if obsolete). Update `DOCS_INDEX.md` so it ends Phase 2 in 1:1 correspondence with the `docs/` tree per D-16.

Special handling for `docs/dev/CONTRIBUTING.md` per D-18/D-19/D-20: read, assess if pre-GSD, flag in `02-CONTRIBUTING-AUDIT.md`, add a one-line note about Phase 3, do NOT delete or rewrite.

Per D-21/D-22: when a doc references load-bearing tech debt (power_cord, _E8, core/parser, batch_audit Excel, HW_TYPE_VOCAB dup, YAML rule order), VERIFY the references are still accurate but do NOT propose fixes; may add `.planning/codebase/CONCERNS.md` pointer.

Purpose: DOC-03 — eliminate doc drift across all 13 docs files; lock in the docs/ tree shape so Phase 3 WF-02 (root CONTRIBUTING.md) inherits a clean baseline.

Output:
- 13 doc files (some edited, some untouched — final `files_modified` is the planner's call after audit, but the frontmatter pre-lists all 13 because the audit must touch every one).
- DOCS_INDEX.md (rewritten 1:1 with tree)
- 02-DOC-AUDIT.md (per-doc findings)
- 02-CONTRIBUTING-AUDIT.md (legacy CONTRIBUTING flag)
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs/02-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@spec_classifier/docs/DOCS_INDEX.md
@spec_classifier/docs/dev/CONTRIBUTING.md
@spec_classifier/CLAUDE.md
@spec_classifier/CHANGELOG.md

<interfaces>
<!-- 13 doc files in scope (per D-14 + STRUCTURE.md tree). -->

| # | Path | Approx lines | Audience | Type |
|---|---|---|---|---|
| 1 | `spec_classifier/docs/DOCS_INDEX.md` | 41 | All | Index |
| 2 | `spec_classifier/docs/dev/CONTRIBUTING.md` | 75 | Dev | Legacy (D-18/D-20: flag, add 1-line note, do NOT rewrite) |
| 3 | `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` | 123 | Dev | Normative |
| 4 | `spec_classifier/docs/dev/ONE_BUTTON_RUN.md` | 40 | Dev | Normative |
| 5 | `spec_classifier/docs/dev/OPERATIONAL_NOTES.md` | 113 | Dev | Normative |
| 6 | `spec_classifier/docs/dev/TESTING_GUIDE.md` | 118 | Dev | Normative |
| 7 | `spec_classifier/docs/product/TECHNICAL_OVERVIEW.md` | 312 | Product | Normative |
| 8 | `spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md` | 154 | Rules authors | Normative |
| 9 | `spec_classifier/docs/schemas/DATA_CONTRACTS.md` | 232 | All | Normative |
| 10 | `spec_classifier/docs/taxonomy/cycle2_summary.md` | 103 | Taxonomy | Normative |
| 11 | `spec_classifier/docs/taxonomy/hw_type_taxonomy.md` | 462 | Taxonomy | Normative |
| 12 | `spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md` | 105 | User | Normative |
| 13 | `spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` | 281 | User | Normative |
| 14 | `spec_classifier/docs/user/USER_GUIDE.md` | 166 | User | Normative |

(14 entries; the table in DOCS_INDEX.md groups them differently — D-16 is structural, not numeric.)

Tech-debt references the audit MUST verify-but-not-fix (per D-21/D-22):
- `power_cord` `hw_type=None` (rules YAML files; batch_audit `_E8_NO_HW_TYPE_DEVICES`)
- `_E8_NO_HW_TYPE_DEVICES` whitelist
- `spec_classifier/src/core/parser.py` Dell-specificity
- `spec_classifier/batch_audit.py` reading from `*_annotated.xlsx`
- `HW_TYPE_VOCAB` duplication between `classifier.py:28` and `batch_audit.py:44`
- YAML rule order load-bearing first-match-wins

Phase 3 WF-01 hand-off: as the audit reads each doc, capture cross-references to retired prompts (`prompts/00..08`, `COWORK_OPUS_FULL_AUDIT.md`) so Phase 3 knows what to update. Surface in `02-DOC-AUDIT.md`.

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Audit each of the 13 docs end-to-end; capture findings in 02-DOC-AUDIT.md</name>
  <read_first>
    - Every doc listed in `<interfaces>` above (read each file in full — the audit must be complete per D-14).
    - .planning/codebase/CONCERNS.md (BLOCKER + IMPORTANT sections — for tech-debt verification)
    - .planning/codebase/CONVENTIONS.md (canonical business-rule wording)
    - spec_classifier/CLAUDE.md (post-Plan-02-01; cross-reference target for several docs)
    - spec_classifier/CHANGELOG.md (post-Plan-02-02; cross-reference target)
    - spec_classifier/main.py (CLI flags; cross-check CLI_CONFIG_REFERENCE.md and USER_GUIDE.md)
    - spec_classifier/Makefile (cross-check TESTING_GUIDE.md)
    - spec_classifier/conftest.py (skip-ratio gate; cross-check TESTING_GUIDE.md)
  </read_first>
  <action>
    For each doc in `<interfaces>` (excluding `CONTRIBUTING.md` — Task 5.3 handles it), read the file end-to-end and produce a row in `.planning/phases/02-docs/02-DOC-AUDIT.md`. Use this format (create the file if absent):

    ```markdown
    # 02 — Per-doc Audit Findings (DOC-03)

    **Audited:** 2026-05-10
    **Scope:** 13 files in spec_classifier/docs/

    | # | Path | Verdict | Drift items | Tech-debt refs verified | Action taken |
    |---|---|---|---|---|---|
    | 1 | `docs/DOCS_INDEX.md` | DRIFT | enum here | n/a | Task 5.2 rewrites |
    | 2 | `docs/dev/CONTRIBUTING.md` | LEGACY | enum here | n/a | Task 5.3 flags only |
    | … | … | … | … | … | … |

    ## Per-doc detail

    ### `docs/DOCS_INDEX.md`

    - Line N: <description of drift>
    - Line N: <description of drift>
    - Cross-references: [list every link, mark each PASS/BROKEN/UPDATED]
    - Russian residue: <line numbers or "none">
    - Tech-debt mentions: <which BLOCKER/IMPORTANT items referenced; verified accurate? yes/no>
    - Phase 3 hand-off (prompts/00..08 references): <list line numbers if any>

    ### `docs/dev/NEW_VENDOR_GUIDE.md`
    …
    ```

    Verdict values:
    - **CLEAN** — no drift; no edits needed.
    - **DRIFT** — content edits needed (Task 5.2 will apply per-file edits).
    - **LEGACY** — flag in 02-CONTRIBUTING-AUDIT.md (CONTRIBUTING.md only; D-18/D-20).
    - **OBSOLETE** — section purpose obsolete; recommend delete (apply per D-15).

    For each doc, perform these checks systematically:

    1. **Cross-references resolve.** Every `docs/<area>/<FILE>.md`, `../<file>`, `<file>.md` link must point at an existing file. Run:
       ```powershell
       Select-String -Path "spec_classifier/docs/<file>" -Pattern '\[[^\]]+\]\([^)]+\)' -AllMatches | ForEach-Object { $_.Matches.Value }
       ```
       Test each target with `Test-Path`.

    2. **Factual claims about CLI / file paths / flags.** Cross-reference:
       - CLI flags → `spec_classifier/main.py` argparse (around line 244).
       - Make targets → `spec_classifier/Makefile`.
       - Skip-ratio gate / pytest behavior → `spec_classifier/conftest.py:14` (`MAX_SKIP_RATIO = 0.50`).
       - Output run folder pattern → `spec_classifier/src/diagnostics/run_manager.py`.
       - Per-run artifacts list → cross-check against `.planning/codebase/STRUCTURE.md` "Output run folders" section.

    3. **Tech-debt reference verification (D-21/D-22).** For each protected item, find references in the doc and confirm they are still accurate:
       - `power_cord hw_type=None` → confirm `spec_classifier/rules/dell_rules.yaml:278` still has the comment and YAML still maps it to no `hw_type`.
       - `_E8_NO_HW_TYPE_DEVICES = {"power_cord", "enablement_kit"}` → confirm `spec_classifier/batch_audit.py:506` still has it (or near that line — line numbers may drift slightly across phases).
       - `core/parser.py` Dell-specificity → confirm the Dell-only sentinel `"Module Name"` still in `spec_classifier/src/core/parser.py`.
       - `batch_audit.py` Excel-reading → confirm `pd.read_excel` still in `spec_classifier/batch_audit.py:611` (or near).
       - `HW_TYPE_VOCAB` dup → confirm `HW_TYPE_VOCAB` still appears in BOTH `spec_classifier/src/core/classifier.py:28` AND `spec_classifier/batch_audit.py:44`.
       - YAML rule order → confirm the `# first match wins` / `# ORDER MATTERS` comments still in `spec_classifier/rules/dell_rules.yaml:617` and similar.

       If a doc claims any of these AND the claim is still accurate → mark VERIFIED in audit. If the doc proposes fixing one of them, that's drift in the wrong direction; the audit should NOT change the doc's framing per D-21 (the fix decisions belong to a future milestone). If the doc says "this was fixed" when it wasn't, that IS drift to fix in Task 5.2.

    4. **Russian residue.** `Select-String -Path "<file>" -Pattern '[А-Яа-я]'`. Note line numbers; Task 5.2 will translate.

    5. **References to retired prompts.** `Select-String -Path "<file>" -Pattern 'prompts/0[0-8]_|COWORK_OPUS_FULL_AUDIT' -AllMatches`. Surface for Phase 3 WF-01 hand-off; do NOT remove the references (Phase 3 owns prompts/).

    6. **References to CURRENT_STATE.md.** Already partially handled by Plan 02-02 / 02-04 but check each doc here too. Replace with archive pointer or remove (per Plan 02-02 Task 2.1's framing).

    Final output: `.planning/phases/02-docs/02-DOC-AUDIT.md` with one detail block per doc and the summary table at the top.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='.planning/phases/02-docs/02-DOC-AUDIT.md'; if(-not (Test-Path $f)){ $err += 'audit file missing' }; if(Test-Path $f){ $c=Get-Content $f -Raw; foreach($name in 'DOCS_INDEX.md','NEW_VENDOR_GUIDE.md','ONE_BUTTON_RUN.md','OPERATIONAL_NOTES.md','TESTING_GUIDE.md','TECHNICAL_OVERVIEW.md','RULES_AUTHORING_GUIDE.md','DATA_CONTRACTS.md','cycle2_summary.md','hw_type_taxonomy.md','CLI_CONFIG_REFERENCE.md','RUN_PATHS_AND_IO_LAYOUT.md','USER_GUIDE.md'){ if($c -notmatch [regex]::Escape($name)){ $err += \"missing audit row: $name\" } }; $lines=(Get-Content $f).Count; if($lines -lt 50){ $err += \"audit file too short: $lines lines\" } }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - 02-DOC-AUDIT.md exists at `.planning/phases/02-docs/02-DOC-AUDIT.md`
    - Summary table at top with one row per doc (≥ 13 rows for the non-CONTRIBUTING docs)
    - Per-doc detail block exists for each of the 13 docs in scope (DOCS_INDEX.md, NEW_VENDOR_GUIDE.md, ONE_BUTTON_RUN.md, OPERATIONAL_NOTES.md, TESTING_GUIDE.md, TECHNICAL_OVERVIEW.md, RULES_AUTHORING_GUIDE.md, DATA_CONTRACTS.md, cycle2_summary.md, hw_type_taxonomy.md, CLI_CONFIG_REFERENCE.md, RUN_PATHS_AND_IO_LAYOUT.md, USER_GUIDE.md)
    - Each detail block includes: cross-reference status (PASS/BROKEN/UPDATED per link), drift items list, tech-debt reference verification result (VERIFIED / DRIFT), Russian residue line numbers, Phase 3 hand-off list (retired-prompt references)
    - Verdict per doc is one of: CLEAN, DRIFT, LEGACY, OBSOLETE
    - File length ≥ 50 lines
    - No edits to actual doc files (audit is read-only — Task 5.2 applies fixes)
  </acceptance_criteria>
  <done>02-DOC-AUDIT.md exists with one entry per doc (≥ 13 entries). Each entry has cross-reference status, drift items list, tech-debt-reference verification result, Russian residue line numbers, Phase 3 hand-off list. Summary table at top.</done>
</task>

<task type="auto">
  <name>Task 5.2: Apply drift fixes to each doc per the audit; rewrite DOCS_INDEX.md to 1:1 with tree</name>
  <read_first>
    - .planning/phases/02-docs/02-DOC-AUDIT.md (Task 5.1's output — the inventory of edits to apply)
    - Each of the 13 doc files
    - .planning/codebase/STRUCTURE.md (canonical layout for cross-reference fixes)
  </read_first>
  <action>
    **Context-budget note (W-2 fix):** This plan's `files_modified` lists 16 files (13 docs end-to-end + DOCS_INDEX + 2 audit artifacts). To avoid context pressure mid-execution when applying drift fixes across all 13 docs in a single turn, **operate in batches of 4–5 docs per editor turn** and **commit per batch** with messages of the form:

    ```
    chore(02-05): docs/ audit drift fixes — batch N/M (DOC-03)
    ```

    Multiple commits within Task 5.2 are explicitly acceptable (overrides the implicit "one task = one commit" expectation for this task only). Suggested batches:
    - Batch 1/3: dev/ subtree (NEW_VENDOR_GUIDE, ONE_BUTTON_RUN, OPERATIONAL_NOTES, TESTING_GUIDE) — 4 files
    - Batch 2/3: user/ + product/ (CLI_CONFIG_REFERENCE, RUN_PATHS_AND_IO_LAYOUT, USER_GUIDE, TECHNICAL_OVERVIEW) — 4 files
    - Batch 3/3: rules/ + schemas/ + taxonomy/ + DOCS_INDEX (RULES_AUTHORING_GUIDE, DATA_CONTRACTS, cycle2_summary, hw_type_taxonomy, DOCS_INDEX) — 5 files

    The exact batching is the executor's call (boundaries can shift if the audit shows some docs need no edits). Plan 02-06's commit-grouping (Task 6.6 commit 5) absorbs all batch commits under DOC-03 — the gate verification looks at the cumulative diff, not per-batch. Per-batch commits get squashed by user discretion if desired (NOT this plan's job).

    For each doc with verdict `DRIFT` in the audit table, apply the per-doc edits captured in Task 5.1's audit detail block. Edit conservatively — drift fixes only, NOT structural rewrites (mirror Plan 02-04's discipline). Concrete patterns to apply across docs:

    1. **Translate any Russian residue to English** (mechanical translation, preserving identifiers and code blocks).

    2. **Replace `CURRENT_STATE.md` references** with one of:
       - Archive pointer `[.planning/archive/CURRENT_STATE-2026-05-10.md](../../.planning/archive/CURRENT_STATE-2026-05-10.md)` (note relative-path depth depends on file location — 3 `../` from `docs/dev/`, 3 `../` from `docs/user/`, etc.; verify each one).
       - Live status pointer `[.planning/STATE.md](../../.planning/STATE.md)`.
       - Outright removal if the surrounding sentence is purely historical and not load-bearing.

    3. **Fix broken cross-references** to point at existing files in the docs tree, OR drop the link.

    4. **Verify-don't-fix tech debt** (D-21/D-22) — leave the doc's existing wording alone for the 6 protected items; if a doc says "this is intentional" or "see CLAUDE.md / CONCERNS.md", that's correct framing and stays.

    5. **Per-doc specifics** (apply only if Task 5.1 audit verdict was DRIFT):

       - **DOCS_INDEX.md (full rewrite of the table to 1:1, per D-16):** Rewrite the `## Key Documents` table so every row points at an existing file. Add an entry for any docs file present in the tree but missing from the index. Remove any entry whose target does not exist. Keep entries for `CHANGELOG.md` (root-relative path: `../CHANGELOG.md`), `CLAUDE.md` (`../CLAUDE.md`), and `prompts/README.md` (Phase 3 will retire prompts; for Phase 2 the file still exists). Reference for the canonical 13-entry tree:
         ```
         docs/DOCS_INDEX.md (this file — self-reference optional)
         docs/dev/CONTRIBUTING.md
         docs/dev/NEW_VENDOR_GUIDE.md
         docs/dev/ONE_BUTTON_RUN.md
         docs/dev/OPERATIONAL_NOTES.md
         docs/dev/TESTING_GUIDE.md
         docs/product/TECHNICAL_OVERVIEW.md
         docs/rules/RULES_AUTHORING_GUIDE.md
         docs/schemas/DATA_CONTRACTS.md
         docs/taxonomy/cycle2_summary.md
         docs/taxonomy/hw_type_taxonomy.md
         docs/user/CLI_CONFIG_REFERENCE.md
         docs/user/RUN_PATHS_AND_IO_LAYOUT.md
         docs/user/USER_GUIDE.md
         ```
         The current `DOCS_INDEX.md` is missing entries for `cycle2_summary.md` (per audit confirmation in Task 5.1). Add it. Also confirm/add `cluster_audit.py` reference if the index lists scripts at all.
         Update the title from `# Documentation Index — Spec Classifier (Dell + Cisco CCW + HPE)` to `# Documentation Index — Spec Classifier` (the parenthetical is stale; the project supports 6 vendors now).

       - **NEW_VENDOR_GUIDE.md:** verify every step against `.planning/codebase/STRUCTURE.md` "New vendor (full recipe)". If steps are stale (e.g., still reference `VENDOR_EXTRA_COLS` instead of `get_extra_cols()`), fix per the structure doc. Do NOT change rule-id conventions (rules/ is out of scope per Phase 2).

       - **TESTING_GUIDE.md:** verify skip-ratio gate (`MAX_SKIP_RATIO = 0.50`), conftest behavior, Makefile targets. Fix any path that uses `C:\Users\G\` (should already be scrubbed by Phase 1 HYG-01).

       - **CLI_CONFIG_REFERENCE.md:** cross-check every CLI flag against `main.py` argparse. Update if a flag has been renamed or default changed.

       - **RUN_PATHS_AND_IO_LAYOUT.md:** verify the per-run folder pattern, TOTAL folder behavior, and per-run artifact list against `src/diagnostics/run_manager.py` and `STRUCTURE.md`. Per Phase 1 D-04, `C:\venv` references stay literal.

       - **USER_GUIDE.md:** verify vendor table matches the 6 active vendors; sheet/parser hints match.

       - **TECHNICAL_OVERVIEW.md:** verify pipeline-stages description matches `main.py:_run_single` and `.planning/codebase/ARCHITECTURE.md`. Verify references to `power_cord` / `_E8` / etc. are accurate.

       - **DATA_CONTRACTS.md:** verify `device_type` / `hw_type` lists match `HW_TYPE_VOCAB` (`spec_classifier/src/core/classifier.py:28`). The known `HW_TYPE_VOCAB` duplication between `classifier.py` and `batch_audit.py` is a tech-debt item; if the doc references it, leave the framing per D-22.

       - **RULES_AUTHORING_GUIDE.md:** verify rule_id format conventions, applies_to rules (`hw_type_rules.applies_to: [HW]` only). YAML rule order is load-bearing; if doc's wording on this is correct, do not touch.

       - **hw_type_taxonomy.md:** verify the 26 `hw_type` values match `HW_TYPE_VOCAB`. Cross-vendor divergences (Lenovo `bezel→accessory` vs HPE `bezel→chassis`) — verify framing matches `device_type_map` in YAML files.

       - **cycle2_summary.md:** verify any references to `verify_teresa_audit_actionables.py` resolve to the file (per Phase 1 D-09 it's kept).

       - **OPERATIONAL_NOTES.md:** verify operational commands match `run.ps1` flag set.

       - **ONE_BUTTON_RUN.md:** verify the one-button flow points at the repo-root `run.ps1` (the canonical orchestrator since `0080f45 build(launcher): unify run scripts into run.ps1 + PyQt6 GUI`). If still pointing at `scripts/run_full.ps1`, rewrite to point at `..\run.ps1`.

    6. **Length discipline.** Do not balloon any file. Drift fixes should leave file lengths within ±15% of the original.

    7. **Per-batch commit ritual** (W-2 fix): after each batch's edits, stage just that batch's files and commit:
       ```powershell
       git add <batch-N files>
       git commit -m "chore(02-05): docs/ audit drift fixes — batch N/M (DOC-03)"
       ```
       The batch number / total reflects the chosen split (e.g., 1/3, 2/3, 3/3). Plan 02-06's Task 6.6 commit 5 will see ALL batches collectively (it greps the cumulative diff of `spec_classifier/docs/` against the pre-Phase-2 baseline).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $tree=Get-ChildItem -Recurse 'spec_classifier/docs' -Filter '*.md' | ForEach-Object { $_.FullName.Replace((Resolve-Path 'spec_classifier/docs').Path + '\\', 'docs/').Replace('\\','/') }; $idx=Get-Content 'spec_classifier/docs/DOCS_INDEX.md' -Raw; foreach($p in $tree){ $name=Split-Path -Leaf $p; if($idx -notmatch [regex]::Escape($name)){ $err += \"DOCS_INDEX missing tree entry: $p\" } }; foreach($file in $tree){ $full = \"spec_classifier/$file\"; $c=Get-Content $full -Raw; foreach($m in [regex]::Matches($c, '\\]\\(([^)]+)\\)')){ $tgt=$m.Groups[1].Value; if($tgt -match '^https?://' -or $tgt -match '^#'){ continue }; if($tgt -match '\\.md(\\#.*)?$' -or $tgt -match '\\.py$' -or $tgt -match '\\.yaml$'){ $tgtFile=$tgt -replace '#.*$',''; $resolveBase=Split-Path -Parent $full; $resolved=Join-Path $resolveBase $tgtFile; try { $p2=Resolve-Path -ErrorAction Stop $resolved; } catch { $err += \"$full broken link: $tgt\" } } } }; if(($idx | Select-String -Pattern '[А-Яа-я]' -AllMatches).Matches.Count -gt 0){ $err += 'DOCS_INDEX has Russian residue' }; if($err.Count -gt 0){ $err | Select-Object -First 50 | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - DOCS_INDEX.md is 1:1 with the docs/ tree: every present file has an index entry; every index entry resolves to an existing file
    - Title `# Documentation Index — Spec Classifier (Dell + Cisco CCW + HPE)` updated to drop the stale parenthetical
    - All flagged docs (verdict DRIFT in 02-DOC-AUDIT.md) have drift fixes applied
    - Russian residue (`[А-Яа-я]`) gone from edited docs (excluding D-06/D-18-sanctioned exceptions: YAML sub-comments + CONTRIBUTING.md)
    - Cross-references resolve filesystem-wide: every `]([^)]+\.md)` in the docs tree points at an existing file
    - No `scripts/run_full.ps1` or `scripts/run_tests.ps1` references remain in `ONE_BUTTON_RUN.md` (rewritten to `..\run.ps1` per the launcher unification commit `0080f45`)
    - Tech-debt references (D-21/D-22 protected items) verified — NOT proposing fixes; framing preserved
    - Length discipline: every edited file is within ±15% of its pre-edit line count
    - **W-2 batch-commit allowance:** multiple commits with message pattern `chore(02-05): docs/ audit drift fixes — batch N/M (DOC-03)` are acceptable for this task; total batch commits matches the chosen M (e.g., 3 commits for a 3-batch split)
    - No edits outside `spec_classifier/docs/` (rules/, golden/, src/, tests/ all untouched per `git diff --stat HEAD -- spec_classifier/{rules,golden,src,tests}/` empty)
  </acceptance_criteria>
  <done>DOCS_INDEX.md is 1:1 with the docs/ tree (every present file listed; every listed file resolves). All flagged docs have drift fixes applied. Russian residue gone from edited docs. Cross-references resolve filesystem-wide. Tech-debt references verified (not fixed). Length discipline within ±15%. Per-batch commits acceptable per W-2 fix.</done>
</task>

<task type="auto">
  <name>Task 5.3: Flag legacy CONTRIBUTING.md per D-18/D-19/D-20; add Phase-3 forwarding note</name>
  <read_first>
    - spec_classifier/docs/dev/CONTRIBUTING.md (75 lines)
    - .planning/phases/02-docs/02-CONTEXT.md (D-18, D-19, D-20)
    - .planning/REQUIREMENTS.md (WF-02 — Phase 3 plan reference)
  </read_first>
  <action>
    Step 1: Read `spec_classifier/docs/dev/CONTRIBUTING.md` end-to-end. Assess:
    - Is the content pre-GSD? (Check for references to `prompts/0X` workflow, "Cowork", PRE-CHECK/MASTER-PLAN/etc.)
    - Does it reference any retired prompts directly?
    - What is its current scope (PR Definition of Done; repo-structure overview; style; versioning)?

    Step 2: Create `.planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md`:

    ```markdown
    # 02 — CONTRIBUTING.md Legacy Assessment (D-18 / D-19 / D-20)

    **Audited:** 2026-05-10
    **File:** `spec_classifier/docs/dev/CONTRIBUTING.md` (75 lines)

    ## Scope
    <one-paragraph summary of what the file covers — read end-to-end>

    ## Pre-GSD content?
    <yes/no with evidence — quote ≤2 lines>

    ## References to retired prompts
    <list line-numbers and the reference; if none, say "none">

    ## Russian residue
    <line-numbers; if none, say "none">

    ## Phase 3 hand-off
    Phase 3 WF-02 lands a NEW `CONTRIBUTING.md` at the repo root documenting the GSD development cycle. The retire/rewrite decision for `spec_classifier/docs/dev/CONTRIBUTING.md` belongs to Phase 3 per D-19. Phase 2's responsibilities here:
    - Read and assess (this document).
    - Add a one-line forwarding note to the file (Step 3 below).
    - Do NOT delete or restructure the file in Phase 2.
    - DOCS_INDEX.md continues to list it through Phase 2 per D-16.

    ## Recommendation to Phase 3
    <option A: archive verbatim under .planning/archive/ + delete; option B: rewrite as a stub forwarding to root CONTRIBUTING.md>
    ```

    Step 3: Edit `spec_classifier/docs/dev/CONTRIBUTING.md`. Add the following one-line note at the top of the file, immediately after the H1 title (`# Участие в разработке — spec_classifier (teresa)` — yes, the H1 is currently Russian; D-20 framing is to add a note, not to translate the existing file):

    ```
    > **Note:** This file describes pre-GSD conventions. The current GSD-native development cycle will be documented at `<repo-root>/CONTRIBUTING.md` in Phase 3 (WF-02). For now, refer to `spec_classifier/CLAUDE.md` § Development Cycle for the active workflow.
    ```

    Per D-20 the note is added VERBATIM to the file regardless of the file's other content. Per D-18 the file is otherwise untouched in Phase 2.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $a='.planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md'; if(-not (Test-Path $a)){ $err += 'audit file missing' }; if(Test-Path $a){ $c=Get-Content $a -Raw; foreach($t in 'Pre-GSD','Phase 3','D-18','D-19','D-20','References to retired prompts','Recommendation to Phase 3'){ if($c -notmatch [regex]::Escape($t)){ $err += \"audit file missing: $t\" } }; $lines=(Get-Content $a).Count; if($lines -lt 20){ $err += \"audit file too short: $lines\" } }; $f='spec_classifier/docs/dev/CONTRIBUTING.md'; $cf=Get-Content $f -Raw; foreach($t in 'Phase 3','WF-02','spec_classifier/CLAUDE.md'){ if($cf -notmatch [regex]::Escape($t)){ $err += \"contributing file missing: $t\" } }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - 02-CONTRIBUTING-AUDIT.md exists at `.planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md`
    - Audit file contains all required sections: Scope, Pre-GSD content?, References to retired prompts, Russian residue, Phase 3 hand-off, Recommendation to Phase 3
    - Audit file references decisions D-18, D-19, D-20 explicitly
    - Audit file length ≥ 20 lines
    - `spec_classifier/docs/dev/CONTRIBUTING.md` has the one-line Phase-3 forwarding note inserted immediately after the H1 title
    - The note contains the literal strings "Phase 3", "WF-02", "spec_classifier/CLAUDE.md"
    - The CONTRIBUTING.md file is otherwise byte-identical to its Phase-1 state — `git diff spec_classifier/docs/dev/CONTRIBUTING.md` shows exactly 1 insertion (the inserted note line, possibly plus a blank line for spacing)
  </acceptance_criteria>
  <done>02-CONTRIBUTING-AUDIT.md exists with all required sections; spec_classifier/docs/dev/CONTRIBUTING.md has the one-line Phase 3 note; the file is otherwise unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit edits → 13 doc files | Risk: drift fix accidentally introduces a different drift |
| DOCS_INDEX rewrite → user navigation | Risk: a missing index entry creates a perceived doc that does not exist; a phantom entry creates a broken link |
| CONTRIBUTING.md note → contributor confusion | Risk: contributor follows old file thinking it's canonical |
| Per-batch commits (W-2) → cumulative diff scope | Risk: a batch commit accidentally includes a file outside docs/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Per-doc drift fix | mitigate | Task 5.1 produces an explicit per-doc audit before Task 5.2 applies any edit. Verification gate (02-06) re-reads every doc end-to-end. Cross-reference filesystem check in Task 5.2's `<verify>` walks every link. |
| T-02-05-02 | Tampering | DOCS_INDEX 1:1 | mitigate | Task 5.2 verify script enumerates `spec_classifier/docs/**/*.md` from disk and confirms every present file has an entry; the verification gate (02-06) repeats this check end-of-phase. |
| T-02-05-03 | Repudiation | CONTRIBUTING.md edit | mitigate | Task 5.3 adds exactly the D-20-mandated one-line note; the file is otherwise byte-identical to its Phase-1 state. `git diff spec_classifier/docs/dev/CONTRIBUTING.md` should show only the inserted line. |
| T-02-05-04 | Information Disclosure | Audit file content | accept | The audit files (`02-DOC-AUDIT.md`, `02-CONTRIBUTING-AUDIT.md`) are internal planning artifacts under `.planning/phases/`; no secrets exposed. |
| T-02-05-05 | Tampering | Tech-debt reference change | mitigate | Per D-21/D-22, audit verifies tech-debt references are accurate but does NOT propose fixes. Task 5.1's audit detail block must mark each protected reference VERIFIED, not REWRITTEN. |
| T-02-05-06 | Tampering | Per-batch commit scope creep (W-2) | mitigate | Each batch commit uses explicit `git add <files>` listing only that batch's docs (NOT `git add .` / `git add -A`). Plan 02-06's Task 6.6 commit 5 verifies cumulative diff of `spec_classifier/docs/` against pre-Phase-2 baseline; any non-docs file would surface in the diff-review checkpoint (Task 6.4). |
</threat_model>

<verification>
After all three tasks complete:

1. `Get-ChildItem spec_classifier/docs -Recurse -Filter '*.md' | Measure-Object | Select-Object -ExpandProperty Count` returns 14 (or whatever the post-edit count is).
2. Every present `.md` file in `spec_classifier/docs/` is referenced from `DOCS_INDEX.md`.
3. Every link in `DOCS_INDEX.md` resolves to an existing file.
4. `Select-String -Path 'spec_classifier/docs/**/*.md' -Pattern '[А-Яа-я]' -AllMatches` returns at most the lines flagged in `02-DOC-AUDIT.md` as "Russian residue intentionally preserved" (e.g. CONTRIBUTING.md per D-18).
5. `Select-String -Path 'spec_classifier/docs/**/*.md' -Pattern 'CURRENT_STATE\.md' -SimpleMatch` returns 0 matches OR every match is preceded by the archive path `.planning/archive/CURRENT_STATE-2026-05-10.md`.
6. `git diff --stat HEAD -- spec_classifier/docs/dev/CONTRIBUTING.md` shows exactly 1 insertion (the Phase 3 note line).
7. `02-DOC-AUDIT.md` and `02-CONTRIBUTING-AUDIT.md` exist in `.planning/phases/02-docs/`.
8. Goldens unchanged: `git diff --stat HEAD -- spec_classifier/golden/` empty.
9. Per-batch commits (W-2): `git log --oneline | Select-String 'docs/ audit drift fixes — batch'` returns ≥ 1 line; each commit's diff scope is bounded to `spec_classifier/docs/`.
</verification>

<success_criteria>
- 13 docs audited end-to-end (CONTRIBUTING.md flagged-only); DOCS_INDEX 1:1 with tree; cross-references resolve; tech-debt verified-not-fixed; CONTRIBUTING.md has Phase-3 note; audit artifacts produced; goldens untouched. Per-batch commits permitted by W-2 fix.
</success_criteria>

<output>
After completion, create `.planning/phases/02-docs/02-05-SUMMARY.md` summarizing:
- 13-row audit table reproduced from 02-DOC-AUDIT.md
- DOCS_INDEX.md before/after entry list
- Files actually edited (subset of frontmatter `files_modified`)
- Files audited but unchanged (CLEAN verdict)
- Tech-debt references verified (one row per protected item × doc)
- Phase 3 hand-off list (cross-references to retired prompts surfaced)
- Per-batch commit list (W-2 fix): each batch commit SHA + scope
</output>
