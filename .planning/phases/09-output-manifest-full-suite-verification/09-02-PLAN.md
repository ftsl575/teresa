---
phase: 09-output-manifest-full-suite-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - spec_classifier/src/diagnostics/run_manager.py
  - spec_classifier/main.py
autonomous: true
requirements:
  - MANIFEST-01
must_haves:
  truths:
    - "run_manager.py exports write_manifest(output_root) helper with static hardcoded table"
    - "README.md is written at output_root on every main() invocation (both batch and single-file paths)"
    - "README.md purpose column text is in Russian (Cyrillic)"
    - "README.md groups rows under READY / SPLIT / AUDIT headings with ~14 pattern rows"
    - "Write is idempotent — re-runs overwrite with identical bytes"
    - "main.py calls write_manifest exactly once per invocation, after output_dir is resolved"
  artifacts:
    - path: "spec_classifier/src/diagnostics/run_manager.py"
      provides: "write_manifest helper beside create_spec_folder"
      contains: "def write_manifest(output_root"
    - path: "spec_classifier/main.py"
      provides: "write_manifest call wired once per invocation"
      contains: "write_manifest(output_dir)"
  key_links:
    - from: "spec_classifier/main.py"
      to: "run_manager.write_manifest"
      via: "import + call after output_dir resolved (line ~284)"
      pattern: "write_manifest(output_dir)"
    - from: "run_manager.write_manifest"
      to: "output_root/README.md"
      via: "Path(output_root) / 'README.md'; write_text with utf-8"
      pattern: "output_root / \"README.md\""
---

<objective>
MANIFEST-01: Add write_manifest(output_root) to run_manager.py and wire one call in main.py after output_dir is resolved.

Purpose: Every main() invocation writes a static README.md at output_root listing all v1.2 artifacts (9 SPLIT per-spec, 1 READY branded, 1 AUDIT per-spec, 3 AUDIT-root aggregates) grouped by bucket with purpose text in Russian. The manifest is static and idempotent — same bytes written every time.

Output:
- run_manager.py: new write_manifest function with hardcoded table
- main.py: import write_manifest; call once after output_dir is resolved
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/G/Desktop/teresa/.planning/ROADMAP.md
@C:/Users/G/Desktop/teresa/.planning/phases/09-output-manifest-full-suite-verification/09-CONTEXT.md

<interfaces>
<!-- Extracted from source files. Use these directly — no exploration needed. -->

From spec_classifier/src/diagnostics/run_manager.py (current full content — 41 lines):
```python
"""
Run folder management: create and prepare per-spec output directories.

Naming convention:
  Spec folder: <output_root>/<bucket>/<vendor>/<spec>/  (e.g. SPLIT/dell/dl1/)
"""

import shutil
from pathlib import Path


def create_spec_folder(output_root: Path, bucket: str, vendor: str, spec: str) -> Path:
    """
    Create (or wipe-and-recreate) <output_root>/<bucket>/<vendor>/<spec>/.
    ...
    """
    output_root = Path(output_root)
    folder = output_root / bucket / vendor / spec
    if not folder.resolve().is_relative_to(output_root.resolve()):
        raise ValueError(...)
    if folder.exists():
        shutil.rmtree(folder)
    folder.mkdir(parents=True)
    return folder
```
Note: If Plan 09-01 has already run, run_manager.py will also have import sys and detect_vendor_from_path. Re-read before editing.

From spec_classifier/main.py (key lines):
Line 18:  `from src.diagnostics.run_manager import create_spec_folder`
Lines 277-283: output_dir resolution (once, before batch/single dispatch)
Line 286: `if batch_dir is not None:` — batch path begins
Line 342: `return 1 if len(failed) > 0 else 0` — batch path ends
Line 353: `return _run_single(...)` — single-file path return

The write_manifest call must be placed AFTER output_dir is resolved (line 283) and BEFORE the `if batch_dir is not None:` branch (line 286). This single placement covers both batch and single-file paths.
</interfaces>

<artifact_semantics>
<!-- Source: spec_classifier/CLAUDE.md + .planning/PROJECT.md bucket semantics -->

READY bucket: clean human-facing Russian documents delivered to the customer.
SPLIT bucket: English technical/debug/DB-feed artifacts produced by the classifier.
AUDIT bucket: annotated_audited workbooks + batch/cluster audit reports.

Artifact roles (for Russian purpose text):
- Коммерческое предложение_<spec>.xlsx (READY): готовое коммерческое предложение для заказчика
- cleaned_spec.xlsx (SPLIT): очищенная спецификация (исходные данные без служебных строк)
- classification.jsonl (SPLIT): результаты классификации в формате JSONL
- <spec>_annotated.xlsx (SPLIT): аннотированная спецификация с классификацией строк
- rows_raw.json (SPLIT): сырые данные строк до нормализации
- rows_normalized.json (SPLIT): нормализованные данные строк
- run_summary.json (SPLIT): сводка по запуску (статистика, хэши файлов)
- unknown_rows.csv (SPLIT): строки с неизвестным типом сущности
- header_rows.csv (SPLIT): заголовочные строки из спецификации
- run.log (SPLIT): лог выполнения классификатора
- <spec>_annotated_audited.xlsx (AUDIT): аннотированная спецификация с результатами аудита
- audit_report.json (AUDIT root): отчёт аудита по всем спецификациям
- audit_summary.xlsx (AUDIT root): сводная таблица результатов аудита
- cluster_summary.xlsx (AUDIT root): сводка кластеризации неизвестных строк
</artifact_semantics>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write_manifest helper to run_manager.py</name>
  <files>spec_classifier/src/diagnostics/run_manager.py</files>
  <read_first>
    - spec_classifier/src/diagnostics/run_manager.py (full file — read before editing; it may have been modified by Plan 09-01)
    - .planning/phases/09-output-manifest-full-suite-verification/09-CONTEXT.md D-01, D-04, D-05, D-06
  </read_first>
  <action>
Read run_manager.py before editing to get the current state (it may already have detect_vendor_from_path from Plan 09-01).

Add `write_manifest` after all existing functions (at the end of the file). The function takes a single `output_root: Path` argument, coerces it with `Path(output_root)`, writes `output_root / "README.md"` using `write_text(encoding="utf-8")`, and returns None.

The EXACT content to write to README.md (hardcoded, static — do NOT generate dynamically):

```markdown
# Выходные артефакты классификатора

Папка содержит результаты классификации аппаратных спецификаций.
Артефакты сгруппированы по трём директориям (`READY/`, `SPLIT/`, `AUDIT/`).
Структура вложения: `<bucket>/<vendor>/<spec>/`.

---

## READY — Готовые документы для заказчика

| Файл | Директория | Назначение |
|------|-----------|------------|
| `Коммерческое предложение_<spec>.xlsx` | `READY/<vendor>/<spec>/` | Готовое коммерческое предложение для заказчика |

---

## SPLIT — Технические артефакты классификации

| Файл | Директория | Назначение |
|------|-----------|------------|
| `cleaned_spec.xlsx` | `SPLIT/<vendor>/<spec>/` | Очищенная спецификация (исходные данные без служебных строк) |
| `classification.jsonl` | `SPLIT/<vendor>/<spec>/` | Результаты классификации строк в формате JSONL |
| `<spec>_annotated.xlsx` | `SPLIT/<vendor>/<spec>/` | Аннотированная спецификация с классификацией каждой строки |
| `rows_raw.json` | `SPLIT/<vendor>/<spec>/` | Сырые данные строк до нормализации |
| `rows_normalized.json` | `SPLIT/<vendor>/<spec>/` | Нормализованные данные строк |
| `run_summary.json` | `SPLIT/<vendor>/<spec>/` | Сводка по запуску (статистика, хэши файлов) |
| `unknown_rows.csv` | `SPLIT/<vendor>/<spec>/` | Строки с неопределённым типом сущности |
| `header_rows.csv` | `SPLIT/<vendor>/<spec>/` | Заголовочные строки из спецификации |
| `run.log` | `SPLIT/<vendor>/<spec>/` | Лог выполнения классификатора |

---

## AUDIT — Результаты аудита и кластеризации

| Файл | Директория | Назначение |
|------|-----------|------------|
| `<spec>_annotated_audited.xlsx` | `AUDIT/<vendor>/<spec>/` | Аннотированная спецификация с результатами E-code аудита |
| `audit_report.json` | `AUDIT/` | Сводный отчёт аудита по всем спецификациям |
| `audit_summary.xlsx` | `AUDIT/` | Таблица результатов аудита (все ошибки по всем файлам) |
| `cluster_summary.xlsx` | `AUDIT/` | Сводка кластеризации неизвестных и неклассифицированных строк |
```

The function implementation to add to run_manager.py:

```python
# README content is defined at module level so write_manifest stays a thin writer.
_MANIFEST_CONTENT = """\
# Выходные артефакты классификатора

Папка содержит результаты классификации аппаратных спецификаций.
Артефакты сгруппированы по трём директориям (`READY/`, `SPLIT/`, `AUDIT/`).
Структура вложения: `<bucket>/<vendor>/<spec>/`.

---

## READY — Готовые документы для заказчика

| Файл | Директория | Назначение |
|------|-----------|------------|
| `Коммерческое предложение_<spec>.xlsx` | `READY/<vendor>/<spec>/` | Готовое коммерческое предложение для заказчика |

---

## SPLIT — Технические артефакты классификации

| Файл | Директория | Назначение |
|------|-----------|------------|
| `cleaned_spec.xlsx` | `SPLIT/<vendor>/<spec>/` | Очищенная спецификация (исходные данные без служебных строк) |
| `classification.jsonl` | `SPLIT/<vendor>/<spec>/` | Результаты классификации строк в формате JSONL |
| `<spec>_annotated.xlsx` | `SPLIT/<vendor>/<spec>/` | Аннотированная спецификация с классификацией каждой строки |
| `rows_raw.json` | `SPLIT/<vendor>/<spec>/` | Сырые данные строк до нормализации |
| `rows_normalized.json` | `SPLIT/<vendor>/<spec>/` | Нормализованные данные строк |
| `run_summary.json` | `SPLIT/<vendor>/<spec>/` | Сводка по запуску (статистика, хэши файлов) |
| `unknown_rows.csv` | `SPLIT/<vendor>/<spec>/` | Строки с неопределённым типом сущности |
| `header_rows.csv` | `SPLIT/<vendor>/<spec>/` | Заголовочные строки из спецификации |
| `run.log` | `SPLIT/<vendor>/<spec>/` | Лог выполнения классификатора |

---

## AUDIT — Результаты аудита и кластеризации

| Файл | Директория | Назначение |
|------|-----------|------------|
| `<spec>_annotated_audited.xlsx` | `AUDIT/<vendor>/<spec>/` | Аннотированная спецификация с результатами E-code аудита |
| `audit_report.json` | `AUDIT/` | Сводный отчёт аудита по всем спецификациям |
| `audit_summary.xlsx` | `AUDIT/` | Таблица результатов аудита (все ошибки по всем файлам) |
| `cluster_summary.xlsx` | `AUDIT/` | Сводка кластеризации неизвестных и неклассифицированных строк |
"""


def write_manifest(output_root: Path) -> None:
    """Write a static README.md artifact index at output_root.

    The table lists every v1.2 artifact type grouped under READY / SPLIT / AUDIT,
    with file pattern, bucket/path, and purpose in Russian.

    Idempotent: subsequent calls overwrite the file with identical bytes.

    Args:
        output_root: top-level output directory (sibling of READY/, SPLIT/, AUDIT/).
    """
    output_root = Path(output_root)
    output_root.mkdir(parents=True, exist_ok=True)
    readme = output_root / "README.md"
    readme.write_text(_MANIFEST_CONTENT, encoding="utf-8")
```

Important notes:
- `_MANIFEST_CONTENT` is a module-level constant string — defined BEFORE `write_manifest`.
- `output_root.mkdir(parents=True, exist_ok=True)` ensures the directory exists before writing (defensive; output_dir is always created by main.py before this call, but belt-and-suspenders is fine here).
- No dynamic scanning, no config access, no imports beyond what's already in run_manager.py.
  </action>
  <verify>
    <automated>
      cd C:\Users\G\Desktop\teresa\spec_classifier && python -c "
from pathlib import Path
import tempfile, os
from src.diagnostics.run_manager import write_manifest
with tempfile.TemporaryDirectory() as tmp:
    p = Path(tmp)
    write_manifest(p)
    readme = p / 'README.md'
    assert readme.exists(), 'README.md not created'
    content = readme.read_text(encoding='utf-8')
    # bucket headings
    assert '## READY' in content, 'READY section missing'
    assert '## SPLIT' in content, 'SPLIT section missing'
    assert '## AUDIT' in content, 'AUDIT section missing'
    # Russian characters present (Cyrillic)
    assert any(ord(c) > 127 for c in content), 'No Russian text found'
    # ~14 rows: count table rows (lines with | that are not header/separator rows)
    rows = [l for l in content.splitlines() if l.startswith('|') and '---' not in l and 'Файл' not in l]
    assert len(rows) >= 14, f'Expected >=14 artifact rows, got {len(rows)}'
    # idempotent
    write_manifest(p)
    assert readme.read_text(encoding='utf-8') == content, 'Not idempotent'
print('write_manifest: OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - run_manager.py contains `def write_manifest(output_root` (grep confirms)
    - run_manager.py contains `_MANIFEST_CONTENT` module-level constant
    - README.md content has sections: `## READY`, `## SPLIT`, `## AUDIT`
    - Content contains at least 14 table data rows (one per artifact type)
    - Content contains Russian/Cyrillic text in the Назначение column
    - Idempotency verified: second call produces identical bytes
    - No new external imports added (only Path from pathlib, already present)
  </acceptance_criteria>
  <done>write_manifest helper added to run_manager.py; smoke test passes; ~14 artifact rows in Russian.</done>
</task>

<task type="auto">
  <name>Task 2: Wire write_manifest call in main.py</name>
  <files>spec_classifier/main.py</files>
  <read_first>
    - spec_classifier/main.py lines 14-22 (import block — to extend with write_manifest)
    - spec_classifier/main.py lines 277-295 (output_dir resolution + batch branch start — exact call site)
    - .planning/phases/09-output-manifest-full-suite-verification/09-CONTEXT.md D-02, D-03
  </read_first>
  <action>
TWO EDITS:

**Edit 1: Update the import at line 18**

Current (line 18):
```python
from src.diagnostics.run_manager import create_spec_folder
```

Change to:
```python
from src.diagnostics.run_manager import create_spec_folder, write_manifest
```

**Edit 2: Add write_manifest call after output_dir resolution**

The output_dir is resolved at line 283:
```python
output_dir = Path(output_dir_raw) if Path(output_dir_raw).is_absolute() else _resolve_path(output_dir_raw, cwd)
```

The `if batch_dir is not None:` block starts at line 286 (a blank line at 284, then comment at 285).

Insert the call at line 284 (after the output_dir assignment, before the blank line / batch branch check):
```python
    write_manifest(output_dir)
```

Full context around the insertion point (after edit):
```python
    output_dir = Path(output_dir_raw) if Path(output_dir_raw).is_absolute() else _resolve_path(output_dir_raw, cwd)
    write_manifest(output_dir)

    # Batch mode: --batch-dir <path> or --batch (use input_root from config or default)
    if args.batch_dir:
```

This placement ensures:
- write_manifest is called exactly once per main() invocation (D-03)
- It runs for both batch mode (exits at line 342) and single-file mode (exits at line 353)
- output_dir is already a fully resolved Path at the call site
- It runs before any per-spec work, so README.md is always present even if classify fails partway through
  </action>
  <verify>
    <automated>
      cd C:\Users\G\Desktop\teresa\spec_classifier && python -c "
import ast, pathlib
src = pathlib.Path('main.py').read_text()
assert 'write_manifest' in src, 'write_manifest not in main.py'
assert 'from src.diagnostics.run_manager import create_spec_folder, write_manifest' in src, 'import line not updated'
# Confirm exactly one call site
call_count = src.count('write_manifest(output_dir)')
assert call_count == 1, f'Expected 1 write_manifest call, found {call_count}'
print('main.py wiring: OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "write_manifest" spec_classifier/main.py` shows the import line and exactly 1 call site
    - Import line reads: `from src.diagnostics.run_manager import create_spec_folder, write_manifest`
    - Call is `write_manifest(output_dir)` placed after output_dir is resolved and before the `if batch_dir is not None:` branch
    - `python -c "import main"` from spec_classifier/ exits without ImportError
    - `grep -c "write_manifest" spec_classifier/main.py` returns 2 (1 import + 1 call)
  </acceptance_criteria>
  <done>main.py imports write_manifest and calls it exactly once after output_dir resolution; covers both batch and single-file paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| output_dir (resolved Path) → filesystem write | The write target is output_root/README.md, derived from the operator-controlled output_dir already resolved by main.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | write_manifest path resolution | accept | output_root is the operator-controlled output directory already resolved by main.py from config or --output-dir. No path traversal: the write target is always `output_root / "README.md"` — a fixed filename, not user-supplied. The function does not accept a filename parameter. |
| T-09-05 | Tampering | Static manifest content | accept | Content is a module-level constant string — not read from any file, not user-supplied, not interpolated from runtime data. The table cannot be injected or tampered with at runtime. |
| T-09-06 | Denial of Service | write_manifest overwrites README.md | accept | write_manifest is idempotent — overwrites with identical bytes on re-runs. If README.md exists with different content (e.g. operator-placed custom file), it is overwritten. This is the documented and intended behavior (D-01, D-02: static, idempotent). Operator documentation should note this. |
</threat_model>

<verification>
After both tasks complete:

```powershell
# 1. Confirm run_manager.py has both new functions
cd C:\Users\G\Desktop\teresa\spec_classifier
python -c "from src.diagnostics.run_manager import write_manifest, create_spec_folder; print('imports OK')"

# 2. Confirm main.py has exactly one write_manifest call
python -c "
import pathlib
src = pathlib.Path('main.py').read_text()
print('write_manifest calls:', src.count('write_manifest(output_dir)'))
print('import present:', 'write_manifest' in src)
"

# 3. Smoke test the writer
python -c "
from pathlib import Path
import tempfile
from src.diagnostics.run_manager import write_manifest
with tempfile.TemporaryDirectory() as tmp:
    write_manifest(Path(tmp))
    content = (Path(tmp) / 'README.md').read_text(encoding='utf-8')
    print('README.md created, length:', len(content))
    print('READY section:', '## READY' in content)
    print('SPLIT section:', '## SPLIT' in content)
    print('AUDIT section:', '## AUDIT' in content)
    print('Russian text:', any(ord(c) > 127 for c in content))
"
```
</verification>

<success_criteria>
1. run_manager.py exports write_manifest(output_root) with static ~14-row Russian-purpose table.
2. main.py imports and calls write_manifest(output_dir) exactly once, after output_dir resolution, covering both batch and single-file invocation paths.
3. README.md content: three bucket sections (READY/SPLIT/AUDIT), ~14 artifact rows, Russian Назначение column.
4. Idempotent: re-running main.py overwrites README.md with identical bytes.
5. No new external dependencies. No classification/audit logic changed. Goldens byte-equal.
</success_criteria>

<output>
After completion, create `.planning/phases/09-output-manifest-full-suite-verification/09-02-SUMMARY.md`
</output>
