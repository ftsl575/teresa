---
phase: 03-workflow
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - CONTRIBUTING.md
  - spec_classifier/docs/dev/CONTRIBUTING.md
  - .planning/archive/CONTRIBUTING-2026-05-10.md
  - spec_classifier/CLAUDE.md
  - CLAUDE.md
  - spec_classifier/docs/DOCS_INDEX.md
  - spec_classifier/CHANGELOG.md
autonomous: true
requirements: [WF-02]
tags: [workflow, contributing, claude-md, docs-index, changelog, cross-ref-cleanup]
must_haves:
  truths:
    - "/CONTRIBUTING.md exists at the repo root and is 150–200 lines (D-10), tool-agnostic (D-11), Windows-only-this-milestone-mention (D-12)"
    - "/CONTRIBUTING.md has 8 sections in order per D-10: what-this-repo-is, Development cycle, Tests, Adding a vendor, PR workflow, Code style, Do not fix, Where to look first"
    - "/CONTRIBUTING.md § Development cycle uses literal command-by-command format per D-13 (4 numbered commands + /gsd-help footnote)"
    - "/CONTRIBUTING.md § Tests cites pytest invocation from spec_classifier/ + the skipped/total > 0.50 skip-ratio gate (spec_classifier/conftest.py:pytest_sessionfinish) per D-10 §3"
    - "/CONTRIBUTING.md § Adding a vendor is pointer-only to spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md per D-14 (no duplication)"
    - "/CONTRIBUTING.md § Do not fix lists all 5 protected items verbatim per D-15 (power_cord, core/parser.py, batch_audit.py, YAML rule order, HW_TYPE_VOCAB) + back-pointer to .planning/codebase/CONCERNS.md"
    - "spec_classifier/docs/dev/CONTRIBUTING.md no longer exists in the working tree (git mv to .planning/archive/CONTRIBUTING-2026-05-10.md per D-06)"
    - "spec_classifier/CLAUDE.md legacy block (Tool Roles, Development Cycle table, Hard Rules R1-R5, Recommended Models, Prompts—Location, ~63 lines at lines 245–307) is replaced with a 3-line pointer to /CONTRIBUTING.md per D-16; the 5 critical business rules and earlier sections (Project, Paths, Current State, Tests, INPUT/OUTPUT layout, Key repo files, CLI Commands, Business Rules, E-codes, Known Tech Debt) stay untouched"
    - "Root CLAUDE.md § Tooling roles (lines 71–74) replaced with one-sentence pointer per D-17; section header '## Tooling roles' kept; .cursor/ informational note kept"
    - "spec_classifier/docs/DOCS_INDEX.md line 27 (CONTRIBUTING.md row) and line 36 (prompts/README.md row) dropped per D-07/D-08; new 'See also: /CONTRIBUTING.md' breadcrumb under § Conventions per D-09"
    - "spec_classifier/CHANGELOG.md historical lines mentioning prompts/ untouched per D-18; new entry added under [Unreleased] noting the 3 Phase-3 changes per D-19"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Root canonical contributor doc — GSD cycle, tests, vendor pointer, PR workflow, code style, do-not-fix, where-to-look (D-10..D-15)"
      min_lines: 150
      contains: "/gsd-discuss-phase"
      contains: "/gsd-plan-phase"
      contains: "/gsd-execute-phase"
      contains: "/gsd-verify-work"
      contains: "/gsd-help"
      contains: "skipped/total"
      contains: "spec_classifier/conftest.py:pytest_sessionfinish"
      contains: "NEW_VENDOR_GUIDE.md"
      contains: "power_cord"
      contains: ".planning/codebase/CONCERNS.md"
    - path: ".planning/archive/CONTRIBUTING-2026-05-10.md"
      provides: "Verbatim archive of the 77-line legacy inner CONTRIBUTING.md (D-06 — git mv preserves history)"
      min_lines: 70
    - path: "spec_classifier/CLAUDE.md"
      provides: "Updated deep ref — legacy block replaced with 3-line pointer to /CONTRIBUTING.md per D-16; earlier sections untouched"
      contains: "## Development Cycle"
      contains: "/CONTRIBUTING.md"
    - path: "CLAUDE.md"
      provides: "Updated root ref — § Tooling roles sentence replaced per D-17"
      contains: "Discuss → Plan → Execute → Verify, GSD-native"
      contains: "[`/CONTRIBUTING.md`](CONTRIBUTING.md)"
    - path: "spec_classifier/docs/DOCS_INDEX.md"
      provides: "Cleaned index — CONTRIBUTING + prompts/README rows dropped (D-07/D-08); See-also breadcrumb added under § Conventions (D-09)"
      contains: "/CONTRIBUTING.md"
    - path: "spec_classifier/CHANGELOG.md"
      provides: "New [Unreleased] entry per D-19 noting the 3 Phase-3 changes; historical lines untouched per D-18"
      contains: "prompts-2026-05-10"
      contains: "CONTRIBUTING-2026-05-10"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md"
      via: "§ Adding a vendor pointer (D-14)"
      pattern: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE\\.md"
    - from: "CONTRIBUTING.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "§ Do not fix back-pointer (D-15)"
      pattern: "\\.planning/codebase/CONCERNS\\.md"
    - from: "spec_classifier/CLAUDE.md"
      to: "CONTRIBUTING.md"
      via: "Replaced § Development Cycle 3-line pointer (D-16)"
      pattern: "\\.\\./CONTRIBUTING\\.md"
    - from: "CLAUDE.md"
      to: "CONTRIBUTING.md"
      via: "§ Tooling roles 1-line pointer (D-17)"
      pattern: "CONTRIBUTING\\.md"
    - from: "spec_classifier/docs/DOCS_INDEX.md"
      to: "CONTRIBUTING.md"
      via: "See-also breadcrumb under § Conventions (D-09)"
      pattern: "CONTRIBUTING\\.md"
---

<objective>
Author the root `/CONTRIBUTING.md` per D-10..D-15 (~150–200 lines, English, tool-agnostic, GSD-cycle command-by-command, pytest + skip-ratio gate, vendor pointer-only, "do not fix" verbatim from PROJECT.md / CONCERNS.md, where-to-look table) AND complete the cross-reference cleanup so no doc points at a retired file. The cleanup spans five touch-points:

- `git mv spec_classifier/docs/dev/CONTRIBUTING.md → .planning/archive/CONTRIBUTING-2026-05-10.md` (D-06)
- replace the legacy block in `spec_classifier/CLAUDE.md` with a 3-line pointer to `/CONTRIBUTING.md` (D-16)
- replace the legacy step-list sentence in root `CLAUDE.md` § Tooling roles with a 1-line pointer (D-17)
- drop `DOCS_INDEX.md` lines 27 + 36 + add a single "See also" breadcrumb under § Conventions (D-07/D-08/D-09)
- add a new `[Unreleased]` entry to `CHANGELOG.md` per D-19 (historical lines untouched per D-18)

Purpose: WF-02 — make GSD the single canonical development cycle by landing the contributor doc that documents it, AND ensuring no surviving doc references the retired prompts/ folder or the retired inner CONTRIBUTING.md. Honors D-22/D-23: "do not fix" items are referenced verbatim in `/CONTRIBUTING.md`, NOT touched in code.

Output:
- `/CONTRIBUTING.md` (new, 150–200 lines)
- `.planning/archive/CONTRIBUTING-2026-05-10.md` (the renamed legacy 77-line file; verbatim Russian preserved)
- `spec_classifier/CLAUDE.md` (legacy block replaced; ~63 lines deleted, 5 lines added)
- `CLAUDE.md` (root § Tooling roles 1-line replacement)
- `spec_classifier/docs/DOCS_INDEX.md` (2 rows dropped, 1 breadcrumb added)
- `spec_classifier/CHANGELOG.md` (new [Unreleased] sub-entry under § Changed)

**Task ordering rationale:** all six tasks are independent enough to run in any order, but the recommended order — Task 2.1 (mv inner CONTRIBUTING) → Task 2.2 (write root CONTRIBUTING) → Task 2.3 (deep CLAUDE) → Task 2.4 (root CLAUDE) → Task 2.5 (DOCS_INDEX) → Task 2.6 (CHANGELOG) — front-loads the file moves so the cross-references in Tasks 2.3–2.5 can target a stable file set. The plan ships as a single commit per WF-02 (one-commit-per-requirement, Phase 1/2 convention).
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow/03-CONTEXT.md
@.planning/phases/03-workflow/03-01-PLAN.md
@.planning/phases/02-docs/02-SUMMARY.md
@.planning/phases/02-docs/02-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@spec_classifier/CLAUDE.md
@spec_classifier/docs/DOCS_INDEX.md
@spec_classifier/CHANGELOG.md
@spec_classifier/docs/dev/CONTRIBUTING.md
@spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md

<interfaces>
<!-- D-15 verbatim "do not fix" content (locked source = .planning/codebase/CONCERNS.md § BLOCKER + IMPORTANT one-liners). -->
<!-- The 5 items below appear in /CONTRIBUTING.md § Do not fix as a bulleted list, in this order. -->

1. **`power_cord` has `hw_type=None` intentionally.** Recovery commit `c3c7cb6 fix(taxonomy): restore power_cord hw_type=None` exists. Do NOT add it to `device_type_map` or remove it from `_E8_NO_HW_TYPE_DEVICES`.
2. **`spec_classifier/src/core/parser.py` is Dell-specific despite living in `core/`.** Sentinel `"Module Name"` is hard-coded; other vendors use their own `parser.py` under `src/vendors/<v>/`. Standalone refactor only — out of scope of any unrelated PR.
3. **`spec_classifier/batch_audit.py` reads from `*_annotated.xlsx` instead of `classification.jsonl`.** Excel leakage; Project CLAUDE.md explicitly says do not "fix" as part of unrelated work — needs dedicated migration.
4. **YAML rule order is load-bearing (first-match-wins).** Never `sort` rule blocks alphabetically. Cosmetic reorders silently flip classification.
5. **`HW_TYPE_VOCAB` is duplicated between `classifier.py` and `batch_audit.py`.** Tracked in CONCERNS.md but not selected for cleanup milestones.

> Full rationale and context: [.planning/codebase/CONCERNS.md](.planning/codebase/CONCERNS.md) § BLOCKER + IMPORTANT.

<!-- D-13 GSD-cycle section content (literal command-by-command, locked). -->

```
1. Discuss — `/gsd-discuss-phase <N>`. Clarifies HOW to implement what's in ROADMAP.md for phase N.
2. Plan    — `/gsd-plan-phase <N>`. Produces NN-PP-PLAN.md task lists with verification gates.
3. Execute — `/gsd-execute-phase <N>`. Runs the plans in dependency order with atomic commits.
4. Verify  — `/gsd-verify-work <N>`. Confirms goal achievement against ROADMAP success criteria.

Run `/gsd-help` for the full command list.
```

<!-- D-16 deep CLAUDE.md replacement (3-line block, locked). -->

```
## Development Cycle

This project uses the GSD-native cycle (Discuss → Plan → Execute → Verify).
See [/CONTRIBUTING.md](../CONTRIBUTING.md) for the canonical workflow,
pytest invocation, PR conventions, and the project's "do not fix" rules.
```

<!-- D-17 root CLAUDE.md replacement (1 sentence, locked). -->

Current root CLAUDE.md lines 71–74 (verbatim):
```
## Tooling roles

The repo carries a `.cursor/` tree (gitignored) used by the GSD (Get Shit Done) workflow
when driving from Cursor; treat it as informational. The current canonical development
cycle (PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A–1G) is documented in
`spec_classifier/CLAUDE.md`.
```

Target root CLAUDE.md lines 71–74 (D-17 keeps the section header AND the .cursor/ note; replaces only the cycle sentence):
```
## Tooling roles

The repo carries a `.cursor/` tree (gitignored) used by the GSD (Get Shit Done) workflow
when driving from Cursor; treat it as informational. The canonical development cycle
(Discuss → Plan → Execute → Verify, GSD-native) is documented in
[`/CONTRIBUTING.md`](CONTRIBUTING.md).
```

<!-- D-09 DOCS_INDEX breadcrumb (added under § Conventions, locked). -->

```
> **Repo-root contributor doc:** see [/CONTRIBUTING.md](../../CONTRIBUTING.md) for the GSD-native development cycle and contribution rules.
```

<!-- D-19 CHANGELOG new sub-entry (added under existing [Unreleased] § Changed; placement: after the existing Phase-2 docs entries). -->

```
### Changed (Phase 3 — Workflow, 2026-05-10)
- chore(workflow): retired `spec_classifier/prompts/` to `.planning/archive/prompts-2026-05-10/`; the GSD-native cycle is now documented at `/CONTRIBUTING.md` (new file).
- chore(workflow): archived `spec_classifier/docs/dev/CONTRIBUTING.md` to `.planning/archive/CONTRIBUTING-2026-05-10.md`; superseded by `/CONTRIBUTING.md`.
- docs(workflow): replaced legacy "Development Cycle" block in `spec_classifier/CLAUDE.md` with a pointer to `/CONTRIBUTING.md`; updated root `CLAUDE.md` § Tooling roles symmetric pointer; cleaned `spec_classifier/docs/DOCS_INDEX.md` (dropped retired-doc rows, added See-also breadcrumb).
```

<!-- D-15 protected-list back-pointer to PROJECT.md Out of Scope items (verbatim source). -->

PROJECT.md Out of Scope items aligned with the 5 do-not-fix items above (cross-checked):
- "Fixing power_cord hw_type=None" — D-15 item 1
- "Fixing batch_audit.py reading from *_annotated.xlsx" — D-15 item 3
- "Moving spec_classifier/src/core/parser.py (Dell-specific)" — D-15 item 2
- "Deduplicating HW_TYPE_VOCAB between classifier.py and batch_audit.py" — D-15 item 5
- "Touching YAML rule order" — D-15 item 4

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 2.1: git mv spec_classifier/docs/dev/CONTRIBUTING.md to .planning/archive/CONTRIBUTING-2026-05-10.md</name>
  <read_first>
    - spec_classifier/docs/dev/CONTRIBUTING.md (the 77-line legacy file with the Phase-2 forwarding note inside; verify it's the file being archived)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-06 archive pattern; D-18 mv-not-rewrite preference)
    - .planning/phases/02-docs/02-CONTEXT.md (D-08 archive pattern precedent — single-file git mv)
    - .planning/phases/02-docs/02-CONTRIBUTING-AUDIT.md (Phase 2 LEGACY assessment; explicit § "Phase 3 Hand-off" — confirms this is the file to archive)
  </read_first>
  <action>
    Archive the legacy inner `CONTRIBUTING.md` to `.planning/archive/` per D-06. Same archive pattern as Phase 2 D-08 (CURRENT_STATE.md) and Phase 3 D-01 (prompts/) — `git mv` preserves history; `spec_classifier/docs/dev/` is freed of the LEGACY-flagged file; `/CONTRIBUTING.md` (created in Task 2.2) is unambiguously canonical. Zero risk of two CONTRIBUTING.mds drifting.

    From repo root (PowerShell):

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa

    # Pre-flight: confirm source exists and target does not yet exist.
    if (-not (Test-Path 'spec_classifier/docs/dev/CONTRIBUTING.md')) {
        Write-Host "FAIL — source spec_classifier/docs/dev/CONTRIBUTING.md missing." -ForegroundColor Red
        exit 1
    }
    if (Test-Path '.planning/archive/CONTRIBUTING-2026-05-10.md') {
        Write-Host "FAIL — target .planning/archive/CONTRIBUTING-2026-05-10.md already exists; refusing to overwrite." -ForegroundColor Red
        exit 1
    }
    if (-not (Test-Path '.planning/archive')) {
        Write-Host "FAIL — .planning/archive/ missing (Phase 2 should have created it)." -ForegroundColor Red
        exit 1
    }

    # Single-file git mv. Git auto-detects rename; history follows.
    git mv spec_classifier/docs/dev/CONTRIBUTING.md .planning/archive/CONTRIBUTING-2026-05-10.md
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL — git mv exited with code $LASTEXITCODE." -ForegroundColor Red
        exit 1
    }

    # Post-flight verification.
    if (Test-Path 'spec_classifier/docs/dev/CONTRIBUTING.md') {
        Write-Host "FAIL — source still exists after git mv." -ForegroundColor Red
        exit 1
    }
    git status -s | Select-String 'CONTRIBUTING-2026-05-10'
    ```

    Per D-18, the file content is preserved verbatim (Russian + the Phase-2 forwarding note inside) — DO NOT translate, edit, or rewrite. The archive is a historical record. Per D-22/D-23, this is a doc-only mv; no code touched.

    The `git status -s` output should show one rename entry:
    ```
    R  spec_classifier/docs/dev/CONTRIBUTING.md -> .planning/archive/CONTRIBUTING-2026-05-10.md
    ```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); if(Test-Path 'spec_classifier/docs/dev/CONTRIBUTING.md'){ $err += 'source still exists' }; if(-not (Test-Path '.planning/archive/CONTRIBUTING-2026-05-10.md')){ $err += 'target missing' }; $st=git status -s 2>&1 | Out-String; if($st -notmatch 'R\\s+spec_classifier/docs/dev/CONTRIBUTING\\.md\\s+->\\s+\\.planning/archive/CONTRIBUTING-2026-05-10\\.md'){ $err += 'git did not detect rename' }; $lines=(Get-Content '.planning/archive/CONTRIBUTING-2026-05-10.md' 2>$null).Count; if($lines -lt 70 -or $lines -gt 90){ $err += \"line count out of range: $lines (expected 70-90; original was 77)\" }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path 'spec_classifier/docs/dev/CONTRIBUTING.md'` returns `False`
    - `Test-Path '.planning/archive/CONTRIBUTING-2026-05-10.md'` returns `True`
    - `git status -s` shows exactly one rename line `R  spec_classifier/docs/dev/CONTRIBUTING.md -> .planning/archive/CONTRIBUTING-2026-05-10.md`
    - Archived file is byte-identical to the pre-mv source — `git diff --cached -- .planning/archive/CONTRIBUTING-2026-05-10.md` returns empty (rename only)
    - Archived file line count is 77 (the original count; tolerance 70–90 to allow for line-ending normalization edge cases)
    - Russian content is preserved (not translated): `Select-String -Pattern '[А-Яа-я]' .planning/archive/CONTRIBUTING-2026-05-10.md` returns ≥ 1 match
    - The Phase-2 forwarding note inside the file is preserved (e.g., `Get-Content '.planning/archive/CONTRIBUTING-2026-05-10.md' | Select-String 'Phase 3 (WF-02)'` returns ≥ 1 hit)
    - Zero edits anywhere else (no incidental file modifications)
  </acceptance_criteria>
  <done>spec_classifier/docs/dev/CONTRIBUTING.md no longer exists; .planning/archive/CONTRIBUTING-2026-05-10.md contains the verbatim 77-line legacy file; git status shows the rename; Russian content preserved.</done>
</task>

<task type="auto">
  <name>Task 2.2: Author root /CONTRIBUTING.md (~150-200 lines, English, tool-agnostic; D-10..D-15)</name>
  <read_first>
    - .planning/phases/03-workflow/03-CONTEXT.md (D-10 8-section structure; D-11 tool-agnostic tone; D-12 Windows-only-this-milestone mention; D-13 literal GSD commands; D-14 vendor pointer-only; D-15 verbatim do-not-fix list)
    - .planning/PROJECT.md (Out of Scope table — verbatim source for some D-15 entries; cross-check wording)
    - .planning/codebase/CONCERNS.md (BLOCKER + IMPORTANT — verbatim source for the 5-item do-not-fix list)
    - .planning/codebase/TESTING.md (pytest invocation; conftest.py:pytest_sessionfinish skip-ratio gate — verbatim source for /CONTRIBUTING.md § Tests)
    - .planning/codebase/CONVENTIONS.md (PEP 8 + 4-space indent + docstrings — informs § Code style)
    - CLAUDE.md (root, current 74 lines — extract the "Where to look first" table to mirror in /CONTRIBUTING.md § 8)
    - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (the vendor-add pointer target; confirm it exists and is current)
    - README.md (root — confirm the "what this repo is" pointer target; current root README from Phase 2)
  </read_first>
  <action>
    Create the file `CONTRIBUTING.md` at the repo root (does not exist yet — Plan 03-01 verified this is the new file Phase 3 ships). Target 150–200 lines, English, tool-agnostic per D-11 ("Claude Code or any AI coding agent that runs the GSD commands"), Windows-only-this-milestone passing mention per D-12, literal command-by-command GSD cycle per D-13, vendor pointer-only per D-14, verbatim do-not-fix list per D-15.

    The file's 8 sections in order (per D-10):

    **Section 1 — What this repo is + pointer to root README**

    ```markdown
    # Contributing to teresa

    teresa is a deterministic, rule-based Excel-spec classifier for six hardware
    vendors (Dell, Cisco CCW, HPE, Lenovo DCSC, xFusion, Huawei). The repo is two
    things stacked: a thin Windows launcher at the root (`run.ps1`, `teresa_gui.py`,
    `teresa.bat`) and the actual codebase under `spec_classifier/`. See the
    [root README](README.md) for installation and a runnable Quick Start.

    This document covers the development cycle, how to run tests, how to add a
    vendor, the PR workflow, code style, and the project's "do not fix" tech-debt
    rules. It is tool-agnostic — use Claude Code or any AI coding agent that runs
    the GSD (Get Shit Done) commands documented below. Windows-only this milestone;
    cross-platform support is on the v2 backlog.
    ```

    **Section 2 — Development cycle (literal command-by-command per D-13)**

    ```markdown
    ## Development cycle

    The project uses the GSD-native cycle. Each phase moves through four commands
    in order:

    1. **Discuss** — `/gsd-discuss-phase <N>`. Clarifies HOW to implement what's in
       `.planning/ROADMAP.md` for phase N. Produces `NN-CONTEXT.md`.
    2. **Plan** — `/gsd-plan-phase <N>`. Produces `NN-PP-PLAN.md` task lists with
       verification gates derived from the phase's success criteria.
    3. **Execute** — `/gsd-execute-phase <N>`. Runs the plans in dependency order,
       producing atomic commits (one per task) and `NN-PP-SUMMARY.md` per plan.
    4. **Verify** — `/gsd-verify-work <N>`. Confirms goal achievement against
       ROADMAP success criteria; produces `NN-VERIFICATION.md` and the gate verdict.

    Run `/gsd-help` for the full command list (audit, code review, doc updates,
    progress checks).

    Phase artifacts live under `.planning/phases/NN-<name>/`. Planning artifacts
    that survive across phases (`PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`,
    `STATE.md`, `codebase/*.md`) live at `.planning/`.
    ```

    **Section 3 — Tests (pytest invocation + skip-ratio gate per D-10 §3)**

    ```markdown
    ## Tests

    Run pytest from `spec_classifier/` (the test config and the session-finish hook
    both live in `spec_classifier/conftest.py`):

    ```powershell
    cd spec_classifier
    pytest tests/ -v --tb=short                            # full suite
    pytest tests/test_lenovo_rules_unit.py -v              # single file
    pytest -k "lenovo and parser" -v                       # by keyword expression
    ```

    The launcher wraps pytest:

    ```powershell
    .\run.ps1 -TestsOnly                   # short-circuit, only pytest
    .\run.ps1 -SkipTests                   # full pipeline run, skip pytest at end
    ```

    **Skip-ratio gate.** `spec_classifier/conftest.py:pytest_sessionfinish` forces
    session FAIL if `skipped/total > 0.50` or if `passed == 0` or if `input_root`
    is missing entirely (`MAX_SKIP_RATIO = 0.50`). A clean clone with no INPUT data
    will trip this — populate `%USERPROFILE%\Desktop\INPUT\<vendor>\` with at least
    one `.xlsx` per vendor before running the suite, or expect the gate to flag a
    "skip guard triggered" message even though all active tests pass.

    **Goldens.** `spec_classifier/golden/*_expected.jsonl` is the regression
    contract — any rule edit MUST update the affected golden via
    `python main.py --vendor <v> --update-golden` before the regression test will
    pass. See [`spec_classifier/docs/dev/TESTING_GUIDE.md`](spec_classifier/docs/dev/TESTING_GUIDE.md)
    for the golden workflow.
    ```

    **Section 4 — Adding a vendor (pointer-only per D-14)**

    ```markdown
    ## Adding a vendor

    See [`spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`](spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md)
    for the full adapter contract, parser/normalizer scaffold, golden workflow, and
    per-vendor sheet conventions. This file does not duplicate that content —
    NEW_VENDOR_GUIDE.md is the canonical doc; updates land there.

    What to expect: a new vendor adds a `src/vendors/<v>/{adapter.py, parser.py,
    normalizer.py}` triplet, a `rules/<v>_rules.yaml` rule file, registers
    `"<v>"` in `$ALL_VENDORS` in `run.ps1`, moves `"<v>"` from `VENDORS_DISABLED`
    to `VENDORS_ACTIVE` in `teresa_gui.py`, and ships at least one golden
    (`golden/<v>1_expected.jsonl`) before merge.
    ```

    **Section 5 — PR workflow (commit convention + atomic-revertability)**

    ```markdown
    ## PR workflow

    **Commit messages.** Follow the existing `<type>(<phase>-<plan>): <subject>`
    pattern from `git log`:

    ```
    docs(02-01): translate spec_classifier/CLAUDE.md to English + dedup root CLAUDE.md
    chore(03-01): retire spec_classifier/prompts/ to archive + repoint LAUNCHER_README
    fix(02-04): tighten paragraph X (DOC-02)
    ```

    Common types: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`. Reference the
    requirement ID (`HYG-01`, `DOC-04`, `WF-02`) in the subject when relevant.

    **One commit per task.** Each `<task>` block in a PLAN.md should produce one
    commit (or one rename + one commit if a `git mv` precedes content changes). A
    failed task is recovered by a NEW follow-up commit, not `git commit --amend`.

    **One requirement per phase-closing commit.** Phase wrap-up commits group
    artifacts by requirement (one commit per `HYG-NN` / `DOC-NN` / `WF-NN`) so any
    individual requirement is atomically revertable.

    There is no PR template file in the repo today; the convention is described
    here, not auto-injected. The PR description should reference the phase SUMMARY
    (`.planning/phases/NN-<name>/NN-SUMMARY.md`) for context.
    ```

    **Section 6 — Code style (one paragraph)**

    ```markdown
    ## Code style

    PEP 8, 4-space indent, docstrings for public functions and modules. Match the
    style of the surrounding file when in doubt. Do not introduce new conventions
    in a hygiene or docs phase. Project-specific rule-authoring style lives in
    [`spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md`](spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md);
    follow it for any YAML rule edit.
    ```

    **Section 7 — Do not fix (verbatim per D-15)**

    Use the EXACT 5-item bulleted list from `<interfaces>` above, ending with the
    `> Full rationale and context: [.planning/codebase/CONCERNS.md](.planning/codebase/CONCERNS.md) § BLOCKER + IMPORTANT.` blockquote. Per D-23, NO "do not fix YET" framing — frame each item as "intentional behavior; see CONCERNS.md for the full rationale," not "planned for later."

    Open the section with one-paragraph context:

    ```markdown
    ## Do not fix

    The five items below are load-bearing on purpose. Each has a recovery commit
    or an explicit Project decision behind it; "fixing" any of them as part of an
    unrelated PR has caused real regressions. Treat them as constraints, not
    technical debt to be paid down.
    ```

    Then list the 5 items VERBATIM from the `<interfaces>` block above (item 1
    through item 5), then the back-pointer blockquote.

    **Section 8 — Where to look first (table mirroring root CLAUDE.md per D-10 §8)**

    ```markdown
    ## Where to look first

    | Question | Authoritative file |
    |---|---|
    | Pipeline stages, E-codes, alias-table semantics | [`spec_classifier/CLAUDE.md`](spec_classifier/CLAUDE.md) (deep reference) |
    | The 5 critical business rules at a glance | [`CLAUDE.md`](CLAUDE.md) (root pointer) |
    | Run a vendor end-to-end on Windows | [`run.ps1`](run.ps1), [`LAUNCHER_README.md`](LAUNCHER_README.md) |
    | Vendor adapter contract | [`spec_classifier/src/vendors/base.py`](spec_classifier/src/vendors/base.py) |
    | Add a new vendor | [`spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`](spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md) |
    | Rules authoring | [`spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md`](spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md), [`spec_classifier/docs/taxonomy/hw_type_taxonomy.md`](spec_classifier/docs/taxonomy/hw_type_taxonomy.md) |
    | Recent changes | [`spec_classifier/CHANGELOG.md`](spec_classifier/CHANGELOG.md) |
    | Project status / current focus | [`.planning/STATE.md`](.planning/STATE.md) |
    | Tech-debt items NOT to fix | [`.planning/codebase/CONCERNS.md`](.planning/codebase/CONCERNS.md) (BLOCKER + IMPORTANT sections) |
    ```

    **Notes for the executor:**

    - **Use the Write tool** to create the file. The file does not exist yet; this is a fresh write.
    - **Total line count target: 150–200.** If the draft comes in under 150, expand the prose paragraphs slightly (don't pad the do-not-fix list). If over 200, tighten Section 5 (PR workflow) — the commit-convention example block can shrink to fewer examples.
    - **Tool-agnostic per D-11:** every reference to GSD commands uses the `/gsd-*` slash form. Do NOT name "Claude Code" / "Cursor" / "ChatGPT" / "Gemini" anywhere; D-11 explicitly drops the deep CLAUDE.md "Tool Roles" table (Cursor/Claude/ChatGPT/Gemini split) — that's GSD-superseded narrative.
    - **D-12 platform mention:** the Section 1 sentence "Windows-only this milestone; cross-platform support is on the v2 backlog" satisfies D-12 in passing without re-litigating PLAT-01/PLAT-02.
    - **D-15 verbatim list:** lift the 5-item bulleted list from `<interfaces>` byte-for-byte. The wording is locked so the source-of-truth is `.planning/codebase/CONCERNS.md` + this file's mirror; future updates need to be mirrored in both.
    - **D-23 framing:** never write "do not fix YET", "deferred to a later milestone", "planned for v2", etc. for the 5 items in Section 7. They are intentional, not deferred.
    - **D-22 honor:** this task creates a new doc that REFERENCES the 5 protected items by name; it does NOT touch any of the underlying code. No edits to `spec_classifier/rules/*.yaml`, `spec_classifier/src/`, `spec_classifier/batch_audit.py`, etc.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='CONTRIBUTING.md'; if(-not (Test-Path $f)){ $err += 'CONTRIBUTING.md missing'; $err | ForEach-Object { Write-Output $_ }; exit 1 }; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -lt 150 -or $lines -gt 220){ $err += \"line-count out of range: $lines (target 150-200; tolerance 150-220)\" }; foreach($t in '# Contributing to teresa','## Development cycle','## Tests','## Adding a vendor','## PR workflow','## Code style','## Do not fix','## Where to look first','/gsd-discuss-phase','/gsd-plan-phase','/gsd-execute-phase','/gsd-verify-work','/gsd-help','spec_classifier/conftest.py:pytest_sessionfinish','skipped/total','MAX_SKIP_RATIO','NEW_VENDOR_GUIDE.md','power_cord','hw_type=None','core/parser.py','batch_audit.py','YAML rule order','HW_TYPE_VOCAB','first-match-wins','.planning/codebase/CONCERNS.md','BLOCKER + IMPORTANT','RULES_AUTHORING_GUIDE.md','Discuss','Plan','Execute','Verify','GSD-native','Windows-only this milestone'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; foreach($t in 'Cursor','ChatGPT','Gemini','do not fix YET','planned for later','planned for v2','deferred to a later milestone'){ if($c -match [regex]::Escape($t)){ $err += \"forbidden token: $t\" } }; if($c -match '[А-Яа-я]'){ $err += 'Russian residue detected' }; if($c -match 'C:\\\\Users\\\\G\\\\'){ $err += 'username residue (Phase 1 HYG-01 hold)' }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `CONTRIBUTING.md` (repo root)
    - Line count is 150–200 (tolerance 150–220)
    - All 8 D-10 section headers present in order: `# Contributing to teresa`, `## Development cycle`, `## Tests`, `## Adding a vendor`, `## PR workflow`, `## Code style`, `## Do not fix`, `## Where to look first`
    - **D-13 GSD-cycle commands all present:** `/gsd-discuss-phase`, `/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-verify-work`, `/gsd-help` (5 commands; numbered list 1–4 + footnote)
    - **D-10 §3 Tests citations present:** `spec_classifier/conftest.py:pytest_sessionfinish`, `skipped/total`, `MAX_SKIP_RATIO`
    - **D-14 vendor pointer present:** `NEW_VENDOR_GUIDE.md` (link form)
    - **D-15 do-not-fix items all present (5 items):** `power_cord`, `hw_type=None`, `core/parser.py`, `batch_audit.py`, `YAML rule order`, `first-match-wins`, `HW_TYPE_VOCAB` (7 keywords covering all 5 items)
    - **D-15 back-pointer present:** `.planning/codebase/CONCERNS.md` AND `BLOCKER + IMPORTANT` (the blockquote sentence)
    - **D-11 tool-agnostic enforcement:** `Cursor`, `ChatGPT`, `Gemini` are FORBIDDEN tokens (zero occurrences)
    - **D-23 framing enforcement:** `do not fix YET`, `planned for later`, `planned for v2`, `deferred to a later milestone` are FORBIDDEN tokens
    - **D-12 platform mention present:** literal text `Windows-only this milestone`
    - **D-04 Phase 1 hold:** zero `C:\Users\G\` username residue
    - Zero Russian residue (D-11 English-only requirement)
    - "Where to look first" table includes ≥ 8 rows (mirrors root CLAUDE.md table per D-10 §8) with at least these targets: `spec_classifier/CLAUDE.md`, `CLAUDE.md`, `LAUNCHER_README.md`, `NEW_VENDOR_GUIDE.md`, `RULES_AUTHORING_GUIDE.md`, `spec_classifier/CHANGELOG.md`, `.planning/STATE.md`, `.planning/codebase/CONCERNS.md`
  </acceptance_criteria>
  <done>/CONTRIBUTING.md exists at repo root, 150-200 lines, all 8 sections in D-10 order, GSD commands literal, pytest + skip-ratio gate cited, vendor pointer-only, 5 do-not-fix items verbatim, where-to-look table mirrors root CLAUDE.md, zero Cursor/ChatGPT/Gemini mentions, zero "do not fix YET" framing, zero Russian/username residue.</done>
</task>

<task type="auto">
  <name>Task 2.3: Replace spec_classifier/CLAUDE.md legacy block (~63 lines) with 3-line pointer per D-16</name>
  <read_first>
    - spec_classifier/CLAUDE.md (current 307 lines — focus on lines 245–307 where § Tool Roles begins through end-of-file at the § Prompts — Location footer)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-16 — exact 3-line replacement; D-22/D-23 — preserve earlier sections including the 5 critical business rules; the 5 critical business rules already deduplicated to root CLAUDE.md per Phase 2 D-01 stay where they are)
    - .planning/phases/02-docs/02-01-PLAN.md (Phase 2 Task 1.2 — context for what was translated; helps locate the legacy block to delete)
  </read_first>
  <action>
    Edit `spec_classifier/CLAUDE.md` to remove the legacy block (~63 lines, lines 245–307, from § Tool Roles through § Prompts — Location footer) and replace it with a 3-line GSD pointer per D-16. The 5 critical business rules already deduplicated to root CLAUDE.md per Phase 2 D-01 stay where they are; this change does NOT touch the business-rules sections.

    **Sections to DELETE** (verbatim section headers, in current file order — appear consecutively at lines 245–307):
    1. `## Tool Roles` (and its 4-row Cursor/Claude/ChatGPT/Gemini table)
    2. `## Development Cycle` (the legacy table with PRE-CHECK → MASTER PLAN → CURSOR IMPLEMENT → POST-CHECK → AUDIT 1A–1G + 4-scenario sub-table)
    3. `## Hard Rules for Claude Windows` (R1–R5 + ### Severity Levels sub-section)
    4. `## Recommended Models per Step` (4-row table with Sonnet/Opus assignments)
    5. `## Prompts — Location` (3-line footer pointing at `prompts/`)

    Plus the horizontal-rule `---` separators between these sections.

    **Section to INSERT in their place** — verbatim from `<interfaces>` D-16 block:

    ```markdown
    ## Development Cycle

    This project uses the GSD-native cycle (Discuss → Plan → Execute → Verify).
    See [/CONTRIBUTING.md](../CONTRIBUTING.md) for the canonical workflow,
    pytest invocation, PR conventions, and the project's "do not fix" rules.
    ```

    The new `## Development Cycle` section sits in the same file position as the deleted `## Tool Roles` heading (i.e., after the `## Known Tech Debt` section that ends at the `For broader BLOCKER/IMPORTANT/NICE-TO-FIX context see .planning/codebase/CONCERNS.md.` line). The relative back-pointer `../CONTRIBUTING.md` is correct: deep CLAUDE.md sits at `spec_classifier/CLAUDE.md`, repo root is one level up.

    **Sections that STAY UNTOUCHED (D-22 + D-16 explicit):**
    - All sections before § Tool Roles: `## Project`, `## Paths (Windows)`, `## Current State`, `## CLI Commands`, `## Business Rules (do not violate when editing)` (the 5 critical rules block + sub-headings ### device_type Aliases, ### Canonical Field Names), `## E-codes (batch_audit.py)` (E1–E18 table + ### pipeline_check tags sub-section), `## Known Tech Debt (P2, out of scope for the current plan)`.
    - The Phase-2 archive forward-pointer `> Historical state snapshot archived to .planning/archive/CURRENT_STATE-2026-05-10.md…` (Phase 2 D-08 — preserved).

    **Use the Edit tool** with `old_string` covering the entire legacy block (from the `---` separator before `## Tool Roles` through the final line of the `## Prompts — Location` section, inclusive). `new_string` is the 3-line replacement above (preceded by the `---` separator that originally sat before `## Tool Roles`, so the section break is preserved).

    Pre-flight check: confirm the post-translation deep file still has all the expected pre-legacy sections.

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa
    $f = 'spec_classifier/CLAUDE.md'
    $c = Get-Content $f -Raw
    $required_pre = @('## Project','## Paths (Windows)','## Current State','## CLI Commands','## Business Rules','## E-codes','## Known Tech Debt')
    foreach ($s in $required_pre) {
        if ($c -notmatch [regex]::Escape($s)) {
            Write-Host "FAIL — pre-legacy section missing: $s. Aborting Task 2.3." -ForegroundColor Red
            exit 1
        }
    }
    Write-Host "Pre-flight: all 7 expected pre-legacy sections present."
    ```

    Per D-23, NO "do not fix YET" / "planned for later" framing in the 3-line replacement — it points at /CONTRIBUTING.md, which has the canonical do-not-fix list. The deep CLAUDE.md no longer documents the workflow itself; it points to the doc that does.

    Post-edit expected line delta: ~63 lines deleted, 5 lines added (3 content + 1 H2 header line + 1 blank line for spacing) → net deletion of ~58 lines. Final spec_classifier/CLAUDE.md line count ~249 (was 307).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='spec_classifier/CLAUDE.md'; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -gt 260 -or $lines -lt 230){ $err += \"line-count: $lines (expected ~249; tolerance 230-260)\" }; foreach($t in '## Project','## Paths (Windows)','## Current State','## CLI Commands','## Business Rules','## E-codes','## Known Tech Debt','power_cord','_E8_NO_HW_TYPE_DEVICES','HW_TYPE_VOCAB','BASE without device_type','## Development Cycle','/CONTRIBUTING.md','GSD-native','../CONTRIBUTING.md'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; foreach($t in '## Tool Roles','| **Cursor** (Pro)','| **Claude** (Pro)','| **ChatGPT** (Plus)','PRE-CHECK | MASTER PLAN','| Маленькая правка YAML','| Small YAML edit','## Hard Rules for Claude Windows','**R1.** Each step','**R2.** Read ONLY','**R3.** Every reply','**R4.** A','**R5.** Only SUMMARY','### Severity Levels','## Recommended Models per Step','| PRE-CHECK, POST-CHECK','## Prompts — Location','prompts/README.md','prompts/'){ if($c -match [regex]::Escape($t)){ $err += \"residual legacy: $t\" } }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File line count: 230–260 (was 307; net deletion of ~58 lines per D-16 expectation)
    - **Pre-legacy sections preserved:** `## Project`, `## Paths (Windows)`, `## Current State`, `## CLI Commands`, `## Business Rules`, `## E-codes`, `## Known Tech Debt` (all 7 still present)
    - **Critical-rule tokens preserved:** `power_cord`, `_E8_NO_HW_TYPE_DEVICES`, `HW_TYPE_VOCAB`, `BASE without device_type` (D-22 — business-rule sections untouched)
    - **New `## Development Cycle` section present:** literal headers `## Development Cycle`, `GSD-native`, `../CONTRIBUTING.md`, `/CONTRIBUTING.md` (the 3-line D-16 replacement)
    - **Legacy section headers GONE:** `## Tool Roles`, `## Hard Rules for Claude Windows`, `### Severity Levels`, `## Recommended Models per Step`, `## Prompts — Location` (5 forbidden headers)
    - **Legacy table content GONE:** `| **Cursor** (Pro)`, `| **Claude** (Pro)`, `| **ChatGPT** (Plus)`, `PRE-CHECK | MASTER PLAN`, `| Small YAML edit`, `**R1.` through `**R5.`, `| PRE-CHECK, POST-CHECK` (Tool Roles + Dev Cycle + Hard Rules + Models tables all gone)
    - **Legacy footer GONE:** `## Prompts — Location`, `prompts/README.md` references in main body (the new `## Development Cycle` section does NOT mention prompts/)
    - **Phase-2 archive pointer preserved:** the `Historical state snapshot archived to .planning/archive/CURRENT_STATE-2026-05-10.md` line is still present in § Current State
    - Zero edits to `spec_classifier/rules/`, `spec_classifier/src/`, `spec_classifier/tests/`, `spec_classifier/golden/`
  </acceptance_criteria>
  <done>spec_classifier/CLAUDE.md is now ~249 lines; legacy ~63-line block (Tool Roles + Dev Cycle table + Hard Rules R1-R5 + Recommended Models + Prompts—Location) replaced by 3-line GSD pointer to ../CONTRIBUTING.md; all earlier sections (Project through Known Tech Debt) preserved; 5 critical business rules untouched.</done>
</task>

<task type="auto">
  <name>Task 2.4: Replace root CLAUDE.md § Tooling roles legacy sentence with D-17 1-line pointer</name>
  <read_first>
    - CLAUDE.md (root, current 74 lines — § Tooling roles is at lines 71–74 per D-17 callout)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-17 — exact 1-sentence replacement; D-17 keeps section header AND .cursor/ informational note)
    - .planning/phases/02-docs/02-01-PLAN.md (Phase 2 — confirms current 74-line root CLAUDE.md shape; section header `## Tooling roles` was added in Phase 2)
  </read_first>
  <action>
    Edit root `CLAUDE.md` to replace the legacy step-list sentence in § Tooling roles with the D-17 1-line pointer to `/CONTRIBUTING.md`. Symmetric with D-16 deep CLAUDE.md update; stays at one sentence; preserves the `.cursor/` informational note above it. Section header `## Tooling roles` stays.

    **Use the Edit tool** with `old_string` and `new_string` from `<interfaces>` D-17 block above (verbatim).

    Specifically:

    `old_string` (the current full 4-line § Tooling roles section):
    ```
    ## Tooling roles

    The repo carries a `.cursor/` tree (gitignored) used by the GSD (Get Shit Done) workflow
    when driving from Cursor; treat it as informational. The current canonical development
    cycle (PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A–1G) is documented in
    `spec_classifier/CLAUDE.md`.
    ```

    `new_string` (the D-17 replacement; keep `## Tooling roles` header, keep the `.cursor/` informational sentence, replace ONLY the cycle-mention sentence):
    ```
    ## Tooling roles

    The repo carries a `.cursor/` tree (gitignored) used by the GSD (Get Shit Done) workflow
    when driving from Cursor; treat it as informational. The canonical development cycle
    (Discuss → Plan → Execute → Verify, GSD-native) is documented in
    [`/CONTRIBUTING.md`](CONTRIBUTING.md).
    ```

    The 4 changes inside the section:
    1. Drop the word `current` (the cycle is canonical, not a "current" snapshot).
    2. Replace `(PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A–1G)` with `(Discuss → Plan → Execute → Verify, GSD-native)`.
    3. Replace `documented in `spec_classifier/CLAUDE.md`.` with `documented in [`/CONTRIBUTING.md`](CONTRIBUTING.md).`.
    4. Section header `## Tooling roles` and the entire `.cursor/` sentence stay byte-identical.

    Pre-flight check: confirm the file's line count is 74 before editing.

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa
    $lines = (Get-Content CLAUDE.md).Count
    if ($lines -ne 74) {
        Write-Host "WARNING — root CLAUDE.md line count is $lines (expected 74). The Phase-2 baseline shifted; verify the section is still at lines 71-74 before editing." -ForegroundColor Yellow
    }
    ```

    Post-edit: file line count stays at 74 (line-for-line replacement; no insertions or deletions). The 5 critical business rules (Phase 2 D-01) at lines ~37–47 stay untouched. The "Where to look first" table at lines ~50–67 stays untouched.

    Per D-22/D-23: this is a one-sentence pointer-only edit. Zero touches to code; zero "do not fix YET" framing.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='CLAUDE.md'; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -lt 70 -or $lines -gt 80){ $err += \"line-count drift: $lines (expected 74; tolerance 70-80)\" }; foreach($t in '## Tooling roles','`.cursor/` tree (gitignored)','Discuss','Plan','Execute','Verify','GSD-native','[`/CONTRIBUTING.md`](CONTRIBUTING.md)','power_cord','LOGISTIC','BASE','is_factory_integrated','applies_to'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; foreach($t in 'PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A','documented in `spec_classifier/CLAUDE.md`.','The current canonical development'){ if($c -match [regex]::Escape($t)){ $err += \"residual: $t\" } }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File line count: 70–80 (was 74; the line-for-line replacement should keep it exactly 74)
    - **Section header preserved:** `## Tooling roles` still present
    - **`.cursor/` informational sentence preserved:** literal text ``.cursor/` tree (gitignored)` still present
    - **D-17 replacement present:** `Discuss`, `Plan`, `Execute`, `Verify`, `GSD-native`, `[`/CONTRIBUTING.md`](CONTRIBUTING.md)` (link form to CONTRIBUTING.md at repo root)
    - **Legacy sentence GONE:** `PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A` (the legacy cycle string), `documented in `spec_classifier/CLAUDE.md`.` (the old pointer), `The current canonical development` (the old sentence opener)
    - **5 critical business rules preserved:** `power_cord`, `LOGISTIC`, `BASE`, `is_factory_integrated`, `applies_to` (Phase 2 D-01 hold)
    - "Where to look first" table preserved (cross-check via known table tokens like `Pipeline stages, E-codes`, `LAUNCHER_README.md`, `.planning/STATE.md`)
    - Zero touches outside § Tooling roles (the only changed lines are 71–74)
  </acceptance_criteria>
  <done>Root CLAUDE.md § Tooling roles preserves the section header AND the .cursor/ informational sentence; legacy "PRE-CHECK → PLAN → IMPLEMENT → POST-CHECK → AUDIT 1A–1G" cycle string is gone; replaced with one-sentence pointer to /CONTRIBUTING.md; line count stays at ~74; 5 critical rules and where-to-look table untouched.</done>
</task>

<task type="auto">
  <name>Task 2.5: Drop DOCS_INDEX.md retired-doc rows + add See-also breadcrumb (D-07/D-08/D-09)</name>
  <read_first>
    - spec_classifier/docs/DOCS_INDEX.md (current 41 lines — locate line 27 `docs/dev/CONTRIBUTING.md` row and line 36 `prompts/README.md` row; § Conventions section starts at line 38)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-07 drop CONTRIBUTING row; D-08 drop prompts/README row; D-09 add See-also breadcrumb under § Conventions)
    - .planning/phases/02-docs/02-CONTEXT.md (D-16 — the 1:1 invariant Phase 2 established; this task's drops preserve it because the targets are leaving docs/)
  </read_first>
  <action>
    Edit `spec_classifier/docs/DOCS_INDEX.md` to make three changes:

    **(a) Drop the row referencing `docs/dev/CONTRIBUTING.md` (line 27 in current file)** per D-07.

    Remove this entire table row line:
    ```
    | `docs/dev/CONTRIBUTING.md` | How do I work in this repo? |
    ```

    Rationale: Task 2.1 archived this file. Phase 2 D-16 established that DOCS_INDEX.md must be 1:1 with `spec_classifier/docs/**/*.md`; with the file gone, the row must go. The new `/CONTRIBUTING.md` lives at the repo root, NOT inside `spec_classifier/docs/`, so it does NOT replace this row in the table — instead, the breadcrumb in step (c) below provides the discoverability hook.

    **(b) Drop the row referencing `prompts/README.md` (line 36 in current file)** per D-08.

    Remove this entire table row line:
    ```
    | `prompts/README.md` | Prompt library — step templates for dev cycle |
    ```

    Rationale: Plan 03-01 archived `spec_classifier/prompts/`. The row's target no longer exists at the listed path. Symmetric treatment with D-07.

    **(c) Add a See-also breadcrumb under § Conventions** per D-09.

    The current § Conventions section ends at line 41 (last line of file: "with code. Update docs in the same commit as any behavior change."). Insert ONE blank line, then this blockquote:

    ```markdown

    > **Repo-root contributor doc:** see [/CONTRIBUTING.md](../../CONTRIBUTING.md) for the GSD-native development cycle and contribution rules.
    ```

    Path discipline: DOCS_INDEX.md sits at `spec_classifier/docs/DOCS_INDEX.md`; the back-pointer needs `../../` to reach the repo root (two levels up: `docs/` → `spec_classifier/` → repo root).

    Per D-09, this is a single breadcrumb under § Conventions — NOT a new table entry, NOT a row in the "Key Documents" table. DOCS_INDEX.md remains scoped to `spec_classifier/docs/`; the breadcrumb is the lightweight pointer that survives the docs/-scoped index.

    **Use the Edit tool** with three separate edits:
    1. Remove the CONTRIBUTING.md row (`old_string` = the full line; `new_string` = empty).
    2. Remove the prompts/README.md row (`old_string` = the full line; `new_string` = empty).
    3. Append the breadcrumb after the last existing line.

    Pre-flight: confirm both target rows currently exist and § Conventions is still at the bottom.

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa
    $f = 'spec_classifier/docs/DOCS_INDEX.md'
    $c = Get-Content $f -Raw
    if ($c -notmatch [regex]::Escape('| `docs/dev/CONTRIBUTING.md` | How do I work in this repo?')) {
        Write-Host "FAIL — D-07 target row not found in current DOCS_INDEX.md." -ForegroundColor Red
        exit 1
    }
    if ($c -notmatch [regex]::Escape('| `prompts/README.md` | Prompt library')) {
        Write-Host "FAIL — D-08 target row not found." -ForegroundColor Red
        exit 1
    }
    if ($c -notmatch '## Conventions') {
        Write-Host "FAIL — § Conventions section not found; D-09 breadcrumb cannot be placed." -ForegroundColor Red
        exit 1
    }
    Write-Host "Pre-flight: all 3 D-07/D-08/D-09 anchors present."
    ```

    Post-edit expected line delta: 2 rows removed, 2 lines added (1 blank + 1 breadcrumb) → net 0 line change (still ~41 lines). The 1:1 invariant is preserved: every remaining row points at a file under `spec_classifier/docs/**/*.md` that still exists.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='spec_classifier/docs/DOCS_INDEX.md'; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -lt 35 -or $lines -gt 50){ $err += \"line-count out of range: $lines (target ~41; tolerance 35-50)\" }; if($c -match [regex]::Escape('| `docs/dev/CONTRIBUTING.md` |')){ $err += 'D-07 row not dropped' }; if($c -match [regex]::Escape('| `prompts/README.md` |')){ $err += 'D-08 row not dropped' }; if($c -notmatch [regex]::Escape('Repo-root contributor doc:')){ $err += 'D-09 breadcrumb missing' }; if($c -notmatch [regex]::Escape('../../CONTRIBUTING.md')){ $err += 'D-09 back-pointer missing or wrong relative path' }; if($c -notmatch '## Conventions'){ $err += '§ Conventions header missing' }; if($c -notmatch '## Structure'){ $err += '§ Structure header missing' }; if($c -notmatch '## Key Documents'){ $err += '§ Key Documents header missing' }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File line count: 35–50 (was 41; net 0 change after 2 row drops + 2 line breadcrumb)
    - **D-07 row gone:** `git grep '| \`docs/dev/CONTRIBUTING.md\`'` returns 0 hits in DOCS_INDEX.md
    - **D-08 row gone:** `git grep '| \`prompts/README.md\`'` returns 0 hits in DOCS_INDEX.md
    - **D-09 breadcrumb present:** literal text `Repo-root contributor doc:` AND `../../CONTRIBUTING.md` (correct relative path from `spec_classifier/docs/`)
    - **Existing structure preserved:** `## Structure`, `## Key Documents`, `## Conventions` (3 section headers all still present)
    - **Other key-document rows preserved:** spot-check that representative non-target rows are intact (e.g., `USER_GUIDE.md`, `CLI_CONFIG_REFERENCE.md`, `CHANGELOG.md`, `hw_type_taxonomy.md`)
    - **DOCS_INDEX 1:1 invariant preserved:** every remaining row in the Key Documents table points at a file that still exists under `spec_classifier/docs/` (or at `README.md` / `CHANGELOG.md` / `batch_audit.py` / `cluster_audit.py` / `CLAUDE.md` which sit one level up — these were intentional outside-the-tree entries from Phase 2)
    - Zero edits anywhere outside DOCS_INDEX.md
  </acceptance_criteria>
  <done>spec_classifier/docs/DOCS_INDEX.md has the CONTRIBUTING.md row dropped (D-07), the prompts/README.md row dropped (D-08), and a single See-also breadcrumb pointing at /CONTRIBUTING.md added under § Conventions (D-09); the docs/-scoped 1:1 invariant from Phase 2 D-16 is preserved.</done>
</task>

<task type="auto">
  <name>Task 2.6: Add new [Unreleased] CHANGELOG entry per D-19; preserve historical lines per D-18</name>
  <read_first>
    - spec_classifier/CHANGELOG.md (current 245 lines — focus on the [Unreleased] section at line 14 onward; locate where the existing Phase 1 + Phase 2 entries end)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-18 historical lines untouched; D-19 new entry content + placement)
    - .planning/phases/02-docs/02-CONTEXT.md (D-07 — Phase 2 release-notes immutability convention that D-18 carries forward)
  </read_first>
  <action>
    Edit `spec_classifier/CHANGELOG.md` to add a NEW sub-entry under the existing `[Unreleased]` section per D-19. Per D-18, all historical lines (including line 44 "docs: prompts/ — prompt library" and line 208 "docs/prompts/" and any other historical mentions of `prompts/` in past version banners) are LEFT VERBATIM — rewriting historical entries falsifies history (Phase 2 D-07 explicitly chose release-notes immutability).

    **Placement:** the new entry sits under `[Unreleased]` § Changed, AFTER the existing two `### Changed (Phase N — ...)` sub-sections (Phase 1 hygiene at lines 16–19; Phase 2 docs at lines 21–25). The new sub-section heading mirrors that pattern: `### Changed (Phase 3 — Workflow, 2026-05-10)`.

    **Content** — verbatim from `<interfaces>` D-19 block above:

    ```markdown
    ### Changed (Phase 3 — Workflow, 2026-05-10)
    - chore(workflow): retired `spec_classifier/prompts/` to `.planning/archive/prompts-2026-05-10/`; the GSD-native cycle is now documented at `/CONTRIBUTING.md` (new file).
    - chore(workflow): archived `spec_classifier/docs/dev/CONTRIBUTING.md` to `.planning/archive/CONTRIBUTING-2026-05-10.md`; superseded by `/CONTRIBUTING.md`.
    - docs(workflow): replaced legacy "Development Cycle" block in `spec_classifier/CLAUDE.md` with a pointer to `/CONTRIBUTING.md`; updated root `CLAUDE.md` § Tooling roles symmetric pointer; cleaned `spec_classifier/docs/DOCS_INDEX.md` (dropped retired-doc rows, added See-also breadcrumb).
    ```

    Three bullets covering the three Phase-3 changes per D-19. Insertion site: BEFORE the existing `### Added` sub-section in `[Unreleased]` (line 27 in current file).

    **Use the Edit tool** with `old_string` = the existing `### Added` line (and the lines around it for unique anchoring) and `new_string` = the new `### Changed (Phase 3 — Workflow, 2026-05-10)` block followed by a blank line then the same `### Added` line.

    Pre-flight: confirm the existing structure is intact and the line 44 + line 208 historical references are present.

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa
    $f = 'spec_classifier/CHANGELOG.md'
    $c = Get-Content $f -Raw
    if ($c -notmatch '## \[Unreleased\]') {
        Write-Host "FAIL — [Unreleased] section missing." -ForegroundColor Red
        exit 1
    }
    if ($c -notmatch '### Changed \(Phase 1 — Hygiene') {
        Write-Host "WARN — Phase 1 sub-section anchor not found; structure may have shifted." -ForegroundColor Yellow
    }
    if ($c -notmatch '### Changed \(Phase 2 — Docs') {
        Write-Host "WARN — Phase 2 sub-section anchor not found; structure may have shifted." -ForegroundColor Yellow
    }
    # Spot-check that historical prompts/ mentions are preserved (D-18).
    $promptsRefs = ([regex]::Matches($c, 'prompts/')).Count
    Write-Host "Pre-edit: '$promptsRefs' historical 'prompts/' references in CHANGELOG (D-18: all preserved)."
    ```

    Per D-18: AFTER the edit, the count of historical `prompts/` references in lines BEFORE `[Unreleased]` (i.e., in past version banners like v1.3.x, v1.2.x, etc.) is unchanged. The new `[Unreleased]` entry adds 2 NEW `prompts` mentions (the bullet-1 `spec_classifier/prompts/` and `prompts-2026-05-10`), but those are in the new entry, not in historical lines — D-18 is preserved.

    Per D-22/D-23: this is a doc-only update; no code touched; no "do not fix YET" framing in the new entry (it's a release note, not a forward-plan).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='spec_classifier/CHANGELOG.md'; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -lt 245 -or $lines -gt 260){ $err += \"line-count out of range: $lines (expected 248-252; tolerance 245-260)\" }; foreach($t in '## [Unreleased]','### Changed (Phase 1 — Hygiene','### Changed (Phase 2 — Docs','### Changed (Phase 3 — Workflow, 2026-05-10)','spec_classifier/prompts/','prompts-2026-05-10','CONTRIBUTING-2026-05-10','/CONTRIBUTING.md','spec_classifier/docs/DOCS_INDEX.md','dropped retired-doc rows','See-also breadcrumb','§ Tooling roles symmetric pointer','### Added'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File line count: 245–260 (was 245; +3 bullets + 1 sub-heading + 1 blank line ≈ +5 lines → ~250)
    - **D-19 new sub-heading present:** literal `### Changed (Phase 3 — Workflow, 2026-05-10)`
    - **D-19 three bullets present:** all three change descriptions covering (a) prompts retire + /CONTRIBUTING.md, (b) inner CONTRIBUTING archive, (c) deep CLAUDE.md replacement + root CLAUDE.md pointer + DOCS_INDEX.md cleanup
    - **D-19 key tokens present:** `prompts-2026-05-10`, `CONTRIBUTING-2026-05-10`, `/CONTRIBUTING.md`, `dropped retired-doc rows`, `See-also breadcrumb`, `§ Tooling roles symmetric pointer`
    - **D-18 historical preservation:** existing `### Changed (Phase 1 — Hygiene` and `### Changed (Phase 2 — Docs` sub-sections still present (existing entries untouched)
    - **D-18 placement check:** the new sub-heading sits AFTER the Phase 2 sub-section (chronological order) and BEFORE the existing `### Added` sub-section (still present)
    - **Existing `### Added` section preserved** (the unified-launcher entries from line 27 onward are unchanged)
    - Zero edits to historical version banners in lines AFTER `[Unreleased]` (the v1.3.x, v1.2.x, v1.1.x, v1.0 lines and their `prompts/` mentions stay verbatim per D-18)
    - Zero edits anywhere outside CHANGELOG.md
  </acceptance_criteria>
  <done>spec_classifier/CHANGELOG.md has a new ### Changed (Phase 3 — Workflow, 2026-05-10) sub-section under [Unreleased] with three bullets covering the three Phase-3 changes; existing Phase 1 + Phase 2 sub-sections preserved; existing ### Added section preserved; historical version banners (with their existing prompts/ mentions) untouched per D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase; no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | N/A | Documentation-only phase | accept | N/A — documentation-only phase; no code execution paths, network endpoints, auth flows, or external input handling introduced. File moves are git-mv operations preserving history; new doc content is markdown text. The `/CONTRIBUTING.md` § Do not fix block protects the 5 load-bearing tech-debt items by surfacing them at the contributor's first read — strengthens, not weakens, the existing mitigations. |
</threat_model>

<verification>
After all six tasks complete:

1. **WF-02 root /CONTRIBUTING.md present (Task 2.2):** `Test-Path 'CONTRIBUTING.md'` returns `True`; line count 150–200; all 8 D-10 sections present in order; all 5 GSD commands cited per D-13; verbatim 5-item do-not-fix list per D-15; vendor pointer-only per D-14.

2. **Inner CONTRIBUTING archived (Task 2.1):** `Test-Path 'spec_classifier/docs/dev/CONTRIBUTING.md'` returns `False`; `Test-Path '.planning/archive/CONTRIBUTING-2026-05-10.md'` returns `True`; git rename detected.

3. **Deep CLAUDE.md legacy block replaced (Task 2.3):** `git grep -n '## Tool Roles' -- spec_classifier/CLAUDE.md` returns 0 hits; `git grep -n 'Hard Rules for Claude Windows' -- spec_classifier/CLAUDE.md` returns 0 hits; `git grep -n '../CONTRIBUTING.md' -- spec_classifier/CLAUDE.md` returns ≥ 1 hit; line count drops from 307 to ~249.

4. **Root CLAUDE.md § Tooling roles updated (Task 2.4):** `git grep -n 'PRE-CHECK → PLAN → IMPLEMENT' -- CLAUDE.md` returns 0 hits; `git grep -n 'GSD-native' -- CLAUDE.md` returns ≥ 1 hit; `git grep -n '\[`/CONTRIBUTING.md`\](CONTRIBUTING.md)' -- CLAUDE.md` returns 1 hit; line count stays at ~74.

5. **DOCS_INDEX.md cleaned (Task 2.5):** `git grep -n '| `docs/dev/CONTRIBUTING.md`' -- spec_classifier/docs/DOCS_INDEX.md` returns 0 hits; `git grep -n '| `prompts/README.md`' -- spec_classifier/docs/DOCS_INDEX.md` returns 0 hits; `git grep -n 'Repo-root contributor doc' -- spec_classifier/docs/DOCS_INDEX.md` returns 1 hit.

6. **CHANGELOG.md new entry (Task 2.6):** `git grep -n '### Changed (Phase 3 — Workflow' -- spec_classifier/CHANGELOG.md` returns 1 hit; `git grep -n 'CONTRIBUTING-2026-05-10' -- spec_classifier/CHANGELOG.md` returns 1 hit; historical Phase 1 + Phase 2 sub-sections still present.

7. **D-22 honor "do not fix" enforcement:** `git diff --stat -- spec_classifier/golden/ spec_classifier/rules/ spec_classifier/src/ spec_classifier/tests/ spec_classifier/main.py spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/conftest.py run.ps1 teresa.bat teresa_gui.py` is empty.

8. **Single requirement-grouped commit lands at end of plan execution:** `docs(03-02): author root /CONTRIBUTING.md + cross-ref cleanup (WF-02)` — wraps all six tasks atomically (one-commit-per-requirement per Phase 1/2 convention).

Note: Plan 03-03 will run the full 7-step D-20 verification gate; this plan only verifies its own six tasks.
</verification>

<success_criteria>
- /CONTRIBUTING.md exists at repo root, 150-200 lines, 8 sections per D-10, GSD commands literal per D-13, pytest + skip-ratio gate cited, vendor pointer-only per D-14, 5 do-not-fix items verbatim per D-15.
- spec_classifier/docs/dev/CONTRIBUTING.md → archived (git mv to .planning/archive/CONTRIBUTING-2026-05-10.md per D-06).
- spec_classifier/CLAUDE.md legacy block (~63 lines) replaced with 3-line GSD pointer per D-16; earlier sections + 5 critical rules untouched.
- Root CLAUDE.md § Tooling roles 1-line pointer replacement per D-17; section header + .cursor/ note preserved.
- DOCS_INDEX.md lines 27 + 36 dropped per D-07/D-08; See-also breadcrumb under § Conventions per D-09.
- CHANGELOG.md new [Unreleased] sub-entry per D-19; historical lines untouched per D-18.
- Zero edits outside the six task scopes (D-22 honored — no code touched).
- Single commit per WF-02 requirement (Phase 1/2 convention).
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow/03-02-SUMMARY.md` summarizing:
- /CONTRIBUTING.md final line count + section list
- 5 cross-ref cleanup touch-points (Tasks 2.1, 2.3, 2.4, 2.5, 2.6) with before/after metrics
- Confirmation of zero code edits + zero touches to D-22 protected items
- Pointer to next plan (03-03 — D-20 verification gate + final commits + STATE/ROADMAP updates)
</output>
</content>
</invoke>