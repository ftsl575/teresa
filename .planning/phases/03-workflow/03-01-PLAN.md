---
phase: 03-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec_classifier/prompts/00_VENDOR-RECON.md
  - spec_classifier/prompts/01_PRE-CHECK.md
  - spec_classifier/prompts/02_MASTER-PLAN.md
  - spec_classifier/prompts/03_CURSOR-IMPLEMENT.md
  - spec_classifier/prompts/04_POST-CHECK.md
  - spec_classifier/prompts/05_AUDIT-1A-1G.md
  - spec_classifier/prompts/06_BATCH-AUDIT-MASTER-PLAN.md
  - spec_classifier/prompts/07_DOC-UPDATE-MASTER-PLAN.md
  - spec_classifier/prompts/08_CHATGPT-SYSTEM-PROMPTS.md
  - spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md
  - spec_classifier/prompts/README.md
  - .planning/archive/prompts-2026-05-10/00_VENDOR-RECON.md
  - .planning/archive/prompts-2026-05-10/01_PRE-CHECK.md
  - .planning/archive/prompts-2026-05-10/02_MASTER-PLAN.md
  - .planning/archive/prompts-2026-05-10/03_CURSOR-IMPLEMENT.md
  - .planning/archive/prompts-2026-05-10/04_POST-CHECK.md
  - .planning/archive/prompts-2026-05-10/05_AUDIT-1A-1G.md
  - .planning/archive/prompts-2026-05-10/06_BATCH-AUDIT-MASTER-PLAN.md
  - .planning/archive/prompts-2026-05-10/07_DOC-UPDATE-MASTER-PLAN.md
  - .planning/archive/prompts-2026-05-10/08_CHATGPT-SYSTEM-PROMPTS.md
  - .planning/archive/prompts-2026-05-10/COWORK_OPUS_FULL_AUDIT.md
  - .planning/archive/prompts-2026-05-10/README.md
  - LAUNCHER_README.md
autonomous: true
requirements: [WF-01]
tags: [workflow, archive, prompts, git-mv, launcher-repoint]
must_haves:
  truths:
    - "spec_classifier/prompts/ no longer exists in the working tree"
    - ".planning/archive/prompts-2026-05-10/ contains all 11 archived files (10 original .md + 1 rewritten English README.md)"
    - "All 10 0X_*.md and COWORK_OPUS_FULL_AUDIT.md files preserved verbatim in original Russian inside the archive (D-02 historical-record requirement)"
    - "Archive README.md is in English with three sections: archive note + per-file mapping table + back-pointer to /CONTRIBUTING.md (D-02)"
    - "Per-file mapping table contains all 11 entries with the exact wording locked in D-03"
    - "LAUNCHER_README.md line 52 points to spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (D-05) and no longer references prompts/"
    - "git history is preserved via git mv (rename detected, blame survives)"
  artifacts:
    - path: ".planning/archive/prompts-2026-05-10/README.md"
      provides: "English archive note + per-file mapping table per D-02/D-03/D-04"
      min_lines: 30
      contains: "00_VENDOR-RECON.md"
      contains: "/gsd-discuss-phase"
      contains: "NEW_VENDOR_GUIDE.md"
      contains: "audit-mode"
    - path: ".planning/archive/prompts-2026-05-10/00_VENDOR-RECON.md"
      provides: "Verbatim Russian original preserved as historical record (D-02)"
      min_lines: 20
    - path: ".planning/archive/prompts-2026-05-10/COWORK_OPUS_FULL_AUDIT.md"
      provides: "Verbatim Russian original preserved (D-04 uniform treatment)"
      min_lines: 20
    - path: "LAUNCHER_README.md"
      provides: "Repointed line 52 to NEW_VENDOR_GUIDE.md (D-05)"
      contains: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md"
  key_links:
    - from: ".planning/archive/prompts-2026-05-10/README.md"
      to: "/CONTRIBUTING.md"
      via: "back-pointer paragraph (D-02 section c)"
      pattern: "/CONTRIBUTING\\.md"
    - from: ".planning/archive/prompts-2026-05-10/README.md"
      to: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md"
      via: "00_VENDOR-RECON.md mapping row (D-03)"
      pattern: "NEW_VENDOR_GUIDE\\.md"
    - from: "LAUNCHER_README.md"
      to: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md"
      via: "line 52 'Implement adapter' pointer (D-05)"
      pattern: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE\\.md"
---

<objective>
Retire `spec_classifier/prompts/` (10 .md files + README.md, 1345 lines, Russian) by `git mv` to `.planning/archive/prompts-2026-05-10/`. Rewrite the archive's `README.md` in English with a per-file mapping table that maps each retired prompt to its GSD-native equivalent (D-02/D-03/D-04). Repoint `LAUNCHER_README.md` line 52 from `prompts/00_VENDOR-RECON.md` to `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` (D-05).

Purpose: WF-01 — make GSD the single canonical development cycle by retiring the pre-GSD prompt library that documented the legacy PRE-CHECK → MASTER-PLAN → CURSOR-IMPLEMENT → POST-CHECK → AUDIT 1A-1G workflow. Archives stabilize the cross-reference target list so Plan 03-02 (WF-02) can write to those targets without referencing files that still live under `spec_classifier/prompts/`.

Output:
- `.planning/archive/prompts-2026-05-10/` (10 verbatim Russian .md files + 1 new English README.md)
- `LAUNCHER_README.md` (line 52 repointed)
- `spec_classifier/prompts/` no longer exists
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow/03-CONTEXT.md
@.planning/phases/02-docs/02-SUMMARY.md
@.planning/phases/02-docs/02-CONTEXT.md
@.planning/phases/01-hygiene/01-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@LAUNCHER_README.md

<interfaces>
<!-- D-03 per-file mapping table — VERBATIM. Executor must reproduce these 11 rows exactly. -->

| Retired prompt | GSD-native equivalent |
|---|---|
| `00_VENDOR-RECON.md` | `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` |
| `01_PRE-CHECK.md` | `/gsd-discuss-phase` (or `/gsd-progress` for status) |
| `02_MASTER-PLAN.md` | `/gsd-plan-phase` |
| `03_CURSOR-IMPLEMENT.md` | `/gsd-execute-phase` |
| `04_POST-CHECK.md` | `/gsd-verify-work` |
| `05_AUDIT-1A-1G.md` | `/gsd-audit-fix` (and `/gsd-code-review` for the focused review pieces) |
| `06_BATCH-AUDIT-MASTER-PLAN.md` | run `python batch_audit.py` then `/gsd-plan-phase` against the findings |
| `07_DOC-UPDATE-MASTER-PLAN.md` | `/gsd-docs-update` |
| `08_CHATGPT-SYSTEM-PROMPTS.md` | no GSD equivalent (these were ChatGPT-specific system prompts) |
| `COWORK_OPUS_FULL_AUDIT.md` | `/gsd-audit-fix` or `/gsd-code-review` for the audit cycle (audit-mode, not a per-step prompt) |
| `README.md` | superseded by this English README + `/CONTRIBUTING.md` |

<!-- LAUNCHER_README.md line 52 — current state (verbatim). -->

Current line 52: `1. Implement adapter (see `prompts/00_VENDOR-RECON.md`).`
Target line 52: `1. Implement adapter (see [`spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`](spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md)).`

<!-- Phase 2 D-08 archive pattern (precedent): -->

Phase 2 used `git mv spec_classifier/CURRENT_STATE.md .planning/archive/CURRENT_STATE-2026-05-10.md` (single file). This plan uses the same pattern, but for an entire folder: `git mv spec_classifier/prompts/ .planning/archive/prompts-2026-05-10/`. After git mv, `git status -s` shows `R` (rename) entries with similarity index. History is preserved (`git log --follow .planning/archive/prompts-2026-05-10/01_PRE-CHECK.md` returns the original creation history).

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1.1: git mv spec_classifier/prompts/ to .planning/archive/prompts-2026-05-10/</name>
  <read_first>
    - .planning/phases/03-workflow/03-CONTEXT.md (D-01 git mv approach; D-02 archive README rewrite scope; D-04 COWORK file uniform treatment)
    - .planning/phases/02-docs/02-CONTEXT.md (D-08 archive pattern precedent — same shape, single file)
    - .planning/archive/CURRENT_STATE-2026-05-10.md (Phase 2 archive — confirms `.planning/archive/` directory exists)
    - spec_classifier/prompts/ (directory listing — 11 files: 00..08, COWORK_OPUS_FULL_AUDIT.md, README.md)
  </read_first>
  <action>
    Move the entire `spec_classifier/prompts/` folder to `.planning/archive/prompts-2026-05-10/` using `git mv`. After the move, all 11 files (10 `.md` + the `README.md` to be rewritten in Task 1.2) live under `.planning/archive/prompts-2026-05-10/`. Git history is preserved via git's rename detection.

    From repo root (PowerShell):

    ```powershell
    Set-Location C:/Users/G/Desktop/teresa

    # Pre-flight: confirm source exists, target parent exists, target does NOT exist yet.
    if (-not (Test-Path 'spec_classifier/prompts')) {
        Write-Host "FAIL — source spec_classifier/prompts/ missing." -ForegroundColor Red
        exit 1
    }
    if (-not (Test-Path '.planning/archive')) {
        Write-Host "FAIL — .planning/archive/ missing (Phase 2 should have created it)." -ForegroundColor Red
        exit 1
    }
    if (Test-Path '.planning/archive/prompts-2026-05-10') {
        Write-Host "FAIL — target .planning/archive/prompts-2026-05-10/ already exists; refusing to overwrite." -ForegroundColor Red
        exit 1
    }

    # Pre-flight: confirm all 11 expected files are present in the source.
    $expected = @('00_VENDOR-RECON.md','01_PRE-CHECK.md','02_MASTER-PLAN.md','03_CURSOR-IMPLEMENT.md','04_POST-CHECK.md','05_AUDIT-1A-1G.md','06_BATCH-AUDIT-MASTER-PLAN.md','07_DOC-UPDATE-MASTER-PLAN.md','08_CHATGPT-SYSTEM-PROMPTS.md','COWORK_OPUS_FULL_AUDIT.md','README.md')
    $missing = $expected | Where-Object { -not (Test-Path "spec_classifier/prompts/$_") }
    if ($missing.Count -gt 0) {
        Write-Host "FAIL — missing source files: $($missing -join ', ')" -ForegroundColor Red
        exit 1
    }

    # Whole-folder git mv (single command — git handles all 11 files atomically with rename detection).
    git mv spec_classifier/prompts .planning/archive/prompts-2026-05-10
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL — git mv exited with code $LASTEXITCODE." -ForegroundColor Red
        exit 1
    }

    # Post-flight: source must be gone; target must contain all 11 files.
    if (Test-Path 'spec_classifier/prompts') {
        Write-Host "FAIL — spec_classifier/prompts/ still exists after git mv." -ForegroundColor Red
        exit 1
    }
    $movedCount = (Get-ChildItem .planning/archive/prompts-2026-05-10 -Filter '*.md' | Measure-Object).Count
    Write-Host "Moved $movedCount .md files. Expected: 11."

    # Confirm git sees this as renames (not adds + deletes).
    git status -s
    ```

    The `git status -s` output should show `R` (rename) entries for each of the 11 files, e.g.:
    ```
    R  spec_classifier/prompts/00_VENDOR-RECON.md -> .planning/archive/prompts-2026-05-10/00_VENDOR-RECON.md
    R  spec_classifier/prompts/01_PRE-CHECK.md   -> .planning/archive/prompts-2026-05-10/01_PRE-CHECK.md
    ...
    ```

    Per D-02, the 10 `0X_*.md` files and `COWORK_OPUS_FULL_AUDIT.md` are preserved VERBATIM (Russian) as historical record — Task 1.1 does NOT translate or edit their content. Only `README.md` is rewritten (Task 1.2).

    Per D-04, `COWORK_OPUS_FULL_AUDIT.md` receives the same archive treatment as `00..08` — moved alongside, listed in the mapping table flagged "audit-mode" (in Task 1.2's mapping table) rather than "cycle-step" so a reader doesn't expect a step-by-step.

    Honor the "do not fix" framing per D-22/D-23: this task touches NO code, NO YAML rules, NO src/, NO tests/, NO golden/. Only prompt-doc files move.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); if(Test-Path 'spec_classifier/prompts'){ $err += 'source still exists' }; if(-not (Test-Path '.planning/archive/prompts-2026-05-10')){ $err += 'target dir missing' }; $expected=@('00_VENDOR-RECON.md','01_PRE-CHECK.md','02_MASTER-PLAN.md','03_CURSOR-IMPLEMENT.md','04_POST-CHECK.md','05_AUDIT-1A-1G.md','06_BATCH-AUDIT-MASTER-PLAN.md','07_DOC-UPDATE-MASTER-PLAN.md','08_CHATGPT-SYSTEM-PROMPTS.md','COWORK_OPUS_FULL_AUDIT.md','README.md'); foreach($f in $expected){ $p=\".planning/archive/prompts-2026-05-10/$f\"; if(-not (Test-Path $p)){ $err += \"missing in archive: $f\" } }; $st=git status -s 2>&1 | Out-String; $renameCount=([regex]::Matches($st,'^R\\s+spec_classifier/prompts/.*-> \\.planning/archive/prompts-2026-05-10/','Multiline')).Count; if($renameCount -lt 11){ $err += \"git did not detect 11 renames; got $renameCount\" }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path 'spec_classifier/prompts'` returns `False` (source folder gone)
    - `Test-Path '.planning/archive/prompts-2026-05-10'` returns `True`
    - All 11 expected files present in archive: `00_VENDOR-RECON.md`, `01_PRE-CHECK.md`, `02_MASTER-PLAN.md`, `03_CURSOR-IMPLEMENT.md`, `04_POST-CHECK.md`, `05_AUDIT-1A-1G.md`, `06_BATCH-AUDIT-MASTER-PLAN.md`, `07_DOC-UPDATE-MASTER-PLAN.md`, `08_CHATGPT-SYSTEM-PROMPTS.md`, `COWORK_OPUS_FULL_AUDIT.md`, `README.md`
    - `git status -s` shows ≥ 11 `R` (rename) entries with `spec_classifier/prompts/... -> .planning/archive/prompts-2026-05-10/...` pattern
    - All 10 archived original `.md` files (00..08 + COWORK) are unchanged byte-for-byte vs pre-move state — verify via `git diff --cached -- .planning/archive/prompts-2026-05-10/00_VENDOR-RECON.md` returning empty (rename only)
    - Zero edits to source files inside `spec_classifier/src/`, `spec_classifier/rules/`, `spec_classifier/tests/`, `spec_classifier/golden/`, `spec_classifier/main.py`, `spec_classifier/batch_audit.py`, `spec_classifier/cluster_audit.py`
    - `git status -s` shows zero modifications outside the archive move (no incidental edits)
  </acceptance_criteria>
  <done>spec_classifier/prompts/ no longer exists; .planning/archive/prompts-2026-05-10/ contains all 11 files; git status shows 11 rename entries; original .md content is byte-identical to pre-move state (rewrite of README.md happens in Task 1.2).</done>
</task>

<task type="auto">
  <name>Task 1.2: Rewrite .planning/archive/prompts-2026-05-10/README.md in English with per-file mapping table</name>
  <read_first>
    - .planning/archive/prompts-2026-05-10/README.md (the just-renamed Russian original — read to know what's being replaced)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-02 three-section structure; D-03 verbatim mapping table; D-04 COWORK as audit-mode)
    - .planning/phases/01-hygiene/01-CONTEXT.md (D-01 placeholder convention; markdown context uses `<USERNAME>` token if any path examples appear)
  </read_first>
  <action>
    Replace the contents of `.planning/archive/prompts-2026-05-10/README.md` (currently the verbatim Russian original from `spec_classifier/prompts/README.md`) with a brand-new English README per D-02. The file must have THREE sections in this order:

    **(a) One-paragraph archive note** — what this folder is and when it was archived.
    **(b) Per-file mapping table** — VERBATIM the 11 rows from D-03/D-04 (locked in `<interfaces>` above).
    **(c) Back-pointer paragraph** — link to `/CONTRIBUTING.md` as the canonical doc.

    Write exactly this content (no other text allowed):

    ```markdown
    # Archived Prompt Library (Pre-GSD)

    > **Archived:** 2026-05-10
    > **Status:** Historical record — superseded by the GSD-native development cycle documented in [`/CONTRIBUTING.md`](../../../CONTRIBUTING.md).

    This folder is the retired pre-GSD prompt library that documented the legacy
    PRE-CHECK → MASTER-PLAN → CURSOR-IMPLEMENT → POST-CHECK → AUDIT 1A–1G workflow.
    The original 10 prompt files (00..08, plus `COWORK_OPUS_FULL_AUDIT.md`) are
    preserved verbatim in their original Russian as a historical record. They are
    NOT the active workflow — see the per-file mapping below for the GSD-native
    replacement of each retired prompt.

    ## Per-file Mapping

    | Retired prompt | GSD-native equivalent |
    |---|---|
    | `00_VENDOR-RECON.md` | [`spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`](../../../spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md) |
    | `01_PRE-CHECK.md` | `/gsd-discuss-phase` (or `/gsd-progress` for status) |
    | `02_MASTER-PLAN.md` | `/gsd-plan-phase` |
    | `03_CURSOR-IMPLEMENT.md` | `/gsd-execute-phase` |
    | `04_POST-CHECK.md` | `/gsd-verify-work` |
    | `05_AUDIT-1A-1G.md` | `/gsd-audit-fix` (and `/gsd-code-review` for the focused review pieces) |
    | `06_BATCH-AUDIT-MASTER-PLAN.md` | run `python batch_audit.py` then `/gsd-plan-phase` against the findings |
    | `07_DOC-UPDATE-MASTER-PLAN.md` | `/gsd-docs-update` |
    | `08_CHATGPT-SYSTEM-PROMPTS.md` | no GSD equivalent (these were ChatGPT-specific system prompts) |
    | `COWORK_OPUS_FULL_AUDIT.md` | `/gsd-audit-fix` or `/gsd-code-review` for the audit cycle (audit-mode, not a per-step prompt) |
    | `README.md` | superseded by this English README + [`/CONTRIBUTING.md`](../../../CONTRIBUTING.md) |

    ## Canonical Doc

    The current development cycle (Discuss → Plan → Execute → Verify, GSD-native),
    pytest invocation, PR conventions, and the project's "do not fix" tech-debt
    rules are documented in [`/CONTRIBUTING.md`](../../../CONTRIBUTING.md).
    Run `/gsd-help` for the full list of GSD commands.
    ```

    Notes for the executor:

    1. **Path discipline:** the archive lives at `.planning/archive/prompts-2026-05-10/README.md`; back-pointers use `../../../` to reach the repo root (three levels up: `prompts-2026-05-10/` → `archive/` → `.planning/` → repo root). The `00_VENDOR-RECON.md` row uses the same prefix to reach `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`.
    2. **D-02 (a) section:** the one-paragraph note is the prose under the H1 plus the second prose paragraph after the blockquote. Keep it short — readers know they reached an archive folder.
    3. **D-02 (b) section:** the per-file mapping table MUST contain all 11 rows in the order shown above. Per D-04, `COWORK_OPUS_FULL_AUDIT.md` is flagged "audit-mode, not a per-step prompt" so a reader doesn't expect a step-by-step.
    4. **D-02 (c) section:** the `## Canonical Doc` section is the back-pointer; it points at `/CONTRIBUTING.md` (which Plan 03-02 will create — written before Plan 03-03's verification gate runs the read pass).
    5. **No Russian:** this file is the only one in the archive that gets rewritten in English. The 10 archived `0X_*.md` files plus `COWORK_OPUS_FULL_AUDIT.md` stay verbatim Russian per D-02 historical-record requirement.
    6. **No "do not fix YET" framing per D-23:** this is a doc-only retire; the file does NOT discuss whether the legacy workflow could be revived. Frame as superseded, not deferred.
    7. **Use the Write tool**, NOT bash heredoc/cat. The Write tool overwrites the existing file content (currently the Russian README.md from the git mv) with the English version above.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='.planning/archive/prompts-2026-05-10/README.md'; if(-not (Test-Path $f)){ $err += 'README missing'; $err | ForEach-Object { Write-Output $_ }; exit 1 }; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if($lines -lt 30){ $err += \"too short: $lines lines (target >= 30)\" }; foreach($t in 'Archived Prompt Library (Pre-GSD)','Archived: 2026-05-10','/CONTRIBUTING.md','## Per-file Mapping','| `00_VENDOR-RECON.md`','| `01_PRE-CHECK.md`','| `02_MASTER-PLAN.md`','| `03_CURSOR-IMPLEMENT.md`','| `04_POST-CHECK.md`','| `05_AUDIT-1A-1G.md`','| `06_BATCH-AUDIT-MASTER-PLAN.md`','| `07_DOC-UPDATE-MASTER-PLAN.md`','| `08_CHATGPT-SYSTEM-PROMPTS.md`','| `COWORK_OPUS_FULL_AUDIT.md`','| `README.md`','/gsd-discuss-phase','/gsd-plan-phase','/gsd-execute-phase','/gsd-verify-work','/gsd-audit-fix','/gsd-docs-update','NEW_VENDOR_GUIDE.md','audit-mode','/gsd-help','## Canonical Doc'){ if($c -notmatch [regex]::Escape($t)){ $err += \"missing: $t\" } }; if($c -match '[А-Яа-я]'){ $err += 'Russian residue detected' }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/archive/prompts-2026-05-10/README.md`
    - File length >= 30 lines (D-02 three-section structure fits comfortably)
    - H1 is `# Archived Prompt Library (Pre-GSD)`
    - Contains literal `Archived: 2026-05-10` (date stamp)
    - Contains the literal heading `## Per-file Mapping`
    - Contains the literal heading `## Canonical Doc`
    - Mapping table contains ALL 11 entries: `00_VENDOR-RECON.md`, `01_PRE-CHECK.md`, `02_MASTER-PLAN.md`, `03_CURSOR-IMPLEMENT.md`, `04_POST-CHECK.md`, `05_AUDIT-1A-1G.md`, `06_BATCH-AUDIT-MASTER-PLAN.md`, `07_DOC-UPDATE-MASTER-PLAN.md`, `08_CHATGPT-SYSTEM-PROMPTS.md`, `COWORK_OPUS_FULL_AUDIT.md`, `README.md`
    - All 7 GSD command pointers present: `/gsd-discuss-phase`, `/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-verify-work`, `/gsd-audit-fix`, `/gsd-docs-update`, `/gsd-help`
    - Pointer to `NEW_VENDOR_GUIDE.md` present (D-03 row 1; vendor-recon mapping)
    - Literal text `audit-mode` present (D-04 COWORK flag)
    - Three back-pointers to `/CONTRIBUTING.md` (one in blockquote, one in mapping `README.md` row, one in `## Canonical Doc` section)
    - Zero Russian residue: `[А-Яа-я]` regex returns no matches (D-02 English-rewrite requirement)
    - The other 10 archived `.md` files (`00..08`, `COWORK_OPUS_FULL_AUDIT.md`) remain verbatim Russian — `git diff` against pre-move state for those files is empty
  </acceptance_criteria>
  <done>.planning/archive/prompts-2026-05-10/README.md is rewritten in English with three sections (archive note, per-file mapping table with 11 entries, back-pointer to /CONTRIBUTING.md). All 11 mapping rows match D-03/D-04 verbatim. The other 10 archived files remain Russian and verbatim.</done>
</task>

<task type="auto">
  <name>Task 1.3: Repoint LAUNCHER_README.md line 52 from prompts/00_VENDOR-RECON.md to NEW_VENDOR_GUIDE.md</name>
  <read_first>
    - LAUNCHER_README.md (current 73 lines — line 52 is the 'Implement adapter' instruction in the 'Adding a new vendor' section)
    - .planning/phases/03-workflow/03-CONTEXT.md (D-05 — exact target file to repoint to)
    - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (the new pointer target — confirm it exists; this was the canonical guide post-Phase-2)
  </read_first>
  <action>
    Edit `LAUNCHER_README.md` line 52 to replace the legacy `prompts/00_VENDOR-RECON.md` reference with `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` per D-05. The natural successor; self-contained; doesn't send contributors into the archive for content already covered by a live guide.

    **Exact line replacement:**

    Current line 52 (verbatim from current LAUNCHER_README.md):
    ```
    1. Implement adapter (see `prompts/00_VENDOR-RECON.md`).
    ```

    Target line 52:
    ```
    1. Implement adapter (see [`spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`](spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md)).
    ```

    Use the Edit tool with `old_string` = the current line and `new_string` = the target line. No other line in LAUNCHER_README.md changes. The surrounding context (lines 50–55, the `## Adding a new vendor (e.g. xFusion)` section's 3-step list) is preserved.

    Pre-flight: confirm the target file exists.
    ```powershell
    Set-Location C:/Users/G/Desktop/teresa
    if (-not (Test-Path 'spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md')) {
        Write-Host "FAIL — NEW_VENDOR_GUIDE.md missing; cannot repoint." -ForegroundColor Red
        exit 1
    }
    ```

    Per D-22/D-23: this is a pointer-only edit. The "Adding a new vendor" section's other 2 steps (line 53–55: "In `run.ps1`: append `xfusion` to `$ALL_VENDORS`" and "In `teresa_gui.py`: move `xfusion` from `VENDORS_DISABLED` to `VENDORS_ACTIVE`") stay verbatim — the launcher still works the way it works today; only the doc reference changes.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$err=@(); $f='LAUNCHER_README.md'; $c=Get-Content $f -Raw; if($c -match 'prompts/00_VENDOR-RECON\\.md'){ $err += 'old prompts/ reference still present' }; if($c -notmatch [regex]::Escape('spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md')){ $err += 'new pointer to NEW_VENDOR_GUIDE.md missing' }; if($c -notmatch '1\\.\\s+Implement adapter \\(see'){ $err += 'step-1 line shape lost' }; if($c -notmatch 'append `\"xfusion\"` to `\\$ALL_VENDORS`'){ $err += 'step-2 (run.ps1) line lost' }; if($c -notmatch 'VENDORS_DISABLED'){ $err += 'step-3 (teresa_gui) line lost' }; if(-not (Test-Path 'spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md')){ $err += 'pointer target NEW_VENDOR_GUIDE.md does not exist' }; if($err.Count -gt 0){ $err | ForEach-Object { Write-Output $_ }; exit 1 } else { Write-Output 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep 'prompts/00_VENDOR-RECON\.md' -- LAUNCHER_README.md` returns 0 hits (old pointer gone)
    - `git grep 'spec_classifier/docs/dev/NEW_VENDOR_GUIDE\.md' -- LAUNCHER_README.md` returns ≥ 1 hit (new pointer present)
    - Line 52 is the modified line; the literal text `1. Implement adapter (see [` is at the start of the line (Markdown link form)
    - The other two list items in the `## Adding a new vendor` section are unchanged (`run.ps1` step + `teresa_gui.py` step)
    - Total file line count stays at 72 (no insertions or deletions; pure in-place replace)
    - Pointer target file `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` exists on disk
    - No edits anywhere else in LAUNCHER_README.md (`git diff LAUNCHER_README.md` shows exactly one changed line)
  </acceptance_criteria>
  <done>LAUNCHER_README.md line 52 points to spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (markdown link form); the legacy prompts/00_VENDOR-RECON.md reference is gone; the surrounding 3-step list and the rest of the file are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase; no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | N/A | Documentation-only phase | accept | N/A — documentation-only phase; no code execution paths, network endpoints, auth flows, or external input handling introduced. File moves are git-mv operations preserving history; new doc content is markdown text. |
</threat_model>

<verification>
After all three tasks complete:

1. `Test-Path 'spec_classifier/prompts'` returns `False` AND `Test-Path '.planning/archive/prompts-2026-05-10'` returns `True` AND archive contains all 11 files (Tasks 1.1 + 1.2).
2. `git status -s` shows ≥ 11 rename entries `R  spec_classifier/prompts/... -> .planning/archive/prompts-2026-05-10/...` (Task 1.1 git-mv detection).
3. `.planning/archive/prompts-2026-05-10/README.md` is in English, contains the 11-row mapping table verbatim per D-03/D-04, has back-pointers to `/CONTRIBUTING.md` (Task 1.2).
4. `git grep -n 'prompts/00_VENDOR-RECON\.md' -- LAUNCHER_README.md` returns 0 hits; `git grep -n 'NEW_VENDOR_GUIDE\.md' -- LAUNCHER_README.md` returns 1 hit (Task 1.3).
5. `git diff --stat -- spec_classifier/golden/ spec_classifier/rules/ spec_classifier/src/ spec_classifier/tests/ spec_classifier/main.py spec_classifier/batch_audit.py spec_classifier/cluster_audit.py run.ps1 teresa.bat teresa_gui.py` is empty (D-22 honor "do not fix" — no code touched).
6. The 10 archived original `.md` files retain their Russian content byte-for-byte (verifier spot-checks `git diff --cached -- .planning/archive/prompts-2026-05-10/01_PRE-CHECK.md` returning empty after the rename).
7. Single requirement-grouped commit lands at end of plan execution: `chore(03-01): retire spec_classifier/prompts/ to archive + repoint LAUNCHER_README (WF-01)` — wraps all three tasks atomically (one-commit-per-requirement per Phase 1/2 convention).

Note: Plan 03-03 will run the full 7-step D-20 verification gate; this plan only verifies its own three tasks.
</verification>

<success_criteria>
- spec_classifier/prompts/ no longer exists; .planning/archive/prompts-2026-05-10/ contains all 11 files (Russian originals preserved, README.md rewritten in English).
- LAUNCHER_README.md line 52 points to spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md.
- Zero edits outside the three task scopes (no code touched per D-22).
- Single commit per WF-01 requirement (Phase 1/2 convention).
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow/03-01-SUMMARY.md` summarizing:
- Files moved (count + verification of git rename detection)
- Mapping table content (all 11 rows)
- LAUNCHER_README.md change (1 line)
- Confirmation of zero code edits
- Pointer to next plan (03-02 — root /CONTRIBUTING.md + cross-ref cleanup)
</output>
</content>
</invoke>