---
phase: 08-audit-routing-audit
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - spec_classifier/tests/test_batch_audit.py
  - spec_classifier/tests/test_cluster_audit.py
autonomous: true
requirements: [ROUTE-03, ROUTE-04]
must_haves:
  truths:
    - "vendor-detection-from-path tests assert the new SPLIT/<vendor>/<spec>/ layout, not dead {vendor}_run / hp_run paths"
    - "aggregate-destination tests read audit_report.json / cluster_summary.xlsx from the AUDIT/ root, not the bare output_dir root"
    - "dual-bucket discovery tests place fixtures under AUDIT/ and SPLIT/ and assert prefer-audited dedup"
    - "pytest passes within the skip-gate (skipped/total < 0.50, passed > 0) with goldens byte-equal (no --update-golden)"
    - "D-08: dead-matcher removal is routing/path-detection logic (not audit logic); vendor-detection-from-path tests realigned to the new layout (SC#4 / TEST-01)"
  artifacts:
    - path: "spec_classifier/tests/test_batch_audit.py"
      provides: "realigned detect_vendor_from_path + AUDIT-root report assertions"
      contains: "AUDIT"
    - path: "spec_classifier/tests/test_cluster_audit.py"
      provides: "realigned dual-bucket discovery + AUDIT-root aggregate assertions"
      contains: "AUDIT"
  key_links:
    - from: "test_batch_audit detect_vendor_from_path tests"
      to: "new /<vendor>/ layout"
      via: "fixture paths use SPLIT/<vendor>/<spec>/ not <vendor>_run"
      pattern: "SPLIT/.*/.*annotated"
    - from: "test_batch_audit + test_cluster_audit aggregate tests"
      to: "AUDIT/ root reads"
      via: "tmp_path / 'AUDIT' / <name>"
      pattern: "AUDIT.*audit_report|AUDIT.*cluster_summary"
---

<objective>
Realign the `batch_audit` and `cluster_audit` path/layout tests to the new
`<bucket>/<vendor>/<spec>/` structure so the suite passes after Plans 08-01 and 08-02:
fix vendor-detection-from-path tests (dead `{vendor}_run`/`hp_run` removed), fix
aggregate-destination tests to read from the `AUDIT/` root, and fix dual-bucket discovery
tests to place fixtures under `AUDIT/` and `SPLIT/`. Then run the full pytest suite and
confirm it passes within the skip-gate with goldens byte-equal.

Purpose: Closes SC#4 / TEST-01 for Phase 8 (fix tests, not goldens) and provides the green
acceptance gate for ROUTE-03 + ROUTE-04. Routing/test-only — no golden content changes, no
`--update-golden`.
Output: Realigned test files and a passing `pytest -q` session within the skip-ratio gate.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-audit-routing-audit/08-CONTEXT.md
@.planning/phases/08-audit-routing-audit/08-PATTERNS.md
@.planning/phases/08-audit-routing-audit/08-01-PLAN.md
@.planning/phases/08-audit-routing-audit/08-02-PLAN.md
@CLAUDE.md
@spec_classifier/CLAUDE.md

<constraints>
FIX TESTS, NOT GOLDENS. No `--update-golden` anywhere. Golden `*_expected.jsonl` fixtures
stay byte-equal. Only path/layout + vendor-detection assertions change — classification
content is untouched. Skip-gate: `pytest` session fails if `skipped/total > 0.50` or
`passed == 0` (conftest.py:14 MAX_SKIP_RATIO=0.50, sessionfinish at :141-217). Do NOT
weaken or remove the skip-gate. Do NOT touch `cluster_audit._detect_vendor_from_path` tests'
intent beyond what the routing change requires (that function is NOT modified by Phase 8).
</constraints>

<interfaces>
Confirmed test sites (verified during planning):

`tests/test_batch_audit.py` — `class TestDetectVendorFromPath` (lines 438-464), KNOWN = ["cisco","dell","hpe"]:
- `test_dell_run` (:441-442) `OUTPUT/dell_run/file.xlsx` -> "dell"  — BREAKS (relied on {vendor}_run; now "unknown")
- `test_hpe_run` (:444-445) `OUTPUT/hpe_run/file.xlsx` -> "hpe"    — BREAKS
- `test_cisco_run` (:447-448) `OUTPUT/cisco_run/file.xlsx` -> "cisco" — BREAKS
- `test_hp_run_alias_returns_hpe` (:450-451) `OUTPUT/hp_run/file.xlsx` -> "hpe" — BREAKS (alias removed)
- `test_lenovo_run_returns_unknown` (:453-454) `OUTPUT/lenovo_run/file.xlsx` -> "unknown" — STILL PASSES (lenovo not in KNOWN; now passes for a different reason)
- `test_no_vendor_keyword_returns_unknown` (:456-457) -> "unknown" — STILL PASSES
- `test_new_vendor_in_known_vendors` (:459-461) `OUTPUT/lenovo_run/...` extended -> "lenovo" — BREAKS (relied on {vendor}_run)
- `test_vendor_in_directory_path` (:463-464) `OUTPUT/dell/subdir/file.xlsx` -> "dell" — STILL PASSES (this is the new-layout matcher)

`tests/test_batch_audit.py` — `class TestRealBugClassification._run_report` (:325-339):
- builds report via `_generate_report(report_files, out, ...)` then reads
  `Path(out) / "audit_report.json"` (:338). After 08-01, `_generate_report` writes to
  `out/AUDIT/audit_report.json` -> these reads BREAK. Same for the inline test at :389-391.

`tests/test_cluster_audit.py`:
- `class Test_detect_vendor_from_path` (:41-72) tests `cluster_audit._detect_vendor_from_path`
  using `_run` paths (:49-51, :64, :72). This is a SEPARATE function NOT modified by Phase 8
  (D-07 touches only batch_audit). These tests should STILL PASS unchanged — verify, do not
  edit unless they actually fail.
- `test_collect_xlsx_files_prefers_audited` (:96-105), `_includes_annotated_only` (:109-),
  `_empty_dir` (:118-119): place fixtures at flat `tmp_path` and call `_collect_xlsx_files(tmp_path)`.
  After 08-02 the function reads only `tmp_path/AUDIT` and `tmp_path/SPLIT` -> these BREAK
  (flat fixtures no longer discovered).
- `test_write_cluster_summary_creates_xlsx` (:218-231) reads `tmp_path / "cluster_summary.xlsx"`;
  `test_write_cluster_summary_updates_json` (:234-) seeds + reads `tmp_path / "audit_report.json"`;
  `_filters_small_clusters` (:257-274) reads `tmp_path / "cluster_summary.xlsx"`. After 08-02
  these write to `tmp_path/AUDIT/` -> reads/seeds BREAK.

Skip-gate: `spec_classifier/conftest.py:14` MAX_SKIP_RATIO=0.50; sessionfinish forces fail
on ratio>0.50 or passed==0, and also fails if input_root missing entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign batch_audit path/aggregate tests to new layout + AUDIT root</name>
  <files>spec_classifier/tests/test_batch_audit.py</files>
  <read_first>
    - spec_classifier/tests/test_batch_audit.py (lines 320-464 — TestRealBugClassification + TestDetectVendorFromPath — the file being modified)
    - spec_classifier/batch_audit.py (final detect_vendor_from_path 1354-1365 + _generate_report AUDIT dest, post Plan 08-01)
    - .planning/phases/08-audit-routing-audit/08-01-PLAN.md (exact behavior after edits)
    - .planning/phases/08-audit-routing-audit/08-CONTEXT.md (D-07, D-08, SC#4 / TEST-01)
  </read_first>
  <action>
1. `TestDetectVendorFromPath` (test_batch_audit.py:438-464). The function now matches ONLY
   `/{vendor}/` and `\{vendor}\` (no `_run`, no `hp_run`). Realign the broken tests to assert
   the new `SPLIT/<vendor>/<spec>/` layout (fix tests, not behavior). Concrete edits:
   - `test_dell_run` (:441-442): change path to
     `Path("OUTPUT/SPLIT/dell/dl1/dl1_annotated.xlsx")`, keep `== "dell"`.
     (Optionally rename method to `test_dell_split_layout` for clarity — discretion.)
   - `test_hpe_run` (:444-445): path `Path("OUTPUT/SPLIT/hpe/hp1/hp1_annotated.xlsx")`, `== "hpe"`.
   - `test_cisco_run` (:447-448): path `Path("OUTPUT/SPLIT/cisco/ccw_1/ccw_1_annotated.xlsx")`, `== "cisco"`.
   - `test_new_vendor_in_known_vendors` (:459-461): path
     `Path("OUTPUT/SPLIT/lenovo/L1/L1_annotated.xlsx")` with `extended = self.KNOWN + ["lenovo"]`,
     `== "lenovo"`.
   - `test_hp_run_alias_returns_hpe` (:450-451): the `hp_run -> hpe` alias is GONE (D-07).
     The path `OUTPUT/hp_run/file.xlsx` no longer maps to hpe. Replace this test's intent:
     assert that a bare `hp_run` style path now returns "unknown" (alias removed), e.g.
     `assert detect_vendor_from_path(Path("OUTPUT/hp_run/file.xlsx"), self.KNOWN) == "unknown"`,
     and rename to `test_hp_run_alias_removed_returns_unknown`. (Do NOT re-add the alias.)
   - `test_lenovo_run_returns_unknown` (:453-454): leave path or update to
     `Path("OUTPUT/lenovo_run/file.xlsx")` — still "unknown" (lenovo not in KNOWN). No change
     required; verify it still passes.
   - `test_no_vendor_keyword_returns_unknown` (:456-457) and `test_vendor_in_directory_path`
     (:463-464): leave unchanged — they already exercise the new-layout matcher / negative case.

2. `TestRealBugClassification` aggregate reads. `_generate_report` now writes to
   `output_dir/AUDIT/audit_report.json` (Plan 08-01). Update the report-path reads:
   - `_run_report` helper (:338): change
     `report_path = Path(out) / "audit_report.json"`
     to `report_path = Path(out) / "AUDIT" / "audit_report.json"`.
   - inline test `test_device_mismatch_2_files_is_real_bug` (:391): change
     `(tmp_path / "audit_report.json").read_text(...)`
     to `(tmp_path / "AUDIT" / "audit_report.json").read_text(...)`.
   The `_generate_report` call itself is unchanged (still passes `out`/`tmp_path` as
   output_dir; the function appends AUDIT internally and mkdir-parents it).

Do NOT touch classification/E-code assertions, KNOWN_FP tests, or any golden. These are
path/layout-only edits (SC#4 / TEST-01).
  </action>
  <verify>
    <automated>cd spec_classifier; python -m pytest tests/test_batch_audit.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SPLIT/dell\|SPLIT/hpe\|SPLIT/cisco\|SPLIT/lenovo" spec_classifier/tests/test_batch_audit.py` shows the realigned new-layout fixture paths
    - `grep -n "_run/file.xlsx" spec_classifier/tests/test_batch_audit.py` shows no surviving positive-assertion on a `_run` path (only the unknown-returning lenovo_run / hp_run-removed cases may reference `_run`)
    - `grep -nc "AUDIT" spec_classifier/tests/test_batch_audit.py` >= 2 (report reads relocated): verify with `grep -v '^#' spec_classifier/tests/test_batch_audit.py | grep -c '"AUDIT"'`
    - `python -m pytest spec_classifier/tests/test_batch_audit.py -q` exits 0 (all batch_audit tests pass)
  </acceptance_criteria>
  <done>All TestDetectVendorFromPath cases assert the new /<vendor>/ (SPLIT) layout or the alias-removed unknown behavior; TestRealBugClassification reads audit_report.json from the AUDIT/ root; test_batch_audit.py passes 100%.</done>
</task>

<task type="auto">
  <name>Task 2: Realign cluster_audit discovery + aggregate tests to AUDIT/SPLIT buckets</name>
  <files>spec_classifier/tests/test_cluster_audit.py</files>
  <read_first>
    - spec_classifier/tests/test_cluster_audit.py (lines 41-119 detect + collect tests; 215-280 write_cluster_summary tests — the file being modified)
    - spec_classifier/cluster_audit.py (final _collect_xlsx_files + write_cluster_summary, post Plan 08-02)
    - .planning/phases/08-audit-routing-audit/08-02-PLAN.md (exact behavior after edits)
    - .planning/phases/08-audit-routing-audit/08-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
1. `_collect_xlsx_files` discovery tests (test_cluster_audit.py:93-119). The function now
   rglobs only `tmp_path/AUDIT` (audited) and `tmp_path/SPLIT` (annotated). Move each fixture
   into the correct bucket subtree before touching:
   - `test_collect_xlsx_files_prefers_audited` (:96-105): currently
     `(tmp_path / "widget_annotated_audited.xlsx").touch()` + a `widget_annotated.xlsx`.
     Change to create the bucket dirs and place fixtures:
     ```python
     (tmp_path / "AUDIT" / "v" / "widget").mkdir(parents=True)
     (tmp_path / "AUDIT" / "v" / "widget" / "widget_annotated_audited.xlsx").touch()
     (tmp_path / "SPLIT" / "v" / "widget").mkdir(parents=True)
     (tmp_path / "SPLIT" / "v" / "widget" / "widget_annotated.xlsx").touch()
     ```
     Keep the assertion that the audited file is the one selected
     (`"widget_annotated_audited.xlsx" in names`) — dedup prefer-audited is unchanged.
   - `test_collect_xlsx_files_includes_annotated_only` (:109-): place the lone annotated
     fixture under `tmp_path / "SPLIT" / "v" / "<stem>" /` (mkdir parents), keep the assertion
     that it is included.
   - `test_collect_xlsx_files_empty_dir` (:118-119): `_collect_xlsx_files(tmp_path)` on an
     empty tmp_path must still return `[]` (no AUDIT/SPLIT -> is_dir() guards yield empty).
     Leave the assertion `== []`; it passes by the missing-bucket guards. Verify.

2. `write_cluster_summary` aggregate tests (test_cluster_audit.py:215-280). The function now
   writes to `output_dir/AUDIT/`. The call still passes `tmp_path` as output_dir; update the
   read/seed paths:
   - `test_write_cluster_summary_creates_xlsx` (:230-231): change
     `assert (tmp_path / "cluster_summary.xlsx").exists()`
     to `assert (tmp_path / "AUDIT" / "cluster_summary.xlsx").exists()`.
   - `test_write_cluster_summary_updates_json` (:234-250): the seed
     `report_path = tmp_path / "audit_report.json"` must move so the function finds it after
     appending AUDIT. Change seed + read to `tmp_path / "AUDIT" / "audit_report.json"`, and
     create the AUDIT dir before seeding:
     ```python
     (tmp_path / "AUDIT").mkdir(parents=True, exist_ok=True)
     report_path = tmp_path / "AUDIT" / "audit_report.json"
     ```
     (The function also mkdir-guards AUDIT for the xlsx, but seed must pre-exist for the
     update branch to run.) Keep the "clusters" key assertion.
   - `test_write_cluster_summary_filters_small_clusters` (:271-274): change
     `wb = openpyxl.load_workbook(tmp_path / "cluster_summary.xlsx")`
     to `... tmp_path / "AUDIT" / "cluster_summary.xlsx"`.

3. `Test_detect_vendor_from_path` (test_cluster_audit.py:41-72): `cluster_audit._detect_vendor_from_path`
   is NOT modified by Phase 8 (D-07 touches only batch_audit). These tests should still pass
   unchanged. Run them; only edit if a test actually fails (it should not). Do NOT remove the
   `_run` matching from cluster_audit's function — out of scope.

Path/layout-only edits. No golden, no `--update-golden`, no clustering-logic assertions changed.
  </action>
  <verify>
    <automated>cd spec_classifier; python -m pytest tests/test_cluster_audit.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"AUDIT"' spec_classifier/tests/test_cluster_audit.py` shows audited-fixture + aggregate-read relocations
    - `grep -n '"SPLIT"' spec_classifier/tests/test_cluster_audit.py` shows annotated-fixture relocations
    - `grep -n "AUDIT.*cluster_summary.xlsx\|cluster_summary.xlsx" spec_classifier/tests/test_cluster_audit.py` shows reads now under AUDIT (no bare `tmp_path / "cluster_summary.xlsx"` assertion remains)
    - `python -m pytest spec_classifier/tests/test_cluster_audit.py -q` exits 0 (all cluster_audit tests pass)
  </acceptance_criteria>
  <done>Discovery tests place audited fixtures under AUDIT/ and annotated under SPLIT/ and assert prefer-audited dedup; aggregate tests read cluster_summary.xlsx and audit_report.json from the AUDIT/ root; cluster_audit._detect_vendor_from_path tests still pass unchanged; test_cluster_audit.py passes 100%.</done>
</task>

<task type="auto">
  <name>Task 3: Full pytest suite green within skip-gate, goldens byte-equal (SC#4 gate)</name>
  <files>spec_classifier/tests/test_batch_audit.py, spec_classifier/tests/test_cluster_audit.py</files>
  <read_first>
    - spec_classifier/conftest.py (lines 14, 141-217 — skip-gate semantics; sessionfinish failure conditions)
    - .planning/ROADMAP.md (Phase 8 success criteria #4; Milestone-wide invariants — goldens byte-equal, no --update-golden, skip-gate)
    - .planning/phases/08-audit-routing-audit/08-CONTEXT.md (routing-only, fix-tests-not-goldens)
  </read_first>
  <action>
1. Run the full suite from the spec_classifier directory (the package root where conftest.py
   and pytest.ini live):
   ```
   cd spec_classifier
   python -m pytest -q
   ```
   Do NOT pass `--update-golden`. Do NOT pass any flag that weakens the skip-gate.

2. Confirm the session passes within the skip-gate:
   - Exit code 0.
   - `passed > 0` and `skipped / total < 0.50` (conftest.py enforces this; a "Skip guard
     triggered" failure means the INPUT data set is too sparse — that is an environment
     condition, NOT a code regression. If it triggers, report it as a blocker per the skip-gate
     invariant; do not edit conftest to bypass it).

3. Confirm goldens are byte-equal — no golden file was modified this phase:
   ```
   git diff --stat -- spec_classifier/golden/
   ```
   This MUST be empty (no changes to any `*_expected.jsonl`).

4. If any non-path test fails, it indicates an out-of-scope regression in 08-01/08-02 — do
   NOT mask it by editing goldens or weakening assertions. Investigate the routing edit, or
   surface it back to the planner. Only path/layout assertions are legitimately editable
   (already done in Tasks 1-2).

This task is the SC#4 / TEST-01 acceptance gate for Phase 8.
  </action>
  <verify>
    <automated>cd spec_classifier; python -m pytest -q 2>&1 | tail -8; echo "--- golden diff (must be empty) ---"; git -C .. diff --stat -- spec_classifier/golden/</automated>
  </verify>
  <acceptance_criteria>
    - `cd spec_classifier; python -m pytest -q` exits 0 (session passed; skip-guard not triggered)
    - pytest summary line shows `passed` count > 0 and no `failed`
    - skip ratio < 0.50 (no "Skip guard triggered" session failure from conftest sessionfinish)
    - `git diff --stat -- spec_classifier/golden/` is EMPTY (all `*_expected.jsonl` byte-equal — no --update-golden used)
    - `git diff -- spec_classifier/batch_audit.py spec_classifier/cluster_audit.py` shows only routing/path edits (no E-code/logic/alias lines changed)
  </acceptance_criteria>
  <done>Full pytest suite exits 0 within the skip-gate (passed>0, skip-ratio<0.50) with zero golden-fixture diffs and no --update-golden used; Phase 8 ROUTE-03 + ROUTE-04 acceptance bar (SC#1-4) met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> filesystem | Tests construct fixtures under `tmp_path` (pytest-managed temp dir); no untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | (n/a) | test-only edits | accept | This plan edits test files and runs the suite. No production code path, no external input, no new trust boundary. Test fixtures live under pytest `tmp_path`. The only integrity control of interest is the skip-gate and golden byte-equality, which Task 3 explicitly enforces (no --update-golden, golden diff must be empty). |

**No high-severity threats.** This is a test-realignment + verification plan with no
production attack surface. The skip-gate and goldens-byte-equal invariants are upheld, not
weakened. No blocking (high) threat — nothing to escalate.
</threat_model>

<verification>
- `cd spec_classifier; python -m pytest tests/test_batch_audit.py tests/test_cluster_audit.py -q` exits 0.
- `cd spec_classifier; python -m pytest -q` exits 0 within the skip-gate (passed>0, skip-ratio<0.50).
- `git diff --stat -- spec_classifier/golden/` is empty (goldens byte-equal; no --update-golden).
- Realigned assertions target the new bucket layout (greps in acceptance criteria).
</verification>

<success_criteria>
- batch_audit vendor-detection-from-path tests assert the new `/<vendor>/` (SPLIT) layout or alias-removed-unknown behavior; report reads from AUDIT/ root.
- cluster_audit discovery tests place fixtures under AUDIT/ + SPLIT/ with prefer-audited dedup; aggregate tests read from AUDIT/ root.
- Full pytest suite passes within the skip-gate (passed>0, skipped/total<0.50).
- All `*_expected.jsonl` goldens are byte-equal (no --update-golden anywhere). SC#4 / TEST-01 met for Phase 8.
</success_criteria>

<output>
After completion, create `.planning/phases/08-audit-routing-audit/08-03-SUMMARY.md`
</output>
