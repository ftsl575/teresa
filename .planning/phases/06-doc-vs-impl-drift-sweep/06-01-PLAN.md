---
phase: 06-doc-vs-impl-drift-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - CONTRIBUTING.md
  - spec_classifier/docs/DOCS_INDEX.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-01]
tags: [doc-sweep, drift, root-docs]

must_haves:
  truths:
    - "Every claim in README.md, CLAUDE.md, CONTRIBUTING.md, and spec_classifier/docs/DOCS_INDEX.md was mechanically verified against the post-Phase-4-and-5 tree (per D-10)."
    - "Drifted claims in the 4 files were resolved per D-13 (default: remove; patch only if load-bearing AND patched form is stable)."
    - "06-DRIFT-AUDIT.md exists with one row per (file, line, claim_summary, check_command, resolution) tuple, including no_drift rows."
    - "D-22 protected paths byte-equal: zero diff inside spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py} over this plan's window."
    - "Goldens byte-equal: git diff --stat -- spec_classifier/golden/ empty over this plan's window."
  artifacts:
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Per-claim audit log with the file/line/claim/check/resolution table per D-22 and Specifics"
      contains: "| file | line | claim_summary | check_command | resolution |"
    - path: "README.md"
      provides: "Root README with verified claims (drifted lines removed or patched)"
    - path: "CLAUDE.md"
      provides: "Root CLAUDE.md with verified claims (drifted lines removed or patched)"
    - path: "CONTRIBUTING.md"
      provides: "CONTRIBUTING.md with verified claims (drifted lines removed or patched)"
    - path: "spec_classifier/docs/DOCS_INDEX.md"
      provides: "DOCS_INDEX.md verified 1:1 against actual filesystem state of spec_classifier/docs/"
  key_links:
    - from: "06-DRIFT-AUDIT.md"
      to: "the 4 swept files in this plan"
      via: "audit-log row per (file, line, claim) tuple"
      pattern: "README\\.md|CLAUDE\\.md|CONTRIBUTING\\.md|DOCS_INDEX\\.md"
---

<objective>
Mechanically sweep the 3 root markdown files (`README.md`, `CLAUDE.md`, `CONTRIBUTING.md`) plus `spec_classifier/docs/DOCS_INDEX.md` (sweep order Groups 1 + 2 per D-14). For every "code does X" claim in scope per D-11 (path/file existence + behavior, switch/CLI flag, line-number refs), run a mechanical check (`grep` / `Test-Path` / runtime / line-number lookup) and resolve drift per D-13 (default: remove; patch only when claim is load-bearing AND patched form is stable). Initialize `06-DRIFT-AUDIT.md` and append one row per claim swept.

Volatile-count claims (test count, LOC, rule_id counts, vendor counts) are DEFERRED per D-12 — if encountered in IN-scope files, REMOVE per the remove-greater-than-patch heuristic; do not patch to a current count.

Purpose: First wave of the DRIFT-01 sweep. Root files lead the order (D-14) so they constrain the docs/ tree's vocabulary in Plans 02-03.
Output: 4 swept files (or unchanged if no drift), 06-DRIFT-AUDIT.md initialized with rows for every claim considered.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@CLAUDE.md
@CONTRIBUTING.md
@README.md
@spec_classifier/docs/DOCS_INDEX.md

<interfaces>
<!-- Source-of-truth wiring vocabulary (Phase 4 D-08/D-13, Phase 5 D-05/D-06) used to validate "code does X" claims in this sweep -->

Canonical wiring claim that must be true post-Phase-4-and-5:
- `PYTHONPYCACHEPREFIX` and `PYTEST_ADDOPTS` env vars are set by BOTH `run.ps1` AND `teresa_gui.py` from `config.local.yaml::temp_root` (defense-in-depth per Phase 4 D-13).
- `run.ps1` invokes `clean.ps1` by default; `-NoClean` opts out (Phase 4 CACHE-03).
- No `scripts/run_full.ps1` references in `*.toml` / `*.example` files (Phase 5 ORPH-01/02).
- `power_cord` `hw_type=None` is intentional per recovery commit `c3c7cb6`.
- Six vendors registered: dell, cisco, hpe, lenovo, huawei, xfusion.

Audit-log table format (recommended per Specifics):
| file | line | claim_summary | check_command | resolution |
|------|------|---------------|---------------|------------|
| README.md | NN | "..." | `grep -q ... run.ps1` | remove / patch / no_drift |

`resolution` ∈ {remove, patch, no_drift}. `no_drift` rows are recorded too (full inventory per Specifics + SC-5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize 06-DRIFT-AUDIT.md skeleton</name>
  <files>.planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md (D-22 audit-log schema; Specifics audit-log table columns)
    - .planning/ROADMAP.md § "Phase 6: Doc-vs-Impl Drift Sweep" §SC-5 ("every claim flagged" wording)
  </read_first>
  <action>
Create `.planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` with this exact skeleton (the table will be appended to in Tasks 2-5 of this plan and in Plans 02-05; finalized in Plan 06):

```markdown
# Phase 6 Drift Audit Log

**Created:** 2026-05-11
**Phase:** 06-doc-vs-impl-drift-sweep
**Sibling artifacts:** PLAN.md, CONTEXT.md, VERIFICATION.md, SUMMARY.md (per D-22)
**Sweep order:** D-14 — Group 1 (root) → Group 2 (DOCS_INDEX) → Group 3 (docs/dev) → Group 4 (docs/user) → Group 5 (other docs/) → Area A (.planning/codebase/ surgical patches).

## Purpose

Per-claim ledger for the Phase 6 mechanical drift sweep (DRIFT-01..04). Every "code does X" claim across the 16 in-scope files plus the 3 surgical-patch lines in `.planning/codebase/` is recorded here as one row, including `no_drift` rows so the log is a complete inventory of every claim considered (per Specifics + ROADMAP §SC-5 "every file checked, every claim flagged, and the resolution for each").

`resolution` ∈ `{remove, patch, no_drift}`. Default per D-13 is `remove`; `patch` only when the claim is load-bearing for the reader's mental model AND the patched form is itself stable (symbol/section ref, not a line number).

Categories swept (per D-11):
1. Path/file existence + behavior claims (highest load-bearing — class that broke v1.0).
2. Switch/CLI flag claims.
3. Line-number refs (`file.py:N`) — apply remove > patch aggressively; rewrite to symbol/section refs.

Categories DEFERRED per D-12 (folded into v1.2 `/gsd-map-codebase` refresh; if encountered in-scope, REMOVE):
- Volatile counts: test count, LOC counts, rule_id counts, vendor count.

## Audit Table

| file | line | claim_summary | check_command | resolution |
|------|------|---------------|---------------|------------|

<!-- Rows appended by Plans 01-05 below this line. -->

## Tally (filled by Plan 06)

- Total claims swept: TBD
- Resolutions: TBD remove / TBD patch / TBD no_drift
- Files touched: TBD of 19 (16 in-scope + 3 surgical map lines)
- Drift remaining post-phase: 0 (per ROADMAP §SC-1)
```

Commit subject: `docs(06): T1 initialize 06-DRIFT-AUDIT.md ledger`.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md &amp;&amp; grep -q "## Audit Table" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md &amp;&amp; grep -q "| file | line | claim_summary | check_command | resolution |" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0
    - `grep -q "## Audit Table" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0
    - `grep -q "| file | line | claim_summary | check_command | resolution |" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0
    - `grep -q "resolution. ∈" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0 (the `{remove, patch, no_drift}` legend is present)
    - `grep -q "Tally" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
  </acceptance_criteria>
  <done>06-DRIFT-AUDIT.md exists with the skeleton; audit table header present; tally section present (unfilled).</done>
</task>

<task type="auto">
  <name>Task 2: Sweep root README.md</name>
  <files>README.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - README.md (full file — sweep target; never assume contents)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14: sweep mechanism, claim categories, remove>patch heuristic, sweep order)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13 (canonical PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS wiring vocabulary used to validate any cache-redirect claims)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-05, D-06 (canonical replacement vocabulary used to validate any "run.ps1 sets X" claims)
    - run.ps1 (read lines 1-100 to confirm CLI switches, env vars, clean.ps1 invocation, and 6 vendors for any claims that reference them)
  </read_first>
  <action>
Sweep `README.md` mechanically per D-10. Read the file end-to-end. For every claim that falls into one of the D-11 categories below, run the corresponding mechanical check and decide remove vs. patch per D-13 (default: remove).

D-11 claim categories to scan for:
1. **Path/file existence + behavior:** "`run.ps1` exists", "`run.ps1` sets PYTHONPYCACHEPREFIX", "`scripts/clean.ps1` exists", "`run.ps1` invokes clean.ps1 by default", "audit reads `*_annotated.xlsx`", "first-match-wins YAML rules", "six vendors", etc. Validate via `Test-Path` / `grep`. Volatile counts (test count, LOC, rule_id, vendor count) are DEFERRED per D-12 — if encountered, REMOVE.
2. **Switch/CLI flag claims:** any `.\run.ps1 -X`, `python main.py --Y`, `pytest -k ...` example. Validate via `grep` in `run.ps1` / `main.py` / `teresa_gui.py`.
3. **Line-number refs (`file.py:N`):** apply remove > patch aggressively; rewrite to symbol or section refs ("see `run.ps1`'s `param()` block") instead of chasing line numbers.

For each claim found:
- If the check passes (claim still true): append a `no_drift` row to 06-DRIFT-AUDIT.md.
- If the check fails AND the claim is removable without degrading reader comprehension: REMOVE the line/sentence; append a `remove` row.
- If the check fails AND the claim is load-bearing AND patchable to a stable form (symbol/section ref): PATCH; append a `patch` row.

For each row appended to `06-DRIFT-AUDIT.md`, use this exact column shape:
`| README.md | <line_number_or_range> | "<short claim quote>" | \`<exact check command>\` | <remove|patch|no_drift> |`

If README.md has zero claims requiring change, still append at least the no_drift rows you considered (full inventory per Specifics).

Source-of-truth wiring claim used to validate cache-redirect statements (Phase 4 D-13 vocabulary): "`PYTHONPYCACHEPREFIX` + `PYTEST_ADDOPTS` env vars set by `run.ps1` and `teresa_gui.py` from `config.local.yaml::temp_root`". Single-source language ("`run.ps1` sets …" only) is a Phase 4 D-13 regression — flag and patch.

D-22 forbidden: any edit inside `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`.

Commit subject: `docs(06): T2 sweep README.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| README\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| README\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` returns ≥ 1 (at least one row recorded for README.md, which may be `no_drift`)
    - For every line REMOVED or PATCHED in README.md, a corresponding `remove` or `patch` row exists in 06-DRIFT-AUDIT.md (verifiable: `git diff README.md` shows N changed lines AND `grep -c "^| README\.md " 06-DRIFT-AUDIT.md` ≥ N when no_drift rows excluded)
    - No `scripts/run_full.ps1` claim survives in README.md: `grep -q "run_full" README.md` exits non-zero (Phase 5 ORPH-01/02 invariant)
  </acceptance_criteria>
  <done>README.md swept, every applicable claim recorded in 06-DRIFT-AUDIT.md (including no_drift rows), drifted lines removed or patched per D-13, D-22 protected paths byte-equal.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep root CLAUDE.md and CONTRIBUTING.md</name>
  <files>CLAUDE.md, CONTRIBUTING.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - CLAUDE.md (full file — sweep target)
    - CONTRIBUTING.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13 (canonical wiring vocabulary)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-05, D-06 (canonical replacement vocabulary)
    - run.ps1 (lines 1-100, switches + env vars)
  </read_first>
  <action>
Sweep `CLAUDE.md` then `CONTRIBUTING.md` mechanically per D-10. Both files are root-level docs; root group leads the sweep order (D-14) so they constrain the docs/ tree's vocabulary in Plans 02-03.

For each file, scan for the same three D-11 categories as Task 2:
1. Path/file existence + behavior claims.
2. Switch/CLI flag claims.
3. Line-number refs (`file.py:N`) — apply remove > patch aggressively (rewrite to symbol/section refs).

Special attention items in this pair:
- `CLAUDE.md` § "Critical business rules" enumerates `power_cord = None`, LOGISTIC scope, BASE/HW/CONFIG semantics, etc. — most claims here reflect the source-of-truth in `spec_classifier/CLAUDE.md` and the rule files; verify via `grep` against `spec_classifier/rules/dell_rules.yaml` and `spec_classifier/batch_audit.py` for `_E8_NO_HW_TYPE_DEVICES`, etc. Most should be `no_drift`.
- `CLAUDE.md` § "Where to look first" table — verify each cited file path exists via `Test-Path`.
- `CONTRIBUTING.md` § "Tests" CLI examples — verify via `grep` in `spec_classifier/conftest.py` for `MAX_SKIP_RATIO`, in `run.ps1` for `-TestsOnly` / `-SkipTests`.
- `CONTRIBUTING.md` § "Adding a vendor" — verify the `$ALL_VENDORS` and `VENDORS_ACTIVE` references via `grep` in `run.ps1` and `teresa_gui.py`.
- `CONTRIBUTING.md` § "Do not fix" 5-item list — same validation as `CLAUDE.md` business rules; expect `no_drift` for most.
- ANY `--update-golden` claim — verify via `grep -q "update-golden" spec_classifier/main.py` AND read `CLAUDE.md` Critical business rule #2 (goldens byte-equal). Both files reference `--update-golden` as a sanctioned operation; that claim is true (the in-flight v1.1 milestone constraint that no-update-golden is a v1.1-only phase-gate, not a permanent code property — DO NOT remove the `--update-golden` documentation; it's accurate as a code-affordance claim).

For each claim, append one row per file to `06-DRIFT-AUDIT.md`:
`| CLAUDE.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`
`| CONTRIBUTING.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

D-22 forbidden: any edit inside `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`.

Commit subject: `docs(06): T3 sweep CLAUDE.md and CONTRIBUTING.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| CLAUDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| CONTRIBUTING\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| CLAUDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -c "^| CONTRIBUTING\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "run_full" CLAUDE.md` exits non-zero (no `scripts/run_full.ps1` orphan ref survives) — note: CONTRIBUTING.md may legitimately reference historical commit-message examples; if any survive in CONTRIBUTING.md, they are noted as `no_drift` historical per D-18 with a justification in the audit row
    - `grep -q "power_cord" CLAUDE.md` exits 0 (CLAUDE.md continues to document the load-bearing power_cord rule)
  </acceptance_criteria>
  <done>CLAUDE.md and CONTRIBUTING.md swept; per-claim rows recorded in 06-DRIFT-AUDIT.md; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 4: Sweep spec_classifier/docs/DOCS_INDEX.md</name>
  <files>spec_classifier/docs/DOCS_INDEX.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/DOCS_INDEX.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § D-14 (sweep order — DOCS_INDEX is the 1:1 contract; touch only if a doc is renamed — none planned in Phase 6)
  </read_first>
  <action>
Sweep `spec_classifier/docs/DOCS_INDEX.md`. The DOCS_INDEX is a 1:1 contract with the actual filesystem state of `spec_classifier/docs/`. Phase 6 renames zero docs (DRIFT-03 only CREATES `DOC_INVARIANTS.md` in Plan 06; Plan 06 will append the new index entry there).

For this task:
1. List the actual contents of `spec_classifier/docs/` (already known from CONTEXT.md canonical_refs; the 13-file inventory is enumerated there).
2. Read DOCS_INDEX.md and verify each listed entry corresponds to an actual file (via `Test-Path` / `ls`).
3. For each claim in DOCS_INDEX about a doc's purpose / scope / target audience: verify it's still true (most should be `no_drift` — DOCS_INDEX entries are short labels).
4. For any path/CLI claim or behavior summary: validate per D-11 categories (#1 path/file existence, #2 switch/CLI, #3 line-number refs).

Note: `DOC_INVARIANTS.md` does NOT yet exist in DOCS_INDEX; Plan 06 will add the entry as part of creating the file. Do NOT add a placeholder entry in this task.

Append one row per claim to `06-DRIFT-AUDIT.md`:
`| spec_classifier/docs/DOCS_INDEX.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

D-22 forbidden: any edit inside `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`.

Commit subject: `docs(06): T4 sweep DOCS_INDEX.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| spec_classifier/docs/DOCS_INDEX\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| spec_classifier/docs/DOCS_INDEX\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - For each filename mentioned in DOCS_INDEX.md after this task, the file actually exists: a Bash check like `for f in $(grep -oE 'docs/[a-z]+/[A-Z_]+\.md' spec_classifier/docs/DOCS_INDEX.md | sort -u); do test -f "spec_classifier/$f" || exit 1; done` exits 0
    - `grep -q "DOC_INVARIANTS" spec_classifier/docs/DOCS_INDEX.md` exits NON-zero (Plan 06 will add this entry; do not pre-add)
  </acceptance_criteria>
  <done>DOCS_INDEX.md swept; per-claim rows recorded; every doc filename in the index resolves to a real file; D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (doc-only changes; no executable surface added) | Plan 01 modifies markdown files and one planning ledger. No new attack surface introduced; no inputs are deserialized; no shell evaluation; no network egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | 06-DRIFT-AUDIT.md | accept | Audit log is committed to git per task; tampering is detectable via `git log` / `git diff`. Same model as PLAN.md / SUMMARY.md siblings. |
| T-06-02 | Information Disclosure | README.md, CLAUDE.md, CONTRIBUTING.md | accept | These docs are public/committed; sweep only verifies/removes drifted claims, never adds new info. No secrets surfaced. |
| T-06-03 | Repudiation | sweep results in audit log | mitigate | Per-task atomic commits with `docs(06): T<N> ...` subject pattern (D-21) provide non-repudiable change history. |
</threat_model>

<verification>
Plan-level checks (run after Task 4 completes):
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty (D-22 protected paths byte-equal).
2. `git diff --stat -- spec_classifier/golden/` empty (goldens byte-equal).
3. `06-DRIFT-AUDIT.md` exists and contains at least one row for each of: `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `spec_classifier/docs/DOCS_INDEX.md`.
4. Phase 5 ORPH grep gate still passes: `grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning --exclude=CHANGELOG.md --exclude=LAUNCHER_README.md` returns zero matches (this plan does not regress Phase 5).
5. SC #1 partial check (per-file scope): the 4 files swept here have no surviving drift in the D-11 categories — Plan 06's full SC #1 re-sweep covers the global verdict.
</verification>

<success_criteria>
- 4 files swept (`README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `spec_classifier/docs/DOCS_INDEX.md`) per D-10/D-11.
- 06-DRIFT-AUDIT.md initialized with skeleton + per-claim rows for every applicable claim in the 4 files.
- D-22 protected paths byte-equal over the plan window.
- Goldens byte-equal over the plan window.
- All 4 atomic commits use the `docs(06): T<N> <description>` subject pattern (D-21).
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-01-SUMMARY.md` summarizing: number of claims swept per file, number resolved as remove/patch/no_drift, any anomalies encountered, total tally to be carried into Plan 06's final tally.
</output>
