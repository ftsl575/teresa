---
phase: 06-doc-vs-impl-drift-sweep
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - spec_classifier/docs/dev/DOC_INVARIANTS.md
  - spec_classifier/docs/DOCS_INDEX.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-03, DRIFT-04]
tags: [doc-invariants, drift-03, sc-gate, finalize-audit]

must_haves:
  truths:
    - "`spec_classifier/docs/dev/DOC_INVARIANTS.md` exists, is well-formed Markdown per D-20, and contains all 8 invariants from D-18 verbatim as Bash one-liners (matches the ROADMAP §SC-2 ≥5 floor with 3 to spare)."
    - "Every one of the 8 DOC_INVARIANTS.md Bash one-liners exits 0 against the post-phase tree (ROADMAP §SC-4 — invariants accurate, not aspirational)."
    - "Invariant #8 specifically (`grep -q \".SYNOPSIS\" run.ps1`) exits 0 because Plan 04 T1 landed the help block (D-19 ordering — this is why this plan is Wave 2)."
    - "`spec_classifier/docs/DOCS_INDEX.md` is updated to include the new `DOC_INVARIANTS.md` entry under the dev/ subtree."
    - "`06-DRIFT-AUDIT.md` Tally section is filled in (total claims swept, remove/patch/no_drift breakdown, files-touched count, drift-remaining=0)."
    - "ROADMAP §SC-1 re-sweep returns 0 drift claims across the 16 in-scope files (mechanical check: each invariant exits 0; spot-check the diffed lines in Plans 01-05)."
    - "D-22 protected paths byte-equal."
    - "Goldens byte-equal."
    - "Pytest skip-ratio gate (`pytest -q` from `spec_classifier/`) finishes without tripping the 0.50 skip-ratio guard (D-25 sanity check)."
  artifacts:
    - path: "spec_classifier/docs/dev/DOC_INVARIANTS.md"
      provides: "Doc-of-record listing 8 mechanical drift invariants per D-15..D-20"
      contains: "PYTHONPYCACHEPREFIX"
      min_lines: 80
    - path: "spec_classifier/docs/DOCS_INDEX.md"
      provides: "Updated index including DOC_INVARIANTS.md entry"
      contains: "DOC_INVARIANTS"
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Finalized audit log with Tally section filled"
      contains: "Total claims swept"
  key_links:
    - from: "DOC_INVARIANTS.md invariant #1-#8"
      to: "post-Phase-4-and-5-and-Phase-6 tree"
      via: "8 Bash one-liners that all exit 0"
      pattern: "PYTHONPYCACHEPREFIX|PYTEST_ADDOPTS|clean.ps1|run_full|intentionally unmapped|.SYNOPSIS"
    - from: "DOCS_INDEX.md updated entry"
      to: "spec_classifier/docs/dev/DOC_INVARIANTS.md"
      via: "DOCS_INDEX 1:1 contract restored after the new file lands"
      pattern: "DOC_INVARIANTS"
---

<objective>
Wave 2 closure plan. Depends on all 5 Wave-1 plans (01-05) per D-19 (invariant #8 needs Plan 04 T1's help block to exist; SC #1 re-sweep needs all sweeps and surgical patches done).

Three deliverables:

1. **T1: Create `spec_classifier/docs/dev/DOC_INVARIANTS.md`** per D-15..D-20 with all 8 invariants from D-18 verbatim as Bash one-liners. Format per D-20 (H1 title; H2 §Purpose with retrospective link; H2 §How to run with composite invocation footer per D-17; H2 §Invariants with one numbered subsection per invariant — paragraph + fenced `bash` one-liner + "Why this matters" line; H2 §Adding new invariants with the 3-bullet rubric from Specifics).

2. **T2: Update `spec_classifier/docs/DOCS_INDEX.md`** to add the `DOC_INVARIANTS.md` entry under the `dev/` subtree (DOCS_INDEX is a 1:1 contract per Plan 01 Task 4).

3. **T3: Finalize `06-DRIFT-AUDIT.md` Tally + run SC #1 + SC #4 verification gate.** Compute the totals across all rows appended in Plans 01-05, fill in the Tally section, and run the 8 invariants to confirm each exits 0 (SC #4 — invariants accurate, not aspirational). SC #1 re-sweep = the 8 invariants exiting 0 + spot-checking the diffed lines per CONTEXT.md D-10.

Per D-21, T1 + T2 use `docs(06)` commit subject. T3 uses `docs(06)` for the audit-log finalization commit.

Purpose: Closes DRIFT-03 (DOC_INVARIANTS.md created) and the audit-log half of DRIFT-04. Triggers ROADMAP §SC-1 / §SC-2 / §SC-4 / §SC-5 verification gates.
Output: 1 new file (DOC_INVARIANTS.md), 1 updated file (DOCS_INDEX.md), finalized audit log, 3 atomic commits.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
@spec_classifier/docs/DOCS_INDEX.md

<interfaces>
The 8 invariants verbatim from CONTEXT.md D-18 — these are the literal Bash one-liners that ship in DOC_INVARIANTS.md:

| # | Invariant | Bash one-liner |
|---|---|---|
| 1 | PYTHONPYCACHEPREFIX wired in run.ps1 (Phase 4 CACHE-01) | `grep -q "PYTHONPYCACHEPREFIX" run.ps1` |
| 2 | PYTHONPYCACHEPREFIX wired in teresa_gui.py (Phase 4 D-13 defense-in-depth) | `grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py` |
| 3 | PYTEST_ADDOPTS wired in run.ps1 (Phase 4 D-08 cache_dir partner) | `grep -q "PYTEST_ADDOPTS" run.ps1` |
| 4 | clean.ps1 invoked by run.ps1 (Phase 4 CACHE-03) | `grep -q "clean.ps1" run.ps1` |
| 5 | No scripts/run_full.ps1 orphan refs in *.toml / *.example (Phase 5 ORPH-01/02) | `! grep -rqI "run_full" --include='*.toml' --include='*.example' --exclude-dir=.planning --exclude=CHANGELOG.md --exclude=LAUNCHER_README.md .` |
| 6 | power_cord intentionally unmapped in dell_rules.yaml (recovery commit c3c7cb6) | `grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml` |
| 7 | Six vendors registered in run.ps1 $ALL_VENDORS | `for v in dell cisco hpe lenovo huawei xfusion; do grep -q "\"$v\"" run.ps1 || exit 1; done` |
| 8 | Phase 6 help block survives in run.ps1 (D-05 self-protect) | `grep -q ".SYNOPSIS" run.ps1` |

D-19 status (verified by discuss-phase scout 2026-05-11):
- Invariants #1-#7 exited 0 against the pre-Phase-6 tree.
- Invariant #8 currently FAILs against pre-Phase-6 tree (help block is itself the Plan 04 T1 deliverable).
- Plan 06 runs in Wave 2 (depends_on includes plan 04) so by the time T3 runs the verification, #8 also exits 0.

D-17 execution model: doc-of-record + manual run. NO CI integration. NO pre-commit hook. NO Python wrapper script. NO `/scripts/` artifact. The doc IS the tool.

D-20 recommended structure: H1 title; H2 §Purpose (retrospective link); H2 §How to run; H2 §Invariants (numbered subsections with `bash` fence + "Why this matters" line per invariant); H2 §Adding new invariants (3-bullet rubric).

Specifics "Adding new invariants" rubric (verbatim):
- Must exit 0 against the current tree (run it first)
- Must be a Bash one-liner (no scripts, no helpers)
- Must cite a real drift incident: which commit / phase / requirement surfaced the underlying claim. Hypotheticals don't earn an entry.

Audit-log Tally section (filled in T3):
- Total claims swept: count of rows in 06-DRIFT-AUDIT.md Audit Table
- Resolutions: count of remove + count of patch + count of no_drift
- Files touched: count of distinct files in column 1 (target: 19 = 16 in-scope + 3 surgical map lines, where the 3 surgical lines come from the 2 .planning/codebase/ files)
- Drift remaining: 0 (per ROADMAP §SC-1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec_classifier/docs/dev/DOC_INVARIANTS.md (D-15..D-20)</name>
  <files>spec_classifier/docs/dev/DOC_INVARIANTS.md</files>
  <read_first>
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area D (D-15..D-20 — location, language, execution model, all 8 invariants verbatim, format) and § Specifics ("Adding new invariants" rubric verbatim)
    - .planning/REQUIREMENTS.md § "Doc-vs-Impl Drift Sweep (DRIFT) — Plan 3" — DRIFT-03 wording + the "tooling/meta-doc materializing the v1.1 retrospective lesson" justification paragraph (linked from §Purpose)
    - .planning/ROADMAP.md § "Phase 6: Doc-vs-Impl Drift Sweep" §SC-2 + §SC-4 (the ≥5 floor and the exit-0 verification gate)
  </read_first>
  <action>
Create `spec_classifier/docs/dev/DOC_INVARIANTS.md` with the exact content below (D-20 recommended numbered-subsections format; all 8 invariants per D-18 verbatim; D-17 doc-of-record + manual-run model; Specifics rubric in §Adding new invariants).

The file content (write verbatim, including the fenced `bash` blocks):

```
# Doc Invariants

## Purpose

This file is a doc-of-record for mechanical drift invariants — claims about the codebase that documentation makes and that mechanical checks can verify in one Bash line.

It exists because the v1.0 milestone shipped with a real drift incident: `RUN_PATHS_AND_IO_LAYOUT.md:22` claimed `PYTHONPYCACHEPREFIX` was wired up by the launcher, when in fact it was only set by the (since-deleted) `scripts/run_full.ps1`. The v1.0 read-pass DOC-03 audit missed it. The v1.1 milestone retrospective (Phase 6) materialized the lesson: every "code does X" claim that documentation makes should have a one-line mechanical check, sitting in one place, where a future contributor can run them all in 5 seconds before opening a PR.

Each invariant below is one such check. They are intentionally short (Bash one-liners). They run in Git Bash on Windows and on POSIX. They have no runtime dependencies beyond `grep`, `test`, and standard shell utilities — matching the v1.1 no-new-tech-stack stance.

This is a doc, not a tool: there is no runner script, no pre-commit hook, no CI integration. To re-sweep, open this doc and paste the lines into Git Bash. The doc IS the tool.

## How to run

Open Git Bash at the repo root. Either run each invariant individually (copy the fenced one-liner and paste), or run them all at once with this composite loop (also useful for SC #4 verification gates):

(bash fenced block — see action below for the exact loop)

A passing run prints 8 PASS lines and exits 0. Any FAIL means a doc claim has drifted from code reality — fix the code, fix the doc, or both, then re-run.

## Invariants

### 1. PYTHONPYCACHEPREFIX wired in run.ps1
(paragraph + bash fence with `grep -q "PYTHONPYCACHEPREFIX" run.ps1` + Why this matters line)

### 2. PYTHONPYCACHEPREFIX wired in teresa_gui.py
(paragraph + bash fence with `grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py` + Why this matters)

### 3. PYTEST_ADDOPTS wired in run.ps1
(paragraph + bash fence with `grep -q "PYTEST_ADDOPTS" run.ps1` + Why this matters)

### 4. clean.ps1 invoked by run.ps1
(paragraph + bash fence with `grep -q "clean.ps1" run.ps1` + Why this matters)

### 5. No scripts/run_full.ps1 orphan refs
(paragraph + bash fence with the `! grep -rqI "run_full" ...` invocation + Why this matters)

### 6. power_cord intentionally unmapped
(paragraph + bash fence with `grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml` + Why this matters)

### 7. Six vendors registered in run.ps1
(paragraph + bash fence with `for v in dell cisco hpe lenovo huawei xfusion; do grep -q "\"$v\"" run.ps1 || exit 1; done` + Why this matters)

### 8. Phase 6 help block survives in run.ps1
(paragraph + bash fence with `grep -q ".SYNOPSIS" run.ps1` + Why this matters)

## Adding new invariants
- Must exit 0 against the current tree (run it first).
- Must be a Bash one-liner (no scripts, no helpers).
- Must cite a real drift incident: which commit / phase / requirement surfaced the underlying claim. Hypotheticals don't earn an entry.
```

The above is the OUTLINE. The full file content (with paragraphs filled in) is in the executor's expanded version. Each subsection MUST contain:
- A 1-2 paragraph explanation of what the invariant guards.
- A fenced ```bash code block containing the EXACT one-liner from the table in the `<interfaces>` block above.
- A "Why this matters:" line citing the recovery commit / phase / requirement / drift incident that surfaced it.

For the §How to run composite loop, ship this exact bash block (per D-17 and Specifics):

```
fail=0
for line in \
  'grep -q "PYTHONPYCACHEPREFIX" run.ps1' \
  'grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py' \
  'grep -q "PYTEST_ADDOPTS" run.ps1' \
  'grep -q "clean.ps1" run.ps1' \
  '! grep -rqI "run_full" --include="*.toml" --include="*.example" --exclude-dir=.planning --exclude=CHANGELOG.md --exclude=LAUNCHER_README.md .' \
  'grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml' \
  'for v in dell cisco hpe lenovo huawei xfusion; do grep -q "\"$v\"" run.ps1 || exit 1; done' \
  'grep -q ".SYNOPSIS" run.ps1' \
; do
  if eval "$line" >/dev/null 2>&1; then
    echo "PASS: $line"
  else
    echo "FAIL: $line"
    fail=$((fail + 1))
  fi
done
echo "$fail failing invariants"
exit $fail
```

Suggested per-invariant "Why this matters" anchors (use these as starting points, expand if needed):
- #1: Surfaced by Phase 4 CACHE-01. Recovery: re-add the env-var assignment in run.ps1's path-discovery block.
- #2: Surfaced by Phase 4 D-13 (defense-in-depth). Recovery: re-add the env-var block in teresa_gui.py main().
- #3: Surfaced by Phase 4 D-08. Recovery: re-add the $env:PYTEST_ADDOPTS line.
- #4: Surfaced by Phase 4 CACHE-03. Recovery: re-add the & $CleanScript invocation guarded by `if (-not $NoClean)`.
- #5: Surfaced by Phase 5 ORPH-01/ORPH-02. Mirrors Phase 5 §SC-1 grep verbatim.
- #6: Surfaced by recovery commit c3c7cb6 and CLAUDE.md root rule #1. Single most-broken business rule.
- #7: Surfaced by any new-vendor onboarding (CLAS-02 v2.x scope).
- #8: Surfaced by Phase 6 D-05 (self-protect). Recovery: re-insert the <#.SYNOPSIS .DESCRIPTION .PARAMETER .EXAMPLE#> block above param().

Commit subject: `docs(06): T1 create DOC_INVARIANTS.md (DRIFT-03)`.

D-22 forbidden: any edit inside `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`. (DOC_INVARIANTS.md goes under `spec_classifier/docs/dev/`, NOT inside D-22.)
  </action>
  <verify>
    <automated>test -f spec_classifier/docs/dev/DOC_INVARIANTS.md &amp;&amp; grep -q "^# Doc Invariants" spec_classifier/docs/dev/DOC_INVARIANTS.md &amp;&amp; grep -q "^## Purpose" spec_classifier/docs/dev/DOC_INVARIANTS.md &amp;&amp; grep -q "^## How to run" spec_classifier/docs/dev/DOC_INVARIANTS.md &amp;&amp; grep -q "^## Invariants" spec_classifier/docs/dev/DOC_INVARIANTS.md &amp;&amp; grep -q "^## Adding new invariants" spec_classifier/docs/dev/DOC_INVARIANTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0
    - `grep -q "^# Doc Invariants" spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0
    - All 4 D-20 H2 sections present: `for s in "## Purpose" "## How to run" "## Invariants" "## Adding new invariants"; do grep -q "^$s" spec_classifier/docs/dev/DOC_INVARIANTS.md || exit 1; done` exits 0
    - Exactly 8 numbered invariant subsections: `grep -c "^### [1-8]\." spec_classifier/docs/dev/DOC_INVARIANTS.md` returns `8`
    - All 8 D-18 Bash one-liners present (sample literal checks): `grep -q 'grep -q "PYTHONPYCACHEPREFIX" run.ps1' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0; `grep -q 'grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0; `grep -q 'grep -q "PYTEST_ADDOPTS" run.ps1' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0; `grep -q 'grep -q "clean.ps1" run.ps1' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0; `grep -q 'grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0; `grep -q 'for v in dell cisco hpe lenovo huawei xfusion' spec_classifier/docs/dev/DOC_INVARIANTS.md` exits 0
    - "Why this matters" appears for every invariant: `grep -c "Why this matters" spec_classifier/docs/dev/DOC_INVARIANTS.md` returns `8`
    - The Specifics 3-bullet rubric is present: `grep -q "Must exit 0 against the current tree" spec_classifier/docs/dev/DOC_INVARIANTS.md` AND `grep -q "Must be a Bash one-liner" spec_classifier/docs/dev/DOC_INVARIANTS.md` AND `grep -q "Must cite a real drift incident" spec_classifier/docs/dev/DOC_INVARIANTS.md` all exit 0
    - File length ≥ 80 lines: `wc -l spec_classifier/docs/dev/DOC_INVARIANTS.md` returns ≥ 80
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
  </acceptance_criteria>
  <done>DOC_INVARIANTS.md created with 8 invariants matching D-18 verbatim, D-20 structure, Specifics rubric; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 2: Add DOC_INVARIANTS.md entry to DOCS_INDEX.md</name>
  <files>spec_classifier/docs/DOCS_INDEX.md</files>
  <read_first>
    - spec_classifier/docs/DOCS_INDEX.md (full file — read post-Plan-01-T4 state to know the existing entry format under `dev/`)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area D (D-15 — DOC_INVARIANTS.md location is locked at `spec_classifier/docs/dev/`)
  </read_first>
  <action>
Add a new entry for `DOC_INVARIANTS.md` under the `dev/` subtree of `spec_classifier/docs/DOCS_INDEX.md`. The exact entry format depends on DOCS_INDEX's existing convention (read the file to confirm — it is likely a list of `dev/<FILE>.md` lines with a one-line description per entry). Mirror the format of the existing `ONE_BUTTON_RUN.md` / `OPERATIONAL_NOTES.md` / `TESTING_GUIDE.md` entries.

Suggested entry text (adjust to match the existing list/table shape):
- `dev/DOC_INVARIANTS.md` — Mechanical drift invariants (8 Bash one-liners). Doc-of-record per Phase 6 DRIFT-03.

Place the entry alphabetically among the existing `dev/` entries (so it appears between `dev/` start marker and the next non-`dev/` entry, alphabetically: `DOC_INVARIANTS.md` sorts before `NEW_VENDOR_GUIDE.md` and `ONE_BUTTON_RUN.md`).

Commit subject: `docs(06): T2 add DOC_INVARIANTS.md to DOCS_INDEX (DRIFT-03)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>grep -q "DOC_INVARIANTS" spec_classifier/docs/DOCS_INDEX.md &amp;&amp; grep -q "dev/DOC_INVARIANTS\.md" spec_classifier/docs/DOCS_INDEX.md &amp;&amp; test -f spec_classifier/docs/dev/DOC_INVARIANTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DOC_INVARIANTS" spec_classifier/docs/DOCS_INDEX.md` exits 0
    - `grep -q "dev/DOC_INVARIANTS\.md" spec_classifier/docs/DOCS_INDEX.md` exits 0
    - The DOCS_INDEX 1:1 contract holds: every `dev/<FILE>.md` reference in the index resolves to a real file (`for f in $(grep -oE 'dev/[A-Z_]+\.md' spec_classifier/docs/DOCS_INDEX.md | sort -u); do test -f "spec_classifier/docs/$f" || exit 1; done` exits 0)
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
  </acceptance_criteria>
  <done>DOCS_INDEX.md updated with new DOC_INVARIANTS.md entry; 1:1 contract preserved; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize 06-DRIFT-AUDIT.md Tally + run SC #1 + SC #4 verification gate</name>
  <files>.planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md (full current state — needs Tally section filled)
    - spec_classifier/docs/dev/DOC_INVARIANTS.md (created in T1; the 8 invariants are the basis for SC #1 + SC #4 verification)
    - .planning/ROADMAP.md § "Phase 6: Doc-vs-Impl Drift Sweep" §SC-1, §SC-2, §SC-4, §SC-5 (verification gates)
  </read_first>
  <action>
Three substeps in one task / one commit:

**(a) Compute tally for 06-DRIFT-AUDIT.md:**
Run these counts (uses Bash arithmetic; subtract the table-header row to get true claim count):

```
total=$(grep -c "^| " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)
header_rows=2  # 1 header row + 1 separator row
total_claims=$((total - header_rows))
removes=$(grep -cE "\| remove \|" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)
patches=$(grep -cE "\| patch \|" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)
no_drifts=$(grep -cE "\| no_drift \|" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)
files=$(grep -oE "^\| [^|]+\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | sort -u | wc -l)
```

Edit the Tally section at the bottom of 06-DRIFT-AUDIT.md to fill in the actual numbers, replacing the TBD placeholders. Final tally section format:

```
## Tally (filled by Plan 06)

- Total claims swept: <total_claims>
- Resolutions: <removes> remove / <patches> patch / <no_drifts> no_drift
- Files touched: <files> (target: 19 = 16 in-scope + the 2 .planning/codebase/ files contributing 3 surgical lines)
- Drift remaining post-phase: 0 (per ROADMAP §SC-1)
```

**(b) SC #4 — invariant exit-0 check:**
Run each of the 8 D-18 invariants and confirm exit 0. The composite loop block from DOC_INVARIANTS.md §How to run is the canonical way to do this — copy-paste it into Bash and confirm "0 failing invariants" output. Any FAIL means investigate before committing this task. Invariant #8 specifically depends on Plan 04 T1 having landed.

**(c) SC #1 re-sweep:**
Per D-10, the SC #1 re-sweep is "re-run DOC_INVARIANTS.md checks + spot-check newly-added claims by re-reading the diffed lines". Sub-step (b) above covers the invariants. For spot-checks: walk through the audit-log rows where resolution=patch and confirm the patched line still exits 0 against the relevant check. Minimum 5 spot-checks (or all patches if fewer than 5 exist).

Append a brief "SC #1 + SC #4 Verification" subsection to 06-DRIFT-AUDIT.md after the Tally:

```
## SC #1 + SC #4 Verification (filled at end of Plan 06)

- SC #4 — All 8 DOC_INVARIANTS.md Bash one-liners exit 0 against post-phase tree: PASS / FAIL (note any failures)
- SC #1 — Re-sweep returns 0 drift claims: PASS / FAIL
- Spot-checks performed: N (list patched files re-verified)
```

Commit subject: `docs(06): T3 finalize 06-DRIFT-AUDIT.md tally + SC1/SC4 gate (DRIFT-03, DRIFT-04)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>grep -q "Total claims swept: [0-9]" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md &amp;&amp; grep -q "SC #1 + SC #4 Verification" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md &amp;&amp; grep -q "PYTHONPYCACHEPREFIX" run.ps1 &amp;&amp; grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py &amp;&amp; grep -q "PYTEST_ADDOPTS" run.ps1 &amp;&amp; grep -q "clean.ps1" run.ps1 &amp;&amp; grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml &amp;&amp; grep -q "\.SYNOPSIS" run.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Total claims swept: [0-9]" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0 (Tally filled)
    - `grep -q "SC #1 + SC #4 Verification" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` exits 0 (verification subsection appended)
    - All 8 invariants exit 0 against the post-phase tree:
      1. `grep -q "PYTHONPYCACHEPREFIX" run.ps1` exits 0
      2. `grep -q "PYTHONPYCACHEPREFIX" teresa_gui.py` exits 0
      3. `grep -q "PYTEST_ADDOPTS" run.ps1` exits 0
      4. `grep -q "clean.ps1" run.ps1` exits 0
      5. `! grep -rqI "run_full" --include='*.toml' --include='*.example' --exclude-dir=.planning --exclude=CHANGELOG.md --exclude=LAUNCHER_README.md .` exits 0 (no orphan refs)
      6. `grep -q "intentionally unmapped" spec_classifier/rules/dell_rules.yaml` exits 0
      7. `for v in dell cisco hpe lenovo huawei xfusion; do grep -q "\"$v\"" run.ps1 || exit 1; done` exits 0
      8. `grep -q "\.SYNOPSIS" run.ps1` exits 0
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - Pytest skip-ratio gate (D-25): from `spec_classifier/`, `pytest -q` finishes without tripping the 0.50 skip-ratio guard. (Note: requires INPUT data in `%USERPROFILE%\Desktop\INPUT\<vendor>\`; if absent, the gate trips structurally — record passed/skipped/failed in plan summary regardless.)
  </acceptance_criteria>
  <done>06-DRIFT-AUDIT.md tally finalized, SC #1 + SC #4 gate run and PASS recorded, all 8 invariants exit 0 against post-phase tree, D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `DOC_INVARIANTS.md` Bash blocks ↔ Bash interpreter | The doc contains copy-pasteable Bash one-liners. Only contributors copy-paste them; no automated execution path. No deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Tampering | DOC_INVARIANTS.md | mitigate | Atomic per-task commit; the file's own self-protect invariant (#8) catches the deletion of Phase 6's help-block deliverable; future drift surfaces via re-running the doc. |
| T-06-18 | Information Disclosure | DOC_INVARIANTS.md | accept | Bash one-liners reference public file names and code patterns; no secrets, env vars, or credentials embedded. |
| T-06-19 | Tampering | 06-DRIFT-AUDIT.md tally | accept | Tally is computed by counting rows in a human-readable table; tampering is detectable via `git log` / `git diff`. |
| T-06-20 | Repudiation | DRIFT-03 + audit-finalization | mitigate | Three atomic commits using D-21 subject pattern. |
</threat_model>

<verification>
Plan-level checks (run after Task 3):
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty (D-22).
2. `git diff --stat -- spec_classifier/golden/` empty (goldens byte-equal).
3. ROADMAP §SC-1: re-sweep returns 0 drift — verified via the 8 invariants exiting 0 plus spot-checks in T3.
4. ROADMAP §SC-2: `DOC_INVARIANTS.md` exists and contains ≥5 mechanical invariants (8 actual).
5. ROADMAP §SC-3: `RUN_PATHS_AND_IO_LAYOUT.md` line count < 281, `ONE_BUTTON_RUN.md` line count < 54 (verified by Plan 04 T2/T3 acceptance criteria; re-confirmed here via `wc -l`).
6. ROADMAP §SC-4: each of the 8 invariant Bash one-liners exits 0 (T3 step b).
7. ROADMAP §SC-5: 06-DRIFT-AUDIT.md lists every file checked + every claim flagged + resolution; tally finalized in T3.
8. Pytest skip-ratio gate: `pytest -q` from `spec_classifier/` finishes without tripping 0.50 guard (D-25).
9. Phase 5 ORPH grep gate (Phase 5 §SC-1) still passes — also covered by invariant #5.
</verification>

<success_criteria>
- DRIFT-03 closed: DOC_INVARIANTS.md created with 8 invariants per D-15..D-20 / Specifics rubric.
- DRIFT-04 closed (audit-log half): 06-DRIFT-AUDIT.md tally finalized.
- ROADMAP §SC-1, §SC-2, §SC-4, §SC-5 verification gates run and PASS recorded.
- ROADMAP §SC-3 re-confirmed (line counts of trim docs strictly less than baselines).
- D-22 protected paths byte-equal across the entire phase window.
- Goldens byte-equal across the entire phase window.
- Pytest skip-ratio gate runs cleanly (D-25).
- 3 atomic commits using `docs(06): T<N>` subject pattern (D-21).
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-06-SUMMARY.md` recording:
- DOC_INVARIANTS.md final byte count + line count
- DOCS_INDEX.md update entry text used
- 06-DRIFT-AUDIT.md final tally numbers (total / remove / patch / no_drift / files)
- All 8 invariants pass/fail status
- pytest -q output (passed/skipped/failed counts) for D-25 sanity check
- Final D-22 + goldens byte-equal confirmation for the phase window
- Pointer to `.planning/phases/06-doc-vs-impl-drift-sweep/06-VERIFICATION.md` (created later by `/gsd-verify-work 6`)
</output>
