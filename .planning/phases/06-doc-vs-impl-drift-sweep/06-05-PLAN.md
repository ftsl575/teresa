---
phase: 06-doc-vs-impl-drift-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STACK.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-04]
tags: [surgical-patch, area-a, planning-codebase]

must_haves:
  truths:
    - "`.planning/codebase/STACK.md:79` no longer contains `Not set by run.ps1` / `used to be set by the old scripts/run_full.ps1` (D-02 stale claim removed)."
    - "`.planning/codebase/STACK.md:79` references both `PYTHONPYCACHEPREFIX` AND `PYTEST_ADDOPTS`, both `run.ps1` AND `teresa_gui.py`, AND `config.local.yaml::temp_root` (D-03 vocabulary)."
    - "`.planning/codebase/INTEGRATIONS.md:150` no longer contains `Not currently exported by run.ps1` / `was set by the now-retired scripts/run_full.ps1` (D-02 stale claim removed)."
    - "`.planning/codebase/INTEGRATIONS.md:150` references both `PYTHONPYCACHEPREFIX` AND `PYTEST_ADDOPTS`, both `run.ps1` AND `teresa_gui.py`, AND `config.local.yaml::temp_root` (D-03 vocabulary)."
    - "`.planning/codebase/INTEGRATIONS.md:55` no longer contains the hardcoded `C:\\Users\\G\\Desktop\\temporary` username leak (HYG-01 stripping completed retroactively)."
    - "`.planning/codebase/INTEGRATIONS.md:55` references `C:\\Users\\<USERNAME>\\Desktop\\temporary` (HYG-01 placeholder convention)."
    - "Audit log appended with `patch` rows for the 3 surgical lines (D-22 audit-log requirement)."
    - "D-22 protected paths byte-equal."
    - "Goldens byte-equal."
  artifacts:
    - path: ".planning/codebase/STACK.md"
      provides: "Updated STACK.md with line 79 reflecting Phase 4 D-08/D-13 cache-redirect wiring (D-02 patch)"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Updated INTEGRATIONS.md with line 55 (HYG-01 placeholder) and line 150 (Phase 4 wiring) patched"
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Audit log appended with patch rows for the 3 surgical lines"
  key_links:
    - from: "STACK.md:79 + INTEGRATIONS.md:150 patches"
      to: "Phase 4 D-08/D-13 (PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS, run.ps1 + teresa_gui.py, config.local.yaml::temp_root)"
      via: "verbatim D-03 replacement vocabulary"
      pattern: "PYTHONPYCACHEPREFIX.*PYTEST_ADDOPTS|temp_root"
    - from: "INTEGRATIONS.md:55 patch"
      to: "HYG-01 placeholder convention (C:\\Users\\<USERNAME>\\)"
      via: "username placeholder substitution"
      pattern: "C:\\\\\\\\Users\\\\\\\\<USERNAME>\\\\\\\\"
---

<objective>
Apply 3 surgical line patches to `.planning/codebase/` per D-01 / D-02 / D-03 (Area A — sweep-order Group 6 in D-14):

1. `.planning/codebase/STACK.md:79` — replace stale `PYTHONPYCACHEPREFIX … Not set by run.ps1 (used to be set by the old scripts/run_full.ps1)` with the canonical Phase 4 D-08/D-13 + Phase 5 D-05/D-06 vocabulary.
2. `.planning/codebase/INTEGRATIONS.md:150` — replace stale `PYTHONPYCACHEPREFIX … Not currently exported by run.ps1 (was set by the now-retired scripts/run_full.ps1)` with the same vocabulary.
3. `.planning/codebase/INTEGRATIONS.md:55` — replace hardcoded `C:\Users\G\Desktop\temporary` with `C:\Users\<USERNAME>\Desktop\temporary` (HYG-01 placeholder convention).

Per D-01, this is SURGICAL: no broader refresh of the 7-file `.planning/codebase/` map tree. Only these 3 lines.

Append per-claim `patch` rows to `06-DRIFT-AUDIT.md` for each of the 3 lines.

Purpose: Closes DRIFT-04 (the Area A hand-off Phase 5 explicitly punted to Phase 6).
Output: 2 modified files, 3 patched lines, audit log appended, 1 atomic commit.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md

<interfaces>
<!-- Source-of-truth values for the 3 patches (verified 2026-05-11 against current tree) -->

Pre-patch state (verified by reading STACK.md:70-83 and INTEGRATIONS.md:50-56, INTEGRATIONS.md:145-150):

STACK.md:79 (current):
> `PYTHONPYCACHEPREFIX` — referenced in `spec_classifier/pyproject.toml:4-5` as the only mechanism to redirect `__pycache__/`. Not set by `run.ps1` (used to be set by the old `scripts/run_full.ps1`).

INTEGRATIONS.md:150 (current):
> `PYTHONPYCACHEPREFIX` — optional. Referenced in `pyproject.toml:4-5` as the only way to redirect `__pycache__/`. Not currently exported by `run.ps1` (was set by the now-retired `scripts/run_full.ps1`).

INTEGRATIONS.md:55 (current):
> | TEMP (`.pytest_cache/`, `__pycache__/`) | `C:\Users\G\Desktop\temporary` | `temp_root` in `config.local.yaml` |

D-03 canonical replacement vocabulary (Phase 5 D-05/D-06 verbatim):
"PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS env vars set by `run.ps1` and `teresa_gui.py` from `config.local.yaml::temp_root`"

Defense-in-depth requirement (Phase 4 D-13): single-source language ("`run.ps1` sets …" only) is a regression. Both entry points MUST be named.

HYG-01 placeholder convention (per CLAUDE.md root § "Code-only repository policy"): hardcoded `C:\Users\G\` should be `C:\Users\<USERNAME>\`. The `temp_root` example in INTEGRATIONS.md:55 missed v1.0 HYG-01 stripping.

Audit-log row format (matches Plan 01 schema):
| .planning/codebase/<FILE>.md | <line> | "<claim>" | `<check>` | <remove|patch|no_drift> |

D-22 protected paths (UNCHANGED — `.planning/codebase/` is NOT D-22):
spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch STACK.md:79 + INTEGRATIONS.md:55 + INTEGRATIONS.md:150 (D-02 / D-03)</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/INTEGRATIONS.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - .planning/codebase/STACK.md (lines 70-83 — read the surrounding bullet block to confirm the exact current line and preserve list formatting)
    - .planning/codebase/INTEGRATIONS.md (lines 50-56 + lines 145-152 — read both surrounding blocks to confirm current state and preserve table/bullet formatting)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area A (D-01..D-03 — exact patch set, vocabulary, defense-in-depth requirement)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13 (canonical wiring vocabulary source)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-05, D-06 (Phase 5 verbatim replacement vocabulary)
  </read_first>
  <action>
Apply 3 surgical edits in one task / one commit.

**Patch 1: `.planning/codebase/STACK.md:79`**

The current line 79 reads (in a bullet under § "Environment variables:"):
```
- `PYTHONPYCACHEPREFIX` — referenced in `spec_classifier/pyproject.toml:4-5` as the only mechanism to redirect `__pycache__/`. Not set by `run.ps1` (used to be set by the old `scripts/run_full.ps1`).
```

Replace with this exact bullet (preserve the `- ` list-marker indentation matching the surrounding bullets at lines 77-78):
```
- `PYTHONPYCACHEPREFIX` and `PYTEST_ADDOPTS` — set by `run.ps1` and `teresa_gui.py` from `config.local.yaml::temp_root`. Together they redirect `__pycache__/` and `.pytest_cache/` to `$temp_root` so cache artifacts never land inside the repo working tree (Phase 4 CACHE-01/CACHE-02; defense-in-depth — both entry points set both vars independently).
```

**Patch 2: `.planning/codebase/INTEGRATIONS.md:150`**

The current line 150 reads (in a bullet under § "Required env vars:"):
```
- `PYTHONPYCACHEPREFIX` — optional. Referenced in `pyproject.toml:4-5` as the only way to redirect `__pycache__/`. Not currently exported by `run.ps1` (was set by the now-retired `scripts/run_full.ps1`).
```

Replace with this exact bullet (preserve `- ` indentation matching lines 148-149):
```
- `PYTHONPYCACHEPREFIX` and `PYTEST_ADDOPTS` — set by `run.ps1` and `teresa_gui.py` from `config.local.yaml::temp_root`. Together they redirect `__pycache__/` and `.pytest_cache/` to `$temp_root` so cache artifacts never land inside the repo working tree (Phase 4 CACHE-01/CACHE-02; defense-in-depth — both entry points set both vars independently).
```

**Patch 3: `.planning/codebase/INTEGRATIONS.md:55`**

The current line 55 reads (a row in the "External roots (Windows defaults)" markdown table):
```
| TEMP (`.pytest_cache/`, `__pycache__/`) | `C:\Users\G\Desktop\temporary` | `temp_root` in `config.local.yaml` |
```

Replace with this exact row (only the middle column changes; preserve the `|` separators and surrounding columns):
```
| TEMP (`.pytest_cache/`, `__pycache__/`) | `C:\Users\<USERNAME>\Desktop\temporary` | `temp_root` in `config.local.yaml` |
```

This brings the row in line with HYG-01 placeholder convention (the surrounding table rows at lines 53-54 already use `%USERPROFILE%\Desktop\INPUT` / `%USERPROFILE%\Desktop\OUTPUT`; this row was the v1.0 HYG-01 miss).

**Audit-log rows (3 patch rows):**

Append to `06-DRIFT-AUDIT.md`:
```
| .planning/codebase/STACK.md | 79 | "Not set by run.ps1 (used to be set by old scripts/run_full.ps1)" | `grep -q "Not set by .run.ps1." .planning/codebase/STACK.md` (was 0, now non-0) | patch |
| .planning/codebase/INTEGRATIONS.md | 150 | "Not currently exported by run.ps1 (was set by the now-retired scripts/run_full.ps1)" | `grep -q "Not currently exported" .planning/codebase/INTEGRATIONS.md` (was 0, now non-0) | patch |
| .planning/codebase/INTEGRATIONS.md | 55 | "C:\\Users\\G\\Desktop\\temporary" hardcoded username | `grep -q "C:..Users..G.." .planning/codebase/INTEGRATIONS.md` (was 0, now non-0) | patch |
```

D-22 forbidden: any edit inside protected paths.

Commit subject: `docs(06): T1 patch STACK.md + INTEGRATIONS.md surgical lines (DRIFT-04)`.
  </action>
  <verify>
    <automated>grep -q "PYTHONPYCACHEPREFIX. and .PYTEST_ADDOPTS. — set by .run.ps1. and .teresa_gui.py." .planning/codebase/STACK.md &amp;&amp; grep -q "PYTHONPYCACHEPREFIX. and .PYTEST_ADDOPTS. — set by .run.ps1. and .teresa_gui.py." .planning/codebase/INTEGRATIONS.md &amp;&amp; grep -qv "Not set by .run.ps1." .planning/codebase/STACK.md &amp;&amp; grep -qv "Not currently exported" .planning/codebase/INTEGRATIONS.md &amp;&amp; grep -q "C:..Users..<USERNAME>..Desktop..temporary" .planning/codebase/INTEGRATIONS.md &amp;&amp; ! grep -q "C:..Users..G..Desktop..temporary" .planning/codebase/INTEGRATIONS.md &amp;&amp; grep -c "^| .planning/codebase/STACK\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| .planning/codebase/INTEGRATIONS\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 2) ? 0 : 1}' &amp;&amp; git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PYTHONPYCACHEPREFIX. and .PYTEST_ADDOPTS." .planning/codebase/STACK.md` exits 0 (D-03 vocabulary present)
    - `grep -q "run\.ps1. and .teresa_gui\.py" .planning/codebase/STACK.md` exits 0 (defense-in-depth: both entry points named)
    - `grep -q "config\.local\.yaml::temp_root" .planning/codebase/STACK.md` exits 0
    - `grep -q "Not set by .run.ps1." .planning/codebase/STACK.md` exits NON-zero (stale claim removed)
    - `grep -q "scripts/run_full" .planning/codebase/STACK.md` exits NON-zero (orphan ref removed)
    - `grep -q "PYTHONPYCACHEPREFIX. and .PYTEST_ADDOPTS." .planning/codebase/INTEGRATIONS.md` exits 0
    - `grep -q "run\.ps1. and .teresa_gui\.py" .planning/codebase/INTEGRATIONS.md` exits 0
    - `grep -q "config\.local\.yaml::temp_root" .planning/codebase/INTEGRATIONS.md` exits 0
    - `grep -q "Not currently exported" .planning/codebase/INTEGRATIONS.md` exits NON-zero (stale claim removed)
    - `grep -q "now-retired .scripts/run_full" .planning/codebase/INTEGRATIONS.md` exits NON-zero (stale orphan ref removed)
    - `grep -q "C:..Users..<USERNAME>..Desktop..temporary" .planning/codebase/INTEGRATIONS.md` exits 0 (HYG-01 placeholder applied)
    - `grep -q "C:..Users..G..Desktop..temporary" .planning/codebase/INTEGRATIONS.md` exits NON-zero (username leak removed)
    - `grep -c "^| .planning/codebase/STACK\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1 (audit row recorded)
    - `grep -c "^| .planning/codebase/INTEGRATIONS\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 2 (audit rows for both 55 and 150)
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
  </acceptance_criteria>
  <done>3 surgical lines patched per D-02/D-03; HYG-01 placeholder applied; defense-in-depth wording preserved; 3 patch rows in 06-DRIFT-AUDIT.md; D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (planning-doc edits only) | Plan 05 modifies markdown files in `.planning/codebase/`. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | STACK.md, INTEGRATIONS.md | accept | Single atomic commit; revertable. |
| T-06-15 | Information Disclosure | INTEGRATIONS.md:55 (username leak) | mitigate | Patch 3 explicitly removes the `C:\Users\G\` hardcoded username, replacing with `<USERNAME>` placeholder per HYG-01. Reduces information disclosure surface. |
| T-06-16 | Repudiation | surgical patches | mitigate | Atomic commit with `docs(06): T1 ...` subject (D-21). |
</threat_model>

<verification>
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty.
2. `git diff --stat -- spec_classifier/golden/` empty.
3. `grep -q "PYTHONPYCACHEPREFIX" .planning/codebase/STACK.md` AND `grep -q "PYTEST_ADDOPTS" .planning/codebase/STACK.md` AND `grep -q "teresa_gui" .planning/codebase/STACK.md` all exit 0.
4. Same triplet on `.planning/codebase/INTEGRATIONS.md`.
5. No `C:\Users\G\` substring survives in `.planning/codebase/INTEGRATIONS.md`: `grep -q "C:..Users..G.." .planning/codebase/INTEGRATIONS.md` exits NON-zero.
6. Phase 5 ORPH grep gate still passes (Phase 5's grep is scoped to `*.toml`/`*.example` so `.planning/codebase/*.md` stale references were always excluded; this plan additionally removes them).
</verification>

<success_criteria>
- DRIFT-04 closed: 3 surgical patches landed.
- D-22 protected paths byte-equal.
- Goldens byte-equal.
- 1 atomic commit using `docs(06): T1` subject pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-05-SUMMARY.md` recording: pre/post text of each patched line, audit-log rows added, rationale references (D-02/D-03/HYG-01).
</output>
