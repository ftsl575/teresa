---
phase: 06-doc-vs-impl-drift-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md
  - spec_classifier/docs/user/USER_GUIDE.md
  - spec_classifier/docs/product/TECHNICAL_OVERVIEW.md
  - spec_classifier/docs/schemas/DATA_CONTRACTS.md
  - spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md
  - spec_classifier/docs/taxonomy/hw_type_taxonomy.md
  - spec_classifier/docs/taxonomy/cycle2_summary.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-01]
tags: [doc-sweep, drift, user-docs, taxonomy-docs]

must_haves:
  truths:
    - "Every claim in the 7 swept files (CLI_CONFIG_REFERENCE.md, USER_GUIDE.md, TECHNICAL_OVERVIEW.md, DATA_CONTRACTS.md, RULES_AUTHORING_GUIDE.md, hw_type_taxonomy.md, cycle2_summary.md) was mechanically verified against the post-Phase-4-and-5 tree."
    - "Drifted claims resolved per D-13."
    - "Volatile-count claims encountered (test count, LOC, rule_id, vendor count) were REMOVED per D-12."
    - "06-DRIFT-AUDIT.md has at least one row for each of the 7 swept files."
    - "D-22 protected paths byte-equal."
    - "Goldens byte-equal."
  artifacts:
    - path: "spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md"
      provides: "User-facing CLI/config reference with verified claims"
    - path: "spec_classifier/docs/user/USER_GUIDE.md"
      provides: "User guide with verified claims"
    - path: "spec_classifier/docs/product/TECHNICAL_OVERVIEW.md"
      provides: "Technical overview with verified claims"
    - path: "spec_classifier/docs/schemas/DATA_CONTRACTS.md"
      provides: "Data-contract doc with verified claims"
    - path: "spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md"
      provides: "Rules-authoring guide with verified claims"
    - path: "spec_classifier/docs/taxonomy/hw_type_taxonomy.md"
      provides: "Taxonomy doc with verified claims"
    - path: "spec_classifier/docs/taxonomy/cycle2_summary.md"
      provides: "Cycle 2 summary with verified claims"
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Audit log appended with rows for the 7 docs"
  key_links:
    - from: "06-DRIFT-AUDIT.md"
      to: "the 7 docs in this plan"
      via: "audit-log row per (file, line, claim) tuple"
      pattern: "CLI_CONFIG_REFERENCE\\.md|USER_GUIDE\\.md|TECHNICAL_OVERVIEW\\.md|DATA_CONTRACTS\\.md|RULES_AUTHORING_GUIDE\\.md|hw_type_taxonomy\\.md|cycle2_summary\\.md"
---

<objective>
Mechanically sweep `spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md`, `spec_classifier/docs/user/USER_GUIDE.md`, plus the remaining doc subtrees: `spec_classifier/docs/product/TECHNICAL_OVERVIEW.md`, `spec_classifier/docs/schemas/DATA_CONTRACTS.md`, `spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md`, `spec_classifier/docs/taxonomy/hw_type_taxonomy.md`, `spec_classifier/docs/taxonomy/cycle2_summary.md` (sweep order Groups 4 + 5 per D-14, EXCLUDING `spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` which is the DRIFT-02 trim target handled in Plan 04).

Per D-11 categories with D-13 remove > patch heuristic. Per D-12, volatile counts are REMOVED, not patched.

Purpose: Wave-1 parallel sibling of Plans 01 and 02 (no file overlap). Closes the user-docs + taxonomy-docs sweep batch.
Output: 7 swept files (or unchanged if no drift), per-claim rows appended to 06-DRIFT-AUDIT.md.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md
@spec_classifier/docs/user/USER_GUIDE.md
@spec_classifier/docs/product/TECHNICAL_OVERVIEW.md
@spec_classifier/docs/schemas/DATA_CONTRACTS.md
@spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md
@spec_classifier/docs/taxonomy/hw_type_taxonomy.md
@spec_classifier/docs/taxonomy/cycle2_summary.md

<interfaces>
<!-- Source-of-truth wiring vocabulary used to validate "code does X" claims -->

Canonical wiring claims (Phase 4 D-08/D-13, Phase 5 D-05/D-06):
- `PYTHONPYCACHEPREFIX` + `PYTEST_ADDOPTS` set by `run.ps1` AND `teresa_gui.py` from `config.local.yaml::temp_root`.
- `run.ps1 -NoClean` opts out of default-on `clean.ps1`.
- Six vendors: dell, cisco, hpe, lenovo, huawei, xfusion.
- `power_cord` `hw_type=None` per recovery commit `c3c7cb6`.
- LOGISTIC = packaging, documents, freight only (CLAUDE.md root rule #2).
- BASE without device_type: E15 INFO; with device_type: valid; only hw_type on BASE → E10.
- YAML rule order load-bearing (first-match-wins); `hw_type_rules.applies_to=[HW]` only.

Audit-log row format (matches Plan 01 schema):
| spec_classifier/docs/<subtree>/<FILE>.md | <line> | "<claim>" | `<check>` | <remove|patch|no_drift> |

`resolution` ∈ {remove, patch, no_drift}.

Volatile-count categories DEFERRED per D-12 — REMOVE if encountered. Note: hw_type vocabulary enumeration (the named list of types) is stable; only bare counts ("N hw_types defined") are volatile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md and USER_GUIDE.md</name>
  <files>spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md, spec_classifier/docs/user/USER_GUIDE.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md (full file — sweep target)
    - spec_classifier/docs/user/USER_GUIDE.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13, D-11 (cache-redirect wiring vocabulary + ONE_BUTTON_RUN.md edits already landed in Phase 4)
    - run.ps1 (full file — every CLI claim must be checkable here)
    - spec_classifier/main.py (full or grep — verify --vendor / --update-golden / other CLI flags; D-22 forbids EDIT)
    - spec_classifier/config.yaml (verify any config-key claims)
    - spec_classifier/config.local.yaml.example (verify temp_root / paths.input_root / paths.output_root keys)
  </read_first>
  <action>
Sweep `spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md` then `spec_classifier/docs/user/USER_GUIDE.md` per D-10 / D-11. Both are heavy CLI / config density.

High-yield claims:
1. **Every CLI flag** in either doc — verify via `grep` in `run.ps1` AND `spec_classifier/main.py`. Common ones: `-Vendor`, `-NoAi`, `-TestsOnly`, `-SkipTests`, `-NoClean` (Phase 4 added), `--vendor`, `--no-ai`, `--update-golden`, `--provider`.
2. **Every config-file key** — `paths.input_root`, `paths.output_root`, `temp_root`, `vendor_rules`, `cleaned_spec.include_types`. Verify via `grep` in `spec_classifier/config.yaml` and `spec_classifier/config.local.yaml.example`.
3. **Path-fallback claims** — "defaults to `%USERPROFILE%\Desktop\INPUT`" — verify via `grep '$env:USERPROFILE' run.ps1` and `Path.home()` in `teresa_gui.py`.
4. **Cache-redirect claims** (Phase 4) — both `PYTHONPYCACHEPREFIX` and `PYTEST_ADDOPTS`, both `run.ps1` and `teresa_gui.py`. Single-source language is a Phase 4 D-13 regression — patch.
5. **Line-number refs** — REMOVE > PATCH (rewrite to symbol/section refs).
6. **Volatile counts** — REMOVE per D-12.

Cross-doc consistency: USER_GUIDE.md may reference CLI_CONFIG_REFERENCE.md as the canonical CLI doc. Preserve that hierarchy; don't duplicate full CLI tables across both.

Append rows for both files. Single commit covers both files since they are tightly coupled (user-facing docs).

Commit subject: `docs(06): T1 sweep user/CLI_CONFIG_REFERENCE.md and user/USER_GUIDE.md (DRIFT-01)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| spec_classifier/docs/user/CLI_CONFIG_REFERENCE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| spec_classifier/docs/user/USER_GUIDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| spec_classifier/docs/user/CLI_CONFIG_REFERENCE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -c "^| spec_classifier/docs/user/USER_GUIDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "run_full" spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md` exits non-zero
    - `grep -q "run_full" spec_classifier/docs/user/USER_GUIDE.md` exits non-zero
  </acceptance_criteria>
  <done>Both user docs swept; per-claim rows recorded; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep TECHNICAL_OVERVIEW.md and DATA_CONTRACTS.md</name>
  <files>spec_classifier/docs/product/TECHNICAL_OVERVIEW.md, spec_classifier/docs/schemas/DATA_CONTRACTS.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/product/TECHNICAL_OVERVIEW.md (full file — sweep target)
    - spec_classifier/docs/schemas/DATA_CONTRACTS.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14)
    - spec_classifier/src/vendors/base.py (verify any adapter-contract claims; READ only per D-22)
    - spec_classifier/batch_audit.py (verify any E-code / audit-output claims; READ only per D-22)
  </read_first>
  <action>
Sweep `spec_classifier/docs/product/TECHNICAL_OVERVIEW.md` then `spec_classifier/docs/schemas/DATA_CONTRACTS.md` per D-10 / D-11.

High-yield claims:
1. **Pipeline-stage claims** in TECHNICAL_OVERVIEW.md — "Excel → parse → normalize → classify → artifacts". Verify against `spec_classifier/main.py` pipeline shape (READ only).
2. **E-code claims** — E1..E15 etc. Verify against `spec_classifier/batch_audit.py` E-code constants. The E-code semantics for power_cord (E8 exclusion) and BASE (E15 INFO, E10 only on hw_type) are load-bearing; verify they match CLAUDE.md root rule #1 + #3.
3. **Schema claims** in DATA_CONTRACTS.md — JSON shape of `classification.jsonl`, `audit_results.jsonl`, etc. Verify against actual outputs / batch_audit.py output paths if explicitly referenced.
4. **Adapter contract claims** — verify against `spec_classifier/src/vendors/base.py`.
5. **Line-number refs** — REMOVE > PATCH.
6. **Volatile counts** — REMOVE per D-12.

Append rows for both files (one task, two files, one commit).

Commit subject: `docs(06): T2 sweep TECHNICAL_OVERVIEW.md and DATA_CONTRACTS.md (DRIFT-01)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| spec_classifier/docs/product/TECHNICAL_OVERVIEW\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| spec_classifier/docs/schemas/DATA_CONTRACTS\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| spec_classifier/docs/product/TECHNICAL_OVERVIEW\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -c "^| spec_classifier/docs/schemas/DATA_CONTRACTS\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
  </acceptance_criteria>
  <done>TECHNICAL_OVERVIEW.md and DATA_CONTRACTS.md swept; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep RULES_AUTHORING_GUIDE.md, hw_type_taxonomy.md, cycle2_summary.md</name>
  <files>spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md, spec_classifier/docs/taxonomy/hw_type_taxonomy.md, spec_classifier/docs/taxonomy/cycle2_summary.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md (full file — sweep target)
    - spec_classifier/docs/taxonomy/hw_type_taxonomy.md (full file — sweep target)
    - spec_classifier/docs/taxonomy/cycle2_summary.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14)
    - .planning/codebase/CONCERNS.md § BLOCKER (load-bearing taxonomy claims: power_cord, YAML rule order, HW_TYPE_VOCAB duplication)
    - spec_classifier/rules/dell_rules.yaml (READ to verify the "intentionally unmapped" comment for power_cord; D-22 forbids EDIT)
    - CLAUDE.md (root) § "Critical business rules" (5 rules — taxonomy claims must be consistent)
  </read_first>
  <action>
Sweep all three files per D-10 / D-11. These are taxonomy + rule-authoring docs — high load-bearing density for the 5 critical business rules in CLAUDE.md.

High-yield claims:
1. **`power_cord` `hw_type=None` rule** — every reference must be consistent with CLAUDE.md root rule #1 (intentional, recovery commit `c3c7cb6`, in `_E8_NO_HW_TYPE_DEVICES`). Verify via `grep "power_cord" spec_classifier/rules/dell_rules.yaml`. Most should be `no_drift`.
2. **YAML rule order is load-bearing** — verify any "first-match-wins" claims; should be `no_drift`.
3. **`hw_type_rules.applies_to=[HW]` only** — CLAUDE.md root rule #5; verify against any rule examples in RULES_AUTHORING_GUIDE.
4. **`HW_TYPE_VOCAB` duplication** — CONCERNS.md § BLOCKER documents this as known-not-fixed; if either doc claims it's been fixed, that's drift — patch.
5. **device_type / hw_type taxonomy claims** in hw_type_taxonomy.md — verify the named vocabulary against `spec_classifier/src/classifier.py:HW_TYPE_VOCAB` AND `batch_audit.py:HW_TYPE_VOCAB`. Named enumerations are stable; bare counts are volatile per D-12.
6. **cycle2_summary.md historical content** — if this is a historical retrospective doc (Cycle 2 archive), claims may be legitimately historical per D-18 convention. Mark as `no_drift` historical with a note in the audit row, even if specific line numbers/counts are stale-as-of-today.
7. **Line-number refs** — REMOVE > PATCH.
8. **Volatile counts** — REMOVE per D-12 unless explicitly historical (cycle2_summary.md).

Append rows for all three files. One commit covers all three (taxonomy/rules-authoring is a tight cluster).

Commit subject: `docs(06): T3 sweep RULES_AUTHORING_GUIDE + taxonomy docs (DRIFT-01)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "^| spec_classifier/docs/rules/RULES_AUTHORING_GUIDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| spec_classifier/docs/taxonomy/hw_type_taxonomy\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}' &amp;&amp; grep -c "^| spec_classifier/docs/taxonomy/cycle2_summary\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "^| spec_classifier/docs/rules/RULES_AUTHORING_GUIDE\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -c "^| spec_classifier/docs/taxonomy/hw_type_taxonomy\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -c "^| spec_classifier/docs/taxonomy/cycle2_summary\.md " .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "power_cord" spec_classifier/docs/taxonomy/hw_type_taxonomy.md` exits 0 (taxonomy doc continues to document the load-bearing power_cord rule, expected `no_drift`)
  </acceptance_criteria>
  <done>3 docs swept; per-claim rows recorded; D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (doc-only changes) | Plan 03 modifies markdown files only. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | the 7 docs in this plan | accept | Atomic per-task commits; revertable via `git revert <hash>`. |
| T-06-08 | Information Disclosure | TECHNICAL_OVERVIEW.md, DATA_CONTRACTS.md | accept | Public-facing schema/architecture docs; no secrets surfaced. |
| T-06-09 | Repudiation | sweep results | mitigate | Atomic per-task commits with `docs(06): T<N> ...` subject (D-21). |
</threat_model>

<verification>
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty.
2. `git diff --stat -- spec_classifier/golden/` empty.
3. 06-DRIFT-AUDIT.md contains at least one row for each of: CLI_CONFIG_REFERENCE.md, USER_GUIDE.md, TECHNICAL_OVERVIEW.md, DATA_CONTRACTS.md, RULES_AUTHORING_GUIDE.md, hw_type_taxonomy.md, cycle2_summary.md.
4. Phase 5 ORPH grep gate still passes.
</verification>

<success_criteria>
- 7 docs swept (excl. RUN_PATHS_AND_IO_LAYOUT.md which is Plan 04's trim target).
- 06-DRIFT-AUDIT.md updated with per-claim rows.
- D-22 protected paths byte-equal.
- Goldens byte-equal.
- 3 atomic commits using `docs(06): T<N>` subject pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-03-SUMMARY.md` with per-file claim counts and resolutions.
</output>
