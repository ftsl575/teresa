---
phase: 06-doc-vs-impl-drift-sweep
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md
  - spec_classifier/docs/dev/OPERATIONAL_NOTES.md
  - spec_classifier/docs/dev/TESTING_GUIDE.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-01]
tags: [doc-sweep, drift, dev-docs]

must_haves:
  truths:
    - "Every claim in NEW_VENDOR_GUIDE.md, OPERATIONAL_NOTES.md, TESTING_GUIDE.md was mechanically verified against the post-Phase-4-and-5 tree (per D-10)."
    - "Drifted claims resolved per D-13 (default: remove; patch only when load-bearing AND patched form is stable)."
    - "Volatile-count claims (test count, LOC, rule_id, vendor count) encountered in these dev docs were REMOVED per D-12, not patched."
    - "06-DRIFT-AUDIT.md has at least one row for each of the 3 swept files."
    - "D-22 protected paths byte-equal: zero diff inside spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}."
    - "Goldens byte-equal: git diff --stat -- spec_classifier/golden/ empty."
  artifacts:
    - path: "spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md"
      provides: "Vendor-onboarding guide with verified claims"
    - path: "spec_classifier/docs/dev/OPERATIONAL_NOTES.md"
      provides: "Operational notes with verified claims"
    - path: "spec_classifier/docs/dev/TESTING_GUIDE.md"
      provides: "Testing guide with verified claims"
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Audit log appended with rows for the 3 dev docs"
  key_links:
    - from: "06-DRIFT-AUDIT.md"
      to: "the 3 dev docs in this plan"
      via: "audit-log row per (file, line, claim) tuple"
      pattern: "NEW_VENDOR_GUIDE\\.md|OPERATIONAL_NOTES\\.md|TESTING_GUIDE\\.md"
---

<objective>
Mechanically sweep `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md`, `spec_classifier/docs/dev/OPERATIONAL_NOTES.md`, `spec_classifier/docs/dev/TESTING_GUIDE.md` (sweep order Group 3 per D-14, EXCLUDING `ONE_BUTTON_RUN.md` which is the DRIFT-02 trim target handled in Plan 04). Heavy CLI density expected — apply D-11 categories aggressively with remove > patch heuristic per D-13.

Purpose: Wave-1 parallel sibling of Plan 01 (no file overlap). Closes the dev-docs sweep batch.
Output: 3 swept files (or unchanged if no drift), per-claim rows appended to 06-DRIFT-AUDIT.md.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md
@spec_classifier/docs/dev/OPERATIONAL_NOTES.md
@spec_classifier/docs/dev/TESTING_GUIDE.md

<interfaces>
<!-- Source-of-truth wiring vocabulary used to validate "code does X" claims in this sweep -->

Canonical wiring claims (Phase 4 D-08/D-13, Phase 5 D-05/D-06):
- `PYTHONPYCACHEPREFIX` + `PYTEST_ADDOPTS` set by `run.ps1` AND `teresa_gui.py` from `config.local.yaml::temp_root`.
- `run.ps1 -NoClean` opts out of default-on `clean.ps1`.
- Six vendors: dell, cisco, hpe, lenovo, huawei, xfusion.
- `MAX_SKIP_RATIO = 0.50` in `spec_classifier/conftest.py:pytest_sessionfinish`.
- `power_cord` `hw_type=None` per recovery commit `c3c7cb6`; `_E8_NO_HW_TYPE_DEVICES = {"power_cord", "enablement_kit"}` in `batch_audit.py`.

Audit-log row format (matches Plan 01 schema):
| spec_classifier/docs/dev/<FILE>.md | <line> | "<claim>" | `<check>` | <remove|patch|no_drift> |

`resolution` ∈ {remove, patch, no_drift}.

Volatile-count categories DEFERRED per D-12 — if encountered in these CLI-dense docs (e.g., "420 tests", "93 dell rules", "1489 LOC in batch_audit.py", "6 vendors" as a count), REMOVE per remove>patch heuristic; do not patch to a current count. Vendor enumeration by NAME (the named list of dell/cisco/hpe/lenovo/huawei/xfusion) is acceptable as a stable claim — only the bare count "6" is volatile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md</name>
  <files>spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14) and § Area B (D-08 — run.ps1 NOT a D-22 path; rules + main.py ARE D-22)
    - spec_classifier/src/vendors/base.py (read to confirm adapter contract claims if NEW_VENDOR_GUIDE references it; D-22 forbids EDIT but allows READ for verification)
    - run.ps1 (lines 1-50 to verify $ALL_VENDORS / vendor list claims)
    - teresa_gui.py (search for VENDORS_ACTIVE / VENDORS_DISABLED to verify any claims about them)
  </read_first>
  <action>
Sweep `spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` per D-10 / D-11.

Specific high-yield claims to verify:
1. **Adapter contract claims** — verify against `spec_classifier/src/vendors/base.py` symbol set (use `grep` to confirm class/method names referenced in the guide actually exist; D-22 forbids editing `src/`, but reading for verification is fine).
2. **`$ALL_VENDORS` registration in `run.ps1`** — the guide tells contributors to register `"<v>"` in `$ALL_VENDORS`; verify the actual variable name and shape via `grep -n '$ALL_VENDORS' run.ps1`. Patch any wrong variable name.
3. **`VENDORS_ACTIVE` / `VENDORS_DISABLED` in `teresa_gui.py`** — verify both names via `grep -n 'VENDORS_ACTIVE\|VENDORS_DISABLED' teresa_gui.py`. Patch any wrong name.
4. **`golden/<v>1_expected.jsonl` file shape** — verify naming pattern via `ls spec_classifier/golden/ | head`.
5. **`python main.py --vendor <v> --update-golden`** — verify CLI shape via `grep "vendor\|update-golden" spec_classifier/main.py`. Note: `--update-golden` is a real, sanctioned code-affordance; the v1.1 phase-gate constraint "no `--update-golden` runs sanctioned in v1.1" is a milestone constraint, not a permanent code property. DO NOT remove the doc claim that `--update-golden` exists; it documents code reality.
6. **Line-number refs (`base.py:N`, `main.py:N`)** — apply remove > patch aggressively. Rewrite to symbol/section refs.
7. **Volatile counts** ("N rules", "N vendors as a count", "N LOC") — REMOVE per D-12.

For each claim, append:
`| spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

D-22 forbidden: any EDIT inside `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`. Reading those for VERIFICATION is allowed and required.

**(W-3 grep spacing fix per checker feedback)** All `grep -c "FILE\.md"` checks in this plan's acceptance criteria use the BROADER pattern (no leading `^| ` anchor, no trailing space). This removes a spacing dependency between the audit-log row template's pipe-table format and the verification regex. Any audit-log row containing the filename as a column value will be counted, regardless of column-spacing variations. Trade-off: the broader regex would also match the filename appearing in prose; this is acceptable because the audit-log file's prose sections (Purpose, Tally, Verification) do not enumerate per-file claim filenames as raw tokens — only the table rows do.


Commit subject: `docs(06): T1 sweep NEW_VENDOR_GUIDE.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "NEW_VENDOR_GUIDE\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "NEW_VENDOR_GUIDE\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "run_full" spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` exits non-zero
  </acceptance_criteria>
  <done>NEW_VENDOR_GUIDE.md swept; per-claim rows recorded; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep spec_classifier/docs/dev/OPERATIONAL_NOTES.md</name>
  <files>spec_classifier/docs/dev/OPERATIONAL_NOTES.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/dev/OPERATIONAL_NOTES.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area C (D-10..D-14)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13 (cache-redirect wiring vocabulary)
    - run.ps1 (full file — for any operational claims about switches, env vars, clean.ps1, header banner)
    - spec_classifier/scripts/clean.ps1 (read to verify any sweep-list claims)
    - spec_classifier/conftest.py (verify any pytest skip-ratio / MAX_SKIP_RATIO claims)
  </read_first>
  <action>
Sweep `spec_classifier/docs/dev/OPERATIONAL_NOTES.md` per D-10 / D-11.

High-yield claims expected in operational docs:
1. **Cache-redirect operational claims** — "PYTHONPYCACHEPREFIX is set by run.ps1", "`-NoClean` opts out". Validate against `run.ps1` (use Phase 4 D-08 + D-13 vocabulary; both `run.ps1` AND `teresa_gui.py` set both env vars).
2. **`clean.ps1` sweep targets** — what folders does it sweep? Verify against the actual `clean.ps1` script body.
3. **Pytest skip-guard wording** — "skip ratio > 0.50 fails the session". Verify via `grep "MAX_SKIP_RATIO\|0.50" spec_classifier/conftest.py`.
4. **Run-recipe / smoke-test CLI lines** — verify each `.\run.ps1 ...` example via `grep` in `run.ps1`.
5. **Line-number refs** — apply remove > patch aggressively.
6. **Volatile counts** — REMOVE per D-12.

Defense-in-depth check (Phase 4 D-13): if OPERATIONAL_NOTES.md uses single-source language ("`run.ps1` sets PYTHONPYCACHEPREFIX" without mentioning `teresa_gui.py` does too in defense-in-depth), this is a regression — patch to mention BOTH entry points (per Phase 5 D-05/D-06 canonical phrase).

Append rows:
`| spec_classifier/docs/dev/OPERATIONAL_NOTES.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

D-22 forbidden: any edit inside protected paths.

Commit subject: `docs(06): T2 sweep OPERATIONAL_NOTES.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "OPERATIONAL_NOTES\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "OPERATIONAL_NOTES\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "run_full" spec_classifier/docs/dev/OPERATIONAL_NOTES.md` exits non-zero
  </acceptance_criteria>
  <done>OPERATIONAL_NOTES.md swept; per-claim rows recorded; D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep spec_classifier/docs/dev/TESTING_GUIDE.md</name>
  <files>spec_classifier/docs/dev/TESTING_GUIDE.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/dev/TESTING_GUIDE.md (full file — sweep target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § D-12 (volatile counts including test count are DEFERRED — REMOVE if encountered)
    - spec_classifier/conftest.py (verify pytest skip-ratio gate, MAX_SKIP_RATIO, pytest_sessionfinish hook)
    - run.ps1 (verify `-TestsOnly` and `-SkipTests` switches and PYTEST_ADDOPTS env var)
    - spec_classifier/main.py (verify any --update-golden CLI claims; D-22 forbids EDIT, allows READ)
  </read_first>
  <action>
Sweep `spec_classifier/docs/dev/TESTING_GUIDE.md` per D-10 / D-11. This file is high-volatility-claim territory: test counts, golden workflow, pytest CLI examples, skip-ratio behavior.

**(N-3 read-access clarification per checker feedback)** READ access to all D-22 protected paths is permitted during sweep verification. For TESTING_GUIDE.md specifically this includes `spec_classifier/conftest.py`, `spec_classifier/main.py`, and any test files under `spec_classifier/tests/` referenced by the guide. EDIT remains forbidden under D-22.

High-yield claims to verify:
1. **Test count claims** ("420 tests", "N test files") — VOLATILE per D-12; REMOVE.
2. **`pytest_sessionfinish` skip-ratio gate** — verify "skipped/total > 0.50 fails" language via `grep "MAX_SKIP_RATIO\|0.50" spec_classifier/conftest.py`. This is a stable code claim; expect `no_drift`.
3. **Golden workflow CLI** ("`python main.py --vendor <v> --update-golden`") — verify the `--update-golden` flag exists in `main.py` via `grep "update-golden" spec_classifier/main.py`. The flag is real code reality; DO NOT remove the doc claim. (The v1.1 milestone-level "no --update-golden runs sanctioned in v1.1" is a phase-gate, not a permanent code property.)
4. **`spec_classifier/golden/*_expected.jsonl` count claims** ("40 golden fixtures") — VOLATILE per D-12; REMOVE.
5. **Run recipes** — `pytest tests/`, `pytest -k`, `.\run.ps1 -TestsOnly`, `.\run.ps1 -SkipTests` — verify each via `grep`.
6. **PYTEST_ADDOPTS / cache_dir claim** — Phase 4 D-08 added this. If TESTING_GUIDE.md previously said "pytest cache lands in repo", that's now a drift — patch to mention `temp_root\.pytest_cache` redirect via `PYTEST_ADDOPTS` env var in `run.ps1`.
7. **Line-number refs** — apply remove > patch aggressively.

Append rows:
`| spec_classifier/docs/dev/TESTING_GUIDE.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

D-22 forbidden: any edit inside protected paths.

Commit subject: `docs(06): T3 sweep TESTING_GUIDE.md (DRIFT-01)`.
  </action>
  <verify>
    <automated>git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q . &amp;&amp; exit 1; grep -c "TESTING_GUIDE\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md | awk '{exit ($1 &gt;= 1) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
    - `grep -c "TESTING_GUIDE\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1
    - `grep -q "run_full" spec_classifier/docs/dev/TESTING_GUIDE.md` exits non-zero
  </acceptance_criteria>
  <done>TESTING_GUIDE.md swept; per-claim rows recorded; D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (doc-only changes; no executable surface added) | Plan 02 modifies markdown files. No new attack surface; no shell evaluation of doc content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | the 3 dev docs | accept | Doc edits ride atomic per-task commits; reverting is `git revert <hash>`. |
| T-06-05 | Information Disclosure | TESTING_GUIDE.md | accept | Test workflow doc; no secrets surfaced. The `--update-golden` claim documents existing code, not a secret. |
| T-06-06 | Repudiation | sweep results | mitigate | Atomic per-task commits with `docs(06): T<N> ...` subject (D-21). |
</threat_model>

<verification>
Plan-level checks (run after Task 3):
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty.
2. `git diff --stat -- spec_classifier/golden/` empty.
3. 06-DRIFT-AUDIT.md contains at least one row for each of: NEW_VENDOR_GUIDE.md, OPERATIONAL_NOTES.md, TESTING_GUIDE.md.
4. Phase 5 ORPH grep gate still passes.
</verification>

<success_criteria>
- 3 dev docs swept (excl. ONE_BUTTON_RUN.md which is Plan 04's trim target).
- 06-DRIFT-AUDIT.md updated with per-claim rows.
- D-22 protected paths byte-equal.
- Goldens byte-equal.
- 3 atomic commits using `docs(06): T<N>` subject pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-02-SUMMARY.md` summarizing per-file claim counts and resolutions.
</output>
