---
phase: 06-doc-vs-impl-drift-sweep
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - run.ps1
  - spec_classifier/docs/dev/ONE_BUTTON_RUN.md
  - spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md
  - .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md
autonomous: true
requirements: [DRIFT-01, DRIFT-02]
tags: [doc-trim, run-ps1-help, drift-02]

must_haves:
  truths:
    - "`run.ps1` has a `<#.SYNOPSIS / .DESCRIPTION / .PARAMETER / .EXAMPLE#>` comment-based help block per D-05 — `Get-Help .\\run.ps1` and `.\\run.ps1 -?` return useful help."
    - "`run.ps1` RU comment header at lines 1-13 is unchanged (D-06 / Phase 5 D-02 historical-content convention)."
    - "`run.ps1` `param()` block, `if`/`foreach` blocks, and every executable line are byte-identical to phase start except for the inserted `<#...#>` comment block (D-08 — comments-only edit, no behavior change)."
    - "`spec_classifier/docs/dev/ONE_BUTTON_RUN.md` line count strictly decreases from 54 (pre-phase baseline per CONTEXT.md Specifics)."
    - "`spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` line count strictly decreases from 281 (pre-phase baseline per CONTEXT.md Specifics)."
    - "Both trimmed docs include a pointer paragraph mentioning `run.ps1 -?` per D-09."
    - "Both trim files swept per D-10/D-11 (DRIFT-01 sweep covers them in addition to the trim)."
    - "D-22 protected paths byte-equal."
    - "Goldens byte-equal."
  artifacts:
    - path: "run.ps1"
      provides: "Comment-based help block (`<#.SYNOPSIS#>` etc.) added below the existing RU header per Specifics recommendation"
      contains: ".SYNOPSIS"
    - path: "spec_classifier/docs/dev/ONE_BUTTON_RUN.md"
      provides: "Trimmed dev doc with line count < 54 and pointer to `run.ps1 -?`"
    - path: "spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md"
      provides: "Trimmed user doc with line count < 281 and pointer to `run.ps1 -?` + ONE_BUTTON_RUN.md"
    - path: ".planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md"
      provides: "Audit log appended with rows for ONE_BUTTON_RUN.md and RUN_PATHS_AND_IO_LAYOUT.md (DRIFT-01 sweep portion)"
  key_links:
    - from: "run.ps1 help block"
      to: "ONE_BUTTON_RUN.md and RUN_PATHS_AND_IO_LAYOUT.md pointers"
      via: "literal string `run.ps1 -?` in both trimmed docs"
      pattern: "run\\.ps1 -\\?"
    - from: "DOC_INVARIANTS.md invariant #8 (Plan 06)"
      to: "the help block landed by this plan's T1"
      via: "`grep -q \"\\.SYNOPSIS\" run.ps1` check"
      pattern: "\\.SYNOPSIS"
---

<objective>
Land the three DRIFT-02 deliverables together, in this exact task order so dependencies are honored:

1. **T1:** Add the comment-based help block to `run.ps1` per D-05 (six `.EXAMPLE`s mirroring the RU header at lines 1-13, five `.PARAMETER` blocks for `Vendor` / `NoAi` / `TestsOnly` / `SkipTests` / `NoClean`, one `.SYNOPSIS`, one `.DESCRIPTION`). Placement: BELOW the existing RU header, immediately before `param()` (per Specifics recommendation). RU header at lines 1-13 stays byte-identical per D-06.
2. **T2:** Trim `spec_classifier/docs/dev/ONE_BUTTON_RUN.md` (pre-phase baseline 54 lines per CONTEXT.md Specifics) per D-09, adding the pointer "These switches are also documented in `run.ps1`'s comment-based help (run `.\run.ps1 -?`)." and collapsing/removing duplicated sections. Line count post-edit MUST be < 54. Sweep ONE_BUTTON_RUN.md per D-10/D-11 in the same task (record DRIFT-01 audit rows).
3. **T3:** Trim `spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` (pre-phase baseline 281 lines per CONTEXT.md Specifics) per D-09, adding the pointer paragraph "For run.ps1 switches, see `run.ps1 -?` or [docs/dev/ONE_BUTTON_RUN.md](../dev/ONE_BUTTON_RUN.md). For path discovery internals, see below." and trimming the duplicated CLI prose. Line count post-edit MUST be < 281. Sweep RUN_PATHS_AND_IO_LAYOUT.md per D-10/D-11 in the same task (record DRIFT-01 audit rows).

Per D-19, T1 (help block) MUST land before Plan 06 runs invariant #8 verification. This plan's wave assignment + Plan 06's `depends_on` enforces that ordering.

Per D-08, the `run.ps1` edit is comments-only: `param()`, `if`/`foreach`, header banner, env-var assignments, `python` invocations — every executable token byte-identical to phase start.

Purpose: Closes DRIFT-02 (trim CLI-flag prose + make `run.ps1 -?` literally true) and contributes the trim-target portion of DRIFT-01.
Output: 3 modified files, audit log appended, 3 atomic commits.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@run.ps1
@spec_classifier/docs/dev/ONE_BUTTON_RUN.md
@spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md

<interfaces>
<!-- Source-of-truth values for the help block content (from run.ps1:1-13 RU header + param() block) -->

The 6 examples to mirror in `.EXAMPLE` blocks (translated from RU header at run.ps1:1-13):
1. `.\run.ps1` — full pipeline + AI audit + cluster + tests
2. `.\run.ps1 -NoAi` — full pipeline + rule-only audit (no OpenAI)
3. `.\run.ps1 -Vendor dell` — only dell, otherwise as usual
4. `.\run.ps1 -TestsOnly` — pytest only
5. `.\run.ps1 -SkipTests` — full pipeline without pytest at end
6. `.\run.ps1 -Vendor huawei -NoAi -SkipTests` — minimal smoke run

The 5 parameters to document in `.PARAMETER` blocks (from `param()` block at run.ps1:14-19):
- `Vendor` ([string], default `""`)
- `NoAi` ([switch])
- `TestsOnly` ([switch])
- `SkipTests` ([switch])
- `NoClean` ([switch], Phase 4 D-05)

Pre-phase line-count baselines (from CONTEXT.md Specifics, verified at 2026-05-11):
- `spec_classifier/docs/dev/ONE_BUTTON_RUN.md` = **54 lines**
- `spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` = **281 lines**

Both must STRICTLY DECREASE per ROADMAP §SC-3.

Audit-log row format (matches Plan 01 schema):
| spec_classifier/docs/<subtree>/<FILE>.md | <line> | "<claim>" | `<check>` | <remove|patch|no_drift> |

D-22 protected paths (UNCHANGED — `run.ps1` is NOT D-22, but the comments-only edit per D-08 must leave executable tokens byte-identical):
spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comment-based help block to run.ps1 (D-05)</name>
  <files>run.ps1</files>
  <read_first>
    - run.ps1 lines 1-25 (RU header at 1-13 stays byte-identical per D-06; `param()` block at 14-19 read for the 5 switch names)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area B (D-04..D-08; Specifics "Help block placement")
  </read_first>
  <action>
Insert this exact comment-based help block into `run.ps1` BELOW the existing RU comment header (which ends at line 13) and IMMEDIATELY ABOVE the `param(` block (which starts at line 14). The RU header at lines 1-13 stays byte-identical per D-06 / Phase 5 D-02 / D-18 historical-content convention.

The exact block to insert (12 trailing newline preserved before `param(`):

```powershell
<#
.SYNOPSIS
    teresa launcher: end-to-end run of parse, normalize, classify, audit, cluster, and tests for one or all vendors.

.DESCRIPTION
    Single entry point for the spec_classifier pipeline. Reads INPUT/OUTPUT/temp roots from
    spec_classifier/config.local.yaml (falls back to %USERPROFILE%\Desktop\{INPUT,OUTPUT,temporary}).
    Sets PYTHONPYCACHEPREFIX and PYTEST_ADDOPTS so __pycache__ and .pytest_cache land under
    temp_root, never inside the repo working tree. Invokes scripts/clean.ps1 by default at the
    start of every run; pass -NoClean to opt out.

.PARAMETER Vendor
    Run only the named vendor (dell, cisco, hpe, lenovo, huawei, xfusion). Empty (default) runs all.

.PARAMETER NoAi
    Skip the OpenAI/Anthropic audit pass; rule-only audit still runs.

.PARAMETER TestsOnly
    Short-circuit: run pytest only, skip the full pipeline.

.PARAMETER SkipTests
    Run the full pipeline but skip pytest at the end.

.PARAMETER NoClean
    Skip the default-on scripts/clean.ps1 invocation at the start of the run.

.EXAMPLE
    .\run.ps1
    Full pipeline + AI audit + cluster + tests (all vendors).

.EXAMPLE
    .\run.ps1 -NoAi
    Full pipeline + rule-only audit (no OpenAI).

.EXAMPLE
    .\run.ps1 -Vendor dell
    Only dell, otherwise as usual.

.EXAMPLE
    .\run.ps1 -TestsOnly
    Pytest only.

.EXAMPLE
    .\run.ps1 -SkipTests
    Full pipeline without pytest at the end.

.EXAMPLE
    .\run.ps1 -Vendor huawei -NoAi -SkipTests
    Minimal smoke run on a single vendor.
#>
```

Constraints (per D-08 — comments-only, no behavior change):
- The `param()` block (current lines 14-19), `$ErrorActionPreference = "Stop"`, the path-discovery `if ($cfg -match ...)` blocks, the header banner, the `clean.ps1` invocation, the env-var sets, the `python` calls — all stay byte-identical.
- Only insertion: the `<#...#>` block above immediately before `param(`.
- The RU header at the current lines 1-13 is byte-identical (line numbers shift down by the help-block height, but the content of those original 13 lines is unchanged).

**(W-3 grep spacing fix per checker feedback)** All `grep -c "FILE\.md"` checks in this plan's acceptance criteria use the BROADER pattern (no leading `^| ` anchor, no trailing space). This removes a spacing dependency between the audit-log row template's pipe-table format and the verification regex. Any audit-log row containing the filename as a column value will be counted, regardless of column-spacing variations. Trade-off: the broader regex would also match the filename appearing in prose; this is acceptable because the audit-log file's prose sections (Purpose, Tally, Verification) do not enumerate per-file claim filenames as raw tokens — only the table rows do.


Commit subject: `chore(06): T1 add comment-based help block to run.ps1 (DRIFT-02)`.
  </action>
  <verify>
    <automated>! (git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q .) &amp;&amp; ! (git diff run.ps1 | grep -E '^-[^-]' | grep -v '^--- ' | grep -q .) &amp;&amp; [ "$(head -n 13 run.ps1 | sha256sum | awk '{print $1}')" = "2c7dd607da4a860554b22409748fe3df6c8becdadd9c240bf8f6d66409c99c70" ] &amp;&amp; grep -q "\.SYNOPSIS" run.ps1 &amp;&amp; grep -q "\.DESCRIPTION" run.ps1 &amp;&amp; [ "$(grep -c '\.EXAMPLE' run.ps1)" = "6" ] &amp;&amp; [ "$(grep -c '\.PARAMETER' run.ps1)" = "5" ] &amp;&amp; grep -q "Заменяет: run_audit.ps1" run.ps1 &amp;&amp; grep -q "^param(" run.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\.SYNOPSIS" run.ps1` exits 0
    - `grep -q "\.DESCRIPTION" run.ps1` exits 0
    - `grep -c "\.EXAMPLE" run.ps1` returns exactly `6`
    - `grep -c "\.PARAMETER" run.ps1` returns exactly `5` (one each for Vendor, NoAi, TestsOnly, SkipTests, NoClean)
    - All 5 PARAMETER names present: `for p in Vendor NoAi TestsOnly SkipTests NoClean; do grep -q "\.PARAMETER $p" run.ps1 || exit 1; done` exits 0
    - RU header at lines 1-13 byte-identical (B-3 frozen-SHA-256 gate per checker fix): `[ "$(head -n 13 run.ps1 | sha256sum | awk '{print $1}')" = "2c7dd607da4a860554b22409748fe3df6c8becdadd9c240bf8f6d66409c99c70" ]` exits 0. The literal hash `2c7dd607da4a860554b22409748fe3df6c8becdadd9c240bf8f6d66409c99c70` was computed by the planner during revision against the pre-phase working-tree contents of `run.ps1` lines 1-13 (CRLF preserved); the executor MUST NOT recompute or substitute a different reference value — re-running `head -n 13 run.ps1 | sha256sum` after the edit must still yield this exact hash, since the help block is inserted BELOW the RU header (D-06).
    - Help-block addition is insertion-only (no executable-line edits): `! (git diff run.ps1 | grep -E '^-[^-]' | grep -v '^--- ' | grep -q .)` exits 0 — zero deletion lines in the diff (excluding the `--- a/run.ps1` header line itself); the help block is purely additive and no `param()`, `if`, `foreach`, `Write-Host`, `python`, or env-var line is changed.
    - RU header preserved verbatim: `grep -q "Заменяет: run_audit.ps1, scripts/run_full.ps1, scripts/run_tests.ps1" run.ps1` exits 0 (per D-06)
    - `param()` block intact: `grep -q "^param(" run.ps1` exits 0
    - All 5 switches still present in `param()` declarations (anchored to `[string]$X` / `[switch]$X`, not `.PARAMETER` comments — W-1 fix): `for p in Vendor NoAi TestsOnly SkipTests NoClean; do grep -qE "^\s*\[(string|switch)\]\\\$$p" run.ps1 || exit 1; done` exits 0
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty (D-22 — `run.ps1` is NOT in this set)
    - `git diff --stat -- spec_classifier/golden/` empty
    - (B-3 replaces the prior multi-`grep -v` pipeline whose empty-result PASS was reachable even when executable lines were modified.) The two checks above (zero deletion lines + frozen RU-header SHA-256) together enforce "only insertions inside the new `<#...#>` block; no executable-line edits; RU header at lines 1-13 unchanged" — see verification point #9 from the checker prompt.
  </acceptance_criteria>
  <done>run.ps1 has the help block; RU header byte-identical; param() and all executable lines byte-identical; D-22 byte-equal; invariant #8 (Plan 06) will now exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Trim ONE_BUTTON_RUN.md and sweep it (D-09 + DRIFT-01)</name>
  <files>spec_classifier/docs/dev/ONE_BUTTON_RUN.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/dev/ONE_BUTTON_RUN.md (full file — pre-phase baseline 54 lines per CONTEXT.md Specifics; trim target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area B (D-04, D-09 — pointer language and trim mechanism) and § Area C (D-10..D-14 — sweep mechanism)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-11 (Phase 4 already edited ONE_BUTTON_RUN.md sections; trim must not regress those edits)
    - run.ps1 (post-T1 state — help block now exists; trim pointer references it)
  </read_first>
  <action>
Two-step task: (a) trim per D-09, (b) sweep per D-10/D-11.

**(a) Trim per D-09:**
- Add a one-line pointer ABOVE the "Useful run.ps1 switches" section: `These switches are also documented in \`run.ps1\`'s comment-based help (run \`.\run.ps1 -?\`).`
- Achieve a STRICT line-count decrease from 54 (pre-phase baseline) by collapsing or removing duplicated sections. Per D-09, the candidates are the "Quick start" / "Workspace cleanup" sections that duplicate the switches block. Phase 4 D-11 made the "Workspace cleanup" section the canonical home for the `clean.ps1`/`-NoClean` co-occurrence — DO NOT remove that co-occurrence (it's a Phase 4 verification gate). Trim duplication WITHIN those sections rather than removing them entirely if needed to preserve Phase 4's gate.
- Maintain the existing tone: imperative, terse, code-fenced PowerShell (per Phase 4 D-11 / Specifics).
- Final line count `wc -l` MUST be strictly less than 54.

**(b) Sweep per D-10/D-11:**
- Sweep the post-trim file for D-11 categories (path/file existence, switch/CLI flag, line-number refs).
- Verify all CLI examples against `run.ps1` post-T1 (which now also has the help block).
- Append per-claim rows to `06-DRIFT-AUDIT.md`:
  `| spec_classifier/docs/dev/ONE_BUTTON_RUN.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

Single commit covers both parts.

Commit subject: `docs(06): T2 trim ONE_BUTTON_RUN.md and sweep (DRIFT-01, DRIFT-02)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>! (git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q .) &amp;&amp; [ "$(wc -l &lt; spec_classifier/docs/dev/ONE_BUTTON_RUN.md)" -lt 54 ] &amp;&amp; grep -q "run.ps1 -?" spec_classifier/docs/dev/ONE_BUTTON_RUN.md &amp;&amp; grep -q "\-NoClean" spec_classifier/docs/dev/ONE_BUTTON_RUN.md &amp;&amp; grep -q "clean.ps1" spec_classifier/docs/dev/ONE_BUTTON_RUN.md &amp;&amp; [ "$(grep -c 'ONE_BUTTON_RUN' .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l spec_classifier/docs/dev/ONE_BUTTON_RUN.md` returns a value strictly less than 54 (pre-phase baseline per CONTEXT.md Specifics)
    - `grep -q "run.ps1 -?" spec_classifier/docs/dev/ONE_BUTTON_RUN.md` exits 0 (D-09 pointer added)
    - `grep -q "\-NoClean" spec_classifier/docs/dev/ONE_BUTTON_RUN.md` exits 0 (Phase 4 CACHE-04 gate preserved)
    - `grep -q "clean.ps1" spec_classifier/docs/dev/ONE_BUTTON_RUN.md` exits 0 (Phase 4 CACHE-04 gate preserved — `clean.ps1` and `-NoClean` co-occur)
    - `grep -c "ONE_BUTTON_RUN\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1 (DRIFT-01 sweep portion recorded)
    - `grep -q "run_full" spec_classifier/docs/dev/ONE_BUTTON_RUN.md` exits non-zero
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
  </acceptance_criteria>
  <done>ONE_BUTTON_RUN.md trimmed (line count < 54), pointer added, Phase 4 CACHE-04 co-occurrence gate preserved, DRIFT-01 sweep rows recorded, D-22 byte-equal.</done>
</task>

<task type="auto">
  <name>Task 3: Trim RUN_PATHS_AND_IO_LAYOUT.md and sweep it (D-09 + DRIFT-01)</name>
  <files>spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md, .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md</files>
  <read_first>
    - spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md (full file — pre-phase baseline 281 lines per CONTEXT.md Specifics; trim target)
    - .planning/phases/06-doc-vs-impl-drift-sweep/06-CONTEXT.md § Area B (D-09 trim mechanism for this file specifically) and § Area C (D-10..D-14)
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-12 (Phase 4 explicitly did NOT touch this file; cross-doc reconciliation deferred to Phase 6)
    - run.ps1 (post-T1 state)
    - spec_classifier/docs/dev/ONE_BUTTON_RUN.md (post-T2 state — referenced in the trim pointer)
  </read_first>
  <action>
Two-step task: (a) trim per D-09, (b) sweep per D-10/D-11.

**(a) Trim per D-09 — RUN_PATHS_AND_IO_LAYOUT.md specifics:**
- Add this exact pointer paragraph at the TOP of the document (after the H1 title, before any other content): `For run.ps1 switches, see \`run.ps1 -?\` or [docs/dev/ONE_BUTTON_RUN.md](../dev/ONE_BUTTON_RUN.md). For path discovery internals, see below.`
- Trim CLI prose: per D-09, the bulk lives in §"Configuration path priority" + §"Default folders" + the end-of-file IDE notes. Trim the "Do not run python -m pytest directly without run.ps1" / "Run `clean.ps1`" prose into the pointer paragraph above; keep the path/I/O TABLES as-is — those are the doc's load-bearing content per D-09.
- Final line count `wc -l` MUST be strictly less than 281.

**(b) Sweep per D-10/D-11:**
- Sweep the post-trim file for D-11 categories.
- Cross-reference any "PYTHONPYCACHEPREFIX" claim against Phase 4 D-08/D-13 wiring (both env vars, both entry points). The v1.0 DOC-03 audit MISSED a `RUN_PATHS_AND_IO_LAYOUT.md:22` PYTHONPYCACHEPREFIX claim — this is the file that broke v1.0 (per REQUIREMENTS.md DRIFT-03 justification). Apply the cache-redirect verification with extra rigor here.
- Append per-claim rows to `06-DRIFT-AUDIT.md`:
  `| spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md | <line> | "<claim>" | \`<check>\` | <remove|patch|no_drift> |`

Single commit covers both parts.

Commit subject: `docs(06): T3 trim RUN_PATHS_AND_IO_LAYOUT.md and sweep (DRIFT-01, DRIFT-02)`.

D-22 forbidden: any edit inside protected paths.
  </action>
  <verify>
    <automated>! (git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py | grep -q .) &amp;&amp; [ "$(wc -l &lt; spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md)" -lt 281 ] &amp;&amp; grep -q "run.ps1 -?" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md &amp;&amp; grep -q "ONE_BUTTON_RUN" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md &amp;&amp; [ "$(grep -c 'RUN_PATHS_AND_IO_LAYOUT' .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` returns a value strictly less than 281 (pre-phase baseline per CONTEXT.md Specifics)
    - `grep -q "run.ps1 -?" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exits 0 (D-09 pointer added)
    - `grep -q "ONE_BUTTON_RUN" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exits 0 (D-09 cross-link to canonical doc)
    - `grep -c "RUN_PATHS_AND_IO_LAYOUT\.md" .planning/phases/06-doc-vs-impl-drift-sweep/06-DRIFT-AUDIT.md` ≥ 1 (DRIFT-01 sweep portion recorded)
    - `grep -q "run_full" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exits non-zero
    - If the file references PYTHONPYCACHEPREFIX, both `run.ps1` AND `teresa_gui.py` must be co-mentioned (Phase 4 D-13 defense-in-depth): EITHER `grep -q "PYTHONPYCACHEPREFIX" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exits non-zero (claim removed per remove>patch heuristic) OR both `grep -q "run\.ps1" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` AND `grep -q "teresa_gui" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exit 0 (defense-in-depth wording preserved)
    - `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty
    - `git diff --stat -- spec_classifier/golden/` empty
  </acceptance_criteria>
  <done>RUN_PATHS_AND_IO_LAYOUT.md trimmed (line count < 281), pointer added, defense-in-depth cache-redirect wording preserved (or claim removed), DRIFT-01 sweep rows recorded, D-22 byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run.ps1` shell parser ↔ comment-based help block | The `<#...#>` block is parsed by PowerShell's help system but never executed. No code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | run.ps1 executable lines | mitigate | T1 acceptance criteria assert byte-identity of `param()` and all executable lines via diff check; only `<#...#>` block changes. |
| T-06-11 | Information Disclosure | run.ps1 help block | accept | Help block content is documentation already published in ONE_BUTTON_RUN.md / CLI_CONFIG_REFERENCE.md; no new info surfaced. |
| T-06-12 | Denial of Service | run.ps1 startup parse | accept | Comment-based help blocks are PowerShell standard; parsing overhead is negligible (microseconds). |
| T-06-13 | Repudiation | trim + sweep changes | mitigate | Atomic per-task commits per D-21. |
</threat_model>

<verification>
1. `git diff --stat -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` empty.
2. `git diff --stat -- spec_classifier/golden/` empty.
3. `grep -q "\.SYNOPSIS" run.ps1` exits 0 (this satisfies DOC_INVARIANTS.md invariant #8 ahead of Plan 06).
4. `grep -c "\.EXAMPLE" run.ps1` returns 6.
5. `grep -c "\.PARAMETER" run.ps1` returns 5.
6. `wc -l spec_classifier/docs/dev/ONE_BUTTON_RUN.md` < 54.
7. `wc -l spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` < 281.
8. `grep -q "run.ps1 -?" spec_classifier/docs/dev/ONE_BUTTON_RUN.md` exits 0.
9. `grep -q "run.ps1 -?" spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` exits 0.
10. 06-DRIFT-AUDIT.md has rows for both ONE_BUTTON_RUN.md and RUN_PATHS_AND_IO_LAYOUT.md.
11. Phase 5 ORPH grep gate still passes.
</verification>

<success_criteria>
- DRIFT-02 closed: trim done on both named docs, pointer to `run.ps1 -?` added, help block makes the pointer literally true.
- DRIFT-01 portion done for the 2 trim-target docs.
- D-22 protected paths byte-equal.
- Goldens byte-equal.
- 3 atomic commits: T1=`chore(06)`, T2/T3=`docs(06)`.
- ROADMAP §SC-3 satisfied (line counts strictly decreased).
</success_criteria>

<output>
After completion, create `.planning/phases/06-doc-vs-impl-drift-sweep/06-04-SUMMARY.md` recording: pre/post line counts of both trim docs, run.ps1 diff stats (additions only inside `<#...#>` block), per-file claim counts swept and resolutions.
</output>
