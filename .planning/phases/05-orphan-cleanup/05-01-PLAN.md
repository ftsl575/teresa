---
phase: 05-orphan-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - spec_classifier/pyproject.toml
  - spec_classifier/config.local.yaml.example
  - .cursor/
  - teresa.zip
autonomous: true
requirements:
  - ORPH-01
  - ORPH-02
  - ORPH-03
  - ORPH-04
tags:
  - cleanup
  - config
  - powershell
  - orphan-removal

must_haves:
  truths:
    - "ROADMAP.md Phase 5 §SC-1 grep is scoped to *.toml + *.example files (post-T1)"
    - "spec_classifier/pyproject.toml lines 4-6 name PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS and run.ps1 + teresa_gui.py (per D-05)"
    - "spec_classifier/config.local.yaml.example:11 names scripts/clean.ps1, run.ps1, AND teresa_gui.py (per D-06)"
    - "Working tree contains no .cursor/ directory"
    - "Working tree contains no teresa.zip artifact"
    - "LAUNCHER_README.md still contains its historical 'replaces three legacy scripts' mention (D-19 untouched)"
    - "spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py} have zero bytes diff over phase window (D-22)"
    - "pytest -q from spec_classifier/ passes without tripping the 0.50 skip-ratio guard (D-20)"
    - "spec_classifier/golden/ directory has zero-byte diff over phase window (D-21)"
    - ".gitignore lines 48 (.cursor/) and 56 (teresa.zip) remain unmodified (D-13)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 §SC-1 grep gate scoped to *.toml + *.example"
      contains: "--include='*.toml' --include='*.example'"
    - path: "spec_classifier/pyproject.toml"
      provides: "Cache-redirect comment block referencing run.ps1 + teresa_gui.py + both env vars"
      contains: "PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS"
    - path: "spec_classifier/config.local.yaml.example"
      provides: "temp_root consumer comment naming all three consumers"
      contains: "Used by scripts/clean.ps1, run.ps1, and teresa_gui.py."
  key_links:
    - from: "spec_classifier/pyproject.toml:4-6"
      to: "run.ps1 + teresa_gui.py (Phase 4 wiring)"
      via: "Comment block referencing both env vars and both entry points"
      pattern: "run\\.ps1 and teresa_gui\\.py set both"
    - from: "spec_classifier/config.local.yaml.example:11"
      to: "Three consumers of temp_root"
      via: "Single comment line listing scripts/clean.ps1, run.ps1, teresa_gui.py"
      pattern: "Used by scripts/clean\\.ps1, run\\.ps1, and teresa_gui\\.py"
    - from: ".planning/ROADMAP.md §SC-1"
      to: "REQUIREMENTS.md ORPH verification grep"
      via: "Identical --include flags"
      pattern: "--include='\\*\\.toml' --include='\\*\\.example'"
---

<objective>
Phase 5 — Orphan Cleanup. Five sequential, atomic edits in one plan:

1. Tighten ROADMAP.md Phase 5 §SC-1's `run_full` grep gate to scope it to `*.toml` and `*.example` files (matching REQUIREMENTS.md verbatim) so the gate ignores `run.ps1:3` (RU historical comment) and `teresa_gui.py` `_run_full` method-name false positives.
2. ORPH-01 — Replace `spec_classifier/pyproject.toml:4-6` with a 3-line block that names BOTH env vars (`PYTHONPYCACHEPREFIX` + `PYTEST_ADDOPTS`) and BOTH entry points (`run.ps1` + `teresa_gui.py`), reflecting Phase 4's defense-in-depth wiring.
3. ORPH-02 — Replace `spec_classifier/config.local.yaml.example:11` with a single comment naming all three consumers of `temp_root` (`scripts/clean.ps1`, `run.ps1`, `teresa_gui.py`).
4. ORPH-03 — Remove the residual `.cursor/` directory from the working tree (gitignored at `.gitignore:48`; entry stays).
5. ORPH-04 — Remove the residual `teresa.zip` sandbox artifact (gitignored at `.gitignore:56`; entry stays).

Purpose: Close the v1.1 cleanup milestone's "orphan references" requirement set. Replacement wording aligns with the post-Phase-4 reality (`run.ps1` and `teresa_gui.py` set the cache-redirect env vars automatically); removed artifacts unclutter the working tree while remaining gitignored against future reaccumulation.

Output: 5 atomic commits (`chore(05): T<N> ...`), each ≤72 chars on the subject. No code/rules/golden/test diffs (D-22 protected). No new files. No `.gitignore` edits.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-orphan-cleanup/05-CONTEXT.md
@.planning/phases/04-cache-redirect/04-CONTEXT.md
@CLAUDE.md
@spec_classifier/CLAUDE.md
@.gitignore
@LAUNCHER_README.md
@spec_classifier/pyproject.toml
@spec_classifier/config.local.yaml.example

<interfaces>
<!-- Pre-edit state of every file Phase 5 will touch. Executor should rely on these
     literal contents instead of re-deriving them. Each task's <action> includes the
     EXACT replacement text to apply; no indirection through D-XX references. -->

# Pre-edit state — ROADMAP.md §SC-1 (Phase 5)
# (line ~63 in current ROADMAP.md, inside the "### Phase 5: Orphan Cleanup" block)
Current text of success criterion 1:
  1. `grep -rn "run_full" .` (excluding `CHANGELOG.md`, `LAUNCHER_README.md`, `.planning/`) returns zero matches across the entire repo.

# Pre-edit state — spec_classifier/pyproject.toml (FULL FILE, 7 lines)
[tool.pytest.ini_options]
cache_dir = "../../temporary/.pytest_cache"

# __pycache__ redirection requires PYTHONPYCACHEPREFIX env var.
# Set it in your shell profile or use scripts/run_full.ps1.
# pyproject.toml cannot redirect __pycache__ (only env var can).
[tool.python]

# Pre-edit state — spec_classifier/config.local.yaml.example (FULL FILE, 12 lines)
# config.local.yaml — Personal overrides (NOT committed to git).
# Copy to config.local.yaml and edit paths for your machine.
# These override values from config.yaml.
# CLI arguments (--output-dir, --input, --batch-dir) override everything.

paths:
  input_root: "C:\\Users\\<USERNAME>\\Desktop\\INPUT"
  output_root: "C:\\Users\\<USERNAME>\\Desktop\\OUTPUT"

# Where to redirect temporary files (.pytest_cache, __pycache__).
# Used by scripts/clean.ps1 and scripts/run_full.ps1.
temp_root: "C:\\Users\\<USERNAME>\\Desktop\\temporary"

# Pre-edit state — .gitignore lines 48 + 56 (MUST remain untouched per D-13)
# Line 48: .cursor/
# Line 56: teresa.zip

# Pre-edit state — LAUNCHER_README.md (lines 1-5)
# teresa launcher
#
# One launcher to rule them all. Replaces three legacy scripts (`run_audit.ps1`,
# `scripts/run_full.ps1`, `scripts/run_tests.ps1`) with a single PowerShell
# entry point and a PyQt6 GUI on top.
# (the phrase "replaces three legacy scripts" appears on line 3, NOT line 4 as the
#  context block referenced — verify with literal grep instead of line number)
</interfaces>

<phase4_handoff>
Phase 4 (already shipped) wired BOTH env vars in BOTH entry points:
- `run.ps1` sets `$env:PYTHONPYCACHEPREFIX` AND `$env:PYTEST_ADDOPTS` from `config.local.yaml::temp_root`
- `teresa_gui.py` sets `os.environ["PYTHONPYCACHEPREFIX"]` AND `os.environ["PYTEST_ADDOPTS"]` from the same source
- Defense-in-depth (Phase 4 D-13): both are set independently with identical values; this is the canonical phrasing the Phase 5 replacement text MUST mirror.

Phase 4 D-14 explicitly DEFERRED the `config.local.yaml.example:11` rewrite to Phase 5's ORPH-02 (T3 below). The hand-off is intentional.

Replacement-text vocabulary lock (do NOT regress to looser REQUIREMENTS.md wording):
- pyproject.toml MUST name BOTH env vars (`PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS`) AND BOTH entry points (`run.ps1 and teresa_gui.py`).
- config.local.yaml.example MUST name ALL THREE consumers (`scripts/clean.ps1, run.ps1, and teresa_gui.py`).
</phase4_handoff>
</context>

<tasks>

<task type="auto">
  <name>Task T1: Tighten ROADMAP.md Phase 5 §SC-1 grep scope (per D-01, D-04)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read the full "### Phase 5: Orphan Cleanup" block, especially success criterion 1 — line ~63)
    - .planning/REQUIREMENTS.md § "Orphan Cleanup (ORPH) — Plan 2" — Verification line — to mirror the grep flags verbatim
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-01, D-04 — gate-realignment rationale
  </read_first>
  <action>
Open `.planning/ROADMAP.md`. Locate the line in the "### Phase 5: Orphan Cleanup" block whose current literal text is:

```
  1. `grep -rn "run_full" .` (excluding `CHANGELOG.md`, `LAUNCHER_README.md`, `.planning/`) returns zero matches across the entire repo.
```

Replace it with the following EXACT line (one continuous markdown line, leading two spaces preserved to match the existing list indentation):

```
  1. `grep -rn "run_full" . --include='*.toml' --include='*.example'` (excluding `CHANGELOG.md`, `LAUNCHER_README.md`, `.planning/`) returns zero matches across `*.toml` / `*.example` files.
```

Substantive changes:
- Added `--include='*.toml' --include='*.example'` flags to the grep invocation (matches REQUIREMENTS.md verbatim).
- Replaced the trailing phrase "across the entire repo" with "across `*.toml` / `*.example` files" so the prose matches the new gate semantics.

Do NOT change the surrounding success criteria (#2, #3, #4, #5), the D-22 guard step, the pytest skip-ratio gate paragraph, or anything else in the Phase 5 block. The change is one line only.

After applying the edit, commit:

```powershell
git add .planning/ROADMAP.md
git commit -m "chore(05): T1 align ROADMAP §SC-1 grep scope with REQUIREMENTS"
```
  </action>
  <verify>
    <automated>grep -c -- "--include='\*\.toml' --include='\*\.example'" .planning/ROADMAP.md</automated>
    Expected: returns at least `1`.

    Additional checks (run all):
    - `grep -c "across the entire repo" .planning/ROADMAP.md` → `0` (the old wording is gone from §SC-1; if any other phase contains the exact phrase elsewhere, this check is over-strict — narrow to the Phase 5 block as needed via `sed -n '/### Phase 5: Orphan Cleanup/,/### Phase 6/p' .planning/ROADMAP.md | grep -c "across the entire repo"` → `0`).
    - `git log -1 --pretty=%s` → starts with `chore(05): T1 `.
  </verify>
  <acceptance_criteria>
    - `grep -c -- "--include='\*\.toml' --include='\*\.example'" .planning/ROADMAP.md` returns ≥ `1`.
    - `sed -n '/### Phase 5: Orphan Cleanup/,/### Phase 6/p' .planning/ROADMAP.md | grep -c "across the entire repo"` returns `0` (old wording removed from Phase 5 block).
    - `sed -n '/### Phase 5: Orphan Cleanup/,/### Phase 6/p' .planning/ROADMAP.md | grep -c "across \`\*\.toml\` / \`\*\.example\` files"` returns `1` (new wording present).
    - `git diff --stat HEAD~1 -- .planning/ROADMAP.md` shows the file changed by exactly one commit.
    - `git log -1 --pretty=%s` matches pattern `^chore\(05\): T1 `.
    - `git diff --stat HEAD~1 -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` produces NO output (D-22 guard).
  </acceptance_criteria>
  <done>ROADMAP.md §SC-1 contains the new include-flag-scoped grep wording and a single atomic commit `chore(05): T1 ...` exists. The post-T1 grep spec is now in effect for T2..T5 verification.</done>
</task>

<task type="auto">
  <name>Task T2: ORPH-01 — Rewrite spec_classifier/pyproject.toml lines 4-6 (per D-05)</name>
  <files>spec_classifier/pyproject.toml</files>
  <read_first>
    - spec_classifier/pyproject.toml (full file — 7 lines; confirm lines 1-3 and line 7 remain untouched)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-05 — exact 3-line replacement block
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13 — env-var wiring vocabulary the replacement text mirrors
  </read_first>
  <action>
Open `spec_classifier/pyproject.toml`. The full pre-edit file is exactly:

```toml
[tool.pytest.ini_options]
cache_dir = "../../temporary/.pytest_cache"

# __pycache__ redirection requires PYTHONPYCACHEPREFIX env var.
# Set it in your shell profile or use scripts/run_full.ps1.
# pyproject.toml cannot redirect __pycache__ (only env var can).
[tool.python]
```

Replace lines 4, 5, 6 (the 3-comment block) with the following EXACT 3 lines:

```
# __pycache__ + .pytest_cache redirection requires PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS env vars.
# run.ps1 and teresa_gui.py set both from config.local.yaml::temp_root automatically.
# pyproject.toml cannot redirect __pycache__ (only env var can).
```

Resulting full file (7 lines, line 3 blank, lines 1-2 unchanged, line 7 unchanged):

```toml
[tool.pytest.ini_options]
cache_dir = "../../temporary/.pytest_cache"

# __pycache__ + .pytest_cache redirection requires PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS env vars.
# run.ps1 and teresa_gui.py set both from config.local.yaml::temp_root automatically.
# pyproject.toml cannot redirect __pycache__ (only env var can).
[tool.python]
```

Do NOT collapse the 3 comments into 1, do NOT add or remove blank lines, do NOT modify line 1 (`[tool.pytest.ini_options]`), line 2 (`cache_dir = ...`), line 3 (blank), or line 7 (`[tool.python]`). The 3-line shape is load-bearing per CONTEXT.md "Specific Ideas".

After applying the edit, commit:

```powershell
git add spec_classifier/pyproject.toml
git commit -m "chore(05): T2 rewrite pyproject.toml run_full reference"
```
  </action>
  <verify>
    <automated>grep -c "PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS" spec_classifier/pyproject.toml</automated>
    Expected: `1`.

    Additional automated checks (all must pass):
    - `grep -c "run.ps1 and teresa_gui.py set both" spec_classifier/pyproject.toml` → `1`
    - `grep -c "pyproject.toml cannot redirect __pycache__ (only env var can)." spec_classifier/pyproject.toml` → `1`
    - `grep -c "scripts/run_full.ps1" spec_classifier/pyproject.toml` → `0`
    - Re-run the post-T1 gate grep on the whole repo (expecting only T3..T5 still-pending matches in `config.local.yaml.example`):
      `grep -rn "run_full" . --include='*.toml' --exclude-dir=.planning` → `0` matches.
    - Line count check: `wc -l spec_classifier/pyproject.toml` → `7` (unchanged from pre-edit).
    - `git log -1 --pretty=%s` matches pattern `^chore\(05\): T2 `.
  </verify>
  <acceptance_criteria>
    - `grep -c "PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS" spec_classifier/pyproject.toml` returns `1`.
    - `grep -c "run.ps1 and teresa_gui.py set both" spec_classifier/pyproject.toml` returns `1`.
    - `grep -c "pyproject.toml cannot redirect" spec_classifier/pyproject.toml` returns `1`.
    - `grep -c "scripts/run_full.ps1" spec_classifier/pyproject.toml` returns `0`.
    - `grep -rn "run_full" . --include='*.toml' --exclude-dir=.planning` returns NO matches.
    - `wc -l spec_classifier/pyproject.toml` reports `7` lines (file shape preserved).
    - `git log -1 --pretty=%s` matches `^chore\(05\): T2 `.
    - `git diff --stat HEAD~1 -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` produces NO output (D-22 guard).
  </acceptance_criteria>
  <done>`pyproject.toml` lines 4-6 contain the exact D-05 3-line block; no `run_full` reference remains in any `*.toml` file outside `.planning/`; one atomic commit `chore(05): T2 ...`.</done>
</task>

<task type="auto">
  <name>Task T3: ORPH-02 — Rewrite spec_classifier/config.local.yaml.example:11 (per D-06)</name>
  <files>spec_classifier/config.local.yaml.example</files>
  <read_first>
    - spec_classifier/config.local.yaml.example (full file — 12 lines; confirm lines 1-10 and line 12 remain untouched)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-06 — exact replacement comment line
    - .planning/phases/04-cache-redirect/04-CONTEXT.md § D-08, D-13, D-14 — three-consumer rationale and Phase-4→Phase-5 hand-off
  </read_first>
  <action>
Open `spec_classifier/config.local.yaml.example`. The full pre-edit file is exactly:

```yaml
# config.local.yaml — Personal overrides (NOT committed to git).
# Copy to config.local.yaml and edit paths for your machine.
# These override values from config.yaml.
# CLI arguments (--output-dir, --input, --batch-dir) override everything.

paths:
  input_root: "C:\\Users\\<USERNAME>\\Desktop\\INPUT"
  output_root: "C:\\Users\\<USERNAME>\\Desktop\\OUTPUT"

# Where to redirect temporary files (.pytest_cache, __pycache__).
# Used by scripts/clean.ps1 and scripts/run_full.ps1.
temp_root: "C:\\Users\\<USERNAME>\\Desktop\\temporary"
```

Replace line 11 (currently `# Used by scripts/clean.ps1 and scripts/run_full.ps1.`) with this EXACT line:

```
# Used by scripts/clean.ps1, run.ps1, and teresa_gui.py.
```

Resulting full file (12 lines, only line 11 changed):

```yaml
# config.local.yaml — Personal overrides (NOT committed to git).
# Copy to config.local.yaml and edit paths for your machine.
# These override values from config.yaml.
# CLI arguments (--output-dir, --input, --batch-dir) override everything.

paths:
  input_root: "C:\\Users\\<USERNAME>\\Desktop\\INPUT"
  output_root: "C:\\Users\\<USERNAME>\\Desktop\\OUTPUT"

# Where to redirect temporary files (.pytest_cache, __pycache__).
# Used by scripts/clean.ps1, run.ps1, and teresa_gui.py.
temp_root: "C:\\Users\\<USERNAME>\\Desktop\\temporary"
```

Do NOT change line 10 (the section heading `# Where to redirect temporary files ...`), line 12 (`temp_root: ...`), or any other line. Only line 11.

After applying the edit, commit:

```powershell
git add spec_classifier/config.local.yaml.example
git commit -m "chore(05): T3 rewrite config.local.yaml.example temp_root note"
```
  </action>
  <verify>
    <automated>grep -c "Used by scripts/clean.ps1, run.ps1, and teresa_gui.py." spec_classifier/config.local.yaml.example</automated>
    Expected: `1`.

    Additional automated checks (all must pass):
    - `grep -c "scripts/run_full.ps1" spec_classifier/config.local.yaml.example` → `0`
    - Re-run the post-T1 gate grep on the whole repo (expecting `0` matches now that T2 + T3 are both done):
      `grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning` → `0` matches.
    - Line count check: `wc -l spec_classifier/config.local.yaml.example` → `12`.
    - `git log -1 --pretty=%s` matches pattern `^chore\(05\): T3 `.
  </verify>
  <acceptance_criteria>
    - `grep -c "Used by scripts/clean.ps1, run.ps1, and teresa_gui.py." spec_classifier/config.local.yaml.example` returns `1`.
    - `grep -c "scripts/run_full.ps1" spec_classifier/config.local.yaml.example` returns `0`.
    - `grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning` returns NO matches across the whole repo.
    - `wc -l spec_classifier/config.local.yaml.example` reports `12` lines.
    - `git log -1 --pretty=%s` matches `^chore\(05\): T3 `.
    - `git diff --stat HEAD~1 -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` produces NO output (D-22 guard).
  </acceptance_criteria>
  <done>`config.local.yaml.example:11` contains the exact D-06 comment naming all three consumers; no `run_full` reference remains in any `*.toml` or `*.example` file outside `.planning/`; one atomic commit `chore(05): T3 ...`.</done>
</task>

<task type="auto">
  <name>Task T4: ORPH-03 — Remove .cursor/ directory (per D-08)</name>
  <files>.cursor/</files>
  <read_first>
    - .gitignore (lines 47-49 — confirm `.cursor/` is at line 48 and MUST remain untouched per D-13)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-08, D-09, D-10, D-11, D-12, D-13 — removal mechanism rationale
  </read_first>
  <action>
Run the following EXACT PowerShell command from the repo root (`C:\Users\G\Desktop\teresa`). It is a Test-Path-guarded `Remove-Item` call so re-execution after partial completion is a no-op:

```powershell
if (Test-Path .\.cursor) { Remove-Item .\.cursor -Recurse -Force }
```

Constraints (from CONTEXT.md):
- Do NOT use `git clean -fdx` (D-11 explicitly rejects it).
- Do NOT add a `try`/`catch` wrapper (D-12 — fail-fast on lock; phase halts immediately if the directory is locked, e.g., by an open IDE).
- Do NOT augment `clean.ps1` to include `.cursor/` (D-09).
- Do NOT create a new sweep script (D-10).
- Do NOT touch `.gitignore` line 48 — the `.cursor/` ignore rule MUST remain in place so the directory stays ignored if Cursor is ever opened in the repo again (D-13).
- `.cursor/` is gitignored, so no `git rm` is needed; the removal is purely a filesystem operation.

After the removal succeeds, commit (the commit will record no file content change since `.cursor/` was gitignored — but it is the atomic-commit anchor for T4 per D-18):

```powershell
git commit --allow-empty -m "chore(05): T4 remove .cursor directory from working tree"
```

(Allow-empty is required because the deleted contents were gitignored — `git status` will show no staged changes. The commit serves as the audit-trail marker that ORPH-03 was executed in this phase window.)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path .\.cursor) { exit 1 } else { exit 0 }"</automated>
    Expected: exit code `0` (i.e., `.cursor/` does NOT exist).

    Additional automated checks (all must pass):
    - PowerShell-native: `powershell -NoProfile -Command "Test-Path .\.cursor"` → `False`.
    - `.gitignore` line 48 still contains `.cursor/`: `sed -n '48p' .gitignore` → `.cursor/`.
    - `git log -1 --pretty=%s` matches pattern `^chore\(05\): T4 `.
    - `grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning` → `0` matches (gate still green; T2+T3 unchanged).
  </verify>
  <acceptance_criteria>
    - `Test-Path .\.cursor` returns `$false` (verified via PowerShell exit-code check above).
    - The `.cursor/` directory does NOT appear in a fresh `Get-ChildItem .\` listing.
    - `.gitignore` line 48 still reads exactly `.cursor/` (D-13 guard — no edit to the ignore rule).
    - `git log -1 --pretty=%s` matches `^chore\(05\): T4 `.
    - `git diff --stat HEAD~1 -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` produces NO output (D-22 guard).
    - `git diff --stat HEAD~1 -- .gitignore` produces NO output (D-13 guard — `.gitignore` untouched).
  </acceptance_criteria>
  <done>`.cursor/` directory absent from working tree; `.gitignore` line 48 untouched; one atomic commit `chore(05): T4 ...` (allow-empty since contents were gitignored).</done>
</task>

<task type="auto">
  <name>Task T5: ORPH-04 — Remove teresa.zip artifact (per D-08)</name>
  <files>teresa.zip</files>
  <read_first>
    - .gitignore (lines 55-57 — confirm `teresa.zip` is at line 56 and MUST remain untouched per D-13; line 45 `*.zip` also matches)
    - .planning/phases/05-orphan-cleanup/05-CONTEXT.md § D-08, D-11, D-12, D-13 — removal mechanism rationale
  </read_first>
  <action>
Run the following EXACT PowerShell command from the repo root (`C:\Users\G\Desktop\teresa`). It is a Test-Path-guarded `Remove-Item` call so re-execution after partial completion is a no-op:

```powershell
if (Test-Path .\teresa.zip) { Remove-Item .\teresa.zip -Force }
```

Constraints (from CONTEXT.md):
- Do NOT use `git clean -fdx` (D-11 explicitly rejects it).
- Do NOT add a `try`/`catch` wrapper (D-12 — fail-fast on lock).
- Do NOT touch `.gitignore` line 56 (or line 45's `*.zip` rule) — both ignore rules MUST remain in place so the artifact stays ignored if it ever reappears (D-13).
- `teresa.zip` is gitignored (both via `*.zip` on line 45 and the explicit `teresa.zip` on line 56), so no `git rm` is needed; the removal is purely a filesystem operation.
- `Recurse` flag is omitted because `teresa.zip` is a single file, not a directory.

After the removal succeeds, commit (allow-empty since the deleted file was gitignored):

```powershell
git commit --allow-empty -m "chore(05): T5 remove teresa.zip from working tree"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path .\teresa.zip) { exit 1 } else { exit 0 }"</automated>
    Expected: exit code `0` (i.e., `teresa.zip` does NOT exist).

    Additional automated checks (all must pass):
    - PowerShell-native: `powershell -NoProfile -Command "Test-Path .\teresa.zip"` → `False`.
    - `.gitignore` line 56 still contains `teresa.zip`: `sed -n '56p' .gitignore` → `teresa.zip`.
    - `.gitignore` line 45 still contains `*.zip`: `sed -n '45p' .gitignore` → `*.zip`.
    - `git log -1 --pretty=%s` matches pattern `^chore\(05\): T5 `.
    - Re-run the gate grep one more time: `grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning` → `0` matches.
  </verify>
  <acceptance_criteria>
    - `Test-Path .\teresa.zip` returns `$false` (verified via PowerShell exit-code check above).
    - `teresa.zip` does NOT appear in a fresh `Get-ChildItem .\` listing.
    - `.gitignore` lines 45 (`*.zip`) and 56 (`teresa.zip`) both unchanged (D-13 guard).
    - `git log -1 --pretty=%s` matches `^chore\(05\): T5 `.
    - `git diff --stat HEAD~1 -- spec_classifier/src spec_classifier/rules spec_classifier/golden spec_classifier/tests spec_classifier/batch_audit.py spec_classifier/cluster_audit.py spec_classifier/main.py spec_classifier/conftest.py` produces NO output (D-22 guard).
    - `git diff --stat HEAD~1 -- .gitignore` produces NO output (D-13 guard).
  </acceptance_criteria>
  <done>`teresa.zip` absent from working tree; `.gitignore` lines 45 and 56 untouched; one atomic commit `chore(05): T5 ...` (allow-empty since the file was gitignored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan author → Executor | Phase 5 instructs the executor to apply config-text edits and run two `Remove-Item` calls on filesystem paths. The threat surface is the plan's literal action text and the path arguments to `Remove-Item`. |
| Executor → Filesystem | Two `Remove-Item` calls touch only the working tree. Both are guarded by `Test-Path` against literal-string paths (`.\.cursor`, `.\teresa.zip`). |
| Phase 5 edits → D-22 protected paths | Phase 5 must not write into `spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`. Crossing this boundary = phase gate FAIL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | D-22 protected paths (`spec_classifier/{src,rules,golden,tests,batch_audit.py,cluster_audit.py,main.py,conftest.py}`) | mitigate | Plan-level final verification check #6 runs `git diff --stat HEAD~5 -- <D-22 paths>` and MUST report no changes. Each task's acceptance criteria also re-runs the same per-task `git diff --stat HEAD~1 -- <D-22 paths>` check. Two-layer guard: per-commit (catches single-task drift) + phase-window (catches drift across the 5-commit window). |
| T-05-02 | Tampering | Filesystem outside the two intended paths | mitigate | Both `Remove-Item` calls are wrapped in `if (Test-Path <literal-path>)` guards on EXACT literal paths (`.\.cursor` and `.\teresa.zip`). No globbing, no recursion variables, no relative-path arithmetic. Recurse flag is scoped to T4 (directory) and absent from T5 (single file). |
| T-05-03 | Repudiation / Spoofing | Replacement wording regressing to REQUIREMENTS.md's looser version (omitting `PYTEST_ADDOPTS` / `teresa_gui.py`) | mitigate | T2 acceptance criteria require `grep -c "PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS"` AND `grep -c "run.ps1 and teresa_gui.py set both"` AND `grep -c "pyproject.toml cannot redirect"` to all return `1`. T3 acceptance criteria require `grep -c "Used by scripts/clean.ps1, run.ps1, and teresa_gui.py."` to return `1`. Exact-string greps make wording regression mechanically detectable. |

ASVS L1 sufficient: no new attack surface added. Phase 5 is config-text edits + two filesystem deletions on gitignored paths.
</threat_model>

<verification>
## Plan-Level Final Verification (run after T5 commits)

All 8 checks below MUST pass. Failure of any single check = phase 5 FAIL.

### 1. ROADMAP.md §SC-1 contains the new include-flag-scoped grep wording
```powershell
sls -Path .planning/ROADMAP.md -Pattern "--include='\*\.toml' --include='\*\.example'" -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count
```
POSIX: `grep -c -- "--include='\*\.toml' --include='\*\.example'" .planning/ROADMAP.md`
Expected: ≥ `1`.

### 2. spec_classifier/pyproject.toml lines 4-6 are the exact D-05 block
```bash
grep -c "PYTHONPYCACHEPREFIX + PYTEST_ADDOPTS" spec_classifier/pyproject.toml          # → 1
grep -c "run.ps1 and teresa_gui.py set both"   spec_classifier/pyproject.toml          # → 1
grep -c "pyproject.toml cannot redirect"        spec_classifier/pyproject.toml         # → 1
```
All three `grep -c` results MUST be `1`.

### 3. spec_classifier/config.local.yaml.example:11 is the exact D-06 line
```bash
grep -c "Used by scripts/clean.ps1, run.ps1, and teresa_gui.py." spec_classifier/config.local.yaml.example
```
Expected: `1`.

### 4. Working-tree absence of `.cursor/` AND `teresa.zip`
```powershell
Test-Path .\.cursor      # MUST return $false
Test-Path .\teresa.zip   # MUST return $false
```

### 5. LAUNCHER_README.md historical content preserved (negative check)
```bash
grep -c "replaces three legacy scripts" LAUNCHER_README.md
```
Expected: `1` (the historical mention is intact per D-19).

Also verify the explicit `scripts/run_full.ps1` historical reference is still in LAUNCHER_README.md (it sits inside the parenthetical of the "replaces three legacy scripts" sentence and is exempt from the gate per D-02 / D-19):
```bash
grep -c "scripts/run_full.ps1" LAUNCHER_README.md
```
Expected: `1` (untouched historical reference).

### 6. D-22 guard: zero bytes changed inside protected paths over the phase window
```bash
git diff --stat HEAD~5 -- \
  spec_classifier/src \
  spec_classifier/rules \
  spec_classifier/golden \
  spec_classifier/tests \
  spec_classifier/batch_audit.py \
  spec_classifier/cluster_audit.py \
  spec_classifier/main.py \
  spec_classifier/conftest.py
```
Expected: NO output (zero files listed, zero bytes changed). Any nonzero diff = phase gate FAIL.

### 7. Pytest skip-ratio gate (D-20)
```powershell
cd spec_classifier
pytest -q
```
Expected: session passes WITHOUT tripping `conftest.py::pytest_sessionfinish`'s 0.50 skip-ratio guard. Phase 5 doesn't touch tests, so the ratio is structurally unchanged — but run as a sanity check.

### 8. Goldens byte-equal gate (D-21)
```bash
git diff --stat HEAD~5 -- spec_classifier/golden/
```
Expected: NO output (40 `*_expected.jsonl` fixtures unchanged byte-for-byte). Already covered by check #6's `spec_classifier/golden` path, listed separately for emphasis.

### 9. Post-T1 gate grep (cumulative — all `*.toml` + `*.example` files clean)
```bash
grep -rn "run_full" . --include='*.toml' --include='*.example' --exclude-dir=.planning
```
Expected: NO matches (zero output lines). This is the canonical Phase 5 success criterion #1 verbatim from the post-T1 ROADMAP §SC-1.

### 10. .gitignore lines 48 + 56 untouched (D-13)
```bash
sed -n '48p' .gitignore   # → .cursor/
sed -n '56p' .gitignore   # → teresa.zip
git diff --stat HEAD~5 -- .gitignore   # → no output
```
Expected: lines unchanged; `.gitignore` not in the phase-window diff.

### 11. Five atomic commits exist with the expected subject pattern
```bash
git log -5 --pretty=%s
```
Expected: 5 lines, each matching `^chore\(05\): T[1-5] ` (T5 first / most recent, T1 last / oldest).
</verification>

<success_criteria>
Phase 5 complete when ALL of the following are TRUE:

1. **ORPH-01 closed:** `spec_classifier/pyproject.toml:4-6` contains the exact 3-line block from D-05; no `scripts/run_full.ps1` reference remains in any `*.toml` file outside `.planning/`.
2. **ORPH-02 closed:** `spec_classifier/config.local.yaml.example:11` is exactly `# Used by scripts/clean.ps1, run.ps1, and teresa_gui.py.`; no `scripts/run_full.ps1` reference remains in any `*.example` file outside `.planning/`.
3. **ORPH-03 closed:** `Test-Path .\.cursor` returns `$false`; `.gitignore` line 48 untouched.
4. **ORPH-04 closed:** `Test-Path .\teresa.zip` returns `$false`; `.gitignore` lines 45 and 56 untouched.
5. **Gate-realignment shipped (T1):** ROADMAP.md Phase 5 §SC-1 contains `--include='*.toml' --include='*.example'`.
6. **Historical content preserved:** `LAUNCHER_README.md` still contains "replaces three legacy scripts"; CHANGELOG.md and `run.ps1:3` (RU comment) untouched; `_run_full` method names in `teresa_gui.py:294,489` untouched.
7. **D-22 invariant holds:** `git diff --stat HEAD~5 -- <D-22 paths>` produces no output across the entire phase window.
8. **Pytest skip-ratio gate green:** `cd spec_classifier && pytest -q` passes without tripping the 0.50 skip-ratio guard.
9. **Goldens byte-equal:** `git diff --stat HEAD~5 -- spec_classifier/golden/` produces no output.
10. **5 atomic commits:** `git log -5 --pretty=%s` shows 5 lines each matching `^chore\(05\): T[1-5] `, ≤72 chars per subject.
</success_criteria>

<output>
After completion, create `.planning/phases/05-orphan-cleanup/05-01-SUMMARY.md` per the GSD execute-plan summary template, including:

- Per-task commit SHAs (one for each of T1..T5)
- Verification log: each of the 11 final-verification checks with PASS / FAIL + the literal command output
- D-22 guard log: full `git diff --stat HEAD~5 -- <D-22 paths>` output (expected: empty)
- Pytest run summary: `passed / skipped / failed / xfailed` counts (D-20 record)
- Goldens diff log: full `git diff --stat HEAD~5 -- spec_classifier/golden/` output (expected: empty)
- Removed-artifact pre-state notes (e.g., approximate size of `.cursor/`, `teresa.zip` if recoverable; otherwise note "size not recorded — gitignored, not in any commit")
</output>
