---
phase: 01-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec_classifier/Makefile
  - spec_classifier/batch_audit.py
  - spec_classifier/config.local.yaml.example
  - spec_classifier/README.md
  - spec_classifier/CHANGELOG.md
  - spec_classifier/CURRENT_STATE.md
  - spec_classifier/CLAUDE.md
  - spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md
  - spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md
  - spec_classifier/docs/dev/OPERATIONAL_NOTES.md
  - spec_classifier/docs/dev/TESTING_GUIDE.md
  - spec_classifier/docs/product/TECHNICAL_OVERVIEW.md
  - spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md
  - spec_classifier/docs/taxonomy/cycle2_summary.md
  - spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md
  - spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md
  - spec_classifier/docs/user/USER_GUIDE.md
autonomous: true
requirements:
  - HYG-01

must_haves:
  truths:
    - "Zero `C:\\Users\\G\\` (any escape variant) remains in tracked files outside .planning/"
    - "`<USERNAME>` placeholder is present in at least the markdown / YAML-example files that were rewritten (negative-control)"
    - "spec_classifier/batch_audit.py docstring uses Path.home() / forward-slash placeholder consistent with project pattern (D-01)"
    - "spec_classifier/Makefile INPUT default uses $(HOME) (D-01)"
    - "spec_classifier/config.local.yaml.example uses <USERNAME> token (D-01)"
    - "Behavior unchanged: pytest passes, all 40 goldens still match byte-for-byte"
  artifacts:
    - path: "spec_classifier/Makefile"
      provides: "INPUT default scrubbed"
      contains: "$(HOME)"
    - path: "spec_classifier/batch_audit.py"
      provides: "Docstring username scrubbed"
      contains: "Path.home()"
    - path: "spec_classifier/config.local.yaml.example"
      provides: "Example uses placeholder"
      contains: "<USERNAME>"
  key_links:
    - from: "spec_classifier/Makefile"
      to: "config.local.yaml override"
      via: "documented `INPUT ?=` default + `# override on command line` comment"
      pattern: "INPUT.*HOME"
    - from: "spec_classifier/batch_audit.py"
      to: "module docstring"
      via: "the `Usage:` example block"
      pattern: "Path\\.home"
---

<objective>
HYG-01: Scrub the literal `C:\Users\G\` username from every tracked file outside `.planning/`. Per D-01 each file family gets the placeholder its consumer can read cleanly:

- Python / Makefile (runtime / build defaults) → `Path.home() / "Desktop" / "INPUT"` style or `$(HOME)/Desktop/INPUT`
- YAML example → `<USERNAME>` token
- Markdown docs → `<USERNAME>` token in path strings (preserve example shape per D-02 — only username segment is replaced, not the whole path)

`C:\venv\` references are NOT scrubbed (D-04 — explicit exception). `.planning/` is OUT of scope (D-03). `spec_classifier/rules/`, `tests/`, `golden/`, `src/` load-bearing files are NOT touched (none of them match the grep, but stated for safety).

Purpose: Remove committed identity leakage so the repo can be shared/forked without exposing the original author's username, while preserving runnable examples and historical changelog meaning.

Output: 17 files edited, ~98 literal `C:\Users\G\` occurrences (counted across 17 files outside .planning/) replaced with the per-context placeholder; ~3 escaped `C:\\Users\\G\\` occurrences in `config.local.yaml.example` rewritten to `<USERNAME>`.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hygiene/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@spec_classifier/teresa_gui.py

<interfaces>
<!-- Reference pattern for Python placeholder: spec_classifier/teresa_gui.py:408 already uses Path.home() / "Desktop" / "INPUT". -->
<!-- Reference pattern for PowerShell: run.ps1:33-34 already uses $env:USERPROFILE\Desktop\INPUT (no edits needed in run.ps1 — already clean). -->
<!-- Reference pattern for `<USERNAME>` placeholder: D-01 prescribes this for markdown and YAML example. -->

Existing Python convention (teresa_gui.py:408):
```python
default_input = Path.home() / "Desktop" / "INPUT"
```

Existing PowerShell convention (run.ps1:33):
```powershell
$InputRoot  = "$env:USERPROFILE\Desktop\INPUT"
```

Markdown convention to introduce (D-01, D-02):
- `C:\Users\G\Desktop\INPUT\dl1.xlsx` → `C:\Users\<USERNAME>\Desktop\INPUT\dl1.xlsx`
- Preserve example shape (D-02). Do NOT collapse to `<INPUT_ROOT>` or `~/Desktop/INPUT`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Scrub username in code & build files (Makefile, batch_audit.py, config.local.yaml.example)</name>
  <files>
    spec_classifier/Makefile,
    spec_classifier/batch_audit.py,
    spec_classifier/config.local.yaml.example
  </files>
  <read_first>
    spec_classifier/Makefile,
    spec_classifier/batch_audit.py (lines 1-30 — module docstring with Usage examples),
    spec_classifier/config.local.yaml.example,
    spec_classifier/teresa_gui.py (line 408 only — confirm Path.home() pattern),
    .planning/phases/01-hygiene/01-CONTEXT.md (D-01 placeholder convention)
  </read_first>
  <action>
    Edit three code/build files using Edit tool (NOT Write — preserve byte-for-byte where unchanged):

    **(a) `spec_classifier/Makefile` line 5:** comment line currently reads
    `# Default: C:\Users\G\Desktop\INPUT\ (set in config.local.yaml → paths.input_root).`
    Replace with
    `# Default: $(HOME)/Desktop/INPUT (set in config.local.yaml → paths.input_root).`

    **(b) `spec_classifier/Makefile` line 19:** currently reads
    `INPUT     ?= C:/Users/G/Desktop/INPUT`
    Replace with
    `INPUT     ?= $(HOME)/Desktop/INPUT`
    Rationale: `$(HOME)` is expanded by GNU Make in Git Bash on Windows (resolves to `/c/Users/<actual-user>`) and works on POSIX (D-01). The forward-slash form is preserved (the file already uses forward slashes per Makefile:10 comment "All paths use forward slashes — GNU Make / Git Bash compatible").

    **(c) `spec_classifier/batch_audit.py` lines 10–14:** module docstring currently has 5 example lines:
    ```
        python batch_audit.py --output-dir C:/Users/G/Desktop/OUTPUT
        python batch_audit.py --output-dir C:/Users/G/Desktop/OUTPUT --no-ai     # rule checks only
        python batch_audit.py --output-dir C:/Users/G/Desktop/OUTPUT --vendor hpe
        python batch_audit.py --output-dir C:/Users/G/Desktop/OUTPUT --since 2026-03-04
        python batch_audit.py --output-dir C:/Users/G/Desktop/OUTPUT --dry-run
    ```
    Replace each `C:/Users/G/Desktop/OUTPUT` with `~/Desktop/OUTPUT` (POSIX-style tilde, which the Path.home() pattern aliases for in narrative contexts; Python docstring is read by humans, not executed). Per D-01: Python files use `Path.home()` style; the docstring is illustrative — `~/Desktop/OUTPUT` is the correct human-readable equivalent and matches `teresa_gui.py:408` pattern intent.

    **(d) `spec_classifier/config.local.yaml.example` lines 7, 8, 12:** currently reads
    ```
      input_root: "C:\\Users\\G\\Desktop\\INPUT"
      output_root: "C:\\Users\\G\\Desktop\\OUTPUT"
    ...
    temp_root: "C:\\Users\\G\\Desktop\\temporary"
    ```
    Replace each `G` with `<USERNAME>` (keep YAML-escaped backslashes intact):
    ```
      input_root: "C:\\Users\\<USERNAME>\\Desktop\\INPUT"
      output_root: "C:\\Users\\<USERNAME>\\Desktop\\OUTPUT"
    ...
    temp_root: "C:\\Users\\<USERNAME>\\Desktop\\temporary"
    ```
    YAML loads strings literally — `<USERNAME>` will appear in any user's local copy unmodified, which is the correct behavior for an example file (the user copies and edits).

    Do NOT touch any other line in any of these three files. Do NOT reformat. Do NOT change quote styles, indentation, or trailing whitespace.
  </action>
  <verify>
    <automated>cd C:/Users/G/Desktop/teresa && grep -n 'C:\\Users\\G\\\|C:/Users/G/' spec_classifier/Makefile spec_classifier/batch_audit.py spec_classifier/config.local.yaml.example; test $? -eq 1</automated>
    <!-- grep exits 1 when no matches found -- which is what we want -->
  </verify>
  <acceptance_criteria>
    - `git grep -n 'C:\\Users\\G\\' -- spec_classifier/Makefile spec_classifier/batch_audit.py spec_classifier/config.local.yaml.example` returns 0 lines
    - `git grep -n 'C:/Users/G/' -- spec_classifier/Makefile spec_classifier/batch_audit.py` returns 0 lines
    - `git grep -n '\$(HOME)/Desktop/INPUT' -- spec_classifier/Makefile` returns at least 1 line (the new INPUT default)
    - `git grep -n '~/Desktop/OUTPUT' -- spec_classifier/batch_audit.py` returns at least 4 lines (the new docstring examples)
    - `git grep -n '<USERNAME>' -- spec_classifier/config.local.yaml.example` returns 3 lines
    - `python -c "import ast; ast.parse(open('spec_classifier/batch_audit.py').read())"` exits 0 (file is well-formed Python)
    - `cd spec_classifier && make -n generate_golden_dell` (dry-run, prints commands) succeeds with non-zero exit only if XLSX missing — but does not error on Makefile syntax
    - Original line count of each file is unchanged (`git diff --stat` shows insertions == deletions for each file)
  </acceptance_criteria>
  <done>
    Three files edited; per-context placeholder semantics from D-01 applied; no behavior change (Makefile, Python, YAML still parse cleanly).
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Scrub username in markdown docs (group A — long-form: README, CLAUDE, CHANGELOG, CURRENT_STATE, COWORK_OPUS_FULL_AUDIT)</name>
  <files>
    spec_classifier/README.md,
    spec_classifier/CLAUDE.md,
    spec_classifier/CHANGELOG.md,
    spec_classifier/CURRENT_STATE.md,
    spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md
  </files>
  <read_first>
    spec_classifier/README.md (full file — 13 occurrences clustered around lines 158, 186-188, 222-277),
    spec_classifier/CLAUDE.md (lines 27-35 path block + lines 60-62 OUTPUT note + lines 104-120 CLI command block — 8 occurrences total),
    spec_classifier/CHANGELOG.md (lines 130-185 — 2 historical entries on lines 137 and 180),
    spec_classifier/CURRENT_STATE.md (line 95 — 1 occurrence in Makefile note),
    spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md (lines 25-40 — 2 occurrences),
    .planning/phases/01-hygiene/01-CONTEXT.md (D-02 — preserve example shape)
  </read_first>
  <action>
    For each file: replace every literal `C:\Users\G\` with `C:\Users\<USERNAME>\` (forward-slash form `C:/Users/G/` becomes `C:/Users/<USERNAME>/` if any — only `CURRENT_STATE.md` line 95 has the forward-slash form). Per D-02: only the username segment is replaced; preserve the rest of the path verbatim (`Desktop\INPUT\dl1.xlsx`, `Desktop\OUTPUT`, `Desktop\teresa`, `Desktop\temporary`, etc).

    Use Edit tool with `replace_all=true` per file when the literal string `C:\Users\G\` appears uniformly; otherwise use targeted Edit calls per occurrence with surrounding context.

    **Per-file specifics (verified line counts):**

    - `spec_classifier/README.md` — 13 occurrences. Lines: 158, 186, 188, 222, 226, 239, 242, 245, 248, 266, 269, 272, 277. Pattern is `C:\Users\G\Desktop\INPUT\<file>.xlsx` and `C:\Users\G\Desktop\OUTPUT`. Replace with `C:\Users\<USERNAME>\Desktop\INPUT\<file>.xlsx` and `C:\Users\<USERNAME>\Desktop\OUTPUT`. Note: lines 17-33 contain `C:\venv` references — DO NOT touch (D-04, explicit exception).

    - `spec_classifier/CLAUDE.md` — 8 occurrences. Lines 29-32 (path block), line 61 (OUTPUT comment), lines 110, 113, 116 (CLI commands). Replace `G\` with `<USERNAME>\`. Do NOT modify any prose, business rules, E-code table, or alias-table content (DOC-04 in Phase 2 owns content dedup; this phase only touches the username string).

    - `spec_classifier/CHANGELOG.md` — 2 occurrences (lines 137, 180), both inside historical changelog prose (e.g., "test_data/ заменён на C:\Users\G\Desktop\INPUT\ во всех примерах команд"). These are *describing* past edits. Replace `G\` with `<USERNAME>\` in-place — historical meaning is preserved (the text still says "what was replaced"); the leak is gone.

    - `spec_classifier/CURRENT_STATE.md` — 1 occurrence on line 95: `Makefile: INPUT ?= C:/Users/G/Desktop/INPUT`. Replace with `Makefile: INPUT ?= $(HOME)/Desktop/INPUT` to mirror the actual Makefile change from Task 1.1 (this doc describes the file's current state — keeping it in sync with the Makefile post-edit is the point).

    - `spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md` — 2 occurrences on lines 31 and 34. Replace `G\` with `<USERNAME>\`. (Per D-08: this file IS Phase 3 / WF-01 territory regarding *retire vs deprecate* — but the username scrub itself stays in HYG-01 scope per D-05's enumeration.)

    Do NOT modify `C:\venv` strings. Do NOT modify any other content. Do NOT reformat or convert encoding.
  </action>
  <verify>
    <automated>cd C:/Users/G/Desktop/teresa && grep -n 'C:\\Users\\G\\\|C:/Users/G/' spec_classifier/README.md spec_classifier/CLAUDE.md spec_classifier/CHANGELOG.md spec_classifier/CURRENT_STATE.md spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'C:\\Users\\G\\' -- spec_classifier/README.md spec_classifier/CLAUDE.md spec_classifier/CHANGELOG.md spec_classifier/CURRENT_STATE.md spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md` returns 0 lines
    - `git grep -n 'C:/Users/G/' -- spec_classifier/CURRENT_STATE.md` returns 0 lines
    - `git grep -c '<USERNAME>' spec_classifier/README.md` returns 13
    - `git grep -c '<USERNAME>' spec_classifier/CLAUDE.md` returns 8
    - `git grep -c '<USERNAME>' spec_classifier/CHANGELOG.md` returns 2
    - `git grep -c '<USERNAME>' spec_classifier/prompts/COWORK_OPUS_FULL_AUDIT.md` returns 2
    - `git grep -n 'C:\\venv' spec_classifier/README.md` returns ≥ 4 lines (D-04 exception preserved)
    - For each file: `git diff --numstat <path>` shows insertions == deletions (no line-count drift)
  </acceptance_criteria>
  <done>
    All five files scrubbed; `<USERNAME>` placeholder present per D-02 example-shape rule; `C:\venv` references intact per D-04.
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Scrub username in markdown docs (group B — docs/ tree: 9 files)</name>
  <files>
    spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md,
    spec_classifier/docs/dev/OPERATIONAL_NOTES.md,
    spec_classifier/docs/dev/TESTING_GUIDE.md,
    spec_classifier/docs/product/TECHNICAL_OVERVIEW.md,
    spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md,
    spec_classifier/docs/taxonomy/cycle2_summary.md,
    spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md,
    spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md,
    spec_classifier/docs/user/USER_GUIDE.md
  </files>
  <read_first>
    spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md (lines 13-17, 80-86 — 2 occurrences),
    spec_classifier/docs/dev/OPERATIONAL_NOTES.md (lines 5-90 — 13 occurrences clustered in command examples),
    spec_classifier/docs/dev/TESTING_GUIDE.md (lines 1-110 — 9 occurrences),
    spec_classifier/docs/product/TECHNICAL_OVERVIEW.md (lines 95-280 — 6 occurrences),
    spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md (lines 85-140 — 4 occurrences),
    spec_classifier/docs/taxonomy/cycle2_summary.md (line 71 — 1 occurrence),
    spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md (lines 10-65 — 13 occurrences),
    spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md (lines 12-20 — 4 occurrences clustered in path table),
    spec_classifier/docs/user/USER_GUIDE.md (lines 60-150 — 7 occurrences),
    .planning/phases/01-hygiene/01-CONTEXT.md (D-02)
  </read_first>
  <action>
    For each of the 9 files: every literal `C:\Users\G\` is replaced with `C:\Users\<USERNAME>\`. Per D-02 the rest of the path is preserved verbatim. None of these files contain forward-slash `C:/Users/G/` form (verified at plan time — only Makefile, batch_audit.py, and CURRENT_STATE.md had that form).

    Use Edit tool with `replace_all=true` for each file — the literal string `C:\Users\G\` appears uniformly in command examples and path tables.

    Per-file occurrence counts (from grep at plan time — re-verify against working tree):
    - NEW_VENDOR_GUIDE.md: 2
    - OPERATIONAL_NOTES.md: 13 (line 80 also has the literal `C:\Users\G\Desktop\teresa\run_audit.ps1` pointing to a script that doesn't exist in the current tree — surface but do NOT delete that line; the path placeholder change is enough for HYG-01; orphan note belongs in Phase 3)
    - TESTING_GUIDE.md: 9
    - TECHNICAL_OVERVIEW.md: 6
    - RULES_AUTHORING_GUIDE.md: 4
    - cycle2_summary.md: 1 (line 71: `Set-Location C:\Users\G\Desktop\teresa\spec_classifier`)
    - CLI_CONFIG_REFERENCE.md: 13
    - RUN_PATHS_AND_IO_LAYOUT.md: 4 (lines 13-16, the path table)
    - USER_GUIDE.md: 7

    Total: 59 replacements across 9 files.

    Do NOT modify any other content. The docs/ tree audit (DOC-03 in Phase 2) handles broken cross-references and stale content — this task only swaps the username string.
  </action>
  <verify>
    <automated>cd C:/Users/G/Desktop/teresa && grep -rn 'C:\\Users\\G\\' spec_classifier/docs/; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'C:\\Users\\G\\' -- 'spec_classifier/docs/**/*.md'` returns 0 lines
    - `git grep -n 'C:/Users/G/' -- 'spec_classifier/docs/**/*.md'` returns 0 lines
    - `git grep -c '<USERNAME>' spec_classifier/docs/dev/NEW_VENDOR_GUIDE.md` returns 2
    - `git grep -c '<USERNAME>' spec_classifier/docs/dev/OPERATIONAL_NOTES.md` returns 13
    - `git grep -c '<USERNAME>' spec_classifier/docs/dev/TESTING_GUIDE.md` returns 9
    - `git grep -c '<USERNAME>' spec_classifier/docs/product/TECHNICAL_OVERVIEW.md` returns 6
    - `git grep -c '<USERNAME>' spec_classifier/docs/rules/RULES_AUTHORING_GUIDE.md` returns 4
    - `git grep -c '<USERNAME>' spec_classifier/docs/taxonomy/cycle2_summary.md` returns 1
    - `git grep -c '<USERNAME>' spec_classifier/docs/user/CLI_CONFIG_REFERENCE.md` returns 13
    - `git grep -c '<USERNAME>' spec_classifier/docs/user/RUN_PATHS_AND_IO_LAYOUT.md` returns 4
    - `git grep -c '<USERNAME>' spec_classifier/docs/user/USER_GUIDE.md` returns 7
    - For each file: `git diff --numstat <path>` shows insertions == deletions
  </acceptance_criteria>
  <done>
    All 9 docs/ tree files scrubbed; placeholder consistently applied per D-02; no other prose modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → committed file | Author runs Edit tool against tracked text files; commit lands in git history. |
| repo → fork / share | Public artifact (or shared with collaborator) reads committed text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information disclosure) | All 17 tracked files containing `C:\Users\G\` | mitigate | This plan: replace literal username with `<USERNAME>` / `$(HOME)` / `Path.home()` style placeholders. Verifier greps for residue (D-11 step 1). |
| T-01-02 | T (Tampering) | spec_classifier/batch_audit.py (Python file modified by docstring edit) | mitigate | `python -c "import ast; ast.parse(...)"` syntax-check in acceptance_criteria; pytest in collective gate (D-11 step 3) catches any accidental code change. |
| T-01-03 | T (Tampering) | spec_classifier/Makefile (Makefile target depended on INPUT default) | mitigate | `make -n generate_golden_dell` dry-run in acceptance_criteria confirms Makefile parses; collective smoke gate (D-11 step 4) confirms the actual pipeline still resolves INPUT. |
| T-01-04 | I (Information disclosure) | New replacement strings | mitigate | Replacement strings (`<USERNAME>`, `$(HOME)`, `~/Desktop/...`) contain no secrets/credentials. Pre-commit grep for credential-like patterns is enforced by the collective gate (Plan 04 diff review). |
| T-01-05 | D (Denial of service) / regression | All 40 golden regression fixtures | accept | Goldens are read-only data files not in `files_modified`. Collective gate (D-11 step 3 — pytest) hard-asserts byte-for-byte equality if the rules engine path resolution didn't drift. Risk is structurally bounded: this plan touches only docstrings, comments, examples, and a Makefile default — none feed into `classify_row` or `RuleSet.load`. |

No new attack surface is introduced. Replacement tokens are inert strings.
</threat_model>

<verification>
Plan-local verification (collective D-11 gate runs in Plan 04):

1. `git grep -n 'C:\\Users\\G\\' -- $(git ls-files | grep -v '^\.planning/')` returns 0 lines — combine with results from Tasks 1.1, 1.2, 1.3.
2. `git grep -n 'C:/Users/G/' -- $(git ls-files | grep -v '^\.planning/')` returns 0 lines.
3. `python -c "import ast; ast.parse(open('spec_classifier/batch_audit.py').read())"` exits 0.
4. `cd spec_classifier && python -c "import yaml; yaml.safe_load(open('config.local.yaml.example'))"` exits 0 — YAML still parses (doc-only file but verifying integrity).
5. Total `<USERNAME>` insertions across the 17 tracked files ≥ 90 (sanity check that placeholders were actually written; exact target is 98 backslash + 3 escaped + 4 forward-slash-style in batch_audit.py replaced with `~`, but the floor is 90).

Diff-review checkpoint and full pytest/smoke-run gate run in Plan 04.
</verification>

<success_criteria>
HYG-01 plan complete when:
- All 17 tracked files outside `.planning/` are scrubbed
- No `C:\Users\G\`, `C:/Users/G/`, or `C:\\Users\\G\\` literal remains in those files
- Placeholders applied per D-01 (per-context: `Path.home()` / `~` for Python, `$(HOME)` for Makefile, `<USERNAME>` for markdown and YAML example)
- `C:\venv\` references in spec_classifier/README.md are intact (D-04 exception)
- No business-rule prose, E-code tables, alias-table content, or rules-engine code is modified
- Each file's insertions == deletions (no spurious line additions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-hygiene/01-01-SUMMARY.md` listing:
- Files modified (17)
- Total replacements per file
- Verification command outputs
- Any deviations from plan (e.g. occurrence count drift if the working tree changed since plan time)
</output>
