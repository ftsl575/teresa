---
phase: 01-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - spec_classifier/.gitignore
autonomous: true
requirements:
  - HYG-02

must_haves:
  truths:
    - "Exactly one .gitignore exists at repo root after this plan completes"
    - "spec_classifier/.gitignore is removed from the working tree and from git tracking (git rm)"
    - "Coverage from D-07 is intact: .venv/, OUTPUT/, output/, test_data/, commits.txt, *.zip, .cursor/, .claude/, config.local.yaml, plus all unique entries from spec_classifier/.gitignore"
    - "Path-relative entries from spec_classifier/.gitignore are rewritten to be unambiguous from repo root"
    - "No previously-ignored file becomes tracked accidentally (no leak of secrets, OUTPUT, test_data, etc.)"
  artifacts:
    - path: ".gitignore"
      provides: "Single root .gitignore with merged coverage"
      contains: "config.local.yaml"
      contains: "spec_classifier/temporary"
      contains: "spec_classifier/diag"
      min_lines: 30
  key_links:
    - from: ".gitignore"
      to: "spec_classifier/config.local.yaml"
      via: "explicit pattern"
      pattern: "config\\.local\\.yaml"
    - from: ".gitignore"
      to: "spec_classifier/output/ and spec_classifier/output/"
      via: "rewritten relative path"
      pattern: "spec_classifier/output|^output/"
---

<objective>
HYG-02: Consolidate the dual `.gitignore` (`/.gitignore` + `spec_classifier/.gitignore`) into a single root `.gitignore`. Per D-06: rewrite path-relative entries from the inner `.gitignore` so they are unambiguous from repo root, then `git rm spec_classifier/.gitignore`. Per D-07: coverage that must be retained = union of both files plus `.cursor/`, `.claude/`, `config.local.yaml`.

Purpose: Single source of truth for ignore rules; eliminate drift risk; make Phase 2 doc audits and Phase 3 workflow setup work against a clean baseline.

Output: Root `.gitignore` rewritten with merged + repo-root-relative entries; inner `.gitignore` deleted from tracked tree. Net: 1 file added/modified, 1 file deleted.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hygiene/01-CONTEXT.md
@.gitignore
@spec_classifier/.gitignore

<interfaces>
<!-- Current root .gitignore (21 lines) -->
<!--
**/__pycache__/
.venv/
venv/
.pytest_cache/
spec_classifier/output/
for cursor/
out/
test_data/
output/
test_Dell_R770.xlsx
make_run_results.ps1
teresa.zip

*.zip
OUTPUT/
.cursor/
.claude/
commits.txt
-->

<!-- Current spec_classifier/.gitignore (33 lines) -->
<!--
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

.pytest_cache/
.coverage
htmlcov/

.ruff_cache/
.mypy_cache/

.DS_Store
Thumbs.db

input/
output/
test_data/
config.local.yaml
temporary/
diag/

audit_run.txt
audit_run_ai.txt
baseline.txt
*.log
-->

<!-- Note: spec_classifier/.gitignore "input/", "output/", "test_data/", "temporary/", "diag/" are interpreted relative to spec_classifier/. From the repo root these become "spec_classifier/input/", "spec_classifier/output/", "spec_classifier/test_data/", "spec_classifier/temporary/", "spec_classifier/diag/". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Author merged root .gitignore + remove spec_classifier/.gitignore</name>
  <files>
    .gitignore,
    spec_classifier/.gitignore
  </files>
  <read_first>
    .gitignore (full file — 21 lines),
    spec_classifier/.gitignore (full file — 33 lines),
    .planning/phases/01-hygiene/01-CONTEXT.md (D-06 + D-07 — consolidation policy and required coverage)
  </read_first>
  <action>
    Two operations:

    **(a) Rewrite root `.gitignore`** using Write tool with the following content (organized by section, deduplicated, repo-root-relative):

    ```
    # ─── Python bytecode / cache ───
    **/__pycache__/
    *.pyc
    *.pyo

    # ─── Virtual environments ───
    .venv/
    venv/
    env/

    # ─── Test / coverage / lint cache ───
    .pytest_cache/
    .coverage
    htmlcov/
    .ruff_cache/
    .mypy_cache/

    # ─── OS junk ───
    .DS_Store
    Thumbs.db

    # ─── Repo-local data dirs (live outside the repo per code-only policy) ───
    # Lowercase fallbacks for config-driven local paths
    input/
    output/
    test_data/
    OUTPUT/
    spec_classifier/input/
    spec_classifier/output/
    spec_classifier/test_data/
    spec_classifier/temporary/
    spec_classifier/diag/

    # ─── Local config overlay (gitignored — copy from .example) ───
    spec_classifier/config.local.yaml

    # ─── Audit / run scratch artifacts ───
    spec_classifier/audit_run.txt
    spec_classifier/audit_run_ai.txt
    spec_classifier/baseline.txt
    *.log
    commits.txt

    # ─── Archives ───
    *.zip

    # ─── Tooling assets (not part of the repo) ───
    .cursor/
    .claude/

    # ─── Legacy / one-off scratch files seen in past commits ───
    for cursor/
    out/
    test_Dell_R770.xlsx
    make_run_results.ps1
    teresa.zip
    ```

    Rationale per line cluster:
    - `**/__pycache__/`, `*.pyc`, `*.pyo` — Python bytecode anywhere in tree (combines both files)
    - `.venv/`, `venv/`, `env/` — venv dirs at any depth (matches both files plus `env/` from inner)
    - `.pytest_cache/`, `.coverage`, `htmlcov/`, `.ruff_cache/`, `.mypy_cache/` — test + lint caches (from inner)
    - `.DS_Store`, `Thumbs.db` — OS metadata (from inner)
    - `input/`, `output/`, `test_data/`, `OUTPUT/` — top-level fallback (root + inner had `output/`, root had `OUTPUT/` and `test_data/`; inner used relative paths that root expansion resolves to `spec_classifier/...`)
    - `spec_classifier/input/`, `spec_classifier/output/`, `spec_classifier/test_data/`, `spec_classifier/temporary/`, `spec_classifier/diag/` — explicit subdir paths (rewriting inner's relative entries per D-06)
    - `spec_classifier/config.local.yaml` — moved from inner; absolute-from-root path makes it unambiguous (D-06)
    - `spec_classifier/audit_run.txt`, `spec_classifier/audit_run_ai.txt`, `spec_classifier/baseline.txt`, `*.log` — scratch artifacts; `*.log` stays glob (matches anywhere)
    - `commits.txt`, `*.zip`, `.cursor/`, `.claude/` — preserved from root
    - `for cursor/`, `out/`, `test_Dell_R770.xlsx`, `make_run_results.ps1`, `teresa.zip` — legacy entries from root preserved (D-07 says "plus anything currently only in spec_classifier/.gitignore" — but does NOT say drop root entries; conservative: keep)

    **(b) Delete `spec_classifier/.gitignore` from tracking and the working tree:**
    ```powershell
    git rm spec_classifier/.gitignore
    ```
    The file is currently tracked (`git ls-files | grep gitignore` returns both); `git rm` removes it from both the index and the working tree.

    Do NOT use `Remove-Item` alone — that leaves git tracking it as deleted-but-still-tracked. Use `git rm` exactly.
  </action>
  <verify>
    <automated>cd C:/Users/G/Desktop/teresa && test "$(git ls-files | grep -c '\.gitignore$')" = "1" && test ! -f spec_classifier/.gitignore && grep -q '^spec_classifier/config\.local\.yaml$' .gitignore && grep -q '^spec_classifier/temporary/$' .gitignore && grep -q '^\.cursor/$' .gitignore && grep -q '^\.claude/$' .gitignore && grep -q '^commits\.txt$' .gitignore && grep -q '^OUTPUT/$' .gitignore && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    **Tracking checks:**
    - `git ls-files | grep -c '\.gitignore$'` returns exactly 1
    - `git ls-files | grep -c 'spec_classifier/\.gitignore'` returns exactly 0
    - `test -f .gitignore` succeeds (root file exists)
    - `test ! -f spec_classifier/.gitignore` succeeds (inner file removed)
    - `git status --porcelain | grep -E '^(D|R) +spec_classifier/\.gitignore'` shows the deletion staged

    **Per-pattern presence (comment-stripped grep — avoids self-invalidating grep gate from header prose):**
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^spec_classifier/config\.local\.yaml$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^spec_classifier/temporary/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^spec_classifier/diag/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^\.venv/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^OUTPUT/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^output/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^test_data/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^commits\.txt$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^\*\.zip$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^\.cursor/$'` returns 1
    - `grep -v '^#' .gitignore | grep -v '^[[:space:]]*$' | grep -c '^\.claude/$'` returns 1

    **Full D-07 coverage via `git check-ignore` spot-checks (more robust than per-pattern grep — verifies the rule is *effective*, per W-2):**
    - `git check-ignore spec_classifier/config.local.yaml` exits 0 (file is now ignored from root)
    - `git check-ignore spec_classifier/temporary/foo.txt` exits 0
    - `git check-ignore spec_classifier/diag/foo.txt` exits 0
    - `git check-ignore spec_classifier/output/run-2026-01-01__00-00-00-test/foo.txt` exits 0
    - `git check-ignore spec_classifier/htmlcov/index.html` exits 0 (W-2: htmlcov/ pattern coverage)
    - `git check-ignore spec_classifier/.coverage` exits 0 (W-2: .coverage pattern coverage)
    - `git check-ignore spec_classifier/.pytest_cache/foo` exits 0 (W-2: .pytest_cache/ pattern coverage)
    - `git check-ignore spec_classifier/.ruff_cache/foo` exits 0 (W-2: .ruff_cache/ pattern coverage)
    - `git check-ignore spec_classifier/.mypy_cache/foo` exits 0 (W-2: .mypy_cache/ pattern coverage)
    - `git check-ignore spec_classifier/x.pyc` exits 0 (W-2: *.pyc pattern coverage)
    - `git check-ignore spec_classifier/x.pyo` exits 0 (W-2: *.pyo pattern coverage)
    - `git check-ignore env/lib/python.txt` exits 0 (W-2: env/ pattern coverage)
    - `git check-ignore .DS_Store` exits 0 (W-2: .DS_Store pattern coverage)
    - `git check-ignore Thumbs.db` exits 0 (W-2: Thumbs.db pattern coverage)
    - `git check-ignore spec_classifier/audit_run.txt` exits 0 (W-2: audit_run.txt pattern coverage)
    - `git check-ignore spec_classifier/audit_run_ai.txt` exits 0 (W-2: audit_run_ai.txt pattern coverage)
    - `git check-ignore spec_classifier/baseline.txt` exits 0 (W-2: baseline.txt pattern coverage)
    - `git check-ignore spec_classifier/foo.log` exits 0 (W-2: *.log pattern coverage — glob matches anywhere)

    **Regression check (no tracked file becomes ignored):**
    - `git ls-files | xargs -I{} git check-ignore {} 2>/dev/null | wc -l` returns 0 — no tracked file is now ignored
  </acceptance_criteria>
  <done>
    Single root `.gitignore` covers everything D-07 requires; spec_classifier/.gitignore is removed from tracking and the working tree; no tracked file becomes ignored; every D-07 pattern verified effective via `git check-ignore` (W-2 closure).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → repo | Files staged via `git add` then committed; `.gitignore` rules gate inclusion. |
| repo → public/fork | Whatever is committed becomes shareable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information disclosure) | spec_classifier/config.local.yaml — contains absolute INPUT/OUTPUT paths and (potentially in user copies) any field they add | mitigate | Confirm `git check-ignore spec_classifier/config.local.yaml` exits 0 after consolidation (acceptance_criteria). The file is currently gitignored under inner; we're moving the rule to root with an explicit absolute path so the coverage is preserved, not dropped. |
| T-02-02 | I (Information disclosure) | OUTPUT/ data (potentially containing customer Excel content from past runs) | mitigate | Both `OUTPUT/` and `spec_classifier/output/` patterns are present in the new merged file. Acceptance criteria verifies both. |
| T-02-03 | T (Tampering) | Future commits accidentally include test_data/, .venv/, or *.log because consolidated rules drift from prior coverage | mitigate | Acceptance criteria explicitly enumerates each pattern from D-07 plus the inner-file unique entries via both grep AND `git check-ignore` (W-2). The `git ls-files | xargs git check-ignore` self-test ensures no tracked file flips state. |
| T-02-04 | E (Elevation) / leakage | A previously-ignored file becomes tracked by accident if a pattern is dropped | mitigate | Acceptance criterion: `git ls-files | xargs -I{} git check-ignore {}` returns 0 lines (no tracked file is gitignored — the inverse direction is what we care about: a file that USED to be ignored becoming committed in a future PR. Prevented by exhaustive enumeration of patterns and `git check-ignore` per category in acceptance.) |

No new attack surface; the change is entirely about ignore-rule organization.
</threat_model>

<verification>
Plan-local verification (collective D-11 gate runs in Plan 04):

1. `git ls-files | grep -c '\.gitignore$'` returns exactly 1
2. `git check-ignore spec_classifier/config.local.yaml` exits 0
3. `git check-ignore spec_classifier/temporary/foo.txt` exits 0
4. `git check-ignore spec_classifier/diag/foo.txt` exits 0
5. `git check-ignore spec_classifier/output/run-test/foo.txt` exits 0
6. `git check-ignore spec_classifier/htmlcov/x.html` exits 0 (representative for D-07 cache patterns per W-2)
7. `git check-ignore spec_classifier/foo.log` exits 0 (glob coverage check per W-2)

Diff-review checkpoint and full pytest/smoke-run gate run in Plan 04.
</verification>

<success_criteria>
HYG-02 plan complete when:
- Exactly one `.gitignore` exists in tracked tree (at repo root)
- `spec_classifier/.gitignore` is removed (`git rm`)
- All D-07 mandatory coverage is present in root `.gitignore` (verified by `git check-ignore` for every pattern listed in W-2 expansion: htmlcov/, .coverage, .pytest_cache/, .ruff_cache/, .mypy_cache/, *.pyc, *.pyo, env/, .DS_Store, Thumbs.db, audit_run.txt, audit_run_ai.txt, baseline.txt, *.log)
- All previously-ignored paths under `spec_classifier/` remain ignored from the new root file (verified by `git check-ignore`)
- No tracked file becomes ignored as a side-effect (regression check on `git ls-files`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-hygiene/01-02-SUMMARY.md` listing:
- Final root `.gitignore` content (or diff)
- Each entry's source (kept from root / merged from inner / rewritten relative path)
- `git check-ignore` verification outputs for the full D-07 pattern set (W-2 closure)
- Any pattern that was intentionally dropped or merged with rationale
</output>
</content>
</invoke>