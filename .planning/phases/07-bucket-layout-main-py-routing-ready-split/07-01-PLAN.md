---
phase: 07-bucket-layout-main-py-routing-ready-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec_classifier/src/diagnostics/run_manager.py
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - ROUTE-05

must_haves:
  truths:
    - "create_spec_folder exists in run_manager.py and accepts (output_root, bucket, vendor, spec) — centralizes path construction (D-10)"
    - "create_spec_folder wipes the target directory with rmtree if it exists, then recreates it, then returns the Path — wipe-first per-bucket (D-04)"
    - "Dead helpers (get_session_stamp, create_run_folder, create_total_folder, copy_to_total) are gone from run_manager.py — no dead code left behind (D-09)"
    - "datetime import is removed; shutil and Path imports remain"
    - "Module docstring no longer references timestamps or TOTAL"
  artifacts:
    - path: "spec_classifier/src/diagnostics/run_manager.py"
      provides: "create_spec_folder helper; all dead timestamp/TOTAL helpers deleted"
      exports: ["create_spec_folder"]
  key_links:
    - from: "spec_classifier/main.py"
      to: "spec_classifier/src/diagnostics/run_manager.py"
      via: "from src.diagnostics.run_manager import create_spec_folder"
      pattern: "create_spec_folder"
---

<objective>
Replace the timestamp-folder helpers in run_manager.py with the wipe-first bucket helper.

Purpose: main.py (Plan 02) depends on create_spec_folder. Establishing it first, with all dead
code removed, keeps the dependency direction clean and the module focused on bucket routing.

Output: run_manager.py with one exported function — create_spec_folder — and no dead helpers.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-bucket-layout-main-py-routing-ready-split/07-CONTEXT.md
@.planning/phases/07-bucket-layout-main-py-routing-ready-split/07-PATTERNS.md

<interfaces>
<!-- Current run_manager.py — full file (72 lines) extracted for executor reference. -->
<!-- Executor reads the file directly; this section shows the contracts to DELETE and ADD. -->

<!-- DELETE all of these (D-09 per CONTEXT.md): -->
get_session_stamp() -> str
create_run_folder(base_dir: str, input_filename: str, stamp: str = None) -> Path
create_total_folder(base_dir: str, stamp: str) -> Path
copy_to_total(run_folder: Path, total_folder: Path, stem: str) -> None

<!-- ADD this (D-10 per CONTEXT.md): -->
def create_spec_folder(output_root: Path, bucket: str, vendor: str, spec: str) -> Path:
    """
    Create (or wipe-and-recreate) <output_root>/<bucket>/<vendor>/<spec>/.

    Wipe-first: if the directory already exists it is deleted before recreation,
    ensuring no stale artifacts from a previous run survive.

    Args:
        output_root: top-level output directory (e.g. C:\\...\\OUTPUT)
        bucket:      bucket name ("READY" or "SPLIT")
        vendor:      registry key, lowercase (e.g. "dell", "hpe")
        spec:        input file stem (e.g. "dl1")

    Returns:
        Path to the freshly created directory.
    """
    folder = Path(output_root) / bucket / vendor / spec
    if folder.exists():
        shutil.rmtree(folder)
    folder.mkdir(parents=True)
    return folder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite run_manager.py — delete dead helpers, add create_spec_folder</name>
  <files>spec_classifier/src/diagnostics/run_manager.py</files>

  <read_first>
    - spec_classifier/src/diagnostics/run_manager.py (current file — read fully before editing)
    - .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-CONTEXT.md (D-04, D-09, D-10)
    - .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-PATTERNS.md (section: run_manager.py)
  </read_first>

  <action>
Rewrite spec_classifier/src/diagnostics/run_manager.py from scratch. The new file has:

1. NEW MODULE DOCSTRING — replace the timestamp/TOTAL docstring with:
   """
   Run folder management: create and prepare per-spec output directories.

   Naming convention:
     Spec folder: <output_root>/<bucket>/<vendor>/<spec>/  (e.g. SPLIT/dell/dl1/)
   """

2. IMPORTS — keep only two imports (drop `from datetime import datetime`):
   import shutil
   from pathlib import Path

3. ONE FUNCTION — create_spec_folder with the exact body below:
   def create_spec_folder(output_root: Path, bucket: str, vendor: str, spec: str) -> Path:
       """
       Create (or wipe-and-recreate) <output_root>/<bucket>/<vendor>/<spec>/.

       Wipe-first: if the directory already exists it is deleted before recreation,
       ensuring no stale artifacts from a previous run survive.

       Args:
           output_root: top-level output directory (e.g. C:\\...\\OUTPUT)
           bucket:      bucket name ("READY" or "SPLIT")
           vendor:      registry key, lowercase (e.g. "dell", "hpe")
           spec:        input file stem (e.g. "dl1")

       Returns:
           Path to the freshly created directory.
       """
       folder = Path(output_root) / bucket / vendor / spec
       if folder.exists():
           shutil.rmtree(folder)
       folder.mkdir(parents=True)
       return folder

DO NOT keep any part of the old file. All four old functions (get_session_stamp,
create_run_folder, create_total_folder, copy_to_total) are gone entirely. The
`datetime` import is gone. No other helper is added. No default values for bucket/vendor/spec
(they are always supplied by main.py). No exist_ok on mkdir (wipe-first guarantees a clean dir).

DO NOT change any classification, normalization, or audit logic — this is routing only.
  </action>

  <verify>
    <automated>cd /mnt/c/Users/G/Desktop/teresa/spec_classifier && python -c "from src.diagnostics.run_manager import create_spec_folder; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - run_manager.py contains exactly the string "def create_spec_folder(" (grep confirms)
    - run_manager.py does NOT contain "get_session_stamp" (grep returns no match)
    - run_manager.py does NOT contain "create_run_folder" (grep returns no match)
    - run_manager.py does NOT contain "create_total_folder" (grep returns no match)
    - run_manager.py does NOT contain "copy_to_total" (grep returns no match)
    - run_manager.py does NOT contain "from datetime import" (grep returns no match)
    - run_manager.py contains "shutil.rmtree(folder)" (grep confirms)
    - run_manager.py contains "folder.mkdir(parents=True)" (grep confirms) — no exist_ok
    - `python -c "from src.diagnostics.run_manager import create_spec_folder; print('OK')"` exits 0 and prints OK
    - File has fewer than 50 lines total (old file was 72 lines; new file is ~35)
  </acceptance_criteria>

  <done>run_manager.py exports exactly create_spec_folder with wipe-first semantics; all dead helpers are gone; the module is importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → create_spec_folder | output_root, bucket, vendor, spec tokens are passed in from main.py |
| filesystem → shutil.rmtree | rmtree deletes the entire spec directory before recreation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | create_spec_folder / shutil.rmtree | mitigate | Path constructed as `output_root / bucket / vendor / spec`. `bucket` is always a string literal ("READY" or "SPLIT") from main.py — not user-supplied. `vendor` comes from `VENDOR_REGISTRY` dict keys (fixed set: dell/cisco/hpe/lenovo/huawei/xfusion) — no path separators or traversal sequences possible. `spec` = `input_path.stem` from Python's Path.stem, which strips directory parts — cannot contain "/" or "\". Combined, rmtree can only target `output_root/READY|SPLIT/<vendor>/<spec>/`. |
| T-07-02 | Tampering | D-05 boundary — AUDIT/ not touched | accept | `create_spec_folder` receives only "READY" or "SPLIT" as bucket in Plan 02 call sites. No call site passes "AUDIT". The constraint is structural (code-level), not configuration. No additional mitigation required beyond code review of Plan 02 call sites. |
| T-07-03 | Denial of Service | rmtree on wrong target | accept | Low risk: tokens are derived from trusted sources (VENDOR_REGISTRY + Path.stem). An attacker would need to control the input filename to influence spec, and even then Path.stem strips all directory separators. output_root is set by config / --output-dir, not by row data. Accept at current risk level. |
</threat_model>

<verification>
After Plan 01 execution:
- Import check: `python -c "from src.diagnostics.run_manager import create_spec_folder"` exits 0
- Negative import check: `python -c "from src.diagnostics.run_manager import get_session_stamp"` raises ImportError
- File line count is under 50 lines
- pytest tests/ -q (excluding the integration tests that require INPUT) must not newly fail on run_manager import
</verification>

<success_criteria>
run_manager.py is a clean, focused module with one export: create_spec_folder. All timestamp-folder
and TOTAL helpers are gone. The module is importable and create_spec_folder performs wipe-first
folder creation at output_root/bucket/vendor/spec/.
</success_criteria>

<output>
After completion, create .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-01-SUMMARY.md
</output>
