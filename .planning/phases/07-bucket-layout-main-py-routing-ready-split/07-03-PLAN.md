---
phase: 07-bucket-layout-main-py-routing-ready-split
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - spec_classifier/tests/test_output_structure.py
  - spec_classifier/tests/test_cli.py
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - ROUTE-01
  - ROUTE-02

must_haves:
  truths:
    - "test_output_structure.py asserts SPLIT/dell/dl1/ and SPLIT/cisco/ccw_1/ — not dell_run/ or cisco_run/"
    - "test_output_structure.py asserts READY/dell/dl1/Коммерческое предложение_dl1.xlsx — not _branded.xlsx"
    - "test_output_structure.py asserts NO dell_run/ directory exists"
    - "test_cli.py asserts artifacts in SPLIT/dell/dl1/ — not dell_run/run-*"
    - "pytest passes within the skip-gate (skipped/total < 0.50); goldens are byte-equal (no --update-golden)"
    - "RUN_FOLDER_PATTERN constant is removed from test_output_structure.py (no longer used)"
  artifacts:
    - path: "spec_classifier/tests/test_output_structure.py"
      provides: "Layout tests asserting new SPLIT/READY bucket structure"
    - path: "spec_classifier/tests/test_cli.py"
      provides: "CLI smoke test asserting artifacts in SPLIT/dell/dl1/"
  key_links:
    - from: "spec_classifier/tests/test_output_structure.py"
      to: "output_root/SPLIT/dell/dl1/"
      via: "split_folder = output_root / 'SPLIT' / 'dell' / 'dl1'"
      pattern: "SPLIT.*dell.*dl1"
    - from: "spec_classifier/tests/test_output_structure.py"
      to: "output_root/READY/dell/dl1/Коммерческое предложение_dl1.xlsx"
      via: "ready_folder = output_root / 'READY' / 'dell' / 'dl1'"
      pattern: "READY.*dell.*dl1"
---

<objective>
Realign path/layout tests to the new bucket structure (TEST-01 for Phase 7).

Purpose: Plans 01 and 02 change where artifacts land. The tests still assert the old
dell_run/run-* paths and will fail. Realigning them is the final step that makes the
pytest suite green. Golden fixture content is NOT changed — only path assertions are updated.

Output: test_output_structure.py and test_cli.py asserting SPLIT/<vendor>/<spec>/ and
READY/<vendor>/<spec>/ instead of the old timestamp-folder scheme.
</objective>

<execution_context>
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/G/Desktop/teresa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-bucket-layout-main-py-routing-ready-split/07-CONTEXT.md
@.planning/phases/07-bucket-layout-main-py-routing-ready-split/07-PATTERNS.md
@.planning/phases/07-bucket-layout-main-py-routing-ready-split/07-02-SUMMARY.md

<interfaces>
<!-- New path scheme (from Plans 01 + 02): -->
<!-- SPLIT folder: output_root / "SPLIT" / vendor / spec  -->
<!-- READY folder: output_root / "READY" / vendor / spec  -->
<!-- Branded filename: f"Коммерческое предложение_{spec}.xlsx"  -->
<!-- vendor = lowercase registry key, e.g. "dell", "cisco" -->
<!-- spec = input file stem, e.g. "dl1", "ccw_1" -->

<!-- REQUIRED_ARTIFACTS list (unchanged — still applies to SPLIT folder): -->
<!-- "rows_raw.json", "rows_normalized.json", "classification.jsonl", "cleaned_spec.xlsx", -->
<!-- "header_rows.csv", "unknown_rows.csv", "run_summary.json", "run.log" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realign test_output_structure.py to bucket layout</name>
  <files>spec_classifier/tests/test_output_structure.py</files>

  <read_first>
    - spec_classifier/tests/test_output_structure.py (current file — read fully before editing; 149 lines)
    - .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-PATTERNS.md (section: test_output_structure.py)
    - spec_classifier/tests/conftest.py (to understand get_input_root_dell, get_input_root_cisco helpers)
  </read_first>

  <action>
Rewrite spec_classifier/tests/test_output_structure.py. Keep the module docstring updated,
the conftest imports, the REQUIRED_ARTIFACTS constant, and the test structure. Change only
the path assertions and constants as follows:

--- CHANGE 1: Module docstring (lines 2-9) ---
Update the docstring to describe the new bucket layout:
  """
  Regression test: output tree shape for the new bucket layout.
  - output_root / SPLIT / <vendor> / <spec> / artifacts   (nine per-spec artifacts)
  - output_root / READY / <vendor> / <spec> / Коммерческое предложение_<spec>.xlsx  (Dell branded)
  Required always: rows_raw.json, rows_normalized.json, classification.jsonl, cleaned_spec.xlsx,
    header_rows.csv, unknown_rows.csv, run_summary.json, run.log
  Dell: <stem>_annotated.xlsx in SPLIT + Коммерческое предложение_<stem>.xlsx in READY
  Cisco: <stem>_annotated.xlsx in SPLIT ONLY (no branded)
  Uses tmp_path for output_root; no large golden blobs in git.
  """

--- CHANGE 2: Remove RUN_FOLDER_PATTERN constant (line 21) ---
Delete the line:
  RUN_FOLDER_PATTERN = re.compile(r"^run-\d{4}-\d{2}-\d{2}__\d{2}-\d{2}-\d{2}-(.+)$")
Also remove the `import re` at line 11 since it is only used by RUN_FOLDER_PATTERN.

--- CHANGE 3: test_output_tree_shape_dell_run (lines 36-78) ---
Keep the test function name, the CLI subprocess invocation (unchanged), and the skip guard.
Replace ONLY the post-run assertion block (everything after `assert result.returncode == 0`):

  # New assertions — bucket layout:
  stem = "dl1"
  split_folder = output_root / "SPLIT" / "dell" / stem
  ready_folder = output_root / "READY" / "dell" / stem

  assert split_folder.is_dir(), f"SPLIT/dell/{stem} folder must exist under {output_root}"
  assert not (output_root / "dell_run").exists(), "Old dell_run/ folder must NOT exist"

  for name in REQUIRED_ARTIFACTS:
      assert (split_folder / name).exists(), f"Required artifact missing: {name} in {split_folder}"

  assert (split_folder / f"{stem}_annotated.xlsx").exists(), f"{stem}_annotated.xlsx missing in {split_folder}"

  assert ready_folder.is_dir(), f"READY/dell/{stem} folder must exist under {output_root}"
  branded_name = f"Коммерческое предложение_{stem}.xlsx"
  assert (ready_folder / branded_name).exists(), f"{branded_name} missing in {ready_folder} (Dell)"

--- CHANGE 4: test_output_tree_shape_cisco_run (lines 81-120) ---
Keep the test function name, CLI invocation, and skip guard.
Replace the post-run assertion block:

  # New assertions — bucket layout:
  stem = "ccw_1"
  split_folder = output_root / "SPLIT" / "cisco" / stem

  assert split_folder.is_dir(), f"SPLIT/cisco/{stem} folder must exist under {output_root}"
  assert not (output_root / "cisco_run").exists(), "Old cisco_run/ folder must NOT exist"

  for name in REQUIRED_ARTIFACTS:
      assert (split_folder / name).exists(), f"Required artifact missing: {name} in {split_folder}"

  assert (split_folder / f"{stem}_annotated.xlsx").exists(), f"{stem}_annotated.xlsx missing in {split_folder}"

  # Cisco: no branded workbook in READY
  assert not (output_root / "READY" / "cisco" / stem).is_dir() or \
      not any((output_root / "READY" / "cisco" / stem).iterdir()), \
      "Cisco must not produce a branded file in READY"

NOTE: The Cisco test currently asserts that branded.xlsx EXISTS at line 120 —
this is a test bug in the existing file (Cisco does not generate branded). After the
routing change, Cisco will not produce READY/ at all. Remove the incorrect assertion
`assert (run_folder / f"{stem}_branded.xlsx").exists()` entirely; do not replace it
with a positive assertion for READY (Cisco has no branded workbook).

--- CHANGE 5: test_output_root_configurable_via_cli (lines 123-148) ---
Keep the test function name, CLI invocation, and skip guard.
Replace the post-run assertion block:

  assert result.returncode == 0
  assert output_root.exists()
  split_vendor = output_root / "SPLIT" / "dell"
  assert split_vendor.is_dir(), \
      "Output must be under output_root/SPLIT/dell/ (repo stays clean when using external path)"

DO NOT keep the `vendor_root / "dell_run"` or `run-*` glob assertions — they must not appear.

--- IMPORTS ---
After removing `import re` (unused after RUN_FOLDER_PATTERN deletion), verify all remaining
imports are still needed: subprocess, sys, Path, pytest, conftest imports. Keep all of them.

DO NOT modify any golden fixture files. DO NOT pass --update-golden. Do NOT change
classification/normalization/audit logic. Routing-only test realignment.
  </action>

  <verify>
    <automated>cd /mnt/c/Users/G/Desktop/teresa/spec_classifier && python -m pytest tests/test_output_structure.py --collect-only -q 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - test_output_structure.py does NOT contain "dell_run" (grep returns no match)
    - test_output_structure.py does NOT contain "cisco_run" (grep returns no match)
    - test_output_structure.py does NOT contain "run-*" or "run_folders" or "RUN_FOLDER_PATTERN" (grep returns no match for each)
    - test_output_structure.py does NOT contain "import re" (grep returns no match — import removed with constant)
    - test_output_structure.py contains "SPLIT" (grep confirms — new bucket assertion)
    - test_output_structure.py contains "READY" (grep confirms — new bucket assertion)
    - test_output_structure.py contains "Коммерческое предложение_" (grep confirms — Cyrillic filename)
    - test_output_structure.py contains "dell_run.*NOT exist" or "not.*dell_run" (negative assertion present)
    - `python -m pytest tests/test_output_structure.py --collect-only -q` exits 0 (all three tests collected, no syntax error)
    - If INPUT/dell/dl1.xlsx is available: `pytest tests/test_output_structure.py::test_output_tree_shape_dell_run -v` passes (not just collects)
  </acceptance_criteria>

  <done>test_output_structure.py asserts the new SPLIT/READY bucket paths; old timestamp-folder assertions are gone; the Cisco branded bug is fixed; file collects cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Realign test_cli.py and run full pytest suite</name>
  <files>spec_classifier/tests/test_cli.py</files>

  <read_first>
    - spec_classifier/tests/test_cli.py (current file — read fully before editing; 48 lines)
    - .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-PATTERNS.md (section: test_cli.py)
    - spec_classifier/tests/test_output_structure.py (confirm Task 1 is complete before proceeding)
  </read_first>

  <action>
Make surgical changes to spec_classifier/tests/test_cli.py.

--- CHANGE 1: Module docstring (lines 2-5) ---
Update to remove reference to run_folder:
  """
  CLI smoke test: run main.py via subprocess and assert exit code, stdout, and artifacts.
  Uses tmp_path for output so the repo stays code-only.
  """

--- CHANGE 2: test_cli_exit_code_stdout_artifacts assertion block (lines 38-47) ---
Keep everything up to and including `assert "total_rows" in result.stdout`.
Replace the old path-walk block:
  vendor_root = output_dir / "dell_run"
  assert vendor_root.exists(), "output_root/dell_run should exist"
  run_folders = list(vendor_root.glob("run-*"))
  assert run_folders, "At least one run-* folder under dell_run should exist"
  latest = max(run_folders, key=lambda p: p.stat().st_mtime)
  assert (latest / "cleaned_spec.xlsx").exists(), f"cleaned_spec.xlsx missing in {latest}"
  assert (latest / "run_summary.json").exists(), f"run_summary.json missing in {latest}"

With the direct-path block:
  split_folder = output_dir / "SPLIT" / "dell" / "dl1"
  assert split_folder.is_dir(), f"SPLIT/dell/dl1 folder must exist under {output_dir}"
  assert (split_folder / "cleaned_spec.xlsx").exists(), f"cleaned_spec.xlsx missing in {split_folder}"
  assert (split_folder / "run_summary.json").exists(), f"run_summary.json missing in {split_folder}"

Do NOT change the subprocess invocation, the skip guard, the stdout assertion,
the timeout, or any imports.

--- AFTER EDITING test_cli.py: Run the full pytest suite ---
Run:
  cd spec_classifier && python -m pytest tests/ -q --tb=short 2>&1

Observe the output. The following are acceptable outcomes:
  - PASS or SKIP: any test may pass or skip (skip is acceptable if INPUT not present).
  - FAIL on test_output_structure or test_cli: not acceptable — fix before done.
  - The skip ratio must be < 0.50 (skipped / (passed + skipped + failed) < 0.50).
  - No golden fixture file may be changed. Do NOT pass --update-golden.

If any non-layout test fails that was passing before Plan 02, investigate whether a
routing-only change accidentally broke non-routing logic. Do NOT change golden files.
DO NOT change any file other than test_output_structure.py and test_cli.py in this plan.
  </action>

  <verify>
    <automated>cd /mnt/c/Users/G/Desktop/teresa/spec_classifier && python -m pytest tests/ -q --tb=short 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - test_cli.py does NOT contain "dell_run" (grep returns no match)
    - test_cli.py does NOT contain "run_folders" (grep returns no match)
    - test_cli.py does NOT contain "run-*" glob pattern (grep returns no match)
    - test_cli.py contains "SPLIT" (grep confirms)
    - test_cli.py contains "split_folder = output_dir / \"SPLIT\" / \"dell\" / \"dl1\"" (grep confirms)
    - `python -m pytest tests/test_cli.py --collect-only -q` exits 0 (test collected, no syntax error)
    - `python -m pytest tests/ -q --tb=short` exits 0 (full suite green) OR any failures are pre-existing skips only (INPUT not present). Specifically: test_output_structure.py and test_cli.py must not FAIL (they may SKIP if INPUT absent).
    - pytest summary line shows 0 failures (xfailed/skipped are acceptable)
    - Pytest skip ratio: skipped / total < 0.50 — confirm from summary line
    - `ls spec_classifier/golden/` output is identical before and after (no golden files changed)
  </acceptance_criteria>

  <done>test_cli.py asserts the new SPLIT/dell/dl1/ path; full pytest suite is green (or skip-only) within the skip-gate; goldens are byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test subprocess → main.py | Tests invoke main.py via subprocess with controlled --output-dir (tmp_path) |
| test assertions → filesystem | Tests read filesystem paths produced by main.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | test tmp_path output | accept | Tests use pytest tmp_path (OS temp dir, unique per test). No sensitive data written to shared locations. Acceptable for a test environment. |
| T-07-09 | Tampering | golden fixtures via --update-golden | mitigate | Plan explicitly forbids --update-golden. Acceptance criteria require golden file list is unchanged after this plan. The constraint is enforced by the acceptance check `ls spec_classifier/golden/` before and after. |
| T-07-10 | Denial of Service | false-positive skip ratio | mitigate | Acceptance criteria include explicit skip ratio check (skipped/total < 0.50). If INPUT is not populated, output-structure and CLI tests skip — they must not push the ratio to ≥ 0.50. The other ~400 unit tests (batch_audit, cluster_audit, rules, normalizer) do not require INPUT and will pass, keeping the ratio well below 0.50. |
</threat_model>

<verification>
After Plan 03 execution:
- `pytest tests/ -q --tb=short` exits 0 (zero failures)
- No golden file is modified: `git diff spec_classifier/golden/` shows no changes
- Skip ratio is below 0.50
- `pytest tests/test_output_structure.py --collect-only` collects exactly 3 tests
- `pytest tests/test_cli.py --collect-only` collects exactly 1 test
- grep confirms no old timestamp-folder patterns in either test file:
  - grep "dell_run" tests/test_output_structure.py tests/test_cli.py → no matches
  - grep "run_folders\|run-\*\|RUN_FOLDER_PATTERN" tests/test_output_structure.py → no matches
</verification>

<success_criteria>
Both test files assert the new SPLIT/READY bucket structure. The full pytest suite passes
within the skip-gate. Golden fixture content is byte-equal. The Cisco branded-file test bug
(false positive assertion) is corrected as a side effect of the realignment.
</success_criteria>

<output>
After completion, create .planning/phases/07-bucket-layout-main-py-routing-ready-split/07-03-SUMMARY.md
</output>
